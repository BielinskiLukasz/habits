/** @file Wave aggregate computation integration test (WAVE-01..06). Stub — fleshed out in plan 04-04/04-06. */

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { createFakeRepo } from '../helpers/fake-idb.js';

describe('Wave aggregates', () => {
  it('stub — plans 04-04 and 04-06 must flesh out this integration test', () => {
    assert.fail('Plans 04-04 and 04-06 must flesh out this integration test');
  });
});
