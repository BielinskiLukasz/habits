/** @file Wave aggregate computation unit tests (WAVE-01..06). Stub — fleshed out in plan 04-04. */

import { describe, it } from 'node:test';
import assert from 'node:assert/strict';
import { computeWaveAggregates } from '../../js/domain/waveAggregates.js';

describe('computeWaveAggregates', () => {
  it('stub — plan 04-04', () => {
    assert.fail('Plan 04-04 must flesh out this test suite');
  });
});
