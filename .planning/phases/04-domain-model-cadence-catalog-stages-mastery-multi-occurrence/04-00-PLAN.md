---
phase: 04-domain-model-cadence-catalog-stages-mastery-multi-occurrence
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/unit/mastery.test.js
  - tests/unit/stage.test.js
  - tests/unit/waveAggregates.test.js
  - tests/unit/builders.catalog.test.js
  - tests/unit/builders.history.test.js
  - tests/unit/builders.today.numeric.test.js
  - tests/integration/catalog-flow.test.js
  - tests/integration/history-flow.test.js
  - tests/integration/stage-advancement.test.js
  - tests/integration/habit-versions.test.js
  - tests/integration/wave-aggregates.test.js
  - tests/integration/mastery-cadence.test.js
autonomous: true
requirements:
  - CADENCE-01
  - MASTERY-01
  - STAGE-01
  - CATALOG-01
  - LOG-02
  - WAVE-01
  - HISTORY-01
  - NFR-10

must_haves:
  truths:
    - "All 12 new Phase 4 test files exist on disk before Wave 1 TDD RED phase begins"
    - "Each stub file imports the module under test and declares at minimum one failing assertion"
    - "npm test does not fail due to missing file references before Wave 1 starts"
    - "Existing 281 tests remain green after stubs are added"
  artifacts:
    - path: "tests/unit/mastery.test.js"
      provides: "Stub for mastery evaluation TDD (plan 04-02 RED phase)"
      contains: "evaluateMastery"
    - path: "tests/unit/stage.test.js"
      provides: "Stub for stage advancement TDD (plan 04-03 RED phase)"
      contains: "evaluateStageTriggers"
    - path: "tests/unit/waveAggregates.test.js"
      provides: "Stub for wave aggregate TDD (plan 04-04 RED phase)"
      contains: "computeWaveAggregates"
    - path: "tests/integration/catalog-flow.test.js"
      provides: "Integration stub for catalog end-to-end flow"
      contains: "catalog"
    - path: "tests/integration/habit-versions.test.js"
      provides: "Integration stub for NFR-10 history integrity"
      contains: "NFR-10"
  key_links:
    - from: "tests/unit/mastery.test.js"
      to: "js/domain/mastery.js"
      via: "import { evaluateMastery } from '../../js/domain/mastery.js'"
      pattern: "evaluateMastery"
    - from: "tests/unit/stage.test.js"
      to: "js/domain/stage.js"
      via: "import { evaluateStageTriggers, demoteStage } from '../../js/domain/stage.js'"
      pattern: "evaluateStageTriggers"
---

<objective>
Create stub test files for all 12 new Phase 4 test files identified in 04-VALIDATION.md. This is Wave 0 — it must run before the TDD plans (04-01..04-04, Wave 1) so that each TDD RED phase starts from a clearly failing (not "file not found") state.

Purpose: TDD plans rely on `npm test -- {file}` for their RED commit. If the test file doesn't exist, the Node test runner throws a file-not-found error rather than a clean test failure — this breaks the RED/GREEN/REFACTOR sequence. Wave 0 creates minimal stub files so the TDD runner finds the file and sees failing assertions, not missing-file errors.

Output: 12 new test stub files (6 unit + 6 integration). Each stub has the correct import path and one placeholder `assert.fail('not yet implemented')` test that will be fleshed out by the TDD plans in Wave 1 and the execute plans in Wave 2.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-CONTEXT.md
@.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-VALIDATION.md

@tests/unit/cadence.test.js
@tests/unit/date.test.js
@tests/integration/apply.markCompleted.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unit test stubs — mastery, stage, waveAggregates, builders</name>
  <files>tests/unit/mastery.test.js, tests/unit/stage.test.js, tests/unit/waveAggregates.test.js, tests/unit/builders.catalog.test.js, tests/unit/builders.history.test.js, tests/unit/builders.today.numeric.test.js</files>

  <read_first>
  - `tests/unit/cadence.test.js` — read ALL; understand the existing unit test structure (node:test import, describe/it blocks, assert usage) before creating matching stubs
  - `tests/unit/date.test.js` — read ALL; confirm import path convention (`../../js/...` or `../..` relative paths)
  - `tests/integration/apply.markCompleted.test.js` — read to confirm integration test import conventions
  </read_first>

  <action>
  Create each stub file using the project's node:test pattern. Read `tests/unit/cadence.test.js` first to confirm the exact import convention and describe/it structure, then apply the same pattern to each stub.

  **`tests/unit/mastery.test.js`:**
  - File header: `/** @file Mastery evaluation unit tests (MASTERY-01..07). Stub — fleshed out in plan 04-02. */`
  - Import: `import assert from 'node:assert/strict';` and `import { describe, it } from 'node:test';`
  - Import the module under test (will throw "module not found" until plan 04-02 creates it — that IS the RED state): `import { evaluateMastery } from '../../js/domain/mastery.js';`
  - One placeholder test suite: `describe('evaluateMastery', () => { it('stub — will be replaced by plan 04-02 RED phase', () => { assert.fail('Plan 04-02 must flesh out this test suite'); }); });`

  **`tests/unit/stage.test.js`:**
  - Header: `/** @file Stage advancement trigger unit tests (STAGE-01..07). Stub — fleshed out in plan 04-03. */`
  - Import `evaluateStageTriggers`, `demoteStage` from `../../js/domain/stage.js`
  - One placeholder: `describe('evaluateStageTriggers', () => { it('stub — plan 04-03', () => { assert.fail('Plan 04-03 must flesh out this test suite'); }); });`

  **`tests/unit/waveAggregates.test.js`:**
  - Header: `/** @file Wave aggregate computation unit tests (WAVE-01..06). Stub — fleshed out in plan 04-04. */`
  - Import `computeWaveAggregates` from `../../js/domain/waveAggregates.js`
  - One placeholder: `describe('computeWaveAggregates', () => { it('stub — plan 04-04', () => { assert.fail('Plan 04-04 must flesh out this test suite'); }); });`

  **`tests/unit/builders.catalog.test.js`:**
  - Header: `/** @file Catalog view builder unit tests (CATALOG-01..07). Stub — fleshed out in plan 04-08. */`
  - Import from `../../js/views/catalog/builders.js` (buildCatalogHeader, buildHabitListItem, buildEditPanel, buildCreatePanel)
  - One placeholder describe block.

  **`tests/unit/builders.history.test.js`:**
  - Header: `/** @file History view builder unit tests (HISTORY-01..06). Stub — fleshed out in plan 04-09. */`
  - Import from `../../js/views/history/builders.js` (buildHistoryHeader, buildHistoryHabitRow, buildBulkActionBar)
  - One placeholder describe block.

  **`tests/unit/builders.today.numeric.test.js`:**
  - Header: `/** @file Today view numeric/slot builder unit tests (LOG-02..06). Stub — fleshed out in plan 04-09. */`
  - Import `buildNumericRow`, `buildSlotRow` from `../../js/views/today/builders.js`
  - One placeholder describe block.

  Note: `tests/unit/date.test.js` and `tests/unit/cadence.test.js` already exist from Phase 3 and are extended in-place by plan 04-01 — do NOT create stubs for them.
  </action>

  <verify>
    <automated>node --test tests/unit/mastery.test.js tests/unit/stage.test.js tests/unit/waveAggregates.test.js 2>&1 | grep -v "^$" | head -20</automated>
  </verify>

  <done>
  - All 6 unit stub files exist on disk
  - Each file has correct import paths and at least one assert.fail test
  - Running `node --test tests/unit/mastery.test.js` outputs a test failure (not a file-not-found error)
  </done>
</task>

<task type="auto">
  <name>Task 2: Integration test stubs — catalog-flow, history-flow, stage-advancement, habit-versions, wave-aggregates, mastery-cadence</name>
  <files>tests/integration/catalog-flow.test.js, tests/integration/history-flow.test.js, tests/integration/stage-advancement.test.js, tests/integration/habit-versions.test.js, tests/integration/wave-aggregates.test.js, tests/integration/mastery-cadence.test.js</files>

  <read_first>
  - `tests/integration/apply.markCompleted.test.js` — read ALL; understand createFakeRepo, configure, and apply wiring pattern used in integration stubs
  - `tests/integration/apply.createHabit.test.js` — check if it exists from plan 04-05 (likely does not yet); if not, read `apply.setSetting.test.js` for integration test setup pattern
  </read_first>

  <action>
  Create each integration stub using the same node:test + createFakeRepo pattern. Read `tests/integration/apply.markCompleted.test.js` to confirm exact setup import paths, then apply the pattern.

  **`tests/integration/catalog-flow.test.js`:**
  - Header: `/** @file End-to-end catalog flow integration test (CATALOG-01..07). Stub — fleshed out in plan 04-05/04-06. */`
  - Import helpers and placeholder: `describe('Catalog flow', () => { it('stub — plans 04-05 and 04-06 implement this', () => { assert.fail('Plans 04-05 and 04-06 must flesh out this integration test'); }); });`

  **`tests/integration/history-flow.test.js`:**
  - Header: `/** @file End-to-end history navigation flow integration test (HISTORY-01..06). Stub — fleshed out in plan 04-09. */`
  - Placeholder describe with assert.fail.

  **`tests/integration/stage-advancement.test.js`:**
  - Header: `/** @file Stage advancement trigger integration test (STAGE-01..07). Stub — fleshed out in plan 04-06. */`
  - Placeholder describe with assert.fail.

  **`tests/integration/habit-versions.test.js`:**
  - Header: `/** @file History integrity integration test — logs survive definition edits (NFR-10). Stub — fleshed out in plan 04-05. */`
  - Placeholder describe with assert.fail.

  **`tests/integration/wave-aggregates.test.js`:**
  - Header: `/** @file Wave aggregate computation integration test (WAVE-01..06). Stub — fleshed out in plan 04-04/04-06. */`
  - Placeholder describe with assert.fail.

  **`tests/integration/mastery-cadence.test.js`:**
  - Header: `/** @file Mastery evaluation with cadence-aware denominator integration test (MASTERY-01..07). Stub — fleshed out in plan 04-02/04-06. */`
  - Placeholder describe with assert.fail.
  </action>

  <verify>
    <automated>node --test tests/integration/catalog-flow.test.js tests/integration/habit-versions.test.js 2>&1 | grep -v "^$" | head -20</automated>
  </verify>

  <done>
  - All 6 integration stub files exist on disk
  - Each file is syntactically valid JS (no parse errors)
  - Running individual stub files produces test failures (not syntax errors or file-not-found)
  - Full suite `npm test` does not regress existing 281 tests — stubs add 12 failing tests which will become green as Wave 1+ plans complete
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stubs / no production code | Wave 0 only creates test stubs; no production code or IDB writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00 | Tampering | Stub assert.fail tests accidentally committed as green | accept | assert.fail always throws; CI will catch if stubs are shipped as-is past Wave 0 |
</threat_model>

<verification>
After Wave 0 completes:
- `ls tests/unit/mastery.test.js tests/unit/stage.test.js tests/unit/waveAggregates.test.js` — all exist
- `ls tests/integration/catalog-flow.test.js tests/integration/habit-versions.test.js tests/integration/wave-aggregates.test.js tests/integration/mastery-cadence.test.js` — all exist
- `node --test tests/unit/mastery.test.js` — exits with failure (RED state established, not file-not-found)
- `npm test` — prior 281 tests green; 12 new stubs show as failing (expected Wave 0 state)
</verification>

<success_criteria>
- 12 new stub test files exist (6 unit + 6 integration)
- Each stub imports its module under test and declares at least one assert.fail placeholder
- Existing 281 tests remain green
- Running any stub file produces a test failure (not a Node module-not-found error)
</success_criteria>

<output>
Create `.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-00-SUMMARY.md` when done.
</output>
