---
phase: 04-domain-model-cadence-catalog-stages-mastery-multi-occurrence
plan: 07
type: execute
wave: 2
depends_on: []
files_modified:
  - js/state/apply.js
  - js/state/apply/setMasteryThreshold.js
  - js/state/apply/setMasteryWindow.js
  - js/views/settings.js
  - js/views/settings/builders.js
  - css/settings.css
  - tests/integration/apply.setMasteryThreshold.test.js
  - tests/integration/settings.masterySettings.test.js
autonomous: true
requirements:
  - MASTERY-01
  - MASTERY-02
  - SETTINGS-01

must_haves:
  truths:
    - "setMasteryThreshold handler writes settings row {key:'masteryThreshold', value:N} and is undoable (self-inverting like setSetting)"
    - "setMasteryWindow handler writes settings row {key:'masteryWindow', value:N} and is undoable"
    - "Settings view has a new 'Mastery' card with threshold % and window (days) fields"
    - "Settings mastery card reads current values from store.getCachedSettings() or repo.getSetting()"
    - "Changing threshold or window via Settings fires apply({type:'setMasteryThreshold'}) or apply({type:'setMasteryWindow'})"
    - "Default threshold=90, default window=70 displayed when no override is set"
  artifacts:
    - path: "js/state/apply/setMasteryThreshold.js"
      provides: "handleSetMasteryThreshold + broadcastKeys"
      exports: ["handleSetMasteryThreshold"]
    - path: "js/state/apply/setMasteryWindow.js"
      provides: "handleSetMasteryWindow + broadcastKeys"
      exports: ["handleSetMasteryWindow"]
    - path: "js/views/settings.js"
      provides: "mountSettings extended with Mastery card"
    - path: "js/views/settings/builders.js"
      provides: "buildMasteryCard pure builder"
      exports: ["buildMasteryCard"]
  key_links:
    - from: "js/views/settings.js"
      to: "apply setMasteryThreshold / setMasteryWindow"
      via: "action closures wired in mountSettings"
      pattern: "setMasteryThreshold"
    - from: "js/views/settings/builders.js buildMasteryCard"
      to: "settings store cache"
      via: "masteryThreshold and masteryWindow read from store.getCachedSettings()"
      pattern: "masteryThreshold"
---

<objective>
Add two mastery-settings apply handlers (setMasteryThreshold, setMasteryWindow) and extend the Settings view with a new "Mastery" card that displays and edits the global threshold % and window days. This implements SETTINGS-01 and the configurable defaults for MASTERY-01.

Purpose: Settings for mastery threshold/window follow the exact same setSetting pattern already established in Phase 3 — extend that pattern to two new keys. The Mastery card follows the existing settings card builder pattern (pure builder + mounter composition).

Output: 2 handler files, settings.js extended, settings/builders.js extended, new tests — all tests green.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-CONTEXT.md
@js/state/apply/setSetting.js
@js/views/settings.js
@js/views/settings/builders.js
@css/settings.css
@tests/integration/settings.mount.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: setMasteryThreshold + setMasteryWindow apply handlers + tests</name>
  <files>js/state/apply/setMasteryThreshold.js, js/state/apply/setMasteryWindow.js, js/state/apply.js, tests/integration/apply.setMasteryThreshold.test.js</files>

  <read_first>
  - `js/state/apply/setSetting.js` — read ALL; the mastery handlers are near-identical (same self-inverting pattern, D-75)
  - `js/state/apply.js` — HANDLERS table, imports section
  - `tests/integration/apply.setSetting.test.js` — canonical integration test shape to replicate
  </read_first>

  <behavior>
  Both handlers are self-inverting like `setSetting` (D-75):
  - `setMasteryThreshold` payload: `{value: number}` (e.g., 85 for 85%)
  - `setMasteryWindow` payload: `{value: number}` (e.g., 60 for 60 days)
  - Each: reads `prior = await repo.getSetting(key)?.value`; writes `{key, value}` to settings store; inverse = `{type: 'setMasteryThreshold'|'setMasteryWindow', payload: {value: prior ?? undefined}}`.
  - Undo of first-ever write sets value back to `undefined` (same behavior as setSetting D-75).
  </behavior>

  <action>
  Create `js/state/apply/setMasteryThreshold.js`:
  - Header: `/** @file setMasteryThreshold handler — global mastery % configurable (SETTINGS-01, MASTERY-01, D-86). Self-inverting like setSetting (D-75). */`
  - Copy setSetting.js pattern verbatim but key='masteryThreshold'.
  - `export async function handleSetMasteryThreshold(event, repo)`.
  - broadcastKeys: `() => ({key: 'masteryThreshold'})`.

  Create `js/state/apply/setMasteryWindow.js`:
  - Header: `/** @file setMasteryWindow handler — global mastery window days configurable (SETTINGS-01, MASTERY-01, D-86). */`
  - Same pattern with key='masteryWindow'.

  Extend `js/state/apply.js`: import and register `setMasteryThreshold` and `setMasteryWindow` in HANDLERS.

  Write `tests/integration/apply.setMasteryThreshold.test.js`:
  - Apply setMasteryThreshold with value:85 → settings store has `{key:'masteryThreshold', value:85}`.
  - Apply setMasteryWindow with value:60 → settings store has `{key:'masteryWindow', value:60}`.
  - Apply setMasteryThreshold again (undo) with value:undefined → settings store row has value:undefined (reverts to default).
  </action>

  <verify>
    <automated>npm test -- tests/integration/apply.setMasteryThreshold.test.js</automated>
  </verify>

  <done>
  Both handlers write correct settings rows; undo works; integration test green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings mastery card — builder + mounter wiring + integration test</name>
  <files>js/views/settings/builders.js, js/views/settings.js, css/settings.css, tests/integration/settings.masterySettings.test.js</files>

  <read_first>
  - `js/views/settings/builders.js` — read ALL existing builders (5 cards: Storage, Schedule, Install, Data, About); replicate the pure-builder pattern for the new Mastery card
  - `js/views/settings.js` — read ALL; understand how mountSettings composes cards + subscribes to store notify + wires action closures; understand replaceCardChildren and how Settings cards live-refresh
  - `css/settings.css` — read existing card styles; check if new mastery-threshold/window input styles are needed
  - `tests/integration/settings.mount.test.js` — read ALL; understand test setup (fake bundle, fake repo, fake store, setAmbientDoc)
  - `tests/integration/settings.dataCard.test.js` — read for card refresh test pattern
  </read_first>

  <action>
  **`js/views/settings/builders.js` — add `buildMasteryCard`:**
  - Export `buildMasteryCard({masteryThreshold, masteryWindow})` — pure function returning a description tree.
  - Card structure: section.settings-card[data-card="mastery"] with h2 "Mastery" and two rows:
    - Row 1: label "Completion threshold" + number input (`data-key="masteryThreshold"`, min=1, max=100, value=masteryThreshold ?? 90, unit label "%")
    - Row 2: label "Rolling window" + number input (`data-key="masteryWindow"`, min=1, max=365, value=masteryWindow ?? 70, unit label "days")
  - Inputs use `type="number"` with step=1. Change events will be wired by the mounter.
  - Follow exact description-tree format from existing builders: `{tag, attrs, children}` shape, no DOM calls, no innerHTML (D-78).

  **`js/views/settings.js` — extend mountSettings:**
  - In `mountSettings(parent, {repo, store})`, after the existing 5 cards, add mastery card composition:
    1. Read current values: `const threshold = store.getCachedSettings()?.masteryThreshold ?? null` and `masteryWindow ?? null`.
    2. Call `buildMasteryCard({masteryThreshold: threshold, masteryWindow: window_val})`.
    3. Mount via `mount(desc, parent, actions)` where actions wire `'change'` events on the two inputs to call `apply({type:'setMasteryThreshold', payload:{value: parseInt(input.value)}})` and `apply({type:'setMasteryWindow', ...})`.
    4. Subscribe to store notify for live refresh: on change to 'masteryThreshold' or 'masteryWindow' keys, call `replaceCardChildren(cardEl, buildMasteryCard({...updated}), actions)`.

  Verify store.getCachedSettings() returns an object with the mastery keys. If the store cache doesn't include mastery settings yet, extend `store.js` cache hydration: in `hydrate()`, also read `repo.getSetting('masteryThreshold')` and `repo.getSetting('masteryWindow')` and store in `cache.settings`. (Minimal: if cache.settings is a Map, add the two keys.)

  **`css/settings.css`:** add styles for `.settings-card[data-card="mastery"]` and `.mastery-row` if needed (follow existing card pattern).

  Write `tests/integration/settings.masterySettings.test.js`:
  - Use the fake bundle + setAmbientDoc pattern from settings.mount.test.js.
  - mountSettings renders a card with `data-card="mastery"` in the DOM.
  - Card contains two number inputs: one with data-key="masteryThreshold" and one with data-key="masteryWindow".
  - Default values displayed: threshold=90, window=70 when no settings row in store.
  - Changing threshold input fires apply call with type:'setMasteryThreshold'.
  </action>

  <verify>
    <automated>npm test -- tests/integration/settings.masterySettings.test.js</automated>
  </verify>

  <done>
  - Settings view renders Mastery card with threshold + window inputs
  - Inputs show default values (90%, 70 days) when no override set
  - Change events wire to correct apply types
  - `npm test` full suite green
  </done>
</task>

</tasks>

<acceptance_criteria>
- `npm test -- tests/integration/apply.setMasteryThreshold.test.js` green
- `npm test -- tests/integration/settings.masterySettings.test.js` green
- `npm test` full suite green
- settings/builders.js exports `buildMasteryCard` as a pure function (no DOM ops, no imports from apply.js)
- Settings view DOM has `section[data-card="mastery"]` after mountSettings is called
- Two number inputs in mastery card: `[data-key="masteryThreshold"]` and `[data-key="masteryWindow"]`
- Default values 90 and 70 displayed when no IDB settings rows exist
</acceptance_criteria>

## Artifacts this phase produces

**New files:**
- `js/state/apply/setMasteryThreshold.js` — `handleSetMasteryThreshold` with `broadcastKeys`
- `js/state/apply/setMasteryWindow.js` — `handleSetMasteryWindow` with `broadcastKeys`

**Extended files:**
- `js/state/apply.js` — HANDLERS extended with `setMasteryThreshold`, `setMasteryWindow`
- `js/views/settings/builders.js` — new export: `buildMasteryCard({masteryThreshold, masteryWindow})`
- `js/views/settings.js` — Mastery card composed and wired in `mountSettings`
- `css/settings.css` — mastery card styles

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mastery threshold input → IDB settings | Number input value enters IDB as a setting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | invalid threshold/window values (0, negative, > 100) | mitigate | HTML input min/max attributes restrict range; apply handler uses value as-is (no business logic on it), but view only calls apply on valid change event |
| T-04-07b | Tampering | XSS via setting value rendered in settings card | accept | Settings card renders values as input values (not innerHTML); safe |
</threat_model>

<output>
Create `.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-07-SUMMARY.md` when done.
</output>
