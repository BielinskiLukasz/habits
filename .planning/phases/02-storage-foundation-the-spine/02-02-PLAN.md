---
phase: 02-storage-foundation-the-spine
plan: 02
type: tdd
wave: 2
depends_on:
  - 02-01
status: pending
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-05
files_modified:
  - js/db/idb.js
  - js/db/schema.js
  - js/db/repo.js
  - tests/unit/schema.test.js
  - tests/integration/repo.roundtrip.test.js
  - tests/integration/contract.fake-vs-real.test.js
tags:
  - indexeddb
  - schema
  - migrations
  - repo
  - tdd
must_haves:
  truths:
    - "Opening the database creates exactly 7 stores observable in DevTools → Application → IndexedDB → habits: `habits`, `habit_versions`, `logs`, `events`, `settings`, `meta`, `score_snapshots` (D-39, DATA-02, D-42 + ARCHITECTURE.md §3)"
    - "A simulated v1 → v2 upgrade preserves every existing row in v1 stores: tests/unit/schema.test.js walks the MIGRATIONS dispatch (one entry today, v1) and asserts every `createObjectStore` call shape matches the locked schema before any future v2 ships (DATA-02, Pitfall 10)"
    - "`events` rows are addressable by UUID — putting an event with `id: 'evt-uuid-1'` and then getting by that id returns the same row (no autoincrement renumbering) (D-42)"
    - "`score_snapshots` is queryable in DevTools immediately after v1 install (zero rows, indexes `date` + `habitId` present) so P6 starts writing without a v2 migration (D-39, Pitfall 6)"
    - "A log round-trip via fake-IDB preserves the `definitionVersion` field (default null = 'current') — proven in repo.roundtrip.test.js (DATA-05, structurally enforced from row 1)"
    - "A habit_versions row keyed `[habitId, effectiveFrom]` round-trips through the schema (DATA-05 — definition-version preservation supported even though editHabit ships in P3)"
    - "Opening the database connects to `DB_NAME = 'habits'` — visible as the DB name in DevTools and asserted by manual smoke (D-30)"
    - "`js/db/repo.js` exports the same function names as `tests/helpers/fake-idb.js` (minus the `_stores` internal slot) — enforced by tests/integration/contract.fake-vs-real.test.js (A7 contract)"
    - "A new log row with no `definitionVersion` field still round-trips successfully (forward-compat); the canonical shape with `definitionVersion: null` ALSO round-trips (DATA-05)"
    - "`tests/integration/repo.surface.test.js` passes — every name in the expected export set (`getHabit`, `putHabit`, …, `runTx`) is a function on the real repo module (W3 driver for RED→GREEN on Task 3)"
  artifacts:
    - path: "js/db/schema.js"
      provides: "DB_VERSION + MIGRATIONS dispatch table (DATA-02)"
      exports: ["DB_VERSION", "MIGRATIONS"]
      contains: "DB_VERSION = 1"
    - path: "js/db/idb.js"
      provides: "Promise wrapper around IndexedDB; only module that touches `indexedDB` (Anti-Pattern 1)"
      exports: ["openDB", "promisify", "done", "runTx", "get", "getAll", "put", "del", "indexGetAll"]
      contains: "DB_NAME = 'habits'"
    - path: "js/db/repo.js"
      provides: "Typed CRUD facade over idb.js — every store has typed get/put helpers (DATA-01)"
      exports: ["getHabit", "putHabit", "putLog", "getLog", "putEvent", "getEvent", "getMeta", "putMeta", "putSetting", "getSetting", "runTx"]
    - path: "tests/unit/schema.test.js"
      provides: "DATA-02 + DATA-05 schema introspection coverage"
    - path: "tests/integration/repo.roundtrip.test.js"
      provides: "DATA-01 round-trip coverage via fake-IDB"
    - path: "tests/integration/contract.fake-vs-real.test.js"
      provides: "A7 mitigation — asserts fake-idb and repo.js export the same function names"
  key_links:
    - from: "js/db/schema.js"
      to: "js/db/idb.js"
      via: "named exports `DB_VERSION`, `MIGRATIONS`"
      pattern: "import \\{ DB_VERSION, MIGRATIONS \\} from './schema.js'"
    - from: "js/db/repo.js"
      to: "js/db/idb.js"
      via: "promise wrappers `openDB`, `runTx`, `get`, `put`, `del`, `indexGetAll`"
      pattern: "from './idb.js'"
    - from: "tests/integration/repo.roundtrip.test.js"
      to: "tests/helpers/fake-idb.js"
      via: "named import `createFakeRepo`"
      pattern: "createFakeRepo"
---

<objective>
Ship the IndexedDB plumbing layer: the schema declaration (`js/db/schema.js`), the promise wrapper (`js/db/idb.js`), and the typed facade (`js/db/repo.js`). Lock the seven-store layout (D-39) with UUID-keyed events (D-42) and the `definitionVersion`-aware log row shape (DATA-05). Pin the A7 fake-vs-real contract so the test fake never silently drifts from the real facade.

Purpose: Phase 2's storage spine sits on these three modules. The schema declared here is the v1 schema FOREVER — `score_snapshots` is declared empty so P6 doesn't need a v2 migration (Pitfall 6). The repo facade defines the surface every later plan (apply.js, seed.js, undo.js) writes against. The fake-vs-real contract test (A7) is the single most important defense against false-pass tests in this codebase.

Output:
- `js/db/schema.js` (DB_VERSION + MIGRATIONS for v1 seven-store layout)
- `js/db/idb.js` (~80-line promise wrapper, only module that touches `indexedDB`)
- `js/db/repo.js` (typed facade matching fake-idb surface exactly)
- `tests/unit/schema.test.js` (schema introspection, MIGRATIONS dispatch math)
- `tests/integration/repo.roundtrip.test.js` (put + get round-trip via fake-IDB)
- `tests/integration/contract.fake-vs-real.test.js` (A7 surface-contract assertion)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md
@.planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md
@.planning/phases/02-storage-foundation-the-spine/02-PATTERNS.md
@.planning/phases/02-storage-foundation-the-spine/02-VALIDATION.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@tests/helpers/fake-idb.js
@js/util/version.js
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application code → IndexedDB | Origin-scoped storage; only same-origin code reaches the DB |
| fake-idb test surface → real repo.js | A7 contract risk — if the two drift, tests can silently false-pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Future v1 → v2 migration | mitigate | `MIGRATIONS` dispatched via explicit loop `for (let v = oldVersion + 1; v <= newVersion; v++) MIGRATIONS[v](db, tx)` per Pitfall 10 — NOT a switch-fallthrough. Additive-only by convention (no `deleteObjectStore`, no `deleteIndex`); locked in JSDoc header of schema.js. |
| T-02-08 | Tampering | `events` store identity | mitigate | `events.keyPath = 'id'` (UUID per D-42), NOT autoincrement. Cross-device import safety + consistency with `habits`/`habit_versions`. Schema test asserts the keypath shape. |
| T-02-09 | Tampering | A7 fake-vs-real drift | mitigate | `tests/integration/contract.fake-vs-real.test.js` imports both modules and asserts identical export-name sets. Any drift fails CI before merge. |
| T-02-AP1 | Tampering | Anti-Pattern 1 — non-idb.js module calling `indexedDB.open` | mitigate | Grep gate in tests/unit/schema.test.js (or a discipline subtest): walk every `.js` under `js/` excluding `js/db/idb.js` and assert zero matches for `indexedDB\.open\(` (line-comments stripped). Enforces "`js/db/idb.js` is the only module that calls `indexedDB.open`" as a structural invariant (was a must_have truth — moved here per W4). |
| T-02-AP4 | Tampering | Anti-Pattern 4 — switch-statement dispatch in apply.js | mitigate | Carried forward to plan 02-03 threat_model (where apply.js ships). 02-03 `tests/unit/apply.discipline.test.js` enforces "`apply.js` contains `HANDLERS = {` AND does NOT contain `\bswitch\s*\(`". |
| T-02-02 | Tampering | IDB tampering by another origin sharing localhost on dev | accept | IndexedDB is origin-scoped by browser policy. Documented as platform-given. Severity: low. |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: js/db/schema.js + tests/unit/schema.test.js (RED → GREEN → REFACTOR)</name>
  <files>tests/unit/schema.test.js, js/db/schema.js</files>
  <behavior>
    RED — write `tests/unit/schema.test.js` first asserting:
    - `describe('DB_VERSION')`:
      - `DB_VERSION === 1`
      - `typeof DB_VERSION === 'number'`
    - `describe('MIGRATIONS')`:
      - `MIGRATIONS` is an object with a key `1` whose value is a function
      - `Object.keys(MIGRATIONS)` contains exactly `['1']` at this point
      - calling `MIGRATIONS[1](mockDb, null)` against a mock IDB-style `db` (with `createObjectStore` + `createIndex` spies) results in exactly 7 `createObjectStore` calls with the following `(name, options)` argument shapes:
        - `('habits', { keyPath: 'id' })`
        - `('habit_versions', { keyPath: ['habitId', 'effectiveFrom'] })`
        - `('logs', { keyPath: ['habitId', 'date'] })`
        - `('events', { keyPath: 'id' })` — UUID per D-42, NOT autoincrement
        - `('settings', { keyPath: 'key' })`
        - `('meta', { keyPath: 'key' })`
        - `('score_snapshots', { keyPath: ['habitId', 'date'] })` — D-39 declared in v1
      - the `habits` store creates indexes named exactly `wave` and `status`
      - the `habit_versions` store creates an index named `habitId`
      - the `logs` store creates indexes named `date` and `habitId`
      - the `events` store creates indexes named `at`, `type`, and `habitId`
      - the `score_snapshots` store creates indexes named `date` and `habitId`
    Implement the mock `db` as a small in-test factory: `function mockDb() { const stores = []; return { createObjectStore(name, opts) { const indexes = []; stores.push({ name, opts, indexes }); return { createIndex(iname, _path) { indexes.push(iname); return this; } }; }, _stores: stores }; }`.

    Run `node --test tests/unit/schema.test.js` — RED.

    GREEN — implement `js/db/schema.js` per 02-PATTERNS.md §`js/db/schema.js` lines 166-191 + RESEARCH §Code Examples §Pattern 2. Re-run, all green.
  </behavior>
  <read_first>
    - .planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md (§Code Examples Pattern 2 at lines 396-432; §Pitfall 6 score_snapshots in v1; §Pitfall 10 explicit-loop dispatch)
    - .planning/phases/02-storage-foundation-the-spine/02-PATTERNS.md (§`js/db/schema.js` lines 160-194 — verbatim seven-store declaration with the D-39 + D-42 deltas noted)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-39 score_snapshots in v1; D-42 events keyed by UUID; D-30 DB name 'habits')
    - .planning/research/ARCHITECTURE.md (§3 IndexedDB Schema — but note RESEARCH §State of the Art lines 919-920: D-39 + D-42 supersede the 6-store autoincrement sketch)
  </read_first>
  <action>
    Strict RED → GREEN. First, create `tests/unit/schema.test.js` with the mock-db factory described in `<behavior>` and ALL listed assertions. Imports: `import { test, describe } from 'node:test'; import assert from 'node:assert/strict'; import { DB_VERSION, MIGRATIONS } from '../../js/db/schema.js';`. Run — confirm RED (`ERR_MODULE_NOT_FOUND`). Commit `test(02-02): add failing schema tests (DATA-02, DATA-05) (RED)`.

    Implement `js/db/schema.js` per 02-PATTERNS.md §`js/db/schema.js` lines 166-191 verbatim except for the JSDoc file header (use the template at lines 167 referencing DATA-02, Pitfall 10, D-39, D-42). Export `DB_VERSION = 1` and `MIGRATIONS = { 1: (db, _tx) => { ... } }` with the seven `createObjectStore` calls in the order listed in `<behavior>`. Add JSDoc `@type` annotations per D-27.

    Forbidden in this file: any `case`/`switch` migration pattern (Pitfall 10 — loop only), any `deleteObjectStore` or `deleteIndex` call (additive-only), any `autoIncrement: true` flag (D-42).

    Run `node --test tests/unit/schema.test.js` — green. Commit `feat(02-02): implement js/db/schema.js with 7-store v1 schema (DATA-02, DATA-05) (GREEN)`.
  </action>
  <verify>
    <automated>node --test tests/unit/schema.test.js</automated>
  </verify>
  <done>
    Test file exits 0 with all 7 store assertions + index assertions passing. `js/db/schema.js` exports `DB_VERSION` (value `1`) and `MIGRATIONS` (object with key `1` mapped to a function). Grep `js/db/schema.js` for `autoIncrement` — zero matches. Grep for `switch` — zero matches. Two commits (RED + GREEN) exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: js/db/idb.js — promise wrapper + onupgradeneeded dispatch</name>
  <files>js/db/idb.js</files>
  <read_first>
    - .planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md (§Code Examples lines 776-835 verbatim; §Pitfall 1 "Awaiting Unrelated Async API Mid-Transaction"; §Pitfall 2 "Broadcasting Before tx.done"; §Don't Hand-Roll row "Manual IDB transaction commits")
    - .planning/phases/02-storage-foundation-the-spine/02-PATTERNS.md (§`js/db/idb.js` lines 127-156 with the JSDoc-header template + the four-invariant block)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-30 DB_NAME = 'habits'; D-39 + D-42 schema deltas)
    - js/db/schema.js (just created — `DB_VERSION` + `MIGRATIONS` imports)
  </read_first>
  <action>
    Create `js/db/idb.js` per RESEARCH §Code Examples lines 776-835 verbatim. Open with a JSDoc file header per 02-PATTERNS.md §`js/db/idb.js` lines 133-152 — cross-references D-30, D-39, D-42, ARCHITECTURE §3, STACK.md §IndexedDB Approach, and explicitly states the Pitfall 1 invariant: "NEVER `await` an unrelated async API inside a tx body — IDB auto-commits on idle tick."

    Exports (per RESEARCH lines 786-832):
    - `const DB_NAME = 'habits'` (D-30) — internal const, not exported.
    - `openDB()` — returns `Promise<IDBDatabase>`. Calls `indexedDB.open(DB_NAME, DB_VERSION)`. In `onupgradeneeded`, dispatches via the explicit loop `for (let v = e.oldVersion + 1; v <= e.newVersion; v++) MIGRATIONS[v](db, e.target.transaction);` per Pitfall 10. Resolves on `onsuccess`, rejects on `onerror`.
    - `promisify(req)` — wraps an `IDBRequest` as a Promise resolving with `req.result` on `onsuccess` and rejecting with `req.error` on `onerror`.
    - `done(tx)` — returns a Promise that resolves on `tx.oncomplete` and rejects on `tx.onerror` / `tx.onabort` (Pitfall 2 — source of truth for "data durable").
    - `runTx(db, stores, mode, body)` — opens `db.transaction(stores, mode)`, calls `body(tx)`, then `await done(tx)`. The body is responsible for sequencing ops; do NOT await anything unrelated inside (Pitfall 1).
    - Per-op helpers: `get(db, store, key)`, `getAll(db, store)`, `put(db, store, value)`, `del(db, store, key)`, `indexGetAll(db, store, indexName, query)`. Each opens a single-store readonly or readwrite tx as appropriate and uses `promisify` to await the request.

    All exports MUST have JSDoc `@param` / `@returns` annotations per D-27. The file MUST import `{ DB_VERSION, MIGRATIONS }` from `./schema.js` only — no other imports.

    Forbidden constructs: `tx.commit()` calls (use `await done(tx)` per RESEARCH §Don't Hand-Roll); any `await fetch(...)` or `await setTimeout(...)` inside a `runTx` body example; any direct `console.log` of habit content (RESEARCH §Security Domain V7).

    Note: this module is NOT directly tested by Node `node --test` (Pitfall 9 — Node lacks `indexedDB`). It is exercised in the real-browser smoke checklist (02-VALIDATION.md §Manual-Only Verifications) and indirectly by integration tests through the fake-IDB harness. The contract test in Task 4 enforces that the exported surface matches what fake-idb provides.
  </action>
  <verify>
    <automated>node -e "import('./js/db/idb.js').then(() => { console.log('module parses cleanly'); }, (err) => { /* In Node, the indexedDB ReferenceError happens only when openDB() is *called*, not at import time. */ if (String(err).includes('indexedDB is not defined')) { console.error('idb.js called indexedDB at import time — must defer'); process.exit(1); } else { console.log('module parses cleanly'); } })"</automated>
    <human-check>The file does not call `indexedDB` at module top-level (only inside exported functions). Verify with `Grep "^indexedDB\\.|^const .* = indexedDB" js/db/idb.js` — zero top-level matches.</human-check>
  </verify>
  <done>
    `js/db/idb.js` exports `openDB`, `promisify`, `done`, `runTx`, `get`, `getAll`, `put`, `del`, `indexGetAll`. File contains the literal substring `DB_NAME = 'habits'` (D-30). File imports from `./schema.js` only. Grep `js/db/idb.js` for `tx\.commit\(\)` — zero matches. Grep for `autoIncrement` — zero matches. JSDoc file header references D-30, D-39, D-42, and the Pitfall 1 invariant.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: js/db/repo.js + tests/integration/repo.roundtrip.test.js (RED → GREEN → REFACTOR)</name>
  <files>tests/integration/repo.surface.test.js, tests/integration/repo.roundtrip.test.js, js/db/repo.js</files>
  <behavior>
    RED — write `tests/integration/repo.roundtrip.test.js` first asserting (using `createFakeRepo` from `tests/helpers/fake-idb.js`):
    - `describe('repo: habit round-trip')`:
      - `await repo.putHabit({id: 'h1', wave: 1, status: 'active', name: 'Morning walk'})` then `await repo.getHabit('h1')` returns the exact same object (deep equality).
      - `await repo.getHabit('nonexistent')` returns `undefined`.
    - `describe('repo: log round-trip — definitionVersion field (DATA-05)')`:
      - `await repo.putLog({habitId: 'h1', date: '2026-05-26', completed: true, definitionVersion: null})` then `await repo.getLog('h1', '2026-05-26')` returns the same row.
      - The log row INCLUDES `definitionVersion: null` (means "current"); a log row WITHOUT a `definitionVersion` field is still accepted (forward-compat) but tests assert the canonical shape includes it.
      - Re-marking the same `(habitId, date)` overwrites the same row (compound key idempotency per ARCHITECTURE §3).
    - `describe('repo: event round-trip — UUID key (D-42)')`:
      - `await repo.putEvent({id: 'evt-uuid-1', at: '2026-05-26T10:00:00.000Z', type: 'markCompleted', payload: {habitId: 'h1', date: '2026-05-26'}, inverse: null})` then `await repo.getEvent('evt-uuid-1')` returns the same row.
    - `describe('repo: meta + settings — singleton-key shape')`:
      - `await repo.putMeta('undoToken', 'evt-uuid-1')` then `await repo.getMeta('undoToken')` returns `'evt-uuid-1'`.
      - `await repo.putSetting({key: 'defaultThreshold', value: 0.9})` then `await repo.getSetting('defaultThreshold')` returns the same row.

    Per RESEARCH §Pitfall 9, this test uses `createFakeRepo` from `tests/helpers/fake-idb.js` exclusively — NEVER imports `js/db/repo.js` directly (Node has no `indexedDB`). The contract test in Task 4 separately enforces that `repo.js` and `fake-idb.js` expose the same surface.

    Run — RED naturally caused by `js/db/repo.js` not yet existing. The companion test `tests/integration/repo.surface.test.js` (see Task 3a) makes the missing-module RED state explicit by importing `* as repo from ../../js/db/repo.js` and asserting the public surface (`typeof repo.putHabit === 'function'`, etc.) — this fails at module-resolve time until repo.js exists. The fake-IDB roundtrip test stays as-is for the in-memory contract behavior; no artificial wrong-assertion ritual.

    GREEN — implement `js/db/repo.js` so its export surface matches `tests/helpers/fake-idb.js`. The test continues to pass against the fake; the real repo.js implementation is exercised only in the manual browser smoke (02-VALIDATION.md). Commit.
  </behavior>
  <read_first>
    - .planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md (§Code Examples `tests/helpers/fake-idb.js` lines 843-874 — the surface contract; §Pitfall 9 "Tests Importing the Real `js/db/idb.js`"; §Architectural Responsibility Map row "Typed reads/writes (repo facade)")
    - .planning/phases/02-storage-foundation-the-spine/02-PATTERNS.md (§`js/db/repo.js` lines 197-214 — suggested export surface; §`tests/integration/repo.roundtrip.test.js` lines 757-822 — sleep-tracker analog + Habits composition)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (DATA-05: `definitionVersion` field on log rows; D-42 events UUID)
    - tests/helpers/fake-idb.js (just created — the contract surface)
    - js/db/idb.js (just created — the underlying primitives)
  </read_first>
  <action>
    RED — Create TWO test files in a single commit:

    (a) `tests/integration/repo.surface.test.js` — driver test that naturally fails RED until `js/db/repo.js` exists. Imports: `import { test, describe } from 'node:test'; import assert from 'node:assert/strict';` plus a dynamic import `const repo = await import('../../js/db/repo.js');` inside a top-level `describe`. Assertions: for each expected export name in `['getHabit', 'putHabit', 'putLog', 'getLog', 'putEvent', 'getEvent', 'getMeta', 'putMeta', 'getSetting', 'putSetting', 'runTx']` assert `typeof repo[name] === 'function'`. This test fails at module-resolve time with `ERR_MODULE_NOT_FOUND` until `js/db/repo.js` ships — that IS the RED state for this task (W3 — replaces the prior deliberately-broken-assertion ritual).

    (b) `tests/integration/repo.roundtrip.test.js` — in-memory contract test against the fake. Imports: `import { test, describe } from 'node:test'; import assert from 'node:assert/strict'; import { createFakeRepo } from '../helpers/fake-idb.js';` and ALL assertions listed in `<behavior>`. Use a `function makeRepo() { return createFakeRepo(); }` factory inside the file per 02-PATTERNS.md §`tests/integration/repo.roundtrip.test.js` lines 805-820.

    Run `node --test tests/integration/repo.surface.test.js tests/integration/repo.roundtrip.test.js` — confirm RED (surface test errors on missing module; roundtrip test passes against the fake). Commit as `test(02-02): add repo surface + round-trip tests (DATA-01, DATA-05) (RED on surface, GREEN on fake)`.

    GREEN — Create `js/db/repo.js`. JSDoc file header per D-27 referencing DATA-01, DATA-05, D-30, and Anti-Pattern 1 ("Views Calling repo.js Directly"). Import `{ openDB, runTx, get, getAll, put, del, indexGetAll, promisify }` from `./idb.js`. Export the typed surface matching `tests/helpers/fake-idb.js`:
    - `async function getHabit(id)` → `get(db, 'habits', id)` (await `openDB()` internally; this single-store helper opens its own readonly tx via `idb.js`)
    - `async function putHabit(h)` → opens a readwrite tx on `habits` and `put`s
    - `async function putLog(l)` → puts to `logs` using compound key (set by keyPath); l MUST carry `habitId`, `date`, `completed?`, `definitionVersion?` (default null when caller omits)
    - `async function getLog(habitId, date)` → `get(db, 'logs', [habitId, date])`
    - `async function putEvent(e)` → puts to `events` (keyPath `id` per D-42)
    - `async function getEvent(id)` → `get(db, 'events', id)`
    - `async function getMeta(key)` → returns the `value` field of the row `{key, value}` stored in `meta` (NOT the full row — match the fake's signature: `stores.meta.get(key)?.value`)
    - `async function putMeta(key, value)` → puts `{key, value}` to `meta`
    - `async function getSetting(key)` → returns the full row from `settings` (e.g. `{key, value}`)
    - `async function putSetting(s)` → puts `s` to `settings` (keyPath `key`)
    - `async function runTx(stores, mode, body)` → opens a tx via the underlying `idb.runTx(db, stores, mode, body)` where `body(tx)` is given the raw IDB tx (callers compose `tx.objectStore('logs').put(...)` etc.). Used by `apply.js` (plan 03) and `seed.js` (plan 04) for multi-store atomic writes.

    Re-run `node --test tests/integration/repo.roundtrip.test.js` — green (against the fake). Commit `feat(02-02): implement js/db/repo.js typed facade matching fake-IDB surface (DATA-01) (GREEN)`.

    Forbidden in repo.js: any direct `indexedDB.open` call (only `idb.js` may call that — Anti-Pattern 1); any `tx.commit()` call (use `await done(tx)` via `runTx`); any export missing from `tests/helpers/fake-idb.js` (A7 contract enforced by Task 4).
  </action>
  <verify>
    <automated>node --test tests/integration/repo.surface.test.js tests/integration/repo.roundtrip.test.js</automated>
  </verify>
  <done>
    Both `tests/integration/repo.surface.test.js` and `tests/integration/repo.roundtrip.test.js` exit 0. The surface test asserts every expected export name on the real `js/db/repo.js` is a function (W3 — RED until repo.js exists; GREEN after). `js/db/repo.js` exports `getHabit`, `putHabit`, `putLog`, `getLog`, `putEvent`, `getEvent`, `getMeta`, `putMeta`, `getSetting`, `putSetting`, `runTx`. File imports from `./idb.js` ONLY (no direct `indexedDB.*` reference outside of comments). Log round-trip asserts the `definitionVersion` field is preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: A7 fake-vs-real contract test</name>
  <files>tests/integration/contract.fake-vs-real.test.js</files>
  <behavior>
    - `describe('contract: tests/helpers/fake-idb.js vs js/db/repo.js')`:
      - Both modules export the same set of function names (compare via `new Set(Object.keys(realRepo))` vs the keys of `createFakeRepo()` minus the `_stores` internal slot).
      - Specifically, the intersection MUST contain at least: `getHabit`, `putHabit`, `putLog`, `getLog`, `putEvent`, `getEvent`, `getMeta`, `putMeta`, `getSetting`, `putSetting`, `runTx`.
      - The fake's `_stores` key is allowed (test-only escape hatch); any OTHER key present in only one and not the other is a contract violation.

    Per RESEARCH §Pitfall 9, importing `js/db/repo.js` in Node would fail at the first `await openDB()` call. The contract test imports the MODULE NAMESPACE (`import * as realRepo from '../../js/db/repo.js'`) which evaluates the module top-level but does NOT execute any function — so as long as `repo.js` has no top-level `indexedDB.*` reference (Task 2 enforces this for `idb.js`; the same discipline applies to `repo.js`), the import succeeds and `Object.keys` enumerates the exports.

    If `js/db/repo.js` is structured such that even module-load triggers `indexedDB` access (e.g., top-level `const db = await openDB()`), this test will fail at import time — that failure mode is exactly the discipline the contract test enforces. The fix is to defer the `openDB` call until the first repo function is invoked.
  </behavior>
  <read_first>
    - .planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md (§Assumptions Log A7 lines 932-933; §Pitfall 9 lines 643-648)
    - .planning/phases/02-storage-foundation-the-spine/02-VALIDATION.md (§Wave 0 Requirements — contract test is optional but A7 mitigation)
    - tests/helpers/fake-idb.js
    - js/db/repo.js (just created)
  </read_first>
  <action>
    Create `tests/integration/contract.fake-vs-real.test.js`:

    Imports (named import `test`, `describe` from `node:test`; `assert` default from `node:assert/strict`; `* as realRepo` from `../../js/db/repo.js`; `createFakeRepo` from `../helpers/fake-idb.js`).

    Assertions per `<behavior>`. Compute the expected surface as `EXPECTED = ['getHabit', 'putHabit', 'putLog', 'getLog', 'putEvent', 'getEvent', 'getMeta', 'putMeta', 'getSetting', 'putSetting', 'runTx']`. Assert every name in `EXPECTED` is in `Object.keys(realRepo)`. Assert every name in `EXPECTED` is in `Object.keys(createFakeRepo()).filter(k => !k.startsWith('_'))`. Assert the fake has NO unexpected non-`_` keys beyond `EXPECTED`. The real repo MAY have ADDITIONAL helpers (e.g. `getEventsByAt`, `getLogsByDate`) — only the EXPECTED-set intersection is enforced; missing-from-fake is the failure mode being prevented.

    Forbidden: any actual repo function call (`realRepo.getHabit('x')`) — that would throw `indexedDB is not defined` in Node.
  </action>
  <verify>
    <automated>node --test tests/integration/contract.fake-vs-real.test.js</automated>
  </verify>
  <done>
    `node --test tests/integration/contract.fake-vs-real.test.js` exits 0. If `js/db/repo.js` ever drops a method that exists in fake-idb, OR fake-idb ever adds a non-`_` method that repo.js doesn't have, this test will fail in CI — fulfilling A7 mitigation.
  </done>
</task>

<task type="auto">
  <name>Task 5: Wave 2 green-suite check</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/02-storage-foundation-the-spine/02-VALIDATION.md (§Sampling Rate)
  </read_first>
  <action>
    Run `node --test tests/`. ALL tests must pass: `_smoke`, `date`, `id` (from plan 01), `schema`, `repo.roundtrip`, `contract.fake-vs-real` (this plan). If any plan-01 tests regressed, fix immediately — do not advance to plan 03.
  </action>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    All six test files green. Tests counted: `_smoke` + `date` (10+ assertions) + `id` (5+ assertions) + `schema` (9+ assertions) + `repo.roundtrip` (7+ assertions) + `contract.fake-vs-real` (2+ assertions). Zero failing assertions.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/` exits 0 (all six test files green)
- `js/db/schema.js` declares `DB_VERSION = 1` and `MIGRATIONS[1]` creates exactly 7 stores
- `js/db/schema.js` contains zero `autoIncrement` matches and zero `switch` statements (loop-dispatch only per Pitfall 10)
- `js/db/idb.js` contains the literal substring `DB_NAME = 'habits'` (D-30) and imports `DB_VERSION`, `MIGRATIONS` from `./schema.js`
- `js/db/idb.js` exports `openDB`, `promisify`, `done`, `runTx`, plus per-op helpers
- `js/db/repo.js` exports a surface matching `tests/helpers/fake-idb.js` (enforced by contract test)
- `tests/integration/repo.roundtrip.test.js` asserts `definitionVersion` field is preserved on log rows (DATA-05 schema enforcement)
- `tests/integration/contract.fake-vs-real.test.js` enforces A7 surface contract
</verification>

<manual_verification>
**Real IDB persistence smoke (02-VALIDATION.md §Manual-Only Verifications — first opportunity to run):**

After this plan ships, the real `js/db/idb.js` is testable in a browser even though plan 04 (seed) hasn't shipped yet. Optional dry-run:

1. `node scripts/serve.js` then open `http://localhost:8080/` in Chrome.
2. In DevTools console: `(await import('./js/db/idb.js')).openDB().then(db => console.log('opened', db.name, db.version))`. Expected: `opened habits 1`.
3. DevTools → Application → IndexedDB → expand `habits` v1. Verify all 7 stores exist: `habits`, `habit_versions`, `logs`, `events`, `settings`, `meta`, `score_snapshots`.
4. Verify the `events` store keypath is `id` (NOT `++id`) — confirms D-42.
5. Verify the `score_snapshots` store exists and is empty — confirms D-39.

This is a developer-discretion dry-run; the formal manual smoke checklist runs at the phase gate per 02-VALIDATION.md.
</manual_verification>

<success_criteria>
- Schema v1 is the LOCKED layout for the remainder of the project (additive-only migrations only after this — Pitfall 10)
- The repo facade is the only surface views and state modules will ever touch (Anti-Pattern 1 enforced from this point on)
- The A7 fake-vs-real contract is enforced by CI from this commit forward
- Log row shape carries `definitionVersion` (DATA-05) from row 1 — no back-migration needed when `editHabit` ships in P3+
- `score_snapshots` is declared empty in v1 (D-39) — P6 starts writing to it without a v2 migration (Pitfall 6)
</success_criteria>

<output>
Create `.planning/phases/02-storage-foundation-the-spine/02-02-SUMMARY.md` listing the seven stores, the locked indexes, the repo facade surface, and the A7 contract test result. Cite D-30, D-39, D-42, DATA-01, DATA-02, DATA-05, Pitfall 1, Pitfall 6, Pitfall 9, Pitfall 10, Anti-Pattern 1.
</output>
