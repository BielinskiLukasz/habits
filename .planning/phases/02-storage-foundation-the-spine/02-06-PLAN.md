---
phase: 02-storage-foundation-the-spine
plan: 06
type: execute
wave: 6
depends_on:
  - 02-05
status: pending
autonomous: true
requirements: []
files_modified:
  - js/util/version.js
  - CLAUDE.md
  - .planning/PROJECT.md
  - README.md
  - .planning/research/ARCHITECTURE.md
tags:
  - docs
  - versioning
  - phase-gate
must_haves:
  truths:
    - "`js/util/version.js` exports `APP_VERSION = '0.2.0'` (D-28 + VERSIONING.md — Phase 2 completion bump from 0.1.0)"
    - "CLAUDE.md reflects D-30 (BroadcastChannel name `'habits'`) — any reference to `'nawyki'` channel is replaced"
    - "CLAUDE.md reflects D-35 (English habit names primary) — the legacy 'Polish habit names preserved verbatim' constraint is REPLACED with the English-primary + optional `name_pl` field rule per D-35 + D-40"
    - ".planning/PROJECT.md reflects D-35 — same constraint reversal as CLAUDE.md"
    - "README.md replaces the `python -m http.server` quick-start line with `node scripts/serve.js` (D-46)"
    - ".planning/research/ARCHITECTURE.md is forward-edited per RESEARCH §State of the Art lines 916-921: `BroadcastChannel('nawyki')` → `BroadcastChannel('habits')` (D-30); events autoincrement → events UUID keypath (D-42); 6 stores → 7 stores incl. score_snapshots (D-39); `js/util/id.js` 'file:// fallback' note refined per Pitfall 13"
    - "After APP_VERSION bump, `node --test tests/` is still green (no regression)"
    - "After APP_VERSION bump, the sw.js cache name resolves to `habits-0.2.0` (D-28 + D-29)"
  artifacts:
    - path: "js/util/version.js"
      provides: "APP_VERSION = '0.2.0' (Phase 2 ship signal per VERSIONING.md)"
      contains: "APP_VERSION = '0.2.0'"
    - path: "CLAUDE.md"
      provides: "Updated constraints reflecting D-30 + D-35"
    - path: ".planning/PROJECT.md"
      provides: "Updated constraints reflecting D-35"
    - path: "README.md"
      provides: "node scripts/serve.js as the canonical local-dev command (D-46)"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Forward-edits for D-30, D-39, D-42, Pitfall 13"
  key_links:
    - from: "js/util/version.js"
      to: "sw.js cache name (D-29)"
      via: "template `habits-${APP_VERSION}`"
      pattern: "APP_VERSION = '0.2.0'"
    - from: "README.md"
      to: "scripts/serve.js (D-46)"
      via: "documented as canonical local-dev command"
      pattern: "node scripts/serve.js"
---

<objective>
Close Phase 2 by: bumping `APP_VERSION` to `0.2.0` (VERSIONING.md / D-28); editing CLAUDE.md + .planning/PROJECT.md to reverse the now-stale "Polish habit names preserved verbatim" constraint (D-35) and reflect the `'habits'` namespace alignment (D-30); editing README.md to replace `python -m http.server` with `node scripts/serve.js` (D-46); forward-editing ARCHITECTURE.md for D-30 + D-39 + D-42 + Pitfall 13.

Purpose: docs-only edits + version bump that turn the work of plans 02-01..02-05 into the official "Phase 2 ships" signal. After this plan, the cache name is `habits-0.2.0`, a fresh deploy invalidates the old cache (sw.js activate handler), and downstream phases (P3+) inherit a documentation set that matches reality.

Output:
- `js/util/version.js` — APP_VERSION = '0.2.0'
- `CLAUDE.md` — D-30, D-35 reflected (constraint reversal)
- `.planning/PROJECT.md` — D-35 reflected (constraint reversal)
- `README.md` — `node scripts/serve.js` quick-start
- `.planning/research/ARCHITECTURE.md` — forward-edits for D-30, D-39, D-42, Pitfall 13
- Final `node --test tests/` green check
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md
@.planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md
@.planning/phases/02-storage-foundation-the-spine/02-05-SUMMARY.md
@VERSIONING.md
@js/util/version.js
@CLAUDE.md
@.planning/PROJECT.md
@README.md
@.planning/research/ARCHITECTURE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → developer mental model | Stale docs cause future plans to implement against the wrong constraints (D-35 reversal risk) |
| APP_VERSION → SW cache name | Version bump invalidates the prior cache; users on `0.1.0` get a clean transition to `0.2.0` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-DOC | Tampering / Information Disclosure | CLAUDE.md + PROJECT.md stale constraints | mitigate | Explicit edits to remove "Polish habit names preserved verbatim" (now superseded by D-35 + D-40). Verification: grep for the legacy substring — zero matches after this plan. |
| T-02-VER | Tampering | Cache-name collision | mitigate | Version bump `0.1.0` → `0.2.0` per VERSIONING.md table line "Phase 2 complete (Storage Foundation) → 0.2.0". sw.js activate handler (P1) deletes non-`habits-0.2.0` caches automatically. No manual cache reset required. |
| T-02-FWD | Tampering | ARCHITECTURE.md still says `BroadcastChannel('nawyki')` / autoincrement events / 6 stores | mitigate | Forward-edit per RESEARCH §State of the Art lines 916-921. After this plan, every architectural reference matches code. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Edit CLAUDE.md — reverse Polish-names constraint (D-35) + align BroadcastChannel name (D-30)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (the current full file — locate the "Polish habit names preserved verbatim" constraint line under `### Constraints`)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-30 + D-35 + D-40 — verbatim source for the new constraint phrasing)
    - .planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md (§State of the Art lines 911-914)
  </read_first>
  <action>
    Locate the "Polish habit names preserved verbatim (user data)" entry under `## Project → ### Constraints` in CLAUDE.md. REPLACE its bullet text with a new bullet that reads: bold label `UI language`, then "English UI chrome AND English habit names primary; Polish original optionally preserved as a per-habit `name_pl` field (D-35 + D-40, locked Phase 2)." followed by an italicized rationale that notes D-35 reverses the prior constraint and explains that the seed loader writes `{name: "Morning walk", name_pl: "Spacer rano"}` for source-derived habits, and that the quick-check surface for `name_pl` on Today / catalog is a P3 UI-SPEC decision.

    Then `Grep` CLAUDE.md for any remaining occurrence of the literal substring `BroadcastChannel('nawyki')` — if any exists in code blocks or comments, replace with `BroadcastChannel('habits')` per D-30 (note: CLAUDE.md's TL;DR table line uses `BroadcastChannel('nawyki')` — fix that). Also replace any literal substring `'nawyki'` channel-name reference in CLAUDE.md with `'habits'`.

    Also grep for the line referencing `python -m http.server` in CLAUDE.md "Installation" section (`# Option B: serve locally if you want to test the service worker`) and adjust the example commands: change `python -m http.server` to `node scripts/serve.js` (D-46).

    Do NOT touch any other section. Keep the file's structural shape intact.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('./CLAUDE.md', 'utf8'); if (c.includes('Polish habit names preserved verbatim')) { console.error('legacy Polish-names constraint still present'); process.exit(1); } if (c.includes(\"BroadcastChannel('nawyki')\")) { console.error('legacy BroadcastChannel name still present'); process.exit(1); } if (!c.includes('English habit names primary')) { console.error('D-35 new phrasing missing'); process.exit(1); } if (!c.includes('node scripts/serve.js')) { console.error('D-46 dev-server reference missing'); process.exit(1); } console.log('CLAUDE.md updates OK');"</automated>
  </verify>
  <done>
    CLAUDE.md contains the new D-35 phrasing ("English habit names primary"). Legacy substrings `'Polish habit names preserved verbatim'` and `BroadcastChannel('nawyki')` are gone. `node scripts/serve.js` appears in the Installation section.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit .planning/PROJECT.md — reverse Polish-names constraint (D-35)</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md (locate the "Polish habit names preserved verbatim" reference)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-35 + D-40 phrasing)
  </read_first>
  <action>
    Locate the "Polish habit names preserved verbatim (user data)" reference in `.planning/PROJECT.md`. Replace with phrasing matching CLAUDE.md's task 1 edit: a paragraph stating that UI language is English for both UI chrome AND habit names (D-35, locked Phase 2); that the `habits` store carries an optional `name_pl` string field (D-40) so each habit can record its Polish original alongside the English primary `name`; and that the seed loader writes both for source-derived habits.

    Also search for any `BroadcastChannel('nawyki')` reference and replace with `BroadcastChannel('habits')` (D-30) — though the discussion log notes the original PROJECT.md primarily references `'nawyki'` IDB DB name, which D-30 also reverses to `'habits'`. Update IDB DB-name references to `'habits'` consistently.

    Update the Key Decisions table (if present) to add or update entries for D-30, D-35, D-39, D-40, D-42 if those rows don't already exist. If a Key Decisions table doesn't exist in PROJECT.md (it lives in STATE.md per the original setup), skip this part and only edit the constraint paragraph.

    Do NOT touch unrelated sections.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('./.planning/PROJECT.md', 'utf8'); if (c.includes('Polish habit names preserved verbatim')) { console.error('legacy constraint still present'); process.exit(1); } if (!c.includes('English') || !c.includes('name_pl')) { console.error('D-35 + D-40 phrasing missing'); process.exit(1); } console.log('PROJECT.md updates OK');"</automated>
  </verify>
  <done>
    `.planning/PROJECT.md` no longer contains the legacy `'Polish habit names preserved verbatim'` substring. New D-35 phrasing referencing English + `name_pl` is present.
  </done>
</task>

<task type="auto">
  <name>Task 3: Edit README.md — replace python http.server with node scripts/serve.js (D-46)</name>
  <files>README.md</files>
  <read_first>
    - README.md (locate any line referencing `python -m http.server` or `python3 -m http.server` — likely in a "Getting started" or "Local development" section)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-46)
    - scripts/serve.js (just-shipped — the canonical command is `node scripts/serve.js`; defaults to PORT=8080)
  </read_first>
  <action>
    Open `README.md`. Locate every reference to `python -m http.server` or `python3 -m http.server`. Replace with `node scripts/serve.js`. If the README has a section called "Quick start" or "Local development" mentioning the Python command, replace the whole code block with a small bash-fenced block that introduces `node scripts/serve.js` (one-line comment "Local development server"), the bare command itself, an "open http://localhost:8080/" instruction, and a PORT override example using `PORT=9000 node scripts/serve.js`. Note: bash fences in README.md are README documentation, NOT action-prose code — README is a normal user-facing markdown file and may contain fenced examples.

    Update any prose around the command to remove the Python prerequisite. If the README has a "Prerequisites" section listing Python, drop that requirement and add Node 20+ instead (D-47 — Node 20+ is the only runtime requirement).

    Do NOT touch other README sections (changelog, license, etc.).
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('./README.md', 'utf8'); if (c.includes('python -m http.server') || c.includes('python3 -m http.server')) { console.error('legacy python http.server reference still present'); process.exit(1); } if (!c.includes('node scripts/serve.js')) { console.error('D-46 reference missing'); process.exit(1); } console.log('README.md updates OK');"</automated>
  </verify>
  <done>
    `README.md` contains `node scripts/serve.js` and contains zero matches for `python -m http.server` / `python3 -m http.server`.
  </done>
</task>

<task type="auto">
  <name>Task 4: Forward-edit .planning/research/ARCHITECTURE.md (D-30, D-39, D-42, Pitfall 13)</name>
  <files>.planning/research/ARCHITECTURE.md</files>
  <read_first>
    - .planning/research/ARCHITECTURE.md (locate §3 IndexedDB Schema, §6 Cross-Tab Sync + Undo, §1 Module Layout `js/util/id.js`)
    - .planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md (§State of the Art lines 906-921 — the deprecated guidance list with the exact edits to make)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-30, D-39, D-42)
  </read_first>
  <action>
    Apply four forward-edits to `.planning/research/ARCHITECTURE.md` per RESEARCH §State of the Art lines 916-921:

    1. **§6 Cross-Tab Sync + Undo:** Replace any literal `BroadcastChannel('nawyki')` with `BroadcastChannel('habits')` (D-30). Add a one-line note: `// Updated 2026-05-26 per Phase 2 D-30 — channel name aligned with cache prefix + DB name + manifest name.`

    2. **§3 IndexedDB Schema:** Update the store list from 6 to 7 — add `score_snapshots` with `[habitId, date]` keypath and `date` + `habitId` indexes (D-39). Add a one-line note: `// Updated 2026-05-26 per Phase 2 D-39 — score_snapshots declared empty in v1; P6 starts writing.`

    3. **§3 IndexedDB Schema:** Change the `events` store row from `autoincrement id` to `id (UUID via crypto.randomUUID())` per D-42. Add a one-line note: `// Updated 2026-05-26 per Phase 2 D-42 — UUID keys for cross-device import safety; chronological order via index on at.`

    4. **§1 Module Layout (or wherever `js/util/id.js` is mentioned with the comment "crypto.randomUUID() with file:// fallback"):** Refine the comment. The original was based on incomplete info; Pitfall 13 confirmed the fallback is defense-in-depth, NOT a "primary path" requirement. New comment: `// crypto.randomUUID() in secure contexts (HTTPS + localhost; usually file:// too); three-tier fallback for Safari-on-file:// edge case per Pitfall 13.`

    Do NOT rewrite either section wholesale. Only make the four targeted edits above; add the one-line "Updated 2026-05-26 per Phase 2 D-XX" comment after each edit so the change is auditable.

    Optional discretion: if ARCHITECTURE.md has a TL;DR table at the top mentioning "6 stores", update it to "7 stores" consistently.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('./.planning/research/ARCHITECTURE.md', 'utf8'); if (c.match(/BroadcastChannel\('nawyki'\)/)) { console.error('legacy nawyki channel still present in ARCHITECTURE.md'); process.exit(1); } if (!c.includes('score_snapshots')) { console.error('D-39 score_snapshots not added'); process.exit(1); } if (c.match(/events.*autoincrement/i) && !c.includes('Updated 2026-05-26 per Phase 2 D-42')) { console.error('D-42 forward-edit note missing'); process.exit(1); } console.log('ARCHITECTURE.md forward-edits OK');"</automated>
  </verify>
  <done>
    ARCHITECTURE.md contains `BroadcastChannel('habits')` (D-30), `score_snapshots` in the schema list (D-39), and a note pointing to D-42 for the events keypath update. Forward-edit comments stamped `2026-05-26`.
  </done>
</task>

<task type="auto">
  <name>Task 5: Bump APP_VERSION to 0.2.0 (D-28 + VERSIONING.md)</name>
  <files>js/util/version.js</files>
  <read_first>
    - js/util/version.js (the current `APP_VERSION = '0.1.0'` line)
    - VERSIONING.md (the bump-to-0.2.0 row in the mapping table)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-28 carry-forward note: bump to 0.2.0 when Phase 2 ships)
  </read_first>
  <action>
    Change the literal `export const APP_VERSION = '0.1.0';` in `js/util/version.js` to `export const APP_VERSION = '0.2.0';`. Do NOT modify any other line of the file.

    Update the file-header JSDoc IF it still references a specific value (e.g., "Loaded by BOTH the window context..."). The header is mostly version-agnostic — do not edit it unless a specific number appears. Confirm by re-reading the header in context.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('./js/util/version.js', 'utf8'); if (!c.includes(\"APP_VERSION = '0.2.0'\")) { console.error('APP_VERSION not bumped to 0.2.0'); process.exit(1); } if (c.includes(\"APP_VERSION = '0.1.0'\")) { console.error('legacy 0.1.0 still present'); process.exit(1); } console.log('APP_VERSION bump OK'); import('./js/util/version.js').then(m => console.log('imported APP_VERSION =', m.APP_VERSION));"</automated>
  </verify>
  <done>
    `js/util/version.js` exports `APP_VERSION = '0.2.0'`. The `_smoke.test.js` from plan 02-01 (which asserts SemVer regex) still passes. The sw.js `CACHE` template now resolves to `habits-0.2.0`.
  </done>
</task>

<task type="auto">
  <name>Task 6: Final phase-gate green check</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/02-storage-foundation-the-spine/02-VALIDATION.md
    - .planning/phases/02-storage-foundation-the-spine/02-05-SUMMARY.md (manual smoke checklist outcomes from plan 02-05)
  </read_first>
  <action>
    Run `node --test tests/`. ALL tests must pass after the docs edits + version bump (none of the edits touch test-bearing code, so this is a sanity check).

    Confirm by grep that none of the legacy strings remain in the repo (other than archived discussion logs which are immutable history):
    - `Polish habit names preserved verbatim` — should appear ONLY in `.planning/phases/02-storage-foundation-the-spine/02-DISCUSSION-LOG.md`, `02-CONTEXT.md`, `02-RESEARCH.md` (these reference the legacy phrasing as the thing being reversed; they are correct as-is)
    - `BroadcastChannel('nawyki')` — should appear ONLY in the same discussion-log/research files (as the thing being replaced; correct as-is)

    Confirm:
    - The 02-05 manual smoke checklist (task 4 of plan 02-05) was passed before this plan ran. If it was not approved, this plan is the wrong checkpoint — circle back.
    - The single open commit-atomic delivery for this plan is the version bump + docs edits.
  </action>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    Full suite (~14 test files) green. Legacy strings appear only in immutable discussion/research artifacts. Phase 2 is ready to ship.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/` exits 0
- `js/util/version.js` exports `APP_VERSION = '0.2.0'` (literal substring present)
- `CLAUDE.md` contains "English habit names primary" and `name_pl`; legacy "Polish habit names preserved verbatim" substring is gone
- `CLAUDE.md` does NOT contain `BroadcastChannel('nawyki')` (D-30 alignment); `node scripts/serve.js` appears in Installation section (D-46)
- `.planning/PROJECT.md` does NOT contain legacy "Polish habit names preserved verbatim" substring; new D-35 phrasing present
- `README.md` does NOT contain `python -m http.server`; contains `node scripts/serve.js` (D-46)
- `.planning/research/ARCHITECTURE.md` contains `BroadcastChannel('habits')` and `score_snapshots`; has forward-edit comments dated 2026-05-26
</verification>

<manual_verification>
**Browser-level cache-bump confirmation (developer discretion — not a phase gate):**

After this plan ships and the repo is pushed (or `node scripts/serve.js` is restarted), opening `http://localhost:8080/` in a browser should:
1. The `controllerchange` toast from P1 fires ("New version ready — Reload") because `sw.js` activated with a new cache name (`habits-0.2.0`).
2. DevTools → Application → Cache Storage shows `habits-0.2.0` is the active cache; `habits-0.1.0` was deleted by the activate handler (D-10).
3. Diagnostics panel (`?debug=1`) shows `App version: 0.2.0` and `Cache name: habits-0.2.0`.

This is a confirmation of P1's update-toast machinery (already exercised in P1 verification — 30/30 pass); P2 just rides it.
</manual_verification>

<success_criteria>
- APP_VERSION is `0.2.0` (the Phase 2 ship signal per VERSIONING.md)
- Docs match reality (D-30, D-35, D-39, D-40, D-42 all reflected in CLAUDE.md / PROJECT.md / ARCHITECTURE.md / README.md / js/util/version.js)
- No regression in `node --test tests/`
- Phase 2 is gated: the manual smoke checklist (from plan 02-05) passed, the version bump committed, the docs aligned
</success_criteria>

<output>
Create `.planning/phases/02-storage-foundation-the-spine/02-06-SUMMARY.md` describing:
- Each docs edit applied (CLAUDE.md, PROJECT.md, README.md, ARCHITECTURE.md) with a before/after one-line excerpt
- The APP_VERSION bump (0.1.0 → 0.2.0)
- The final `node --test tests/` green count
- A pointer to plan 02-05's manual smoke checklist outcomes (so the phase-completion record is consolidated)
Cite D-28, D-30, D-35, D-39, D-40, D-42, D-46, Pitfall 13, VERSIONING.md.

Also create `.planning/phases/02-storage-foundation-the-spine/PHASE-COMPLETION.md` summarizing the 13 requirements (DATA-01..08, SEED-01..05) and pointing at the plan that delivered each — this is the artifact `/gsd-verify-work` will consume to check coverage.
</output>
