---
phase: 13-code-review-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .planning/phases/13-code-review-documentation/REVIEW.md
  - various (determined at review time — files changed in v1.0..HEAD diff scope)
autonomous: false
requirements:
  - QA-01

estimate:
  tokens: 55000
  raw_tokens: 37000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - REVIEW.md exists in .planning/phases/13-code-review-documentation/ after Task 1
    - No switch statement on log status or cadence types exists in any reviewed file
    - No .innerHTML usage exists in any reviewed file
    - All new .js files added in v1.2 carry a JSDoc @file header
    - No indexedDB.* calls exist outside js/db/idb.js in any reviewed file
    - node --test tests/ exits 0 after any fixes applied by Task 2
  artifacts:
    - .planning/phases/13-code-review-documentation/REVIEW.md
    - any source files patched by --fix
  key_links:
    - Code review scope is git diff v1.0..HEAD (all v1.1 and v1.2 changes)
    - QA-01 compliance checklist is the primary review lens
    - Plan 13-01 must be complete before this plan runs (fixes the code the review validates)
---

<objective>
Run a full code review over all files changed since the v1.0 tag, confirm QA-01 compliance (no switch on log status/cadence types, no .innerHTML, JSDoc headers, no indexedDB outside idb.js), and apply any findings.

Purpose: The v1.2 milestone ships six quick-task features. This review is the gating quality check that confirms the shipped code follows project anti-patterns (D-27, D-78, Anti-Pattern 1, Anti-Pattern 4) — specifically over the modules most likely to carry risk: new apply handlers, i18n files, swipe-UX view changes, and the stale-boolean-fixed files from Plan 13-01.

Output: REVIEW.md with findings, then a fix commit applying any identified issues.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/habits/.planning/ROADMAP.md
@C:/my-code/vibe-coding/habits/.planning/STATE.md
@C:/my-code/vibe-coding/habits/.planning/phases/13-code-review-documentation/13-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <name>Task 1: Run code review over v1.0..HEAD diff scope (QA-01)</name>
  <files>
    .planning/phases/13-code-review-documentation/REVIEW.md
  </files>
  <read_first>
    - .planning/phases/13-code-review-documentation/13-CONTEXT.md section "Files in Scope for Code Review (QA-01)" — the exact file list the review must cover
    - .planning/STATE.md "Concerns (Phase 13 targets)" — confirms scoreSnapshots already patched; review can verify
    - CLAUDE.md "Hardcoded Prohibitions" — the four anti-patterns the review checks
  </read_first>
  <action>
    Invoke the gsd-code-review skill targeting all files changed since the v1.0 git tag.
    Scope: `git diff v1.0..HEAD --name-only` produces the file list. The review agent must
    cover ALL files in that output, with particular attention to:

    New modules added in v1.2 (highest review priority):
    - js/domain/logStatus.js — 4-state cycle; @file header and NEXT_STATE table already confirmed clean by earlier grep; verify no switch on status
    - js/domain/scheduled.js — auto-transition on boot
    - js/state/apply/markFailed.js, markSkipped.js (post-Plan-13-01 fix), promoteHabit.js
    - js/views/desktop/sidebar.js, js/views/desktop/wavePlanning.js
    - js/i18n/index.js, js/i18n/en.js, js/i18n/pl.js

    Modified existing files in v1.2 (secondary review priority):
    - js/views/today.js, js/views/history.js — swipe-right cycle changes
    - js/io/scoreSnapshots.js — binary completion check (already patched 0dbd185; verify)
    - js/domain/waveAggregates.js — post-Plan-13-01 boolean-swap fix
    - js/io/import.js — post-Plan-13-01 normalization block

    QA-01 compliance checklist the review MUST address explicitly:
    1. No `switch` on log status or cadence types (Anti-Pattern 4) — dispatch tables only
    2. No `.innerHTML` anywhere (D-78)
    3. JSDoc `@file` header on every new .js file (D-27)
    4. No `indexedDB.*` calls outside js/db/idb.js (Anti-Pattern 1)

    The review agent writes findings to REVIEW.md. Direct the agent to use
    the path `.planning/phases/13-code-review-documentation/REVIEW.md` as the output file.
    The plan invokes this as: `/gsd-code-review --scope "v1.0..HEAD"` or equivalent.
  </action>
  <verify>
    <automated>test -f .planning/phases/13-code-review-documentation/REVIEW.md</automated>
    <fails_when>file does not exist — review agent did not produce output</fails_when>
  </verify>
  <acceptance_criteria>
    - REVIEW.md exists at .planning/phases/13-code-review-documentation/REVIEW.md
    - REVIEW.md contains an explicit section addressing each of the four QA-01 compliance checks
    - REVIEW.md is non-empty (at least findings or a "clean" attestation per compliance check)
    - Human confirms REVIEW.md looks complete before proceeding to Task 2
  </acceptance_criteria>
  <done>REVIEW.md exists with explicit QA-01 compliance findings. Human has confirmed the review output is complete and plausible.</done>
</task>

<task type="auto">
  <name>Task 2: Apply code review fixes and verify clean test suite</name>
  <files>
    various (files identified by REVIEW.md findings)
  </files>
  <read_first>
    - .planning/phases/13-code-review-documentation/REVIEW.md (full file — findings from Task 1)
    - CLAUDE.md "Hardcoded Prohibitions" — confirm any fix does not introduce a prohibited construct
    - js/db/idb.js lines 1-10 (the @file header — the only file permitted to call indexedDB.*)
  </read_first>
  <action>
    Read REVIEW.md and apply every actionable finding. For each finding:

    - Anti-Pattern violations (switch on status/cadence, .innerHTML, indexedDB outside idb.js):
      Apply the minimal fix that removes the violation. Do not refactor beyond the finding scope.

    - Missing @file headers (D-27): Add the JSDoc block at the top of the file using the
      canonical format: `/** @file <one-line summary>. <rationale referencing the relevant
      D-xx decision> */`. The summary must be accurate for the module's actual responsibility.

    - Code quality findings (bugs, logic errors, misuse of patterns): Apply the fix as
      described in the finding. Do not apply speculative refactors beyond what the finding
      describes.

    After applying all fixes, run the full test suite. If any test fails after a fix,
    investigate the root cause — do not bypass the failing test. Fix the test if the fix
    exposed a real behavior change that the test was checking incorrectly; fix the production
    code if the fix was wrong.

    If REVIEW.md contains no actionable findings (all compliance checks are clean), commit
    with message: `chore: code review v1.0..HEAD — QA-01 all-clear (no findings)`.
    If fixes were applied, commit with message: `fix: apply code review findings from QA-01 review (v1.0..HEAD)`.
  </action>
  <verify>
    <automated>node --test tests/</automated>
    <fails_when>exit code non-zero — any test failure after applying review fixes is a blocker and must be resolved before this task is done</fails_when>
  </verify>
  <acceptance_criteria>
    - `node --test tests/` exits 0 after all fixes are applied
    - Every finding in REVIEW.md that was marked as actionable has a corresponding code change (or an explicit note explaining why the finding was not applicable)
    - No new .innerHTML, switch-on-status, or indexedDB-outside-idb.js patterns were introduced by the fixes
    - Commit exists (either all-clear or fix: prefix)
  </acceptance_criteria>
  <done>All REVIEW.md findings addressed. Full test suite exits 0. QA-01 compliance confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| v1.0..HEAD diff → code review agent | Review agent reads all modified files; findings drive code changes |
| REVIEW.md findings → applied fixes | Code changes from findings could introduce new issues if not tested |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-02-01 | Repudiation | REVIEW.md | low | accept | Human-verify checkpoint before Task 2 ensures the review output is confirmed before fixes are applied |
| T-13-02-02 | Tampering | applied fixes from REVIEW.md | low | mitigate | Every fix is validated by `node --test tests/` before commit; reviewer findings are specific, not speculative |
| T-13-02-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan — vanilla Node test runner only |
</threat_model>

<verification>
After both tasks complete:

```
node --test tests/   # exits 0
test -f .planning/phases/13-code-review-documentation/REVIEW.md   # file exists
```

QA-01 compliance confirmed: no switch on log status or cadence types, no .innerHTML, all new .js files have @file headers, no indexedDB.* outside idb.js.
</verification>

<success_criteria>
- REVIEW.md exists with explicit QA-01 compliance attestation for all four checks
- Every actionable finding in REVIEW.md has been applied or explicitly annotated as non-applicable
- `node --test tests/` exits 0 after fixes
- QA-01 is satisfied: Anti-Pattern 4 (no switch on log status/cadence), D-78 (no .innerHTML), D-27 (JSDoc @file headers), Anti-Pattern 1 (no indexedDB outside idb.js) — all confirmed clean
</success_criteria>

<output>
Create `.planning/phases/13-code-review-documentation/13-02-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Artifact | Type | Consumer |
|----------|------|----------|
| .planning/phases/13-code-review-documentation/REVIEW.md | New findings file | Human review, QA-01 evidence |
| patched source files (if any) | Modified | CI, v1.2 milestone close |
