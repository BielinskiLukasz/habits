---
phase: 13-code-review-documentation
plan: 03
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - .planning/PROJECT.md
autonomous: true
requirements:
  - QA-02

estimate:
  tokens: 30000
  raw_tokens: 18000
  tasks: 2
  confidence: medium

must_haves:
  truths:
    - PROJECT.md Key Decisions table contains an enriched D-43 entry with backwards-compat normalization rationale and stale-caller hotfix context
    - PROJECT.md Key Decisions table contains a new D-44 entry for i18n architecture (locale dict shape, t() signature, no Intl.*, two locales, IDB settings persistence)
    - D-43 row mentions absence-as-undefined design rationale and markUncompleted → status:'failed' choice
    - D-44 row mentions flat key→string dict shape, t(key, vars) with {{var}} interpolation, IDB settings store for locale pref, BroadcastChannel sync on language change
    - All existing Decision rows remain unchanged (no other edits to the table)
  artifacts:
    - .planning/PROJECT.md (D-43 enriched, D-44 added)
  key_links:
    - D-43 is in the Key Decisions markdown table at .planning/PROJECT.md line ~156
    - D-44 uses the next available decision number (D-43 was the last entry)
    - QA-02 compliance is the gating requirement
---

<objective>
Update PROJECT.md's Key Decisions section to document the v1.2 architectural decisions that are not yet formally recorded: enrich D-43 (4-state log model) with rationale detail from the Phase 11 implementation, and add D-44 (i18n architecture) for the EN/PL localisation system introduced in Phase 10.

Purpose: QA-02 requires that new architectural decisions shipped in v1.2 are documented in PROJECT.md before the milestone closes. D-43 exists but is thin on rationale — the Phase 11 and Phase 13 work added important detail (absence-as-undefined pattern, backwards-compat normalization). D-44 does not yet exist; the i18n module was added as a quick-task and its design choices have not been recorded.

Output: A single commit enriching D-43 in-place and adding D-44 as a new row in the Key Decisions table.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/habits/.planning/ROADMAP.md
@C:/my-code/vibe-coding/habits/.planning/STATE.md
@C:/my-code/vibe-coding/habits/.planning/phases/13-code-review-documentation/13-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich D-43 (4-state log model) in PROJECT.md</name>
  <files>
    .planning/PROJECT.md
  </files>
  <read_first>
    - .planning/PROJECT.md lines 121-165 (Key Decisions table — current D-43 row at line ~156)
    - .planning/phases/13-code-review-documentation/13-CONTEXT.md §Specific Ideas (the three points calling out what D-43 needs)
    - js/state/apply/markUncompleted.js lines 1-30 (confirms markUncompleted writes status:'failed', not deletes)
    - js/io/import.js (the normalization block added in Plan 13-01 — reference its shape for the rationale note)
  </read_first>
  <behavior>
    The existing D-43 row documents the status field shape and CSV cell mapping but omits:
    1. Why absence-as-undefined (no row) was chosen over a null status field — keeps IDB free of phantom rows; simpler query logic (absence check vs null check).
    2. The `markUncompleted → status:'failed'` choice — deletion was rejected because it would break streak continuity and undo history; a 'failed' row preserves the explicit "I tried and failed" signal in the log.
    3. Backwards-compat normalization — v1 backup files carry `completed: boolean`; `import.js` normalizes legacy rows (completed:true → status:'completed', completed:false → status:'failed') to keep old backups importable without data loss.
    4. The stale-caller hotfix context — four callers (waveAggregates.js lines 44+198, scoreSnapshots.js line 305, import.js line 130, markSkipped.js line 35) still referenced the old boolean at Phase 13; fixed in Plan 13-01.

    After enrichment, D-43's "Why" column must include all four points. The "Shipped" column should note Phase 13 Plan 01 as the hotfix milestone alongside the original 2026-08-28 date.
  </behavior>
  <action>
    In .planning/PROJECT.md, locate the D-43 row in the Key Decisions markdown table (the row beginning `| D-43: 4-state log model`). Replace the existing "Why" cell content with an expanded version that adds the four rationale points listed in <behavior>. Keep the same table row format (pipe-delimited, single row — do not add line breaks that break the table). Keep the "Decision" cell and "Shipped" cell content substantially intact; append to the Shipped cell: `; stale-caller hotfix in Plan 13-01 (2026-09-08)`.

    The enriched Why cell must include (in this order):
    - Single field `status` is unambiguous at every call-site
    - Absence-as-undefined keeps IDB free of phantom rows and simplifies query logic (row-absent vs null-status are equivalent to one check)
    - `markUncompleted` writes `status:'failed'` rather than deleting the row — deletion would break streak continuity and erase the explicit "consciously failed" signal from the log history; undo also becomes simpler
    - The skipped state allows a distinct "I consciously skipped" signal that maps to `'x'` in CSV without inflating the 0-failure count
    - Backwards-compat: `import.js` normalizes legacy `completed:boolean` fields in v1 backup files (`true`→`'completed'`, `false`→`'failed'`) so old exports remain importable without data loss

    Do not edit any other rows in the Key Decisions table. Do not add or remove table columns.
  </action>
  <verify>
    <automated>grep -c "absence-as-undefined\|phantom rows" .planning/PROJECT.md</automated>
    <fails_when>output is 0 — the enrichment phrase was not written to the file</fails_when>
  </verify>
  <verify>
    <automated>grep -c "stale-caller hotfix" .planning/PROJECT.md</automated>
    <fails_when>output is 0 — the hotfix milestone note was not appended to D-43</fails_when>
  </verify>
  <acceptance_criteria>
    - The D-43 row in PROJECT.md's Key Decisions table contains the phrase "absence-as-undefined" or "phantom rows"
    - The D-43 row mentions the markUncompleted → status:'failed' choice and why deletion was rejected
    - The D-43 row mentions backwards-compat normalization (legacy completed:boolean → status:string)
    - The D-43 Shipped cell notes Plan 13-01 as the stale-caller hotfix milestone
    - All other Decision rows are unchanged (verified by git diff showing only the D-43 row modified)
  </acceptance_criteria>
  <done>D-43 row enriched in-place with all four rationale points and the hotfix milestone note.</done>
</task>

<task type="auto">
  <name>Task 2: Add D-44 (i18n architecture) to PROJECT.md Key Decisions and commit</name>
  <files>
    .planning/PROJECT.md
  </files>
  <read_first>
    - .planning/PROJECT.md lines 121-165 (Key Decisions table — find the row after D-43 to insert D-44)
    - js/i18n/index.js (full file — the t() function signature and how locale pref is read)
    - js/i18n/en.js lines 1-30 (flat key→string dict shape and file structure)
    - .planning/phases/13-code-review-documentation/13-CONTEXT.md §D-07 and §Specific Ideas (the exact fields QA-02 requires)
  </read_first>
  <behavior>
    D-44 does not yet exist in the Key Decisions table. The i18n module was introduced as quick-task 260828-00l. Its design choices:
    - Locale dict: flat ES module export (`export const en = { key: 'string', ... }`) — no namespace nesting, no JSON, no Intl.* API dependency
    - `t(key, vars)` function: looks up `key` in the active locale dict, substitutes `{{var}}` placeholders using the `vars` object. Falls back to the `en` dict if a key is absent from the active locale (English-first fallback, never throws)
    - Two locales: `en` (primary, default) and `pl` (Polish) — both in `js/i18n/en.js` and `js/i18n/pl.js`
    - Locale preference persisted in the IDB `settings` store under key `habitLanguage` (not localStorage — consistent with the project's IDB-first storage policy)
    - Language change broadcasts via `BroadcastChannel('habits')` so all open tabs rerender without reload
    - No build step, no npm dependency — fits the zero-build constraint

    The D-44 row must cover all these design choices in the "Decision" cell and explain why each choice was made in the "Why" cell.
  </behavior>
  <action>
    In .planning/PROJECT.md, locate the D-43 row in the Key Decisions markdown table. Insert a new row immediately after D-43 with this structure (pipe-delimited, no line breaks inside the row):

    Decision cell: `D-44: EN/PL i18n architecture — locale dictionaries are flat ES module exports (`js/i18n/en.js`, `js/i18n/pl.js`) with no namespace nesting and no `Intl.*` API. The `t(key, vars)` function in `js/i18n/index.js` resolves keys from the active locale dict and substitutes `{{var}}` placeholders; falls back to the `en` dict on missing keys. Locale preference is stored in the IDB `settings` store under key `habitLanguage` (not localStorage). Language changes are broadcast via `BroadcastChannel('habits')` so open tabs re-render without reload`

    Why cell: `Flat ES-module dicts need no JSON loader, no `Intl.LocaleMatcher`, and no npm package — fits the zero-build no-npm constraint; the full i18n surface is statically analysable. IDB storage for locale pref is consistent with the project's IDB-first policy (D-2); localStorage is reserved for tiny UI state only. BroadcastChannel reuse (D-30) keeps the language-toggle causally consistent across tabs without a new channel or a page reload`

    Shipped cell: `Shipped 2026-08-28 (quick-task 260828-00l); validated Phase 10`

    After inserting the row, commit both the D-43 enrichment from Task 1 and the D-44 addition together with message: `docs(project): enrich D-43 and add D-44 for i18n architecture (QA-02)`
  </action>
  <verify>
    <automated>grep -c "D-44" .planning/PROJECT.md</automated>
    <fails_when>output is 0 — the D-44 row was not inserted into the file</fails_when>
  </verify>
  <verify>
    <automated>grep -c "habitLanguage\|BroadcastChannel" .planning/PROJECT.md</automated>
    <fails_when>output is 0 — the key design details of D-44 (IDB key name, BroadcastChannel) were not recorded</fails_when>
  </verify>
  <acceptance_criteria>
    - PROJECT.md Key Decisions table contains a D-44 row immediately after D-43
    - D-44 covers: flat ES module dict shape, `t(key, vars)` with `{{var}}` interpolation, English-first fallback, IDB `settings` store with `habitLanguage` key, BroadcastChannel sync, no Intl.* or npm
    - D-44 Shipped cell references quick-task 260828-00l and Phase 10
    - `grep -c "D-44" .planning/PROJECT.md` outputs 1 (exactly one new row)
    - Commit exists with `docs(project):` prefix referencing QA-02
    - `git diff HEAD~1 .planning/PROJECT.md` shows only D-43 enrichment and D-44 insertion — no other sections modified
  </acceptance_criteria>
  <done>D-44 row inserted after D-43. Single commit covers both tasks. QA-02 satisfied: D-43 enriched, D-44 documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROJECT.md edit → git commit | Documentation-only change; no executable code paths modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03-01 | Tampering | PROJECT.md table format | low | mitigate | After editing, verify the table still renders (no broken pipe-delimited rows) by grep-checking the D-43 and D-44 lines parse as valid markdown table rows |
| T-13-03-SC | Tampering | npm/pip/cargo installs | high | accept | No package installs in this plan — documentation edit only |
</threat_model>

<verification>
After both tasks complete:

```
grep -c "D-44" .planning/PROJECT.md   # exits 0 (output should be 1)
grep -c "absence-as-undefined\|phantom rows" .planning/PROJECT.md   # exits 0 (output ≥ 1)
grep -c "habitLanguage" .planning/PROJECT.md   # exits 0 (output ≥ 1)
```

`git diff HEAD~1 .planning/PROJECT.md` shows only two modifications: D-43 row enriched, D-44 row added.
</verification>

<success_criteria>
- D-43 enriched with: absence-as-undefined rationale, markUncompleted→failed choice, backwards-compat normalization, stale-caller hotfix milestone
- D-44 added with: flat ES module dict, t(key, vars) with {{var}}, English-first fallback, IDB habitLanguage key, BroadcastChannel sync, no Intl.* or npm
- Single commit with docs(project): prefix
- No other Key Decisions rows modified
- QA-02 requirement satisfied
</success_criteria>

<output>
Create `.planning/phases/13-code-review-documentation/13-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

| Artifact | Type | Consumer |
|----------|------|----------|
| .planning/PROJECT.md (D-43 enriched, D-44 added) | Modified documentation | v1.2 milestone close, QA-02 evidence |
</output>
