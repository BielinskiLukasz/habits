---
phase: 10-i18n-tests-verification
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - js/views/today.js
  - js/views/toast.js
  - js/views/history.js
  - js/views/settings.js
  - js/views/history/builders.js
autonomous: true
requirements:
  - I18N-02
estimate:
  tokens: 160000
  raw_tokens: 80000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - js/views/today.js imports t from '../i18n/index.js' (I18N-02)
    - js/views/toast.js imports t from '../i18n/index.js' (I18N-02)
    - All showErrorToast and showUndoToast message literals in today.js route through t() (I18N-02)
    - All showErrorToast calls in history.js route through t() (I18N-02)
    - All showErrorToast calls in settings.js route through t() (I18N-02)
    - The aria-label values in history/builders.js route through t() (I18N-02)
    - The aria-label and error toast in toast.js route through t() (I18N-02)
    - node --test tests/ exits 0 after all modifications
  artifacts:
    - js/views/today.js (t() import added; all hardcoded string sites replaced)
    - js/views/toast.js (t() import added; all hardcoded string sites replaced)
    - js/views/history.js (hardcoded error toast strings replaced — t() already imported)
    - js/views/settings.js (hardcoded error toast strings replaced — t() already imported)
    - js/views/history/builders.js (aria-label strings replaced — t() already imported)
  key_links:
    - All t() calls in these files reference keys that were added to en.js and pl.js in Plan 10-02
    - history.js already imports t; history/builders.js already imports t; settings.js already imports t
---

<objective>
Fix the remaining five view files that still contain hardcoded UI strings after Plan 10-02 fixed catalog.js. All locale keys were already added to en.js and pl.js in Plan 10-02, so this plan only edits view files.

Purpose: I18N-02 requires zero hardcoded UI strings across all view files in both shells. today.js and toast.js are the tracer targets — they need a new t() import and multiple string replacements. history.js, settings.js, and history/builders.js already import t() but still use hardcoded strings for error toasts and aria-labels.
Output: All five view files fully using t() for every user-visible string; node --test tests/ exits 0.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-i18n-tests-verification/10-02-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Fix today.js and toast.js — add t() import and replace all hardcoded strings (I18N-02)</name>
  <files>js/views/today.js, js/views/toast.js</files>
  <read_first>
    - js/views/today.js — read the full file (it is ~650 lines); identify: the import block at the top, the listEl aria-label assignment (~line 609), all showErrorToast calls with their exact string arguments (~lines 222, 251, 270, 378, 399, 435, 456, 520), and all showUndoToast calls that contain template-literal message strings (lines ~215-218 and ~244-247 for markComplete / markUncomplete, and similar patterns for other handlers)
    - js/views/toast.js — read the full file (~130 lines); identify: the import block, the closeBtn aria-label assignment (~line 116), and the showErrorToast call in the undo handler (~lines 103-107)
    - js/i18n/en.js — scan the today and toast namespace keys added by Plan 10-02: today.errorMark, today.errorSkip, today.errorUpdateCount, today.errorUpdateSlot, today.listAriaLabel, today.markedComplete, today.markedUncomplete, toast.dismiss, toast.errorUndo
  </read_first>
  <action>
    Edit js/views/today.js:

    1. Add import { t } from '../i18n/index.js'; to the import block, after the existing named imports. Keep the import block structure intact — do not reorder existing imports.

    2. Replace the listEl.setAttribute('aria-label', ...) call that assigns the English literal for the habits list with listEl.setAttribute('aria-label', t('today.listAriaLabel')).

    3. Replace every showErrorToast call whose message is the English mark-failure literal with showErrorToast(t('today.errorMark')). There are approximately five such sites.

    4. Replace every showErrorToast call whose message is the English skip-failure literal with showErrorToast(t('today.errorSkip')).

    5. Replace every showErrorToast call whose message is the English update-count-failure literal with showErrorToast(t('today.errorUpdateCount')). There are approximately two such sites.

    6. Replace every showErrorToast call whose message is the English update-slot-failure literal with showErrorToast(t('today.errorUpdateSlot')).

    7. Replace every showUndoToast call whose message field is a template literal for "Marked X complete" with message: t('today.markedComplete', { name: habitName }). The variable holding the habit name should already be in scope as habitName (confirm the exact variable name by reading the handler).

    8. Replace every showUndoToast call whose message field is a template literal for "Marked X uncomplete" with message: t('today.markedUncomplete', { name: habitName }).

    9. Update the @file JSDoc to note the i18n import and I18N-02 adoption.

    Edit js/views/toast.js:

    1. Add import { t } from '../i18n/index.js'; to the import block.

    2. Replace the closeBtn aria-label assignment with closeBtn.setAttribute('aria-label', t('toast.dismiss')).

    3. Replace the showErrorToast call in the undo error handler with showErrorToast(t('toast.errorUndo')). Read the undo handler carefully — there may be one or two sites near lines 103–107.

    4. Do not modify closeBtn.textContent = '×'; — this is a Unicode symbol, not a natural-language string, and is intentionally excluded from the locale dictionary.

    5. Update the @file JSDoc to note the i18n import.

    Do not change any logic, event listeners, or DOM structure beyond the string replacements. Do not use .innerHTML anywhere (D-78).
  </action>
  <verify>
    <automated>node --test tests/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "import.*{ t" js/views/today.js returns 1
    - grep -c "import.*{ t" js/views/toast.js returns 1
    - node --test tests/ exits 0
  </acceptance_criteria>
  <done>
    today.js and toast.js both import t() and all user-visible string literals — aria-labels, error toasts, and undo toast messages — now route through t() using today and toast namespace keys. node --test tests/ exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix history.js, settings.js, history/builders.js — replace hardcoded strings (I18N-02)</name>
  <files>js/views/history.js, js/views/settings.js, js/views/history/builders.js</files>
  <read_first>
    - js/views/history.js — read the full file; confirm t() is already imported; identify the showErrorToast calls with hardcoded mark and skip failure strings (~lines 164, 360, 374)
    - js/views/settings.js — read the full file; confirm t() is already imported; identify all showErrorToast calls (~12 total) including those that interpolate a reason string using string concatenation (e.g. 'JSON export failed: ' + err.message)
    - js/views/history/builders.js — read the file; confirm t() is already imported; identify the two hardcoded aria-label values in the day-navigation builder (Previous day and Next day, ~lines 40 and 56)
    - js/i18n/en.js — confirm the history, settings keys from Plan 10-02 are present: history.errorMark, history.errorSkip, history.nextDay, history.prevDay, settings.errorExportJson, settings.errorExportCsv, settings.errorImport, settings.errorUndo, settings.errorWeekStart, settings.errorMasteryThreshold, settings.errorMasteryWindow, settings.errorRecompute, settings.errorScoringModel, settings.errorPersistenceUnsupported, settings.errorPersistenceRequest, settings.errorDismissNag
  </read_first>
  <action>
    Edit js/views/history.js:
    1. Replace the showErrorToast call with the mark-failure message with showErrorToast(t('history.errorMark')). There are approximately three such sites.
    2. Replace the showErrorToast call with the skip-failure message with showErrorToast(t('history.errorSkip')).
    3. Do not add a new import — t() is already imported.
    4. Check whether any showUndoToast calls contain English template literals for marking history entries; if so, determine whether a shared today.markedComplete / today.markedUncomplete key is appropriate or whether a history-specific key is needed. If a history-specific variant is necessary and the key does not yet exist in en.js / pl.js, add it to both locale files before using it in history.js.

    Edit js/views/history/builders.js:
    1. Replace the 'Previous day' aria-label value in the day-back navigation builder with t('history.prevDay').
    2. Replace the 'Next day' aria-label value in the day-forward navigation builder with t('history.nextDay').
    3. Do not add a new import — t() is already imported.

    Edit js/views/settings.js:
    1. For each showErrorToast call with a hardcoded English error message, replace the string literal with the appropriate t() call using the settings namespace key from en.js:
       - Persistence not supported → t('settings.errorPersistenceUnsupported')
       - Persistence request failed → t('settings.errorPersistenceRequest')
       - Week start change failed → t('settings.errorWeekStart')
       - Undo failed → t('settings.errorUndo')
       - Mastery threshold change failed → t('settings.errorMasteryThreshold')
       - Mastery window change failed → t('settings.errorMasteryWindow')
       - JSON export failed (with reason) → t('settings.errorExportJson', { msg: err?.message ?? String(err) })
       - CSV export failed (with reason) → t('settings.errorExportCsv', { msg: err?.message ?? String(err) })
       - Import failed (with reason) → t('settings.errorImport', { msg: err?.message ?? String(err) })
       - Dismiss nag failed (with reason) → t('settings.errorDismissNag', { msg: err?.message ?? String(err) })
       - Scoring model change failed → t('settings.errorScoringModel')
       - Recompute failed (with reason) → t('settings.errorRecompute', { msg: err?.message ?? String(err) })
    2. For the string-concatenation variants (e.g. 'JSON export failed: ' + err.message), replace the entire argument with the t() call with {msg} substitution. The {msg} placeholder in the locale value will be replaced by t() at runtime.
    3. Do not add a new import — t() is already imported.

    In all three files: do not change logic, event handlers, or DOM structure. Do not use .innerHTML (D-78). If any hardcoded string that was not anticipated is found during editing, add the missing key to en.js and pl.js before using it.
  </action>
  <verify>
    <automated>node --test tests/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/ exits 0
    - grep -c "t('history.prevDay')" js/views/history/builders.js returns 1
    - grep -c "t('history.nextDay')" js/views/history/builders.js returns 1
    - grep -c "t('settings.errorExportJson" js/views/settings.js returns 1
  </acceptance_criteria>
  <done>
    history.js, settings.js, and history/builders.js no longer contain hardcoded English UI strings. All error toasts and aria-labels now route through t() using history and settings namespace keys. Combined with Task 1 and Plan 10-02, every view file across both shells satisfies I18N-02. node --test tests/ exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n key → DOM textContent / aria-label | t() output assigned to textContent or setAttribute; never innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Tampering | locale string injection via t() | low | accept | t() output goes to textContent or setAttribute only; D-78 grep gate enforces no innerHTML across the entire codebase |
| T-10-06 | Information Disclosure | error reason in t() {msg} substitution | low | accept | The {msg} substitution puts err.message into a toast that is only shown to the local user (single-user app); no network transmission |
</threat_model>

<verification>
node --test tests/ exits 0. grep -c "import.*{ t" js/views/today.js returns 1. grep -c "import.*{ t" js/views/toast.js returns 1.
</verification>

<success_criteria>
I18N-02 complete: all five view files (today.js, toast.js, history.js, settings.js, history/builders.js) use t() for every user-visible string. Combined with Plan 10-02's catalog.js fix, zero hardcoded UI strings remain in any view builder or event handler across both shells.
</success_criteria>

<output>
Create .planning/phases/10-i18n-tests-verification/10-03-SUMMARY.md when done.

Artifacts produced by this plan:
- js/views/today.js (t() import added; all string sites replaced)
- js/views/toast.js (t() import added; all string sites replaced)
- js/views/history.js (error toast strings replaced)
- js/views/settings.js (all 12 error toast strings replaced)
- js/views/history/builders.js (2 aria-label strings replaced)
</output>
