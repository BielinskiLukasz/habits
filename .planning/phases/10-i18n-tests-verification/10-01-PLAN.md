---
phase: 10-i18n-tests-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/i18n.test.js
autonomous: true
requirements:
  - I18N-01
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - node --test tests/unit/i18n.test.js exits 0 with all existing tests plus new tests passing (I18N-01)
    - setLang('pl') valid case is covered — getLang() returns 'pl' after the call
    - displayName() is covered — en locale returns name, pl locale returns name_pl, null name_pl falls back to name
    - applyStaticTranslations() is covered — DOM-present path sets textContent via t(), DOM-absent path is a no-op that does not throw
  artifacts:
    - tests/unit/i18n.test.js with at least 14 test cases (up from 8)
  key_links:
    - displayName and applyStaticTranslations imported from js/i18n/index.js in the test file
---

<objective>
Expand tests/unit/i18n.test.js to cover the three exported functions that the original quick-task test file left untested: setLang() valid case, displayName(), and applyStaticTranslations().

Purpose: I18N-01 requires unit tests for locale lookup, missing-key fallback, and t() with variable interpolation. Those three areas are already covered by the existing 8 tests. This plan adds the remaining coverage so every public export of js/i18n/index.js has at least one test case.
Output: tests/unit/i18n.test.js with full public-API test coverage for the i18n module.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Verify existing i18n test baseline passes (I18N-01)</name>
  <files>tests/unit/i18n.test.js</files>
  <read_first>
    - tests/unit/i18n.test.js — read the entire file: understand existing describe blocks, the import line, and the _resetLangForTest usage pattern
    - js/i18n/index.js — read the full module: confirm exported API (t, getLang, setLang, displayName, applyStaticTranslations, _resetLangForTest), the typeof-document guard in applyStaticTranslations, and the _storage null-coalescing pattern
  </read_first>
  <action>
    Run `node --test tests/unit/i18n.test.js` without modifying any file. Verify in the output that all 8 existing tests pass. Confirm the imports line includes t, getLang, setLang, _resetLangForTest but does NOT yet include displayName or applyStaticTranslations — that gap is what Task 2 fills.

    Do not create or modify any file in this task. This task's only purpose is to confirm a green baseline before expanding coverage.
  </action>
  <verify>
    <automated>node --test tests/unit/i18n.test.js</automated>
  </verify>
  <done>All 8 existing i18n unit tests pass; the coverage gap (displayName, applyStaticTranslations, setLang valid case) is confirmed before Task 2 adds it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add test coverage for setLang valid case, displayName, applyStaticTranslations (I18N-01)</name>
  <files>tests/unit/i18n.test.js</files>
  <read_first>
    - tests/unit/i18n.test.js — read before editing; note the existing coding style (describe/it/assert, afterEach for _resetLangForTest)
    - js/i18n/index.js — re-confirm displayName({name, name_pl?}) signature, applyStaticTranslations() DOM guard line, _resetLangForTest(lang='en') default
    - tests/helpers/fake-document.js — read to check available helpers; note it provides visibilitychange / createFakeWindow but NOT a querySelectorAll fake; create an inline fake object in the test body rather than importing from this file
  </read_first>
  <behavior>
    setLang valid cases:
    - _resetLangForTest('en'), setLang('pl'), getLang() returns 'pl'
    - _resetLangForTest('en'), setLang('en'), getLang() returns 'en'

    displayName cases (use _resetLangForTest to set locale before each assertion):
    - at lang=en: displayName({name: 'Walk', name_pl: 'Spacer'}) returns 'Walk'
    - at lang=pl: displayName({name: 'Walk', name_pl: 'Spacer'}) returns 'Spacer'
    - at lang=pl: displayName({name: 'Walk', name_pl: null}) returns 'Walk' (null fallback)
    - at lang=pl: displayName({name: 'Walk'}) (no name_pl field) returns 'Walk' (undefined fallback)

    applyStaticTranslations cases:
    - when globalThis.document is not defined (Node default): applyStaticTranslations() does not throw
    - when globalThis.document is an inline object whose querySelectorAll returns [{getAttribute: () => 'nav.today', textContent: ''}]: calling applyStaticTranslations() sets the element's textContent to t('nav.today') which is 'today' in the default English locale; delete globalThis.document immediately after to clean up
  </behavior>
  <action>
    Edit tests/unit/i18n.test.js by appending three new describe blocks at the end of the file. Do not touch any of the three existing describe blocks.

    Step 1: Extend the existing import line to add displayName and applyStaticTranslations to the named imports from js/i18n/index.js.

    Step 2: Add a describe block labelled setLang — valid locale cases. Use an afterEach that calls _resetLangForTest('en') to keep tests order-independent. Write two it() tests: one that calls setLang('pl') and asserts getLang() equals 'pl', and one that calls setLang('en') and asserts getLang() equals 'en'.

    Step 3: Add a describe block labelled displayName. Use afterEach to call _resetLangForTest('en'). Write four it() tests matching the four behavior cases above. For the pl-locale tests, call _resetLangForTest('pl') at the start of the test body.

    Step 4: Add a describe block labelled applyStaticTranslations. Write two it() tests: one that wraps applyStaticTranslations() in assert.doesNotThrow (no globalThis.document set), and one that sets globalThis.document to an inline fake with querySelectorAll, calls applyStaticTranslations(), asserts element.textContent equals 'today', then deletes globalThis.document in an after() or inline cleanup call.

    Step 5: Update the @file JSDoc at the top of the test file to list displayName, applyStaticTranslations, and setLang valid case alongside the functions already mentioned.

    Style notes: match the existing node:test style exactly (import { describe, it, after, afterEach } from 'node:test'; import assert from 'node:assert/strict'). Do not use fenced code blocks in this action — the description above is directive prose only.
  </action>
  <verify>
    <automated>node --test tests/unit/i18n.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/unit/i18n.test.js exits 0
    - Test count is at least 14 (8 original + at least 6 new)
    - grep -c "displayName" tests/unit/i18n.test.js returns at least 2 (import and at least one assertion)
    - grep -c "applyStaticTranslations" tests/unit/i18n.test.js returns at least 2 (import and at least one call)
    - No line in the three original describe blocks is modified
  </acceptance_criteria>
  <done>
    All exported functions of js/i18n/index.js — t(), getLang(), setLang() valid and invalid, displayName() both locale branches and null fallback, applyStaticTranslations() DOM-present and DOM-absent — have at least one passing test case. node --test tests/unit/i18n.test.js exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test scope | node:test runs in-process; no network calls, no user data, no production code paths modified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure | locale strings in test assertions | low | accept | Tests reference locale key names and their string values; no secrets or PII are present in the i18n dictionaries |
| T-10-02 | Tampering | globalThis.document monkey-patch in test | low | accept | Patch is scoped to a single it() block and cleaned up immediately; no production execution path is affected |
</threat_model>

<verification>
node --test tests/unit/i18n.test.js exits 0 with 14+ passing tests covering the complete public API of js/i18n/index.js.
</verification>

<success_criteria>
I18N-01 verified: the i18n unit test file covers locale lookup (t() with EN and PL locales), missing-key fallback (returns the key string), t() with variable interpolation ({n} placeholder), getLang() default, setLang() valid and invalid cases, displayName() both locale branches with name_pl null fallback, and applyStaticTranslations() both DOM-present and DOM-absent paths.
</success_criteria>

<output>
Create .planning/phases/10-i18n-tests-verification/10-01-SUMMARY.md when done.

Artifacts produced by this plan:
- tests/unit/i18n.test.js (expanded — 6+ new test cases, 14+ total)
</output>
