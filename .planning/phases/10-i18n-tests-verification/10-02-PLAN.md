---
phase: 10-i18n-tests-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/i18n/en.js
  - js/i18n/pl.js
  - js/views/catalog.js
autonomous: true
requirements:
  - I18N-02
estimate:
  tokens: 130000
  raw_tokens: 65000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - js/i18n/en.js contains all 37 new locale keys spanning catalog, today, toast, history, and settings namespaces (I18N-02)
    - js/i18n/pl.js contains exactly the same 37 keys with Polish translations (I18N-02)
    - js/views/catalog.js imports t from '../i18n/index.js' (I18N-02)
    - All 12 hardcoded string sites in catalog.js now route through t() (I18N-02)
    - node --test tests/ exits 0 after all modifications
  artifacts:
    - js/i18n/en.js (37 new keys added across catalog/today/toast/history/settings namespaces)
    - js/i18n/pl.js (37 matching Polish translations added)
    - js/views/catalog.js (t() import added; all 12 hardcoded string sites replaced)
  key_links:
    - Every key added to en.js has an exactly matching key name in pl.js
    - catalog.js t() calls reference keys that exist in the updated en.js and pl.js
---

<objective>
Add all new locale keys needed across the entire Phase 10 string-fix effort in a single authoring pass over the locale dictionaries, then apply the catalog.js fixes as the end-to-end proof that the new keys work.

Purpose: I18N-02 requires zero hardcoded UI strings in any view file. catalog.js is the most impacted — it never imported t() and has 12 hardcoded string sites. By adding ALL new keys to the locale files here, Plan 10-03 only needs to edit the view files, eliminating any risk of locale-file conflicts between concurrent plans.
Output: js/i18n/en.js and js/i18n/pl.js expanded with 37 new keys; js/views/catalog.js fully using t() for all user-visible strings.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add all new locale keys to en.js and pl.js (I18N-02)</name>
  <files>js/i18n/en.js, js/i18n/pl.js</files>
  <read_first>
    - js/i18n/en.js — read the full file to understand key grouping, namespace ordering, and the last key in each namespace section; identify the existing catalog.stagePlaceholder key already present
    - js/i18n/pl.js — read the full file to understand the parallel structure and translation style
    - js/views/catalog.js — read lines 180–480 to confirm all hardcoded string sites (12 total) including showErrorToast calls and DOM property assignments
    - js/views/today.js — read lines 200–620 to identify all hardcoded string sites: showUndoToast message templates and showErrorToast calls
    - js/views/toast.js — read lines 95–125 to identify the aria-label and the undo error toast string
    - js/views/settings.js — read lines 150–500 to identify all showErrorToast calls including those with string interpolation (exported failed messages with a reason suffix)
    - js/views/history/builders.js — read lines 35–65 to identify the two hardcoded aria-label values
    - js/views/history.js — read lines 155–380 to identify showErrorToast calls
  </read_first>
  <action>
    In js/i18n/en.js, add the following 37 new keys. Insert them after their respective namespace blocks (after the last existing key in each namespace, or in a new block if the namespace has no existing keys). Where a key already exists with identical content, skip it — do not duplicate. Add a brief inline comment marking the start of new keys in each namespace to aid future readers.

    Catalog namespace additions (11 keys):
    - 'catalog.empty' with value 'No habits yet. Tap "New habit" to create one.'
    - 'catalog.errorAdvanceStage' with value "Couldn't advance stage — try again"
    - 'catalog.errorArchive' with value "Couldn't archive habit — try again"
    - 'catalog.errorCreate' with value "Couldn't create habit — try again"
    - 'catalog.errorPromote' with value "Couldn't promote habit — try again"
    - 'catalog.errorRestore' with value "Couldn't restore habit — try again"
    - 'catalog.errorSave' with value "Couldn't save habit — try again"
    - 'catalog.listAriaLabel' with value 'Habit catalog'
    - 'catalog.stageTargetPlaceholder' with value 'Target (optional)'
    - 'catalog.upcomingAriaLabel' with value 'Upcoming habits'
    - 'catalog.upcomingHeading' with value 'Upcoming'

    History namespace additions (4 keys):
    - 'history.errorMark' with value "Couldn't mark — try again"
    - 'history.errorSkip' with value "Couldn't skip — try again"
    - 'history.nextDay' with value 'Next day'
    - 'history.prevDay' with value 'Previous day'

    Settings namespace additions (12 keys):
    - 'settings.errorDismissNag' with value "Couldn't dismiss reminder: {msg}"
    - 'settings.errorExportCsv' with value 'CSV export failed: {msg}'
    - 'settings.errorExportJson' with value 'JSON export failed: {msg}'
    - 'settings.errorImport' with value 'Import failed: {msg}'
    - 'settings.errorMasteryThreshold' with value "Couldn't change mastery threshold — try again"
    - 'settings.errorMasteryWindow' with value "Couldn't change mastery window — try again"
    - 'settings.errorPersistenceRequest' with value "Couldn't request persistence"
    - 'settings.errorPersistenceUnsupported' with value 'Storage persistence not supported in this browser'
    - 'settings.errorRecompute' with value 'Recompute failed: {msg}'
    - 'settings.errorScoringModel' with value "Couldn't change scoring model — try again"
    - 'settings.errorUndo' with value "Couldn't undo — try again"
    - 'settings.errorWeekStart' with value "Couldn't change week start — try again"

    Toast namespace additions (2 keys):
    - 'toast.dismiss' with value 'Dismiss'
    - 'toast.errorUndo' with value "Couldn't undo — try again"

    Today namespace additions (7 keys):
    - 'today.errorMark' with value "Couldn't mark — try again"
    - 'today.errorSkip' with value "Couldn't skip — try again"
    - 'today.errorUpdateCount' with value "Couldn't update count — try again"
    - 'today.errorUpdateSlot' with value "Couldn't update slot — try again"
    - 'today.listAriaLabel' with value "Today's habits"
    - 'today.markedComplete' with value 'Marked {name} complete'
    - 'today.markedUncomplete' with value 'Marked {name} uncomplete'

    Total: 37 new keys. If during editing you discover any of these keys already exist in en.js with identical values, do not add duplicates.

    Then in js/i18n/pl.js, add the exact same 37 key names with the following Polish translations:

    Catalog:
    - 'catalog.empty': 'Brak nawyków. Dotknij \u201aNowy nawyk\u201c, aby dodać.'
    - 'catalog.errorAdvanceStage': 'Nie można przejść do kolejnego etapu — spróbuj ponownie'
    - 'catalog.errorArchive': 'Nie można zarchiwizować nawyku — spróbuj ponownie'
    - 'catalog.errorCreate': 'Nie można utworzyć nawyku — spróbuj ponownie'
    - 'catalog.errorPromote': 'Nie można aktywować nawyku — spróbuj ponownie'
    - 'catalog.errorRestore': 'Nie można przywrócić nawyku — spróbuj ponownie'
    - 'catalog.errorSave': 'Nie można zapisać nawyku — spróbuj ponownie'
    - 'catalog.listAriaLabel': 'Katalog nawyków'
    - 'catalog.stageTargetPlaceholder': 'Cel (opcjonalny)'
    - 'catalog.upcomingAriaLabel': 'Nadchodzące nawyki'
    - 'catalog.upcomingHeading': 'Nadchodzące'

    History:
    - 'history.errorMark': 'Nie można zaznaczyć — spróbuj ponownie'
    - 'history.errorSkip': 'Nie można pominąć — spróbuj ponownie'
    - 'history.nextDay': 'Następny dzień'
    - 'history.prevDay': 'Poprzedni dzień'

    Settings:
    - 'settings.errorDismissNag': 'Nie można odrzucić przypomnienia: {msg}'
    - 'settings.errorExportCsv': 'Eksport CSV nie powiódł się: {msg}'
    - 'settings.errorExportJson': 'Eksport JSON nie powiódł się: {msg}'
    - 'settings.errorImport': 'Import nie powiódł się: {msg}'
    - 'settings.errorMasteryThreshold': 'Nie można zmienić progu biegłości — spróbuj ponownie'
    - 'settings.errorMasteryWindow': 'Nie można zmienić okna biegłości — spróbuj ponownie'
    - 'settings.errorPersistenceRequest': 'Nie można zażądać trwałości magazynu'
    - 'settings.errorPersistenceUnsupported': 'Trwałość magazynu nie jest obsługiwana w tej przeglądarce'
    - 'settings.errorRecompute': 'Przeliczenie nie powiodło się: {msg}'
    - 'settings.errorScoringModel': 'Nie można zmienić modelu punktacji — spróbuj ponownie'
    - 'settings.errorUndo': 'Nie można cofnąć — spróbuj ponownie'
    - 'settings.errorWeekStart': 'Nie można zmienić początku tygodnia — spróbuj ponownie'

    Toast:
    - 'toast.dismiss': 'Zamknij'
    - 'toast.errorUndo': 'Nie można cofnąć — spróbuj ponownie'

    Today:
    - 'today.errorMark': 'Nie można zaznaczyć — spróbuj ponownie'
    - 'today.errorSkip': 'Nie można pominąć — spróbuj ponownie'
    - 'today.errorUpdateCount': 'Nie można zaktualizować liczby — spróbuj ponownie'
    - 'today.errorUpdateSlot': 'Nie można zaktualizować pola — spróbuj ponownie'
    - 'today.listAriaLabel': 'Nawyki na dziś'
    - 'today.markedComplete': 'Oznaczono {name} jako wykonane'
    - 'today.markedUncomplete': 'Oznaczono {name} jako niewykonane'

    Maintain the existing formatting style of each locale file (object literal with comma-separated quoted key-value pairs). Update the @file JSDoc comment in both en.js and pl.js to mention that the namespace list now includes catalog, history, settings, toast, and today error and UI string keys.
  </action>
  <verify>
    <automated>node --test tests/unit/i18n.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/unit/i18n.test.js exits 0 (locale files are syntactically valid and the module loads without error)
    - grep -c "catalog.empty" js/i18n/en.js returns 1
    - grep -c "catalog.empty" js/i18n/pl.js returns 1
    - grep -c "today.markedComplete" js/i18n/en.js returns 1
    - grep -c "today.markedComplete" js/i18n/pl.js returns 1
    - grep -c "toast.dismiss" js/i18n/en.js returns 1
    - grep -c "toast.dismiss" js/i18n/pl.js returns 1
  </acceptance_criteria>
  <done>All 37 new locale keys exist in both en.js and pl.js with correct English values and Polish translations. The i18n module loads without error.</done>
</task>

<task type="auto">
  <name>Task 2: Fix catalog.js — add t() import and replace all 12 hardcoded string sites (I18N-02)</name>
  <files>js/views/catalog.js</files>
  <read_first>
    - js/views/catalog.js — read the full file before editing; identify the import block at the top to insert the t() import, and locate all 12 hardcoded string sites: the two DOM placeholder assignments (stage label and target), the list aria-label, the empty-state textContent, the upcomingHeading textContent, the upcoming list aria-label, and the six showErrorToast calls (archive, restore, promote, advanceStage, save, create)
    - js/i18n/en.js — confirm catalog.stagePlaceholder (already exists) and all 11 new catalog keys added in Task 1 are present
  </read_first>
  <action>
    Edit js/views/catalog.js:

    1. Add import { t } from '../i18n/index.js'; to the import block at the top of the file, after the existing named imports and before any blank line that separates imports from the module body. Keep the import alphabetically consistent with the existing pattern.

    2. Replace the stage label DOM placeholder assignment (line ~196) with t('catalog.stagePlaceholder'). Note: this key already existed in en.js before this plan, so no new key is needed for it.

    3. Replace the stage target DOM placeholder assignment (line ~205) with t('catalog.stageTargetPlaceholder').

    4. Replace the main habit list aria-label attribute assignment (line ~250) with t('catalog.listAriaLabel').

    5. Replace the empty-state textContent assignment (line ~261) with t('catalog.empty').

    6. Replace the upcoming section heading textContent assignment (line ~273) with t('catalog.upcomingHeading').

    7. Replace the upcoming list aria-label attribute assignment (line ~278) with t('catalog.upcomingAriaLabel').

    8. Replace the six showErrorToast calls in the archive, restore, promote, advanceStage, save, and create handlers (lines ~367, ~381, ~396, ~413, ~446, ~476) with showErrorToast(t('catalog.errorArchive')), showErrorToast(t('catalog.errorRestore')), showErrorToast(t('catalog.errorPromote')), showErrorToast(t('catalog.errorAdvanceStage')), showErrorToast(t('catalog.errorSave')), showErrorToast(t('catalog.errorCreate')) respectively.

    9. Update the @file JSDoc at the top of catalog.js to reference the i18n import and note that all user-visible strings route through t() per I18N-02.

    Do not change any logic, DOM structure, or event handler behaviour — only string literal arguments and property assignments are replaced. Do not use .innerHTML anywhere (D-78).
  </action>
  <verify>
    <automated>node --test tests/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "import.*{ t" js/views/catalog.js returns 1
    - grep -c "from '../i18n/index.js'" js/views/catalog.js returns at least 1
    - node --test tests/ exits 0
  </acceptance_criteria>
  <done>
    js/views/catalog.js imports t from js/i18n/index.js and all 12 previously-hardcoded string sites now route through t() using the catalog-namespace keys. node --test tests/ exits 0 confirming no regressions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| i18n key → DOM textContent | t() output is assigned to element.textContent (never innerHTML), preventing XSS injection via locale strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-03 | Tampering | locale dictionary injection | low | accept | t() output goes to textContent only; D-78 grep gate forbids innerHTML; locale files are static JS shipped with the app and never fetched remotely |
| T-10-04 | Information Disclosure | locale strings | low | accept | All locale values are static UI labels; no PII, secrets, or sensitive data in the dictionary |
| T-10-SC | Tampering | npm/pip/cargo installs | low | accept | This plan has zero package-manager installs; no legitimacy audit required |
</threat_model>

<verification>
node --test tests/ exits 0. grep -c "import.*{ t" js/views/catalog.js returns 1. Both en.js and pl.js contain the 37 new keys with no duplicate keys.
</verification>

<success_criteria>
I18N-02 (partial): js/i18n/en.js and js/i18n/pl.js are expanded with all 37 new locale keys needed across all view files. js/views/catalog.js is fully i18n-adopted: it imports t() and all 12 hardcoded string sites use t() with catalog-namespace keys. Plan 10-03 can proceed to fix the remaining view files without touching the locale files.
</success_criteria>

<output>
Create .planning/phases/10-i18n-tests-verification/10-02-SUMMARY.md when done.

Artifacts produced by this plan:
- js/i18n/en.js (37 new keys added)
- js/i18n/pl.js (37 matching Polish translations added)
- js/views/catalog.js (t() import added; 12 string sites replaced)
</output>
