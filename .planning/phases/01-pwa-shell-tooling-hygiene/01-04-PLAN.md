---
phase: 01-pwa-shell-tooling-hygiene
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
files_modified:
  - js/platform/sw-register.js
  - js/main.js
  - js/desktop.js
  - index.html
  - desktop.html
autonomous: true
requirements:
  - PWA-04
  - NFR-09
  - NFR-04
tags:
  - sw-registration
  - file-safe
  - controllerchange
  - had-controller
  - html-shells
  - vertical-slice

must_haves:
  truths:
    - "Opening index.html via file:// renders the empty Today scaffold with zero console errors and navigator.serviceWorker.controller === null (per D-20 + PWA-04 + NFR-09)"
    - "Opening index.html via http(s):// registers ./sw.js silently; on file:// the protocol guard early-returns BEFORE calling register() (per D-20 + research §Pattern 3)"
    - "controllerchange fires showUpdateToast() ONLY when navigator.serviceWorker.controller was non-null BEFORE registration (real update, not first install — per D-08 + Pitfall 6)"
    - "Both index.html and desktop.html link manifest.json + css/main.css + iOS Safari standalone meta trio (apple-mobile-web-app-capable, apple-mobile-web-app-status-bar-style, apple-mobile-web-app-title) per Pattern D + D-04"
    - "index.html body holds the empty Today scaffold (header, list area, footer-nav stub) with three labels today · history · settings (D-01)"
    - "desktop.html body holds the 'Switch to mobile' stub per D-04 + research §Example 5"
    - "Both HTML shells use only ./ relative paths for href/src/link href (D-19 + NFR-12)"
    - "main.js wires registerServiceWorker() + ?debug=1 trigger + long-press attach on app title; desktop.js wires registerServiceWorker() + ?debug=1 trigger only (per D-04 — no long-press on desktop stub)"
  artifacts:
    - path: "js/platform/sw-register.js"
      provides: "Single integration point for navigator.serviceWorker (D-20)"
      exports: ["registerServiceWorker"]
      contains: "location.protocol.startsWith('http')"
    - path: "js/main.js"
      provides: "Mobile entry point — wires SW register + diagnostics triggers"
      contains: "attachLongPress"
    - path: "js/desktop.js"
      provides: "Desktop entry point — SW register + ?debug=1 only (D-04)"
      contains: "mountDiagnostics"
    - path: "index.html"
      provides: "Mobile shell with empty Today scaffold (D-01)"
      contains: "data-app-title"
    - path: "desktop.html"
      provides: "Desktop stub with 'Switch to mobile' link (D-04)"
      contains: "Open the mobile view"
  key_links:
    - from: "js/platform/sw-register.js"
      to: "js/views/toast.js showUpdateToast"
      via: "ES module import + controllerchange listener"
      pattern: "import\\s*\\{\\s*showUpdateToast\\s*\\}\\s*from\\s*['\"]\\.\\./views/toast\\.js['\"]"
    - from: "js/platform/sw-register.js"
      to: "navigator.serviceWorker.register('./sw.js')"
      via: "registration call behind protocol guard + silent catch"
      pattern: "navigator\\.serviceWorker\\.register\\(['\"]\\./sw\\.js['\"]\\)"
    - from: "js/main.js"
      to: "js/views/diagnostics.js attachLongPress + mountDiagnostics"
      via: "ES module import + URLSearchParams trigger + element-bound long-press"
      pattern: "attachLongPress\\(.*mountDiagnostics\\)"
    - from: "index.html"
      to: "js/main.js"
      via: "<script type=\"module\" src=\"./js/main.js\">"
      pattern: "<script\\s+type=\"module\"\\s+src=\"\\./js/main\\.js\""
    - from: "index.html"
      to: "manifest.json"
      via: "<link rel=\"manifest\" href=\"./manifest.json\">"
      pattern: "<link\\s+rel=\"manifest\"\\s+href=\"\\./manifest\\.json\""
---

<objective>
Wire the chassis. Ship the SW-registration module (`js/platform/sw-register.js`) with the protocol guard + silent catch + controllerchange handler, the two HTML shells (`index.html` mobile with empty Today scaffold, `desktop.html` desktop stub) per D-01 and D-04, and the two entry points (`js/main.js`, `js/desktop.js`) that call `registerServiceWorker()` and mount the diagnostics triggers. After this plan, the Walking Skeleton acceptance sequence (steps 1–7 from SKELETON.md) becomes runnable.

Purpose: This is the vertical-slice payoff. Plans 01-01 / 01-02 / 01-03 each ship pieces; this plan integrates them into the deliverable. PWA-04 (silent SW fail on file://) and NFR-09 (loads on file://) land here; NFR-04 (no network calls except export/import — which don't exist in P1) is verified here by the absence of any `fetch()` calls in the wiring.

Output: Five new files: `js/platform/sw-register.js`, `js/main.js`, `js/desktop.js`, `index.html`, `desktop.html`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pwa-shell-tooling-hygiene/01-CONTEXT.md
@.planning/phases/01-pwa-shell-tooling-hygiene/01-RESEARCH.md
@.planning/phases/01-pwa-shell-tooling-hygiene/01-PATTERNS.md
@.planning/phases/01-pwa-shell-tooling-hygiene/01-SKELETON.md
@.planning/phases/01-pwa-shell-tooling-hygiene/01-01-SUMMARY.md
@.planning/phases/01-pwa-shell-tooling-hygiene/01-02-SUMMARY.md
@.planning/phases/01-pwa-shell-tooling-hygiene/01-03-SUMMARY.md
@js/util/version.js
@js/views/toast.js
@js/views/diagnostics.js
@sw.js
@manifest.json
@css/main.css
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create sw-register.js with protocol guard, silent catch, and controllerchange wiring</name>
  <files>js/platform/sw-register.js</files>
  <read_first>
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-RESEARCH.md §"Pattern 3: sw-register.js (file://-safe registration + update wiring)" — the locked code shape including the hadController capture pattern
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-RESEARCH.md §"Pitfall 2: SW Registration Errors Crash the App on file://" — the two-defense rule (protocol guard + silent catch)
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-RESEARCH.md §"Pitfall 6: Update Toast Shows on First-Ever Install" — the hadController capture rationale
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-PATTERNS.md §"js/platform/sw-register.js" + §"Pattern A: Silent-Fail SW Registration"
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-CONTEXT.md decisions D-08, D-20
    - C:/Users/lukasz.bielinski/projects/mindful-breathing/index.html lines 1898-1903 — the silent-catch baseline this extends
    - js/views/toast.js — confirms `showUpdateToast` is the exported name (already shipped by Plan 01-03)
  </read_first>
  <behavior>
    - Exports `registerServiceWorker()` (named function export).
    - On call, the function:
      1. Returns early if `!('serviceWorker' in navigator)` (feature detect — first defense).
      2. Returns early if `!location.protocol.startsWith('http')` (protocol guard — second defense; D-20; mitigates T-01-FileSafe).
      3. Captures `const hadController = !!navigator.serviceWorker.controller` BEFORE registering (per research §Pitfall 6 — distinguishes real updates from first installs).
      4. Defers the actual `navigator.serviceWorker.register('./sw.js')` call until `window.addEventListener('load', ...)`, with `.catch(() => { /* silent */ })` appended (third defense — silent catch).
      5. Adds a `navigator.serviceWorker.addEventListener('controllerchange', ...)` listener that calls `showUpdateToast()` ONLY if `hadController` was true (per D-08 + Pitfall 6 — no toast on first install).
    - Module imports `showUpdateToast` from `../views/toast.js`.
    - The registration path is `./sw.js` (relative — D-19; NOT bare `sw.js` like mindful-breathing's analog).
  </behavior>
  <action>
    Create `js/platform/sw-register.js` per RESEARCH.md §Pattern 3 verbatim.

    Top: `import { showUpdateToast } from '../views/toast.js';`.

    Export `export function registerServiceWorker()`:
    1. `if (!('serviceWorker' in navigator)) return;` — first defense.
    2. `if (!location.protocol.startsWith('http')) return;` — second defense, the file:// silent skip per D-20.
    3. `const hadController = !!navigator.serviceWorker.controller;` — capture BEFORE registering (Pitfall 6).
    4. `window.addEventListener('load', () => { navigator.serviceWorker.register('./sw.js').catch(() => { /* silent */ }); });` — third defense, silent catch. The `load`-deferral ensures registration does not contend with first paint.
    5. `navigator.serviceWorker.addEventListener('controllerchange', () => { if (!hadController) return; showUpdateToast(); });` — D-08 wiring; per Pitfall 6, only fire on real updates.

    Per D-19, path is `'./sw.js'` (relative). Per Pattern C, never absolute, never `https://`. The `hadController` is captured synchronously before `load` fires; on first install it is `false`; on a refresh of a tab that already had a controller, it is `true`.

    Use the Write tool.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('js/platform/sw-register.js','utf8').replace(/\/\/[^\n]*/g,'').replace(/\/\*[\s\S]*?\*\//g,''); const k = (re,msg) => { if (!re.test(c)) { console.error('FAIL',msg); process.exit(1); } }; k(/import\s*\{\s*showUpdateToast\s*\}\s*from\s*['\"]\.\.\/views\/toast\.js['\"]/, 'import showUpdateToast'); k(/export\s+function\s+registerServiceWorker/, 'export registerServiceWorker'); k(/'serviceWorker'\s+in\s+navigator/, 'feature detect'); k(/location\.protocol\.startsWith\(['\"]http['\"]\)/, 'protocol guard D-20'); k(/const\s+hadController\s*=\s*!!navigator\.serviceWorker\.controller/, 'hadController capture'); k(/window\.addEventListener\(['\"]load['\"]/, 'load-deferred registration'); k(/navigator\.serviceWorker\.register\(['\"]\.\/sw\.js['\"]\)/, 'register ./sw.js relative'); k(/\.catch\(\(\)\s*=>\s*\{[^}]*\}\)/, 'silent catch'); k(/addEventListener\(['\"]controllerchange['\"]/, 'controllerchange listener'); k(/if\s*\(\s*!\s*hadController\s*\)\s*return/, 'first-install guard before toast'); k(/showUpdateToast\(\)/, 'showUpdateToast call'); console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: imports `showUpdateToast` from `../views/toast.js`.
    - Source assertion: `'serviceWorker' in navigator` feature-detect early-return.
    - Source assertion: `location.protocol.startsWith('http')` protocol-guard early-return (D-20 second defense; T-01-FileSafe mitigation).
    - Source assertion: `const hadController = !!navigator.serviceWorker.controller` captured BEFORE `register()` call.
    - Source assertion: `navigator.serviceWorker.register('./sw.js').catch(() => { ... })` inside `window.addEventListener('load', ...)`.
    - Source assertion: `controllerchange` listener checks `if (!hadController) return` before calling `showUpdateToast()`.
    - Behavior assertion (after Tasks 2–5 land): opening `index.html` via `file://` → console clean, `navigator.serviceWorker.controller === null`, no SecurityError. Opening via `http://localhost:8000/` → SW registers, cache populated; refresh → toast does NOT appear (first install). Bump APP_VERSION, redeploy, refresh → SW updates, controllerchange fires, toast appears (real update; hadController was true).
  </acceptance_criteria>
  <done>
    `js/platform/sw-register.js` exists, exports `registerServiceWorker`, ships the three-layer defense (feature detect + protocol guard + silent catch), captures `hadController` before registering, and wires `controllerchange → showUpdateToast` only for real updates. Both T-01-FileSafe (mitigated via protocol guard) and T-01-StaleCache (mitigated via update toast UX) land here.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create main.js and desktop.js entry points</name>
  <files>js/main.js, js/desktop.js</files>
  <read_first>
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-RESEARCH.md §"Pattern 5: ?debug=1 detection" — the URLSearchParams trigger
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-RESEARCH.md §"Example 6: js/desktop.js (the trivial entry)"
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-PATTERNS.md §"js/main.js" + §"js/desktop.js" — full shapes
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-CONTEXT.md decisions D-02 (two diagnostics triggers), D-04 (desktop stub minimal)
    - js/platform/sw-register.js — confirms `registerServiceWorker` is the export
    - js/views/diagnostics.js — confirms `mountDiagnostics` and `attachLongPress` are the exports
  </read_first>
  <behavior>
    - `js/main.js`: calls `registerServiceWorker()` synchronously; reads `?debug=1` via URLSearchParams and calls `mountDiagnostics()` if matched; queries `document.querySelector('[data-app-title]')` and calls `attachLongPress(titleEl, mountDiagnostics)` if the element exists.
    - `js/desktop.js`: calls `registerServiceWorker()`; reads `?debug=1` and calls `mountDiagnostics()` if matched. No long-press attach (D-04: desktop stub is minimal; no app title to long-press).
    - Both files use only ES module imports with `./` relative specifiers.
  </behavior>
  <action>
    Create `js/main.js` per RESEARCH.md §Pattern 5 + §"Mobile entry shape" in PATTERNS.md:

    ```
    import { registerServiceWorker } from './platform/sw-register.js';
    import { mountDiagnostics, attachLongPress } from './views/diagnostics.js';

    registerServiceWorker();

    // ?debug=1 trigger (D-02)
    const params = new URLSearchParams(location.search);
    if (params.get('debug') === '1') mountDiagnostics();

    // Long-press title trigger (D-02)
    const titleEl = document.querySelector('[data-app-title]');
    if (titleEl) attachLongPress(titleEl, mountDiagnostics);
    ```

    Create `js/desktop.js` per RESEARCH.md §Example 6:

    ```
    import { registerServiceWorker } from './platform/sw-register.js';
    import { mountDiagnostics } from './views/diagnostics.js';

    registerServiceWorker();

    const params = new URLSearchParams(location.search);
    if (params.get('debug') === '1') mountDiagnostics();
    ```

    Both files use `./platform/…` and `./views/…` relative imports (D-19). No `fetch()` calls anywhere (NFR-04 / T-01-NoNet). Use the Write tool.
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const main = fs.readFileSync('js/main.js','utf8').replace(/\/\/[^\n]*/g,'').replace(/\/\*[\s\S]*?\*\//g,''); const desk = fs.readFileSync('js/desktop.js','utf8').replace(/\/\/[^\n]*/g,'').replace(/\/\*[\s\S]*?\*\//g,''); const k = (c, re, msg) => { if (!re.test(c)) { console.error('FAIL',msg); process.exit(1); } }; k(main, /import\s*\{\s*registerServiceWorker\s*\}\s*from\s*['\"]\.\/platform\/sw-register\.js['\"]/, 'main imports registerServiceWorker'); k(main, /import\s*\{\s*mountDiagnostics\s*,\s*attachLongPress\s*\}\s*from\s*['\"]\.\/views\/diagnostics\.js['\"]/, 'main imports diagnostics'); k(main, /registerServiceWorker\(\)/, 'main calls registerServiceWorker'); k(main, /new\s+URLSearchParams\(location\.search\)/, 'main URLSearchParams'); k(main, /params\.get\(['\"]debug['\"]\)\s*===\s*['\"]1['\"]/, 'main ?debug=1 check'); k(main, /document\.querySelector\(['\"]\[data-app-title\]['\"]\)/, 'main querySelector title'); k(main, /attachLongPress\([^,]+,\s*mountDiagnostics\)/, 'main attachLongPress'); k(desk, /import\s*\{\s*registerServiceWorker\s*\}\s*from\s*['\"]\.\/platform\/sw-register\.js['\"]/, 'desktop imports registerServiceWorker'); k(desk, /import\s*\{\s*mountDiagnostics\s*\}\s*from\s*['\"]\.\/views\/diagnostics\.js['\"]/, 'desktop imports mountDiagnostics'); k(desk, /registerServiceWorker\(\)/, 'desktop calls registerServiceWorker'); if (/attachLongPress/.test(desk)) { console.error('FAIL: desktop.js must NOT attach long-press per D-04'); process.exit(2); } if (/fetch\(/.test(main) || /fetch\(/.test(desk)) { console.error('FAIL T-01-NoNet: fetch() in entry'); process.exit(3); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `js/main.js` imports `registerServiceWorker` from `./platform/sw-register.js`, and `mountDiagnostics + attachLongPress` from `./views/diagnostics.js`.
    - Source assertion: `js/main.js` calls `registerServiceWorker()` and reads `?debug=1` via `new URLSearchParams(location.search).get('debug') === '1'`.
    - Source assertion: `js/main.js` queries `[data-app-title]` and calls `attachLongPress(titleEl, mountDiagnostics)` when found.
    - Source assertion: `js/desktop.js` imports `registerServiceWorker` and `mountDiagnostics` only (no `attachLongPress` per D-04).
    - Source assertion: neither file contains `fetch(` (T-01-NoNet).
    - Behavior assertion (after Tasks 3–5 land + manifest/SW loaded on HTTPS): mobile shell calls SW register; long-press app title 1.5 s mounts diagnostics; `?debug=1` mounts diagnostics. Desktop stub mounts diagnostics on `?debug=1` but has no long-press surface.
  </acceptance_criteria>
  <done>
    Both entry-point files exist with the locked imports and wiring. Mobile entry attaches the two D-02 triggers; desktop entry attaches only the `?debug=1` trigger. Neither file produces outbound fetches.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create index.html mobile shell with empty Today scaffold</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-PATTERNS.md §"index.html" — the head shape diff against mindful-breathing
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-PATTERNS.md §"Pattern D: iOS Safari Standalone Meta Trio" — the apple-mobile-web-app-* trio
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-CONTEXT.md decisions D-01 (empty Today scaffold contents), D-13/D-14/D-15/D-16/D-19
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-RESEARCH.md §"State of the Art" row 4 — keep both legacy meta and modern manifest for iOS safety
    - C:/Users/lukasz.bielinski/projects/mindful-breathing/index.html (lines 1-14 for head shape, lines 9-11 for the apple-* trio)
    - css/today.css — confirms the `.today-header`, `.today-list`, `.today-footer-nav` classes already exist (Plan 01-01)
    - js/main.js — confirms it expects an element with `[data-app-title]` attribute
  </read_first>
  <behavior>
    - Valid HTML5 document with `<!DOCTYPE html>` and `<html lang="en">`.
    - Head section contains: `<meta charset="UTF-8">`, `<meta name="viewport" content="width=device-width, initial-scale=1">`, `<title>Habits</title>`, `<meta name="description" content="Personal multi-year habit tracker.">`, `<meta name="theme-color" content="#f5a623">`, the iOS Safari standalone meta trio (apple-mobile-web-app-capable=yes, apple-mobile-web-app-status-bar-style=black-translucent, apple-mobile-web-app-title=Habits), `<link rel="icon" href="./icon.svg">`, `<link rel="apple-touch-icon" href="./icon.svg">`, `<link rel="manifest" href="./manifest.json">`, and `<link rel="stylesheet" href="./css/main.css">`.
    - Body contains the empty Today scaffold per D-01: a header element with class `today-header` containing an `<h1 data-app-title>Habits</h1>` (the long-press target via the `data-app-title` selector) plus placeholders for "Today's date" and "Current wave"; a list element (`<ul class="today-list">` empty); a footer nav stub (`<nav class="today-footer-nav">`) with three non-clickable label spans: "today", "history", "settings". The footer labels are intentionally non-anchors — D-01 says "they're not clickable yet."
    - Single `<script type="module" src="./js/main.js"></script>` at the end of body.
    - All paths relative `./*` per D-19. No absolute paths, no off-origin URLs.
  </behavior>
  <action>
    Create `index.html` at the project root.

    Head shape per mindful-breathing analog + D-13/D-14/D-15/D-16/D-19:
    - `<!DOCTYPE html>` then `<html lang="en">`.
    - `<head>` with: `<meta charset="UTF-8">`; `<meta name="viewport" content="width=device-width, initial-scale=1">`; `<title>Habits</title>`; `<meta name="description" content="Personal multi-year habit tracker.">`; `<meta name="theme-color" content="#f5a623">`.
    - iOS Safari standalone trio (Pattern D, mandatory per research §State of the Art row 4): `<meta name="apple-mobile-web-app-capable" content="yes">`, `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">`, `<meta name="apple-mobile-web-app-title" content="Habits">`.
    - Links: `<link rel="icon" href="./icon.svg">`, `<link rel="apple-touch-icon" href="./icon.svg">`, `<link rel="manifest" href="./manifest.json">`, `<link rel="stylesheet" href="./css/main.css">`.

    Body per D-01 (empty Today scaffold):
    - `<header class="today-header">` containing `<h1 data-app-title>Habits</h1>` (the long-press target) and two `<div>` placeholders for the date and the current wave (e.g., `<div class="today-date" aria-label="Today's date"></div>` and `<div class="today-wave" aria-label="Current wave"></div>` — content is empty in P1; P3 fills it).
    - `<main>` containing `<ul class="today-list" aria-label="Today's habits"></ul>` — empty in P1.
    - `<nav class="today-footer-nav" aria-label="Primary navigation">` containing three `<span>` labels: `<span>today</span>`, `<span>history</span>`, `<span>settings</span>`. They are spans (NOT anchors) because per CONTEXT §Specifics they are placeholders proving the layout, not clickable — wiring lands in later phases.
    - Final element: `<script type="module" src="./js/main.js"></script>` right before `</body>`.

    Every `href` and `src` is `./…` relative (D-19). Per NFR-12, the same file deploys at `file://`, `http://localhost:8000/`, and `https://bielinskilukasz.github.io/habits/` with zero changes.

    Use the Write tool.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('index.html','utf8'); const k = (re,msg) => { if (!re.test(c)) { console.error('FAIL',msg); process.exit(1); } }; k(/<!DOCTYPE html>/i, 'doctype'); k(/<html\s+lang=\"en\">/i, 'lang=en'); k(/<title>Habits<\/title>/, 'title Habits'); k(/<meta\s+name=\"description\"\s+content=\"Personal multi-year habit tracker\."/, 'description'); k(/<meta\s+name=\"theme-color\"\s+content=\"#f5a623\"/, 'theme-color'); k(/<meta\s+name=\"apple-mobile-web-app-capable\"\s+content=\"yes\"/, 'apple capable'); k(/<meta\s+name=\"apple-mobile-web-app-status-bar-style\"\s+content=\"black-translucent\"/, 'apple status-bar'); k(/<meta\s+name=\"apple-mobile-web-app-title\"\s+content=\"Habits\"/, 'apple title'); k(/<link\s+rel=\"manifest\"\s+href=\"\.\/manifest\.json\"/, 'manifest link'); k(/<link\s+rel=\"icon\"\s+href=\"\.\/icon\.svg\"/, 'icon link'); k(/<link\s+rel=\"apple-touch-icon\"\s+href=\"\.\/icon\.svg\"/, 'apple-touch-icon link'); k(/<link\s+rel=\"stylesheet\"\s+href=\"\.\/css\/main\.css\"/, 'stylesheet main.css'); k(/<h1\s+data-app-title>Habits<\/h1>/, 'h1 data-app-title hook'); k(/<ul\s+class=\"today-list\"/, 'today-list ul'); k(/<nav\s+class=\"today-footer-nav\"/, 'footer-nav'); k(/<span>today<\/span>/, 'today label'); k(/<span>history<\/span>/, 'history label'); k(/<span>settings<\/span>/, 'settings label'); k(/<script\s+type=\"module\"\s+src=\"\.\/js\/main\.js\"/, 'main.js module script'); if (/href=\"\/[a-z]/i.test(c) || /src=\"\/[a-z]/i.test(c)) { console.error('FAIL NFR-12: absolute path in index.html'); process.exit(2); } if (/https?:\/\//i.test(c)) { console.error('FAIL T-01-NoNet: off-origin URL in index.html'); process.exit(3); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file is a valid HTML5 document (`<!DOCTYPE html>` + `<html lang="en">`).
    - Source assertion: all five `<link>` tags use `./…` relative paths (manifest, icon, apple-touch-icon, stylesheet).
    - Source assertion: iOS Safari standalone meta trio is present (Pattern D).
    - Source assertion: `<h1 data-app-title>Habits</h1>` exists (long-press target).
    - Source assertion: footer nav has three non-anchor `<span>` labels: `today`, `history`, `settings`.
    - Source assertion: single `<script type="module" src="./js/main.js"></script>` at end of body.
    - Source assertion (NFR-12): `grep -nE 'href="\s*/[a-z]|src="\s*/[a-z]' index.html` returns empty (no absolute paths).
    - Source assertion (T-01-NoNet): `grep -E 'https?://' index.html` returns empty (no off-origin URLs).
    - Behavior assertion: open `index.html` via `file://` → page renders the empty scaffold, console clean, no SW registered, `navigator.serviceWorker.controller === null`. Open via `http://localhost:8000/?debug=1` → diagnostics panel mounts.
  </acceptance_criteria>
  <done>
    `index.html` exists with the head shape from mindful-breathing extended for D-13..D-16, the iOS standalone meta trio, all `./…` relative paths, the empty Today scaffold (D-01), the `data-app-title` long-press anchor, and the `js/main.js` module entry. PWA-04 + NFR-09 + NFR-12 are demonstrable from this file alone.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Create desktop.html stub with 'Switch to mobile' link</name>
  <files>desktop.html</files>
  <read_first>
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-RESEARCH.md §"Example 5: Minimal desktop.html stub (D-04)"
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-PATTERNS.md §"desktop.html" — same head shape as index.html; trivial body
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-CONTEXT.md decision D-04 — "minimal Switch to mobile stub … Same `<meta>`s, same manifest link, same `sw-register.js` call"
    - index.html (Task 3 above) — for the head shape to mirror
  </read_first>
  <behavior>
    - Valid HTML5 document with the same head shape as `index.html` (manifest link, icon, stylesheet, iOS meta trio) so the install path exists for both shells from day one (D-04).
    - Body contains a `<main class="stub">` with an `<h1>Habits</h1>`, a `<p>` "The desktop analytics layout ships in a later phase.", and a `<p><a href="./index.html">Open the mobile view →</a></p>`.
    - Single `<script type="module" src="./js/desktop.js"></script>` at end of body.
  </behavior>
  <action>
    Create `desktop.html` at the project root per RESEARCH.md §Example 5.

    Head: identical to `index.html` from Task 3 EXCEPT the title is `<title>Habits — Desktop (coming soon)</title>` and the script is `./js/desktop.js`. All other head elements (meta charset, viewport, description, theme-color, the iOS meta trio, the four link tags) are byte-equal so installability is identical for both shells (D-04).

    Body:
    - `<main class="stub">` containing:
      - `<h1>Habits</h1>` (no `data-app-title` attribute — desktop has no long-press surface per D-04).
      - `<p>The desktop analytics layout ships in a later phase.</p>`
      - `<p><a href="./index.html">Open the mobile view →</a></p>`
    - `<script type="module" src="./js/desktop.js"></script>` at end of body.

    All paths relative `./…`. Use the Write tool.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('desktop.html','utf8'); const k = (re,msg) => { if (!re.test(c)) { console.error('FAIL',msg); process.exit(1); } }; k(/<!DOCTYPE html>/i, 'doctype'); k(/<html\s+lang=\"en\">/i, 'lang=en'); k(/<title>Habits\s+—\s+Desktop\s+\(coming soon\)<\/title>/, 'desktop title'); k(/<link\s+rel=\"manifest\"\s+href=\"\.\/manifest\.json\"/, 'manifest link'); k(/<link\s+rel=\"stylesheet\"\s+href=\"\.\/css\/main\.css\"/, 'stylesheet link'); k(/<meta\s+name=\"apple-mobile-web-app-capable\"\s+content=\"yes\"/, 'iOS meta trio capable'); k(/<a\s+href=\"\.\/index\.html\">Open the mobile view/, 'switch-to-mobile link'); k(/<script\s+type=\"module\"\s+src=\"\.\/js\/desktop\.js\"/, 'desktop.js module script'); if (/data-app-title/.test(c)) { console.error('FAIL: desktop.html must NOT carry data-app-title per D-04'); process.exit(2); } if (/href=\"\/[a-z]/i.test(c) || /src=\"\/[a-z]/i.test(c)) { console.error('FAIL NFR-12: absolute path in desktop.html'); process.exit(3); } if (/https?:\/\//i.test(c)) { console.error('FAIL T-01-NoNet: off-origin URL in desktop.html'); process.exit(4); } console.log('OK');"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: file is a valid HTML5 document.
    - Source assertion: same head shape as `index.html` (manifest + icon + stylesheet + iOS meta trio).
    - Source assertion: body has `<main class="stub">` with the three elements (h1, intro p, switch link p).
    - Source assertion: no `data-app-title` attribute (no long-press on desktop per D-04).
    - Source assertion: single `<script type="module" src="./js/desktop.js">`.
    - Source assertion (NFR-12): no absolute paths.
    - Source assertion (T-01-NoNet): no off-origin URLs.
    - Behavior assertion: open `desktop.html` via `file://` → renders the stub, console clean. Open via HTTPS → SW registers (same code path as mobile). Click "Open the mobile view →" → navigates to `index.html`.
  </acceptance_criteria>
  <done>
    `desktop.html` exists as the minimal D-04 stub: same head shape as mobile (so installability paths exist for both), trivial body with switch-to-mobile link, no long-press surface, dedicated `desktop.js` entry that shares `sw-register.js` and `diagnostics.js` modules.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Walking-Skeleton smoke verification (steps 1–7 from SKELETON.md)</name>
  <what-built>
    - Plan 01-01 shipped: `js/util/version.js`, six CSS files, `icon.svg`, `manifest.json`.
    - Plan 01-02 shipped: `sw.js` (classic SW + versioned cache + activate cleanup + SWR + same-origin guard).
    - Plan 01-03 shipped: `js/views/toast.js`, `js/views/diagnostics.js`.
    - Plan 01-04 (this plan): `js/platform/sw-register.js`, `js/main.js`, `js/desktop.js`, `index.html`, `desktop.html`.
    - The Walking Skeleton acceptance sequence (SKELETON.md steps 1–7) is now runnable on the developer machine.
  </what-built>
  <how-to-verify>
    Run each step in order. Each step unlocks the next. The full sequence is documented in `.planning/phases/01-pwa-shell-tooling-hygiene/01-SKELETON.md` §"Walking Skeleton Acceptance Sequence" and mirrors `01-RESEARCH.md` §"Manual Smoke-Test Checklist".

    1. **File:// open** — Double-click `index.html` from File Explorer (Windows). DevTools (F12) → Console: ZERO errors. Run `navigator.serviceWorker.controller` in the console → returns `null`. The empty Today scaffold renders (header "Habits", empty list, footer with three labels).
    2. **HTTPS open** — From the repo root, run `python -m http.server 8000`. Open `http://localhost:8000/` in Chrome. DevTools → Application → Service Workers: `sw.js` is registered and activated. DevTools → Application → Cache Storage: a cache named `nawyki-v1` exists containing all 17 SHELL entries.
    3. **Offline reload** — In DevTools → Network, check "Offline". Reload the page. The empty Today scaffold renders identically. No console errors.
    4. **`?debug=1` trigger** — Visit `http://localhost:8000/?debug=1`. The diagnostics panel mounts. All six rows are visible: App version = "v1", Schema version = "n/a (P2)", Service worker = "controlled" (or similar), Cache name = "nawyki-v1", Install state = "browser", Persistence = "n/a (P2)". Three buttons visible: Reset shell (enabled), Reset data (disabled, tooltip "available in P2"), Check for update (enabled).
    5. **Long-press trigger** — On the page (NOT in `?debug=1` mode — close the panel by reloading without the query param), hold the "Habits" title with mouse (or touch on a mobile DevTools emulation) for 1.5 s without moving the pointer. The diagnostics panel mounts. Repeat but immediately drag the pointer after pressing down — the panel does NOT mount (movement-tolerance cancellation works).
    6. **Reset shell** — In the diagnostics panel, click "Reset shell". A confirm dialog appears with the EXACT text: `"Reset shell — unregister service worker and clear all caches. Logs are NOT affected. Reload to a fresh install."`. Click OK. The page reloads. DevTools → Application → Service Workers: no SW immediately (then re-registers on the next page load). DevTools → Application → Cache Storage: empty briefly (then `nawyki-v1` reappears as the SW re-installs).
    7. **Update toast** — Open `js/util/version.js`, change `'v1'` to `'v2'`, save. Hard-reload the page (`Ctrl+F5`). On the second reload (Ctrl+R after the hard-reload), the toast "New version ready — Reload" appears at the bottom of the page. DevTools shows `nawyki-v1` deleted and `nawyki-v2` active. Click "Reload" on the toast → page reloads to the new version. Now refresh the page once more — the toast does NOT reappear (the second reload is no longer a "real update"). Revert `js/util/version.js` back to `'v1'` and bump again if needed for the next round.

    Run the static gates documented in `01-VALIDATION.md`:
    - `grep -rE "fetch\\(\\s*['\"]https?:" js/` → empty (NFR-04 / T-01-NoNet).
    - `ls` shows no `node_modules/`, `package.json`, `dist/`, `build/` (NFR-11).
    - `grep -nE '"\\s*/[a-z]' index.html desktop.html manifest.json sw.js` → empty (NFR-12).
  </how-to-verify>
  <resume-signal>
    Reply with "approved" once all 7 acceptance-sequence steps pass AND the three static-gate greps return empty. If any step fails, describe the failure (which step, exact symptom, DevTools output) so we can revise the relevant plan. The phase-gate steps (Android / iOS / desktop install on real devices, GitHub Pages sub-path deploy) are scheduled separately in Plan 01-05.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → SW registration call | The `register('./sw.js')` call is the gate that gives the SW thread its authority; both the protocol guard and the silent catch defend it |
| file:// runtime → main thread | Opening from disk bypasses the secure-context requirement; the silent-fail discipline keeps the page renderable even when the SW cannot register |
| User pointer → app title element | The `data-app-title` element is the sole long-press surface; the diagnostics-mount action is reachable only via this element OR `?debug=1` |
| HTML head → install pipeline | Manifest link + iOS standalone meta trio constitute the configuration the OS install pipeline consumes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-FileSafe | Denial of Service | `js/platform/sw-register.js` registration call | mitigate | Two defenses: `'serviceWorker' in navigator` feature detect AND `location.protocol.startsWith('http')` protocol guard. Plus the silent `.catch(() => {})` as third defense. Mirrors mindful-breathing/index.html lines 1898-1903. Verified by: opening `index.html` via `file://` → console clean + `navigator.serviceWorker.controller === null`. |
| T-01-StaleCache | Tampering | `controllerchange` handler in sw-register.js | mitigate | Pairs with Plan 02's versioned-cache cleanup. The `hadController` capture distinguishes real updates from first installs (Pitfall 6). User sees the toast on real updates and can reload at their own pace per D-08; no auto-reload mid-tap per D-09. |
| T-01-V14 (manifest scope) | Tampering | `index.html` / `desktop.html` head links | mitigate | All `<link href="./…">` and `<script src="./…">` are relative + same-origin. No `<script src="https://…">` or off-origin manifest reference. Verified by the NFR-12 grep gate in Task 3 + Task 4. |
| T-01-NoNet | Information Disclosure | All five files in this plan | mitigate | Zero `fetch()` calls in `js/main.js`, `js/desktop.js`, `js/platform/sw-register.js`. Zero `https://` URLs in HTML. Verified by the per-file grep gates in Tasks 2, 3, 4. |
| T-01-ResetSurface | Repudiation | "Reset shell" reachable via diagnostics (mounted by Plan 04 wiring) | mitigate | Plan 01-03's `confirm()` dialog (verbatim D-06 phrasing) provides the user-confirmation gate. This plan makes the recovery surface reachable from both `?debug=1` and the long-press gesture per D-02. |
</threat_model>

<verification>
1. **Static gates (every plan in this wave):**
   - `grep -E 'location\.protocol\.startsWith\(.http.\)' js/platform/sw-register.js` → match.
   - `grep -E 'hadController' js/platform/sw-register.js` → match (Pitfall 6).
   - `grep -E 'data-app-title' index.html` → match.
   - `grep -E '<link rel="manifest" href="\\./manifest\\.json"' index.html desktop.html` → 2 matches.
   - `grep -E 'https?://' index.html desktop.html js/platform/sw-register.js js/main.js js/desktop.js` → empty.
   - `grep -nE '"\\s*/[a-z]' index.html desktop.html` → empty.
2. **Walking-Skeleton acceptance sequence:** Task 5 checkpoint executes all 7 steps from SKELETON.md.
3. **Phase-gate hands-on session** (deferred to Plan 01-05): real-device installs on Android Chrome, iOS Safari, desktop Chrome/Edge + GitHub Pages sub-path deploy verification.
</verification>

<success_criteria>
- [ ] `js/platform/sw-register.js` exists with feature detect + protocol guard + silent catch + hadController capture + controllerchange listener.
- [ ] `js/main.js` calls `registerServiceWorker()`, supports `?debug=1`, and attaches long-press to `[data-app-title]`.
- [ ] `js/desktop.js` calls `registerServiceWorker()` and supports `?debug=1` (no long-press per D-04).
- [ ] `index.html` ships the head shape (manifest, icon, apple-touch-icon, stylesheet, iOS meta trio) + the empty Today scaffold (header with `data-app-title`, empty list, three-label footer-nav stub) + module script.
- [ ] `desktop.html` ships the same head shape + the "Switch to mobile" stub body + module script.
- [ ] All paths in HTML and JS are `./…` relative (NFR-12, D-19).
- [ ] No `https://` URLs anywhere in this plan's files (NFR-04 / T-01-NoNet).
- [ ] Walking Skeleton acceptance steps 1–7 from SKELETON.md all pass (Task 5 checkpoint).
- [ ] PWA-04 (silent SW fail on file://) demonstrated by Task 5 step 1.
- [ ] NFR-09 (loads on file://) demonstrated by Task 5 step 1.
</success_criteria>

<output>
Create `.planning/phases/01-pwa-shell-tooling-hygiene/01-04-SUMMARY.md` when done. Record: the locked path conventions used (`./*` everywhere), the long-press anchor selector (`[data-app-title]`), and the Task 5 checkpoint outcome (which steps passed cleanly, any issues encountered). Note any deviations from RESEARCH.md or PATTERNS.md.
</output>
