---
phase: 05-backup-restore-json-csv-exports-json-import-nag
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/io/export.js
  - tests/unit/export.csv.test.js
autonomous: true
requirements: [EXPORT-03, EXPORT-06]
user_setup: []

must_haves:
  truths:
    - "CSV cell values correctly encode 1 (applicable + completed), 0 (applicable + not completed), x (not applicable)"
    - "Multi-occurrence habits show numeric count in CSV cells, not 1"
    - "CSV fields containing ;, \", newlines, or whitespace are properly quoted"
  artifacts:
    - path: "js/io/export.js"
      provides: "exportCSV functions with cell encoding logic"
      exports: ["csvCellValue", "escapeCSVField"]
    - path: "tests/unit/export.csv.test.js"
      provides: "CSV cell encoding test suite"
      contains: "csvCellValue"
  key_links:
    - from: "js/io/export.js"
      to: "js/domain/cadence.js"
      via: "appliesToday()"
      pattern: "import.*appliesToday"
---

<objective>
Implement the core CSV cell encoding logic that translates a (habit, date, logs, cadence context) tuple into `1`/`0`/`x` cells, correctly handling cadence rules, multi-occurrence habits, and archived/future-scheduled habits.

Purpose: CSV cell calculation is the load-bearing logic for EXPORT-03 (habit × day matrix) and EXPORT-06 (multi-occurrence numeric counts). This TDD plan red-gates the behavior before wiring file I/O.

Output: `js/io/export.js` module with pure `csvCellValue()` and `escapeCSVField()` functions; comprehensive test suite covering cadence edge cases, multi-occurrence types, and special characters.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-CONTEXT.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md
@.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-CONTEXT.md
@.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md

@js/domain/cadence.js
@js/db/schema.js
</context>

<tasks>

<task type="tdd">
  <name>Task 1: CSV cell encoding logic (RED→GREEN→REFACTOR)</name>
  <files>
    tests/unit/export.csv.test.js
    js/io/export.js
  </files>
  <read_first>
    @js/domain/cadence.js
    @js/db/schema.js
    @.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-CONTEXT.md
    @.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md (Pitfall 1/2)
  </read_first>
  <behavior>
    - Test 1: Binary habit, applicable today, no log → cell value is `'0'`
    - Test 2: Binary habit, applicable today, completed log → cell value is `'1'`
    - Test 3: Binary habit, not applicable (cadence exclusion) → cell value is `'x'`
    - Test 4: Binary habit, future startDate (before today) → cell value is `'x'`
    - Test 5: Numeric habit (counter), applicable, count=5 target=7 → cell value is `'5'`
    - Test 6: Slot-checklist habit, applicable, checked=3/7 → cell value is `'3'`
    - Test 7: Habit completed (no log today) → cell value is `'0'`
    - Test 8: CSV field "Morning; walk" → escaped as `"Morning; walk"`
    - Test 9: CSV field with newline → escaped and quoted
    - Test 10: CSV field with trailing space → quoted
    - Test 11: Archived habit, post-archival date → cell value is `'x'` (needs status check per Pitfall 1)
  </behavior>
  <action>
    Create `tests/unit/export.csv.test.js` following the existing test pattern (`node --test`). Write failing tests for each behavior above covering:
    - Binary, numeric, and slot-checklist log shapes
    - Cadence exclusion via `appliesToday(habit, date, cadenceCtx)` — pass a mock context with weekCompletions + monthCompletions closures that read from an in-memory logs array
    - Multi-occurrence encoding: log.completed as number (numeric) vs array of {name, checked} (slots) vs boolean (binary)
    - CSV field escaping: semicolon, double-quote, newline, carriage return, leading/trailing whitespace all trigger quoting per RFC 4180
    - Pitfall 1 handling: archived habits must check `habit.status === 'archived'` AND compare date to archival. For P5 MVP, store status in habit record; no explicit archivedAt timestamp (defer if discovered missing)
    - Pitfall 2 handling: multi-occurrence partial counts are logged; verify cell outputs the raw count, not 1

    Then create `js/io/export.js` with:
    - `csvCellValue(habit, date, logs, cadenceCtx)`: returns `'1'` / `'0'` / `'x'` / numeric count string per decision D-93/D-94. Calls `appliesToday(habit, date, cadenceCtx)` for cadence check. Checks `habit.startDate > date` for future-scheduled (return `'x'`). Checks `habit.status === 'archived'` for archived (return `'x'` for dates after archival — requires querying `habit_versions` effective-at-date if status is a versioned field, or checking a separate `archivedAt` timestamp). Finds log by `[habitId, date]` compound key and returns appropriate cell value based on log shape.
    - `escapeCSVField(field)`: returns the field as-is if it's safe, or quoted + escaped per RFC 4180 if it contains special characters. Character set: `;`, `"`, `\n`, `\r`, or leading/trailing whitespace. Escape `"` as `""` inside quoted strings.
    - Module exports both functions; no DI yet (DI wiring happens in Plan 02).

    Commits (TDD gate):
    1. `test(05-01): add failing CSV cell encoding tests` — RED
    2. `feat(05-01): implement csvCellValue and escapeCSVField` — GREEN
    3. `refactor(05-01): consolidate cadence context handling` — (if refactoring is needed; else skip)
  </action>
  <verify>
    <automated>node --test tests/unit/export.csv.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/export.csv.test.js` contains ≥11 test cases covering binary/numeric/slot-checklist, cadence rules, future-scheduled, archived, and CSV escaping
    - `js/io/export.js` exports `csvCellValue(habit, date, logs, cadenceCtx)` and `escapeCSVField(field)`
    - All tests pass (green)
    - `csvCellValue` correctly dispatches on `habit.targetType` (or infers from log shape) and returns correct cell values
    - `escapeCSVField` correctly identifies fields needing quotes and escapes `"` as `""`
    - Cadence context is passed via closure (weekCompletions/monthCompletions as functions that read from in-memory logs)
  </acceptance_criteria>
  <done>
    CSV cell encoding logic is complete and tested. `csvCellValue()` and `escapeCSVField()` are pure functions ready for integration with file export (Plan 02). Tests verify EXPORT-03 and EXPORT-06 behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV generation | In-memory data (habits, logs, cadence context) → CSV text string |
| Field escaping | User-supplied habit names and slot labels → CSV-safe quoted strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | CSV cell encoding | mitigate | `escapeCSVField` must quote any field with special characters (`;`, `"`, `\r`, `\n`, whitespace); RFC 4180 compliance prevents injection |
| T-05-02 | Information Disclosure | Habit names with sensitive content | accept | CSV is user-controlled export; user responsible for content sensitivity |

</threat_model>

<verification>
Unit test suite for CSV cell encoding: `node --test tests/unit/export.csv.test.js` must pass (11+ cases).
</verification>

<success_criteria>
- CSV cell values correctly encode cadence, log state, and multi-occurrence types
- CSV field escaping prevents injection via special characters
- Tests pass and cover edge cases (archived, future-scheduled, partial counts)
</success_criteria>

<output>
Create `.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-01-SUMMARY.md` when done
</output>
