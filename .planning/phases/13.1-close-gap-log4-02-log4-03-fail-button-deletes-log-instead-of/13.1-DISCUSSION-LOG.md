# Phase 13.1: Close gap: LOG4-02/LOG4-03 — Fail button deletes log instead of marking failed - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-18
**Phase:** 13.1-Close gap: LOG4-02/LOG4-03 — Fail button deletes log instead of marking failed
**Areas discussed:** Related pending todo fold-in, bug scope confirmation, historical data-loss handling

---

## Related pending todo — fold in or keep deferred?

| Option | Description | Selected |
|--------|-------------|----------|
| Keep deferred | Broader feature (retroactive status-setting UI + scoring changes) than this phase's narrow bug fix; leave in `.planning/todos/pending/` | ✓ (with amendment) |
| Fold into 13.1 | Expand scope to also address the retroactive status-setting concept | |

**User's choice:** Keep deferred, but confirm it is also tracked in `BACKLOG.md` (not just the pending-todos folder).
**Notes:** Added `.planning/BACKLOG.md` §B-028 "Explicit habit-day status model (done/failed/skipped/not tracked)" cross-referencing the pending todo file. The todo file itself remains in `.planning/todos/pending/` — this was an additive tracking step, not a resolution of the todo.

---

## Bug root cause and fix scope

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, exactly these two | `today.js:410` (`handleMarkFailTap`) and `history.js:385` (`history-swipe-fail`) both dispatch `markUncompleted` (deletes the log row) instead of `markFailed` (writes `status:'failed'`); the other 3 `markUncompleted` call sites are correct as-is | ✓ |
| Something else too | Another Fail-button surface or related symptom exists | |

**User's choice:** Confirmed — exactly the two call sites named above.
**Notes:** Verified via `grep -n "type: 'markUncompleted'|type: 'markFailed'" js/` — no other call sites exist. `markFailed.js` handler already exists, already registered in `apply.js`'s `HANDLERS` table, and is already correctly used by the swipe-right cycle logic (`_swipeCycleLog`) and by history.js's bulk-mark-uncompleted action — proof the correct plumbing was already available, just not wired to the Fail buttons.

---

## Historical data-loss handling

| Option | Description | Selected |
|--------|-------------|----------|
| Silent forward-fix only | No recovery is possible (delete already happened, no audit trail); just fix the code | ✓ |
| Note it in PROJECT.md | Add a decision-log entry acknowledging the 2026-09-01→fix-date data loss window | |

**User's choice:** Silent forward-fix only.
**Notes:** Bug has been live since Phase 12 shipped (2026-09-01). Any habit-day swiped "Fail" on in that window was deleted (now indistinguishable from "never tracked"). No changelog or PROJECT.md note will be added.

---

## Claude's Discretion

- Test file placement: extend `tests/integration/today.tap.test.js` / add a symmetric history test, or a dedicated `tests/integration/swipe-fail.test.js` covering both views.
- Exact commit/PR wording regarding the historical data-loss window (informational only).
- Whether to update `.planning/REQUIREMENTS.md` / `PROJECT.md` LOG4-02/LOG4-03 status to Validated once the fix lands (proposed in CONTEXT.md D-06, no objection raised).

## Deferred Ideas

- **Explicit habit-day status model (done/failed/skipped/not tracked) + retroactive status-setting UI** — `.planning/todos/pending/2026-09-18-habit-day-status-model.md`, now also `.planning/BACKLOG.md` §B-028. Materially larger scope than this phase; deferred to a future phase.
