---
phase: 13.1-close-gap-log4-02-log4-03-fail-button-deletes-log-instead-of
plan: 01
subsystem: state/views
tags: [apply-chokepoint, swipe-ux, regression-test, today, history, log4-state-model]

requires:
  - phase: Phase 11 (4-State Log Model Tests)
    provides: handleMarkFailed / markFailed HANDLERS registration, already correct and unchanged by this plan
provides:
  - Today's swipe-left Fail action dispatches markFailed (writes status:'failed') instead of markUncompleted (which deleted the log row)
  - History's swipe-left Fail action dispatches markFailed with the same fix
  - tests/integration/swipe-fail.test.js — permanent regression coverage for both call sites via a real DOM click through the real apply() chokepoint
  - REQUIREMENTS.md / PROJECT.md now correctly reflect LOG4-02/LOG4-03 as complete (Phase 12 had marked them complete prematurely)
affects: [today-view, history-view, log4-state-model, csv-export, scoring (S1/S2/S3 denominators depend on status:'failed' being recorded, not silently deleted)]

actuals:
  tokens: 4499
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns: [Extended the today.tap.test.js fake-document/freshAll()/wire() integration-test pattern to a second view (mountHistory) in the same file, sharing one fake-document implementation across both describe blocks]

key-files:
  created:
    - tests/integration/swipe-fail.test.js
  modified:
    - js/views/today.js
    - js/views/today/builders.js
    - js/views/history.js
    - .planning/REQUIREMENTS.md
    - .planning/PROJECT.md

key-decisions:
  - "Both RED tests (Today + History) landed in a single dedicated tests/integration/swipe-fail.test.js rather than splitting into today.tap.test.js and a new history.tap.test.js — per the plan's D-05 discretion clause, keeping the fake-document helper defined once and shared."
  - "Fail button located via .today-row__action--fail class selector, not the [data-action=\"swipeFail\"] attribute — the hand-rolled fake DOM's querySelector only supports .class and tag-name selectors."

patterns-established: []

requirements-completed: [LOG4-02, LOG4-03]

coverage:
  - id: D1
    description: "Today view: swiping Fail on a habit row writes {status:'failed', definitionVersion:null} for that (habitId, date) instead of deleting the log row"
    requirement: "LOG4-02"
    verification:
      - kind: integration
        ref: "tests/integration/swipe-fail.test.js#Today swipe-fail -> markFailed -> log status stays 'failed' (not deleted)"
        status: pass
    human_judgment: false
  - id: D2
    description: "History view: swiping Fail on a past-day habit row writes {status:'failed'} for that (habitId, date) instead of deleting the log row"
    requirement: "LOG4-03"
    verification:
      - kind: integration
        ref: "tests/integration/swipe-fail.test.js#History swipe-fail -> markFailed -> log status stays 'failed' (not deleted)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Stale JSDoc comments (today.js handleMarkFailTap, today/builders.js buildTodayRow) corrected to say 'dispatches markFailed' matching the fixed code"
    requirement: "LOG4-02"
    verification: []
    human_judgment: false

duration: 25min
completed: 2026-09-18
status: complete
---

# Phase 13.1 Plan 01: Fix swipe-Fail dispatch bug Summary

**Today's and History's swipe-left "Fail" action now dispatches `apply({type:'markFailed'})` — writing `{status:'failed'}` — instead of `markUncompleted`, which was silently deleting the log row and making a conscious "failed" entry indistinguishable from "never tracked."**

## Performance
- **Duration:** ~25min
- **Started:** 2026-09-18T20:04:54+02:00 (plan created)
- **Completed:** 2026-09-18T20:29:34+02:00
- **Tasks:** 3/3 completed
- **Files modified:** 6 (1 created, 5 modified)

## Accomplishments
- Fixed the two exact call sites named by D-01: `js/views/today.js` line 410 (`handleMarkFailTap`) and `js/views/history.js` line 385 (the `'history-swipe-fail'` action handler) — both now dispatch `markFailed` instead of `markUncompleted`.
- Added `tests/integration/swipe-fail.test.js`, driving both Fail buttons through a real DOM click, the real `apply()` chokepoint, and a fake repo — proving RED (log deleted) before the fix and GREEN (`status:'failed'`) after.
- Corrected two stale JSDoc comments (today.js:398, today/builders.js:133) that described the old, wrong dispatch behavior.
- Confirmed the three legitimate `markUncompleted` call sites named by D-02 (`handleMarkUncompleteTap`, `_swipeCycleLog`'s wrap-around branch in today.js, and `toggle-log`'s uncomplete branch + `handleSwipeEnd`'s ternary in history.js) are byte-for-byte unchanged.
- Full suite (`node --test` across all test files) — 900 tests, 0 failures — confirming no regressions.
- Closed out LOG4-02/LOG4-03 in REQUIREMENTS.md (checkboxes + Traceability table) and PROJECT.md (moved Active → Validated with a Phase 13.1 reference), per D-06.

## Task Commits
1. **Task 1a (RED): add failing test for swipe-fail dispatch** - `6db9241` (test)
2. **Task 1b (GREEN): dispatch markFailed on today swipe-fail tap** - `9d3f52c` (fix)
3. **Task 2 (GREEN): dispatch markFailed on history swipe-fail tap** - `9c6eb34` (fix)
4. **Task 3: close LOG4-02/LOG4-03 as complete** - `83eef3d` (docs)

## Files Created/Modified
- `tests/integration/swipe-fail.test.js` - New regression test file; Today + History describe blocks, each driving the real Fail button through a real DOM click and the real `apply()` chokepoint against a fake repo.
- `js/views/today.js` - Line 410: `type: 'markUncompleted'` → `type: 'markFailed'` inside `handleMarkFailTap`; line 398 JSDoc corrected to match.
- `js/views/today/builders.js` - Line 133 JSDoc corrected: "dispatches `markUncompleted` (status: 'failed')" → "dispatches `markFailed`".
- `js/views/history.js` - Line 385: `type: 'markUncompleted'` → `type: 'markFailed'` inside the `'history-swipe-fail'` handler.
- `.planning/REQUIREMENTS.md` - LOG4-02/LOG4-03 checkboxes flipped to `[x]`; Traceability table rows updated to `Phase 12 (fixed Phase 13.1) | Complete`.
- `.planning/PROJECT.md` - LOG4-02/LOG4-03 removed from `### Active`, added to `### Validated` with a Phase 13.1 reference and a note on the actual fix.

## Decisions Made
- Both RED/GREEN tests landed in one new `tests/integration/swipe-fail.test.js` rather than two separate files — the plan's D-05 left this to discretion; a single file keeps the shared fake-document helper defined once.
- The fake DOM's `querySelector` only supports `.class` and tag-name selectors (confirmed by reading the existing `today.tap.test.js` pattern before writing new tests), so both describe blocks locate the Fail button via `.today-row__action--fail`, not `[data-action="swipeFail"]`.

## Deviations from Plan
None - plan executed exactly as written. All three tasks, all acceptance criteria, and both verification commands matched the plan's specification exactly; no Rule 1-4 deviations were needed.

## Issues Encountered
- `node --test tests/` (as literally written in the plan's `<verify>` block) fails on this Windows/Git-Bash environment with `Cannot find module 'tests'` — Node's test runner glob resolution issue specific to this shell, not a project defect. Worked around by running `node --test ./tests/**/*.test.js` (bash globstar expansion), which is functionally equivalent and confirmed all 900 tests pass, including both new swipe-fail tests. No code or test-runner configuration change was needed; this is an environment note, not a deviation from the plan's intent.

## Known Stubs
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
Phase 13.1 is the only plan in this phase and is now complete. LOG4-02 and LOG4-03 are genuinely closed (not just marked complete) — the fix is proven by a permanent regression test. No blockers for further work. Phase complete, ready for next step.

---
*Phase: 13.1-close-gap-log4-02-log4-03-fail-button-deletes-log-instead-of*
*Completed: 2026-09-18*
