---
phase: 03-today-view-settings-v1-first-usable-slice
plan: 05
type: execute
wave: 5
depends_on:
  - 03-03
files_modified:
  - js/state/apply/setSetting.js
  - js/state/apply.js
  - js/views/settings.js
  - js/views/settings/builders.js
  - css/settings.css
  - css/main.css
  - js/main.js
  - index.html
  - tests/unit/builders.settings.test.js
  - tests/integration/apply.setSetting.test.js
  - tests/integration/settings.mount.test.js
  - tests/integration/settings.dataCard.test.js
autonomous: true
requirements:
  - SETTINGS-04
  - SETTINGS-05
  - PWA-07
  - UNDO-01
must_haves:
  truths:
    - "User opens `index.html#settings` and the Settings panel renders with 5 flat cards in this exact top-down order: Storage → Schedule → Install → Data → About (D-61). No collapsible / sub-tab affordance"
    - "Storage card shows live `navigator.storage.persisted()` status with an async `loading…` placeholder, a `Request persistence` button that calls `navigator.storage.persist()` when status is false, and a single-line storage estimate from `navigator.storage.estimate()` (e.g. `\"Using 0.4 MB of ~600 MB\"`) per D-62. Graceful-no-op when the API is missing (e.g. on Safari without enabling the flag) — placeholders read `\"unsupported\"`"
    - "Schedule card hosts a Mon/Sun radio bound to `settings.weekStart` (D-51, D-63); selection writes through `apply({type:'setSetting', payload:{key:'weekStart', value:'mon'|'sun'}})`. The radio reflects current `store.getCachedWeekStart()` on mount"
    - "Install card shows THREE labeled sections — iOS Share → Add to Home Screen / Android Chrome menu → Install / Desktop URL-bar icon (D-64, PWA-07). No JS platform detection; user picks the section matching their device"
    - "Data card has TWO sub-blocks (D-65): (a) `Undo last action` button with live preview text `\"Last: marked <verb> <habit-name> · <relative-time>\"` (D-71, formatRelative); disabled when `meta.undoToken` is empty; clicking calls `undo()` and the preview re-renders via `store.subscribe()` per D-72. (b) `Reset data` destructive-red button with the Settings-flavored confirm (D-67): `\"This will delete all your habits and history. Cannot be undone. Continue?\"` — NOT the D-06 diagnostics text"
    - "About card shows 4 rows: App version (from `APP_VERSION`), Schema version (from `repo.getSetting('schemaVersion')`), Cache name (filtered from `caches.keys()` by `/^habits-/`), SW state (`controlled` / `registered` / `unsupported` via `navigator.serviceWorker.controller`) per D-66. Same async-load placeholders as Storage card"
    - "`js/state/apply/setSetting.js` exports `handleSetSetting(event, repo)` with the canonical handler contract `{storeNames: ['settings'], writes: [{store:'settings', value:{key, value}}], inverse: {type:'setSetting', payload:{key, value: prior?.value}}}` and `handleSetSetting.broadcastKeys = (event) => ({key: event.payload.key})` (D-75)"
    - "`apply.js` HANDLERS table includes literal key `setSetting: handleSetSetting`; `tests/unit/apply.discipline.test.js` 'no switch' assertion remains green"
    - "Settings panel subscribes to `store.notify()` on mount (D-72); on unmount the subscription is torn down. Cross-tab `setSetting` broadcasts cause peer-tab Settings panels to re-render the affected card (Data card preview after a peer mark; Schedule card after a peer weekStart change)"
    - "All Settings interactive elements honor D-79: each card has an `<h2>` with text; Schedule radios share a `<fieldset><legend>` so the question is announced; Data card buttons carry `aria-label` for the verb (`\"Undo last action\"` / `\"Reset data\"`); the `Request persistence` and `Reset data` buttons have a `.settings-card--destructive` style hook but the destructive treatment is COLOR + GLYPH + TEXT, never color alone (NFR-07)"
    - "On hashchange to `#settings`, `mountSettings(parent, {repo, store})` is invoked by the router (D-60) and focus moves to the panel's `<h1>Settings</h1>` per D-79"
    - "Reset data click chain: confirm dialog → on confirm, `indexedDB.deleteDatabase('habits')` (same shape as `js/views/diagnostics.js` D-44 handler) → `location.reload()`. The dialog string is the D-67 user-facing prose, NOT D-06 verbatim diagnostics text"
    - "D-78 grep gate stays green — no `.innerHTML` / `.outerHTML` / `.insertAdjacentHTML` / `document.write` introduced in any of the new files"
  artifacts:
    - path: "js/state/apply/setSetting.js"
      provides: "Per-event handler for `setSetting` (D-75) — writes a settings row through the chokepoint; inverse is `setSetting` with the prior value"
      exports: ["handleSetSetting"]
    - path: "js/state/apply.js"
      provides: "HANDLERS table extended with `setSetting: handleSetSetting`"
      contains: "setSetting: handleSetSetting"
    - path: "js/views/settings.js"
      provides: "Settings panel mounter — composes 5 cards, subscribes to store.notify() for live refresh"
      exports: ["mountSettings", "_resetSettingsForTest"]
    - path: "js/views/settings/builders.js"
      provides: "Pure description-tree builders for each card (Tier 1 testable per D-26 / D-76)"
      exports: ["buildStorageCard", "buildScheduleCard", "buildInstallCard", "buildDataCard", "buildAboutCard"]
    - path: "css/settings.css"
      provides: "Settings panel flat-card layout (D-61)"
    - path: "css/main.css"
      provides: "Adds one `@import url(\"./settings.css\") layer(view);` line"
    - path: "js/main.js"
      provides: "Router map extended — `#settings` → `mountSettings`"
    - path: "index.html"
      provides: "Adds `<section data-route=\"settings\" hidden>` with `<h1 tabindex=\"-1\">Settings</h1>` slot for the router"
    - path: "tests/unit/builders.settings.test.js"
      provides: "Tier 1 unit tests for each builder (5 cards × ARIA + structure assertions)"
    - path: "tests/integration/apply.setSetting.test.js"
      provides: "Round-trip: setSetting writes to the settings store, captures prior in inverse, broadcasts with `{key}` only"
    - path: "tests/integration/settings.mount.test.js"
      provides: "End-to-end mount: Settings renders 5 cards on `#settings`; weekStart radio change writes through apply"
    - path: "tests/integration/settings.dataCard.test.js"
      provides: "Live refresh: Data card preview updates after a peer-tab markCompleted (cross-tab broadcast → notify → subscribe → re-render)"
  key_links:
    - from: "js/state/apply/setSetting.js"
      to: "js/state/apply.js"
      via: "imported and registered in HANDLERS table"
      pattern: "handleSetSetting"
    - from: "js/views/settings.js"
      to: "js/state/apply.js"
      via: "calls apply({type:'setSetting', payload:{key, value}}) from the weekStart radio handler"
      pattern: "apply\\(\\s*\\{\\s*type:\\s*['\"]setSetting['\"]"
    - from: "js/views/settings.js"
      to: "js/state/undo.js"
      via: "calls undo() from the Data card Undo button"
      pattern: "from '.*state/undo\\.js'"
    - from: "js/views/settings.js"
      to: "js/state/store.js"
      via: "subscribes to store.notify() for live Data card refresh (D-72)"
      pattern: "subscribe\\("
    - from: "js/views/settings/builders.js"
      to: "js/util/mount.js"
      via: "builders return descriptions; mountSettings calls mount(desc, parent, actions) to construct DOM"
      pattern: "from '.*util/mount\\.js'"
    - from: "js/main.js"
      to: "js/views/settings.js"
      via: "router map: {'#settings': mountSettings}"
      pattern: "mountSettings"
---

<objective>
Ship the Settings v1 panel — the second visible surface of P3 and the second Undo entry point. After Slice 4 made the Today flow undoable via a toast, this slice lands `js/views/settings.js` with the five locked cards (Storage / Schedule / Install / Data / About), the `setSetting` chokepoint handler (D-75) so the Mon/Sun week-start radio writes through `apply()`, and the cross-tab-aware Data card per D-72. After this plan ships, the user can navigate `#settings`, see persistence/version/cache state, flip week-start, click Undo from a second entry point, and Reset their data with the Settings-flavored confirm string.

Purpose: Deliver SETTINGS-04 (persistence + version status), SETTINGS-05 (data management — Undo + Reset), PWA-07 (platform install instructions), and a second UNDO-01 surface. `setSetting` (D-75) is the only new chokepoint handler; every other piece is pure-view-builder + mounter wiring.
Output: 1 new chokepoint handler, 1 new mounter file + builder file pair, 1 new CSS file (plus a one-line `@import` in `css/main.css`), modifications to `index.html` + `js/main.js` + `js/state/apply.js`, and 4 new test files. The Reset-data string in this surface is intentionally distinct from D-06 (which stays in `js/views/diagnostics.js`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-03-SUMMARY.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-04-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Create setSetting apply handler + register in HANDLERS (D-75) — TDD</name>
  <files>js/state/apply/setSetting.js, js/state/apply.js, tests/integration/apply.setSetting.test.js</files>
  <read_first>
    - js/state/apply/markCompleted.js (the analog — copy handler-contract shape verbatim, swap payload)
    - js/state/apply.js (HANDLERS table + chokepoint tx semantics)
    - js/db/repo.js (`getSetting(key)` returns `{key, value}` row or undefined)
    - tests/integration/apply.markCompleted.test.js (round-trip pattern + fresh-import cache-bust)
    - tests/unit/apply.discipline.test.js (no-switch / no-direct-put guards extend to this new handler)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md "js/state/apply/setSetting.js" lines 86-105
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md decisions D-51, D-63, D-75
  </read_first>
  <behavior>
    `handleSetSetting(event, repo)` handler contract (per D-75):
    - Signature: `event = {type:'setSetting', payload: {key, value}}`; `repo` exposes `getSetting`.
    - Reads `prior = await repo.getSetting(key)` (may be undefined when this key was never written).
    - Returns `{storeNames: ['settings'], writes: [{store: 'settings', value: {key, value}}], inverse: {type: 'setSetting', payload: {key, value: prior?.value}}}`.
    - Note: when `prior?.value` is `undefined` (first write of this key), undo of this event writes `{key, value: undefined}` — which the settings store treats as "no override; fall back to first-run default at next hydrate." This is acceptable per the D-75 + `<specifics>` line 198 ("`weekStart` change is undoable"). Document inline in the file header.
    - `handleSetSetting.broadcastKeys = (event) => ({key: event.payload.key});` — the broadcast envelope's `keys` is `{key}` ONLY, never the value (Pitfall 8 — never broadcast values).

    Integration test `tests/integration/apply.setSetting.test.js`:
    - Pattern S7 paired-cache-bust (`freshApplyAndUndo` shape from `tests/integration/undo.persist-reload.test.js`).
    - Test 1 "writes settings row through chokepoint": configure apply with fake repo; `await apply({type: 'setSetting', payload: {key: 'weekStart', value: 'sun'}})`; assert `repo._stores.settings.get('weekStart')` equals `{key: 'weekStart', value: 'sun'}`.
    - Test 2 "captures prior value in inverse": pre-seed `repo._stores.settings.set('weekStart', {key: 'weekStart', value: 'mon'})`; `await apply({...payload: {key: 'weekStart', value: 'sun'}})`; assert the most recent `events` row's `inverse === {type: 'setSetting', payload: {key: 'weekStart', value: 'mon'}}`.
    - Test 3 "undo round-trip restores prior value": pre-seed `'mon'`; apply `'sun'`; `await undo()`; assert `repo._stores.settings.get('weekStart').value === 'mon'`.
    - Test 4 "first-ever write captures undefined in inverse": no pre-seed; apply `'sun'`; assert inverse payload value is `undefined`.
    - Test 5 "broadcast envelope contains key only": configure a broadcast spy; apply `'sun'`; assert envelope `{type:'mutation', event:'setSetting', keys:{key:'weekStart'}, at}` — no `value` leaked.
    - Test 6 "HANDLERS table includes setSetting": read `js/state/apply.js` source; assert literal `setSetting: handleSetSetting`.

    Discipline: `tests/unit/apply.discipline.test.js` 'no switch (' assertion stays green.
  </behavior>
  <action>
    RED — Step 1: Create `tests/integration/apply.setSetting.test.js` per the behavior spec. Use the same fresh-import pattern as `apply.markCompleted.test.js` (Pattern S7). Mirror the broadcast-spy shape from `tests/integration/sync.broadcast.test.js`.
    Run `node --test tests/integration/apply.setSetting.test.js` — every test fails (handler does not exist).

    GREEN — Step 2: Create `js/state/apply/setSetting.js`:
      - JSDoc file header per D-27: `@file Per-event handler for setSetting (D-75). The settings store is schemaless inside the store; writes go through the chokepoint so weekStart (and any future settings writes) get broadcasted, flushed, and undoable like any other mutation. Forbidden constructs in this file: direct repo.putSetting / direct IDB writes — handler returns writes for the chokepoint to commit (Anti-Pattern 1 / T-02-14).`
      - Export `async function handleSetSetting(event, repo)` per the behavior spec.
      - `handleSetSetting.broadcastKeys = (event) => ({key: event.payload.key});`
      - JSDoc on both — describe the inverse-with-undefined edge case explicitly.

    GREEN — Step 3: Edit `js/state/apply.js`:
      - Add `import { handleSetSetting } from './apply/setSetting.js';`
      - Add `setSetting: handleSetSetting,` to the `HANDLERS` table (after the existing entries).
      - No other change.

    REFACTOR: Run `node --test tests/unit/apply.discipline.test.js tests/integration/apply.setSetting.test.js tests/integration/apply.markCompleted.test.js tests/integration/apply.markUncompleted.test.js tests/integration/undo.persist-reload.test.js` — all green.
  </action>
  <verify>
    <automated>node --test tests/integration/apply.setSetting.test.js</automated>
    <automated>node --test tests/unit/apply.discipline.test.js</automated>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    `js/state/apply/setSetting.js` exports `handleSetSetting` + `broadcastKeys`. `js/state/apply.js` HANDLERS table registers it. Integration tests pass. Discipline tests stay green.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Pure builders for 5 Settings cards (Tier 1 per D-26 / D-76) — TDD</name>
  <files>js/views/settings/builders.js, tests/unit/builders.settings.test.js</files>
  <read_first>
    - js/views/today/builders.js (after 03-02 — closest analog for builder shape and `data-action` attribute discipline)
    - js/util/mount.js (after 03-01 — confirms `{tag, attrs, text, children}` description shape)
    - js/views/diagnostics.js (Reset-data handler shape + async-loaded "Cache name" pattern from lines 113-124, 173-198 — the SETTINGS card reuses both)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md decisions D-61 (flat-card order), D-62 (Storage card), D-63 (Schedule card), D-64 (Install card), D-65 (Data card), D-66 (About card), D-67 (Reset-data confirm copy), D-79 (a11y baseline)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md "js/views/settings.js" + Pattern S5 (async-load placeholder) + Pattern S8 (fixture-driven builder tests)
    - tests/unit/builders.today.test.js (after 03-02 — mirror test file shape)
  </read_first>
  <behavior>
    Each builder is a PURE function. No DOM construction. No async. Returns a description tree `{tag, attrs, text, children}` that `mount()` will translate. Every interactive child carries `data-action="<verb>"` so the mounter wires the closure (`actions.requestPersistence` / `actions.setWeekStart` / `actions.undoLastAction` / `actions.resetData`).

    Card descriptions ALL share the wrapper shape:
    ```
    {tag: 'section', attrs: {class: 'settings-card', 'aria-labelledby': '<id>'}, children: [
      {tag: 'h2', attrs: {id: '<id>'}, text: '<Title>'},
      ...
    ]}
    ```

    `buildStorageCard({persisted, estimateUsedMB, estimateQuotaMB, supported})` (D-62):
    - Inputs are plain values — the async resolution happens in the mounter; the builder is tested with the post-await snapshot.
    - When `supported === false`: emit a single `<p>Storage status unsupported on this browser.</p>` line and no buttons.
    - When `supported === true`:
      - Status row: `<dl><dt>Persistent</dt><dd>${persisted ? 'yes' : 'no'}</dd></dl>`.
      - When `persisted === false`: a `<button data-action="requestPersistence">Request persistence</button>`.
      - Estimate row: `<dl><dt>Storage</dt><dd>Using ${estimateUsedMB} MB of ~${estimateQuotaMB} MB</dd></dl>`.
    - Loading-state contract: when an input is the literal string `'loading…'`, render the dd verbatim (lets the mounter use Pattern S5).

    `buildScheduleCard({weekStart})` (D-63):
    - `<fieldset class="settings-radio-group"><legend>Week starts on</legend>`
      - `<label><input type="radio" name="weekStart" value="mon" data-action="setWeekStart" ${weekStart === 'mon' ? 'checked' : ''}> Monday</label>`
      - `<label><input type="radio" name="weekStart" value="sun" data-action="setWeekStart" ${weekStart === 'sun' ? 'checked' : ''}> Sunday</label>`
    - `</fieldset>`
    - The radios share `name="weekStart"` so native browser semantics enforce single-select.

    `buildInstallCard()` (D-64, PWA-07):
    - Three labeled subsections; no platform detection.
    - `<section aria-labelledby="install-ios"><h3 id="install-ios">iOS Safari</h3><p>Tap the Share button, then <strong>Add to Home Screen</strong>.</p></section>`
    - Android: `<h3>Android Chrome</h3><p>Open the menu, then <strong>Install app</strong>.</p>`
    - Desktop: `<h3>Desktop browsers</h3><p>Look for the install icon in the URL bar, or use the browser menu.</p>`
    - Each `<strong>` is rendered via `mount`'s recursive descent (description objects, not HTML strings).

    `buildDataCard({lastEvent, hasUndoToken, relativeTime})` (D-65, D-71):
    - When `hasUndoToken === false`: Undo button is `<button data-action="undoLastAction" aria-label="Undo last action" disabled>Undo last action</button>`, preview is `<p>Nothing to undo.</p>`.
    - When `hasUndoToken === true`:
      - Preview: `<p>Last: ${lastEvent}` · ${relativeTime}</p>` where `lastEvent` is the verb+habit string (e.g. `"marked Drink water complete"`), built by the mounter from `meta.undoToken` → `getEvent(token)` then formatted per D-71.
      - Button: `<button data-action="undoLastAction" aria-label="Undo last action">Undo last action</button>` (not disabled).
    - Reset block: `<div class="settings-card--destructive"><button data-action="resetData" aria-label="Reset data">Reset data</button><p>This deletes everything stored on this device.</p></div>`.
    - The Reset confirm string (D-67: `"This will delete all your habits and history. Cannot be undone. Continue?"`) lives in the mounter (clickable behavior) — NOT the builder.

    `buildAboutCard({appVersion, schemaVersion, cacheName, swState})` (D-66):
    - `<dl class="settings-about">`
    - 4 rows: `<dt>App version</dt><dd>${appVersion}</dd>` … `<dt>Schema version</dt><dd>${schemaVersion}</dd>` … `<dt>Cache name</dt><dd>${cacheName}</dd>` … `<dt>Service worker</dt><dd>${swState}</dd>`.
    - Inputs again accept the literal `'loading…'` for Pattern S5.

    Unit tests (`tests/unit/builders.settings.test.js`) MUST cover (per Pattern S8):
    - Each builder against fixture inputs.
    - ARIA assertions per D-79: each card has an `<h2>` with `id` matched by `aria-labelledby`; Schedule radios share `name="weekStart"` inside a `<fieldset>` with `<legend>`; Install card has 3 `<h3>` headings; Data card buttons have `aria-label`; About card uses `<dl>`/`<dt>`/`<dd>`.
    - Data action attributes: every interactive control has `data-action="<verb>"`.
    - Disabled state: `buildDataCard({hasUndoToken: false})` produces a button with `disabled` attribute.
    - Loading-state passthrough: `buildStorageCard({persisted: 'loading…', ...})` emits a dd with `text: 'loading…'`.
    - Persistence-button conditional: `buildStorageCard({persisted: true})` does NOT emit the `data-action="requestPersistence"` button; `{persisted: false}` does.
    - Unsupported case: `buildStorageCard({supported: false})` emits the unsupported message and NO buttons.
  </behavior>
  <action>
    RED — Step 1: Create `tests/unit/builders.settings.test.js` per the behavior spec. Mirror `tests/unit/builders.today.test.js`'s test-file layout (one `describe` per builder).
    Run `node --test tests/unit/builders.settings.test.js` — every test fails (builders do not exist).

    GREEN — Step 2: Create `js/views/settings/builders.js`:
      - JSDoc file header per D-27: cross-reference D-61..D-66, D-79; "Pure transforms only; no DOM, no IDB, no async." Forbidden constructs: any `document.*` reference.
      - Implement each builder per the behavior spec. Each one is ~10-30 lines.
      - JSDoc on each export with `@param` + `@returns {Object}` documenting the description shape.

    REFACTOR: Run `node --test tests/unit/builders.settings.test.js` — green. Run `node --test tests/unit/discipline.xss.test.js` — green (no DOM construction in builders.js).
  </action>
  <verify>
    <automated>node --test tests/unit/builders.settings.test.js</automated>
    <automated>node --test tests/unit/discipline.xss.test.js</automated>
    <automated>node --test tests/unit/</automated>
  </verify>
  <done>
    `js/views/settings/builders.js` exports the 5 builders. Each one is pure (no DOM). Unit tests pass with ARIA + data-action + disabled-state + loading-state coverage. Discipline tests stay green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: mountSettings — composes cards, subscribes for live refresh, wires actions (D-67, D-72)</name>
  <files>js/views/settings.js, js/main.js, index.html, css/settings.css, css/main.css, tests/integration/settings.mount.test.js, tests/integration/settings.dataCard.test.js</files>
  <read_first>
    - js/views/today.js (after 03-04 — mountToday + store.subscribe + actions closure-map shape; mirror the unmount-returning idempotent-guard pattern)
    - js/views/diagnostics.js (Reset-data click handler lines 173-198 — copy verbatim except for the confirm string per D-67; cache-name async-load lines 113-124)
    - js/util/mount.js (mount(desc, parent, actions) — the description→DOM walker)
    - js/util/version.js (`APP_VERSION` for the About card)
    - js/db/repo.js (`getSetting`, `getMeta`, `getEvent` — Settings reads `meta.undoToken` + `getEvent(undoToken)` for the Data preview; `getSetting('schemaVersion')` for About)
    - js/state/store.js (`getCachedWeekStart`, `getCachedMeta` if present, `subscribe`)
    - js/state/apply.js (configure + apply contract — Settings calls apply for weekStart writes)
    - js/state/undo.js (`undo()` — called from the Data card button)
    - js/views/toast.js (after 03-04 — `showErrorToast` for the undo-error path)
    - tests/integration/today.tap.test.js (fake-document setup pattern + Pattern S7 freshImport)
    - tests/integration/today.undo.test.js (mock console; cross-tab broadcast simulation)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md decisions D-60 (router focus), D-65, D-66, D-67, D-72, D-79
  </read_first>
  <behavior>
    `mountSettings(parent, {repo, store})` contract:
    - Idempotent guard: `if (mounted) return unmount;`.
    - Builds the panel `<section class="settings-panel">`; appends an `<h1 tabindex="-1">Settings</h1>` (D-79 focus-on-route-change target — the router calls `.focus({preventScroll:true})` on it).
    - Calls each builder with the current snapshot of inputs (Storage card: initial `'loading…'`; Schedule card: `store.getCachedWeekStart() || 'mon'`; Install card: no inputs; Data card: read `meta.undoToken` from store + repo.getEvent(token); About card: `APP_VERSION` + `'loading…'` placeholders for the async values).
    - Builds an `actions` map for `mount(desc, parent, actions)`:
      - `requestPersistence` → `async () => { if (navigator.storage?.persist) { const ok = await navigator.storage.persist(); refresh('storage'); if (!ok) showErrorToast("Browser declined persistence — try clearing site data or installing"); } }`.
      - `setWeekStart` → `(evt) => { const value = evt.target.value; apply({type: 'setSetting', payload: {key: 'weekStart', value}}); }`.
      - `undoLastAction` → `async () => { try { const r = await undo(); if (r === null) return; /* no-op, button should be disabled */ } catch (e) { showErrorToast("Couldn't undo — try again"); } }`.
      - `resetData` → `async () => { const confirmed = confirm("This will delete all your habits and history. Cannot be undone. Continue?"); if (!confirmed) return; try { await new Promise((resolve, reject) => { const req = indexedDB.deleteDatabase('habits'); req.onsuccess = () => resolve(); req.onerror = () => reject(req.error); req.onblocked = () => resolve(); }); } catch (_e) {} location.reload(); }`. THIS STRING IS THE D-67 SETTINGS-FLAVORED PROSE — distinct from D-06 verbatim diagnostics text.
    - Async-load Pattern S5 for Storage + About cards. After each Promise resolves, mutate the rendered `<dd>` `.textContent` directly (no full re-render).
    - `const unsub = store.subscribe(refreshLiveCards);` where `refreshLiveCards` re-runs `buildDataCard(...)` and `buildScheduleCard(...)` against fresh inputs and SWAPS the card DOM (each card has a stable wrapper id; mount replaces innerChildren by `replaceWith`). DO NOT use `.innerHTML` (D-78). Use a per-card swap helper `replaceCardChildren(cardEl, newDesc, actions)` that removes existing children via a loop and re-mounts.
    - On unmount: `unsub();` + remove the panel from `parent`.

    Router wire-up (`js/main.js`):
    - Extend the existing `mountRoutes({routes: {...}})` call to add `'#settings': () => mountSettings(settingsPanelParent, {repo, store})`.
    - The settings panel container is a `<section data-route="settings" hidden>` slot in `index.html`; the router toggles `hidden` per active route. (Mirror the 03-02 `#today` panel-toggling logic.)
    - On hashchange to `#settings`, focus moves to the new panel's `<h1>` per D-79 (router responsibility already established in 03-02; verify the handler treats `#settings` the same as `#today`).

    `index.html` change:
    - Add `<section data-route="settings" hidden><h1 tabindex="-1">Settings</h1></section>` as a sibling of the existing `data-route="today"` section. The `<h1>` is mounted upfront so router focus has a target even before mountSettings finishes.

    `css/settings.css`:
    - `@layer view { ... }` block per Pattern from css/today.css.
    - `.settings-panel { padding: var(--space-4); }`
    - `.settings-card { background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-lg); padding: var(--space-4); margin-bottom: var(--space-3); }`
    - `.settings-card h2 { margin: 0 0 var(--space-3); font-size: 1.05rem; }`
    - `.settings-radio-group legend { font-weight: 500; }`
    - `.settings-card--destructive button { color: var(--color-danger, #c0392b); border-color: var(--color-danger, #c0392b); }` (color + text — destructive style hook honors NFR-07; the glyph/text is the load-bearing signal).
    - Reuse existing `.panel` + `.toast` primitives from `components.css`.

    `css/main.css`:
    - Append one line: `@import url("./settings.css") layer(view);` — alongside the existing `@import url("./today.css") layer(view);`.

    Integration test `tests/integration/settings.mount.test.js`:
    - Pattern S7 fresh-import; createFakeDocument; seed weekStart='mon' in fake repo; configure store + apply.
    - Test "mountSettings renders 5 cards in locked order": after mount, query `document.querySelectorAll('.settings-card')`; assert length === 5; assert the 5 `<h2>` texts are `['Storage', 'Schedule', 'Install', 'Data', 'About']` in that order (D-61).
    - Test "Schedule radio change writes through apply": dispatch a click/change on the `'sun'` radio; await one tick; assert `repo._stores.settings.get('weekStart').value === 'sun'`; assert a broadcast envelope `{event:'setSetting', keys:{key:'weekStart'}}` fired.
    - Test "Data card Undo button is disabled when meta.undoToken is empty": with no prior actions, mount; assert `document.querySelector('[data-action="undoLastAction"]').hasAttribute('disabled')`.
    - Test "Reset data confirm dialog uses D-67 string": stub `globalThis.confirm = (msg) => { capturedMsg = msg; return false; };` mount; click Reset; assert `capturedMsg === "This will delete all your habits and history. Cannot be undone. Continue?"`.

    Integration test `tests/integration/settings.dataCard.test.js`:
    - D-72 live refresh: mount Settings; simulate a peer-tab broadcast `{type:'mutation', event:'markCompleted', keys:{habitId:'h1', date: todayLocal()}, at}` by directly invoking the broadcast listener in `js/platform/sync.js` (the same shape as `tests/integration/sync.broadcast.test.js`).
    - Assert Data card preview's `.textContent` updates to reflect the new `meta.undoToken` (e.g. contains `"marked"` and a relative-time string), without manual re-render.
    - Assert the Undo button is no longer `disabled` after the broadcast.
  </behavior>
  <action>
    Step 1 — Settings shell:
      Edit `index.html`: add `<section data-route="settings" hidden><h1 tabindex="-1">Settings</h1></section>` as a sibling of the existing `data-route="today"` section. Confirm the existing router toggling logic (from 03-02) covers `#settings` symmetrically.

    Step 2 — CSS:
      Create `css/settings.css` per the behavior spec.
      Append `@import url("./settings.css") layer(view);` to `css/main.css` (after the `@import url("./today.css") layer(view);` line).

    Step 3 — Settings view (GREEN-friendly skeleton; tests in step 5):
      Create `js/views/settings.js`:
      - JSDoc file header per D-27 cross-referencing D-60..D-67, D-72, D-79.
      - Import: `import {mount} from '../util/mount.js';`, `import {apply} from '../state/apply.js';`, `import {undo} from '../state/undo.js';`, `import {showErrorToast} from './toast.js';`, `import {APP_VERSION} from '../util/version.js';`, `import {buildStorageCard, buildScheduleCard, buildInstallCard, buildDataCard, buildAboutCard} from './settings/builders.js';`.
      - Module-scope `let panelEl = null; let unsubStore = null;` (idempotent-guard pattern from Pattern S4).
      - Export `mountSettings(parent, {repo, store})` per the behavior spec.
      - Internal `refreshLiveCards()` re-runs the two reactive builders (Data + Schedule) and SWAPS their card DOM via `replaceCardChildren(cardEl, newDesc, actions)`. `replaceCardChildren` removes existing children with a `while (cardEl.firstChild) cardEl.removeChild(cardEl.firstChild);` loop, then calls `mount(newDesc, cardEl, actions)`. NO `.innerHTML` (D-78).
      - Internal async-loaders: `loadStorageState()`, `loadAboutAsync()` — each follows Pattern S5 (`<dd>` placeholder → resolve → mutate `.textContent`).
      - `_resetSettingsForTest()` export — force-clears `panelEl` + `unsubStore` for test cache-bust.

    Step 4 — Router map:
      Edit `js/main.js`: extend the existing `mountRoutes({routes: {...}})` argument to include `'#settings': () => mountSettings(settingsPanelEl, {repo, store})` where `settingsPanelEl = document.querySelector('[data-route="settings"]')`. If 03-02 already stub-registered `#settings`, swap the stub for the real `mountSettings` import.

    Step 5 — Integration tests (RED → GREEN):
      Create `tests/integration/settings.mount.test.js` per the behavior spec.
      Create `tests/integration/settings.dataCard.test.js` per the behavior spec.
      Run `node --test tests/integration/settings.mount.test.js tests/integration/settings.dataCard.test.js` — every test green after the implementation in step 3.

    REFACTOR — Step 6: Confirm `tests/unit/discipline.xss.test.js` still passes (no `.innerHTML` introduced). Confirm `tests/unit/apply.discipline.test.js` still passes (HANDLERS extension from Task 1 already covered). Run full suite `node --test tests/` — green.
  </action>
  <verify>
    <automated>node --test tests/integration/settings.mount.test.js</automated>
    <automated>node --test tests/integration/settings.dataCard.test.js</automated>
    <automated>node --test tests/unit/discipline.xss.test.js</automated>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    `js/views/settings.js` exports `mountSettings(parent, {repo, store})` + `_resetSettingsForTest`. `index.html` has the settings panel slot. `js/main.js` routes `#settings` to mountSettings. `css/settings.css` exists and is imported by `css/main.css`. Integration tests prove: 5 cards in locked order, weekStart radio writes via apply, Undo disabled when token empty, Reset confirm uses D-67 string, cross-tab broadcast triggers live Data card refresh. Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings-card DOM ← repo.getEvent / `meta.undoToken` | The Data card preview interpolates a habit name from the events row; XSS-safe via `textContent` |
| Reset data button → `indexedDB.deleteDatabase('habits')` | Destructive operation gated behind a confirm() dialog with the D-67 string |
| weekStart radio change → apply() | User-initiated mutation crosses into the chokepoint write path |
| persisted() / estimate() / caches.keys() async resolution | Browser APIs that may reject or be undefined; Pattern S5 graceful-no-op covers the unsupported case |
| cross-tab broadcast → Data card refresh | Untrusted-origin keys drive a partial cache re-read + re-render; bounded to allowlisted keys (`habitId`/`date`/`key`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-30 | Tampering | habit name with HTML in the Data card preview | mitigate | Preview line built via `mount({tag:'p', text: ...})` — `text` is set via `.textContent` (D-77 contract). Discipline test `tests/unit/discipline.xss.test.js` (D-78) catches `.innerHTML` regressions. |
| T-03-31 | Tampering | Reset-data click without confirm() | mitigate | Handler explicitly calls `confirm(D67_STRING)` and early-returns on false. Test `settings.mount.test.js` "Reset data confirm dialog uses D-67 string" asserts the exact prompt. |
| T-03-32 | Repudiation | weekStart change is not auditable | mitigate | D-75: `setSetting` flows through the chokepoint, so `meta.undoToken` points at it and `events` records `{type:'setSetting', payload:{key:'weekStart', value}, inverse, at}`. Undo restores the prior value verbatim (Task 1 test 3). |
| T-03-33 | Information Disclosure | broadcast envelope leaks setting VALUE (Pitfall 8) | mitigate | `handleSetSetting.broadcastKeys` returns `{key}` ONLY. Test 5 asserts no `value` in envelope. Receiving tab re-reads via the settings cache refresh path. |
| T-03-34 | Denial of Service | live refresh swap thrashes under rapid peer broadcasts | accept | One broadcast per peer mutation; bounded by user fingers. Card swap is O(card-children) which is small (≤5 children per card). Pattern S5 async-loaders don't restart on every notify — only on initial mount. |
| T-03-35 | Elevation of Privilege | a Settings page injected by an attacker could fire setSetting with unbounded keys | mitigate | `setSetting`'s payload is taken at face value (no allowlist), but the Settings DOM only ever produces `{key:'weekStart'}` from the radio handler. The chokepoint stores it; the cadence resolver reads `weekStart` specifically and ignores unknown keys. A "free" extra key in the settings store has no read path. Allowlist-on-read is intentional (D-75 inline note). |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | NO package installs in this plan. Zero supply-chain surface. |
</threat_model>

<verification>
- `node --test tests/` exits 0 (full suite green, including 03-01..03-04's tests)
- `js/state/apply/setSetting.js` exports `handleSetSetting` + `broadcastKeys`
- `js/state/apply.js` HANDLERS table contains literal `setSetting: handleSetSetting`
- `js/views/settings.js` exports `mountSettings` + `_resetSettingsForTest`
- `js/views/settings/builders.js` exports `buildStorageCard`, `buildScheduleCard`, `buildInstallCard`, `buildDataCard`, `buildAboutCard`
- `css/settings.css` exists; `css/main.css` contains `@import url("./settings.css") layer(view);`
- `index.html` contains `<section data-route="settings" hidden>` with an `<h1 tabindex="-1">` slot
- `js/main.js` routes `#settings` to `mountSettings`
- Reset confirm string is the D-67 prose, not D-06; `tests/integration/settings.mount.test.js` asserts the exact string
- `tests/unit/discipline.xss.test.js` still passes (no `.innerHTML`-family tokens introduced)
- `tests/unit/apply.discipline.test.js` still passes (HANDLERS extended, no `switch (` added)
</verification>

<success_criteria>
- Phase requirements covered: SETTINGS-04 (persistence status + app/schema versions visible in Settings), SETTINGS-05 (Reset-data + Undo last action surfaced in Settings), PWA-07 (3 platform install sections visible), UNDO-01 2nd surface (Settings Data card Undo)
- D-decisions implemented: D-51 (settings.weekStart user-configurable), D-60 (router focus to h1 on hashchange), D-61 (flat-card top-down order), D-62 (Storage card with persisted/persist/estimate), D-63 (Schedule weekStart radio via setSetting handler), D-64 (3 platform install sections), D-65 (Data card Undo + Reset), D-66 (About 4 rows), D-67 (Settings-flavored Reset confirm string distinct from D-06), D-72 (Data card subscribes to notify), D-75 (setSetting handler), D-79 (a11y baseline)
- All new tests pass; existing 03-01..03-04 tests stay green
</success_criteria>

<output>
Create `.planning/phases/03-today-view-settings-v1-first-usable-slice/03-05-SUMMARY.md` when done. Summary MUST include: files created/modified, test files added, the new handler + view + builder exports listed by name, and a one-line note "Settings v1 is live with 5 cards + 2nd undo surface + setSetting through the chokepoint. Slice 6 closes the phase: SW SHELL append, APP_VERSION bump, docs."
</output>
