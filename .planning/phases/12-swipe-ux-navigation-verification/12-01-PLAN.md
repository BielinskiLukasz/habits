---
phase: 12-swipe-ux-navigation-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/domain/logStatus.js
  - tests/unit/logStatus.test.js
  - js/views/today.js
autonomous: true
requirements:
  - LOG4-02

estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "nextLogState(null) returns 'completed'"
    - "nextLogState(undefined) returns 'completed'"
    - "nextLogState('completed') returns 'failed'"
    - "nextLogState('failed') returns 'skipped'"
    - "nextLogState('skipped') returns null"
    - "nextLogState with any unknown string input returns null (safe fallback)"
    - "logStatus.js uses NEXT_STATE object dispatch — no switch statement (Anti-Pattern 4)"
    - "today.js swipe-right handler is named _swipeCycleLog and dispatches one of four event types based on nextLogState return value"
    - { statement: "swipe dx > 60 (strict greater-than) triggers cycle; dx === 60 does not trigger", verification: backstop }
  artifacts:
    - "js/domain/logStatus.js (exports nextLogState; private NEXT_STATE const)"
    - "tests/unit/logStatus.test.js (6 behavior test cases + 2 discipline assertions)"
  key_links:
    - "today.js adds import { nextLogState } from '../domain/logStatus.js'"
    - "_swipeCycleLog reads getCachedLog(habitId, date)?.status ?? null before calling nextLogState"
    - "null return from nextLogState maps to 'markUncompleted' event type"
  prohibitions:
    - "logStatus.js MUST NOT contain a switch statement on status values — use NEXT_STATE object (flagged-unverified: logStatus.test.js discipline assertion guards this)"
    - "today.js MUST NOT use .innerHTML for any DOM manipulation (D-78, flagged-unverified: grep gate enforced in Phase 13)"
    - "Mobile analytics section in-shell is a deferred idea — MUST NOT be introduced here (flagged-unverified)"
---

<objective>
Create the `nextLogState` pure domain function with TDD coverage (RED then GREEN commits), then wire today.js swipe-right handler to cycle all 4 log states. This is the end-to-end tracer: domain layer proven by tests, view layer wired to consume it.

Purpose: Establishes the single source of truth for the 4-state cycle (D-04, D-05). Today view swipe-right no longer always marks completed — it advances state through the full cycle (LOG4-02 core, D-01).
Output: js/domain/logStatus.js, tests/unit/logStatus.test.js, updated js/views/today.js.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-swipe-ux-navigation-verification/12-CONTEXT.md
@.planning/phases/12-swipe-ux-navigation-verification/12-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: TDD nextLogState domain function (RED then GREEN)</name>
  <files>js/domain/logStatus.js, tests/unit/logStatus.test.js</files>
  <read_first>
    - tests/unit/cadence.test.js — import pattern (node:test, node:assert/strict, readFileSync, fileURLToPath), describe/test block structure, discipline assertions that read source via readFileSync and assert RESOLVERS present and no switch statement
    - js/domain/cadence.js — @file JSDoc header format referencing D-XX decisions, RESOLVERS dispatch table pattern, exported pure function shape, forbidden-constructs comment block
  </read_first>
  <behavior>
    RED commit (logStatus.test.js exists, logStatus.js does not):
    - nextLogState(null) returns 'completed'
    - nextLogState(undefined) returns 'completed'
    - nextLogState('completed') returns 'failed'
    - nextLogState('failed') returns 'skipped'
    - nextLogState('skipped') returns null
    - nextLogState('anything-unknown') returns null
    Discipline assertions (read logStatus.js source via readFileSync):
    - src.includes('NEXT_STATE') === true
    - src.includes('switch (') === false
  </behavior>
  <action>
Step 1 — RED: Create tests/unit/logStatus.test.js. Import nextLogState from '../../js/domain/logStatus.js'. Import test and describe from 'node:test', assert from 'node:assert/strict', readFileSync from 'node:fs', fileURLToPath from 'node:url'. Write 6 behavior test cases as a describe block named 'nextLogState', one per bullet in the behavior block above. Add a separate describe block named 'logStatus.js discipline' containing 2 assertions that call readFileSync to read the logStatus.js source and assert NEXT_STATE is present and no switch statement exists. Run `node --test tests/unit/logStatus.test.js` — it must FAIL with a module-not-found error because logStatus.js does not yet exist. Commit with message: `test(12-01): add failing tests for nextLogState`.

Step 2 — GREEN: Create js/domain/logStatus.js. Open with a JSDoc @file header on line 1: "Pure log-state cycle function for the 4-state swipe model (D-04, D-05). Dispatches via the NEXT_STATE table — NO switch statement (Anti-Pattern 4). Forbidden: switch on status, indexedDB.*, .innerHTML." Define module-level const NEXT_STATE as a plain object literal with string keys: 'null' maps to 'completed', 'completed' maps to 'failed', 'failed' maps to 'skipped', 'skipped' maps to null. Export JSDoc-documented function nextLogState(currentStatus) that coerces the input key via String(currentStatus ?? null), looks up NEXT_STATE using Object.prototype.hasOwnProperty.call to guard the lookup, and returns the mapped value or null if the key is absent. Run `node --test tests/unit/logStatus.test.js` — all 8 assertions must pass. Commit with message: `feat(12-01): implement nextLogState domain function`.
  </action>
  <reversibility rating="costly">Implements D-01 (4-state swipe cycle). The gesture contract is user-facing. Reverting requires updating today.js, history.js, and removing logStatus.js; users who have learned the new cycle would be surprised by the revert.</reversibility>
  <verify>
    <automated>node --test tests/unit/logStatus.test.js</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/unit/logStatus.test.js exits 0 with 8 passing assertions (6 behavior + 2 discipline)
    - js/domain/logStatus.js exists and contains the string 'NEXT_STATE'
    - js/domain/logStatus.js does not contain 'switch ('
    - js/domain/logStatus.js first line begins with '/** @file'
    - tests/unit/logStatus.test.js imports from '../../js/domain/logStatus.js'
  </acceptance_criteria>
  <done>logStatus.js exports nextLogState using NEXT_STATE dispatch; 8 tests pass; no switch statement; JSDoc @file header present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire today.js swipe-right to call _swipeCycleLog</name>
  <files>js/views/today.js</files>
  <read_first>
    - js/views/today.js — read the entire handleSwipeEnd function (lines 324-349) to see the dx > 60 call site; read the entire _swipeMarkComplete function (lines 265-274) to see what it currently dispatches; find an example of getCachedLog usage (line 581 area) for the read-status pattern
    - js/domain/logStatus.js — confirm nextLogState export name and that it accepts a string|null and returns string|null (just created in Task 1)
    - .planning/phases/12-swipe-ux-navigation-verification/12-PATTERNS.md — read the replacement pattern for _swipeCycleLog (the code block starting at "async function _swipeCycleLog")
  </read_first>
  <action>
1. Add import at the top of today.js alongside existing domain imports: `import { nextLogState } from '../domain/logStatus.js';`

2. Rename function _swipeMarkComplete to _swipeCycleLog — update both the function declaration (line ~265) and the single call site inside handleSwipeEnd dx > 60 branch (line ~335).

3. Replace the _swipeCycleLog body entirely. New body: read current status via `const currentStatus = getCachedLog(habitId, date)?.status ?? null;`. Call `const next = nextLogState(currentStatus);`. Derive event type: if next === 'completed' use 'markCompleted'; if next === 'failed' use 'markFailed'; if next === 'skipped' use 'markSkipped'; otherwise use 'markUncompleted'. Dispatch `await apply({ type, payload: { habitId, date } })`. Keep the existing showUndoToast call and error toast in the catch block — they can remain unchanged since they are not state-dependent.

4. Do NOT modify the dx < -60 branch in handleSwipeEnd. The swipe-left actions panel (D-02) must remain intact with its Skip and Fail shortcut buttons.

5. Run `node --test tests/` to confirm no regressions.
  </action>
  <reversibility rating="costly">Implements D-01 gesture contract on Today view. See Task 1 reversibility note.</reversibility>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/ exits 0
    - js/views/today.js contains the string "import { nextLogState } from '../domain/logStatus.js'"
    - js/views/today.js contains the string '_swipeCycleLog' (new function name)
    - js/views/today.js does not contain the string '_swipeMarkComplete' (old function name removed)
    - js/views/today.js contains 'markFailed' (new event type dispatched by swipe handler)
    - js/views/today.js contains 'markUncompleted' (dispatched when nextLogState returns null)
    - js/views/today.js does not contain '.innerHTML'
  </acceptance_criteria>
  <done>today.js swipe-right cycles through all 4 log states using nextLogState; _swipeMarkComplete is gone; all tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch event → swipe handler | dx computed from pointer clientX; triggers apply() dispatch |
| nextLogState return value → event type | domain function output drives which IDB write fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-01 | Tampering | NEXT_STATE dispatch object in logStatus.js | low | accept | Module-level const, not exported; no external mutation surface; single pure function with one input |
| T-12-02 | Spoofing | _swipeCycleLog event type mapping | low | mitigate | Explicit conditional mapping: nextLogState return checked against known strings; null/unexpected value defaults to 'markUncompleted' (idempotent safe state) |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages — no supply-chain surface; vanilla JS only |
</threat_model>

<verification>
- node --test tests/ exits 0 after both tasks complete
- js/domain/logStatus.js exists with NEXT_STATE const and nextLogState export, no switch statement
- tests/unit/logStatus.test.js has 8 passing assertions
- js/views/today.js imports nextLogState and dispatches the correct event type per current log status
</verification>

<success_criteria>
- All 8 logStatus unit tests pass (6 behavior + 2 discipline)
- today.js swipe-right dispatches markCompleted / markFailed / markSkipped / markUncompleted based on the current cached log status
- No regressions in the full test suite (node --test tests/ exits 0)
- Two atomic commits produced: test(12-01) RED commit then feat(12-01) GREEN commit
</success_criteria>

<output>
Create .planning/phases/12-swipe-ux-navigation-verification/12-01-SUMMARY.md when done
</output>

## Artifacts this plan produces

New files:
- `js/domain/logStatus.js` — exports `nextLogState(currentStatus: string|null|undefined): string|null`; private const `NEXT_STATE` object; JSDoc @file header; no switch statement

New test files:
- `tests/unit/logStatus.test.js` — 6 behavior tests for nextLogState transitions + 2 discipline assertions (NEXT_STATE present, no switch)

Modified symbols in existing files:
- `js/views/today.js` — `_swipeMarkComplete` renamed to `_swipeCycleLog`; import of `nextLogState` added; swipe-right now cycles through all 4 log states instead of always marking completed
