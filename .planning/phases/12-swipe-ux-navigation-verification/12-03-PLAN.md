---
phase: 12-swipe-ux-navigation-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - UX-01
  - UX-02

estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "buildFooterNav in today/builders.js includes { href: './desktop.html', text: t('nav.analytics') } link"
    - "js/views/settings.js contains no href or link pointing to desktop.html (D-09: duplicate already removed before Phase 12)"
    - "js/views/desktop/sidebar.js mountSidebarToggle reads from localStorage key 'habits:sidebar-collapsed' — state survives function calls"
    - "js/desktop.js calls mountSidebarToggle(sidebarEl) outside any route handler — called exactly once at boot (line 139)"
    - { statement: "UX-01 and UX-02 are satisfied by existing code — this plan documents that fact via grep-verified acceptance criteria", verification: backstop }
  artifacts:
    - "(no new files — verification plan)"
  key_links:
    - "buildFooterNav analytics link: href './desktop.html', text key nav.analytics (D-08)"
    - "sidebar state: localStorage key 'habits:sidebar-collapsed', module-level var collapsed in sidebar.js, applyState() reads and writes that var"
  prohibitions:
    - "Mobile analytics section in-shell (index.html) is a deferred idea — MUST NOT be added here (flagged-unverified)"
    - "footer nav cross-shell behavior (opens desktop.html in same tab, D-08) MUST NOT be changed to a new-tab or in-shell analytics panel (flagged-unverified)"
---

<objective>
Verify UX-01 (footer nav analytics link present and functional; no duplicate desktop analytics link in settings) and UX-02 (desktop sidebar collapse state persists across hash-route navigation). Both are satisfied by existing code shipped before Phase 12. This plan provides grep-verifiable acceptance criteria confirming each sub-requirement and produces a SUMMARY documenting the verified state.

Purpose: Closes out UX-01 and UX-02 requirements with a documented code scan. If any scan reveals a regression, the executor fixes it within the task scope.
Output: Verification report in SUMMARY.md; no file changes expected (both requirements already met).
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-swipe-ux-navigation-verification/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify UX-01 — footer nav analytics link + no settings duplicate</name>
  <files>js/views/today/builders.js, js/views/settings.js</files>
  <read_first>
    - js/views/today/builders.js — read the buildFooterNav function (lines 96-110 area) to confirm the analytics link definition with href './desktop.html' and t('nav.analytics')
    - js/views/settings.js — scan the entire file for any 'desktop.html' string to confirm the duplicate link is absent (D-09)
    - .planning/phases/12-swipe-ux-navigation-verification/12-CONTEXT.md — D-08 (footer nav cross-shell behavior kept as-is) and D-09 (duplicate scan)
  </read_first>
  <action>
1. Read js/views/today/builders.js and locate buildFooterNav. Confirm it contains a link definition with href './desktop.html' and text key 'nav.analytics'. If this link is missing, add it back to the linkDefs array in buildFooterNav following the existing pattern: `{ href: './desktop.html', text: t('nav.analytics') }`.

2. Read js/views/settings.js in its entirety. Search for any occurrence of 'desktop.html'. If found as an href in a link element or anchor, remove that element. If not found (expected result based on pre-plan grep), document in the SUMMARY: "UX-01 duplicate sub-requirement already satisfied — no desktop.html link found in settings.js."

3. Run `node --test tests/` to confirm no regressions.

4. Do NOT change the cross-shell behavior of the analytics link (D-08). The link opens desktop.html in the same tab — that is correct and intentional.
  </action>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/ exits 0
    - js/views/today/builders.js contains the string "'./desktop.html'" (analytics link href in buildFooterNav)
    - js/views/settings.js does not contain the string 'desktop.html' (no duplicate link)
  </acceptance_criteria>
  <done>Footer nav contains the analytics link pointing to ./desktop.html. Settings view has no duplicate desktop analytics link. Both sub-requirements of UX-01 are confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Verify UX-02 — sidebar collapse state persists across hash-route navigation</name>
  <files>js/views/desktop/sidebar.js, js/desktop.js</files>
  <read_first>
    - js/views/desktop/sidebar.js — read the entire file (53 lines) to understand mountSidebarToggle: verify it reads from localStorage at the start, stores state in a module-level or closure-level var, and that the click handler updates that var and writes back to localStorage
    - js/desktop.js — find the mountSidebarToggle call site (line 139) and confirm it is outside any route handler callback or on() call; confirm it is called once at boot
    - .planning/phases/12-swipe-ux-navigation-verification/12-CONTEXT.md — D-10 (verify persistence; fix if regression found)
  </read_first>
  <action>
1. Read js/views/desktop/sidebar.js. Verify: (a) mountSidebarToggle reads `localStorage.getItem('habits:sidebar-collapsed')` to initialize the collapsed state; (b) the click handler toggles the local state variable and writes to localStorage; (c) the function is self-contained — it does not rely on being re-called to restore state after route changes.

2. Read js/desktop.js around line 139. Verify that `mountSidebarToggle(sidebarEl)` is called at module level (outside any router callback). If it is inside a route handler, move it to the boot sequence just before or after the sidebarEl querySelector.

3. If sidebar.js does NOT persist to localStorage (unexpected based on pre-plan read), fix the applyState function to also call `localStorage.setItem('habits:sidebar-collapsed', String(isCollapsed))` and the initialization to read from `localStorage.getItem('habits:sidebar-collapsed') === 'true'`.

4. Run `node --test tests/` to confirm no regressions.
  </action>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/ exits 0
    - js/views/desktop/sidebar.js contains the string "localStorage.getItem('habits:sidebar-collapsed')" (state read from storage at init)
    - js/views/desktop/sidebar.js contains the string "localStorage.setItem('habits:sidebar-collapsed'" (state written on toggle)
    - js/desktop.js contains the string 'mountSidebarToggle(sidebarEl)' and that call does NOT appear inside a route handler function body (sidebar mounted at boot, not on each route)
  </acceptance_criteria>
  <done>Sidebar collapse state is stored in localStorage and survives hash-route navigation within the desktop shell session. mountSidebarToggle is called once at boot, not inside the router. UX-02 confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| footer nav link → desktop.html | Cross-shell navigation; user-visible link; same-tab open (D-08) |
| localStorage → sidebar state | Client-side persistence; user-controlled; single-user personal app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Tampering | localStorage sidebar-collapsed value | low | accept | Single-user personal app; the only consumer is sidebar.js which coerces the value to boolean via === 'true'; arbitrary strings read as 'false' (collapsed = false, safe default) |
| T-12-06 | Information Disclosure | footer nav analytics link | low | accept | Link opens desktop.html in same tab; no sensitive info exposed; same-origin navigation only |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages |
</threat_model>

<verification>
- node --test tests/ exits 0 after both tasks
- today/builders.js buildFooterNav contains analytics link to './desktop.html'
- settings.js contains no 'desktop.html' string
- sidebar.js reads and writes to localStorage key 'habits:sidebar-collapsed'
- desktop.js calls mountSidebarToggle at boot outside any route handler
</verification>

<success_criteria>
- UX-01 confirmed: analytics footer nav link is present; settings has no duplicate link
- UX-02 confirmed: sidebar collapse state persists via localStorage; no re-mount on route change
- Full test suite passes with no regressions
</success_criteria>

<output>
Create .planning/phases/12-swipe-ux-navigation-verification/12-03-SUMMARY.md when done
</output>

## Artifacts this plan produces

No new files or symbols. This is a verification plan.

SUMMARY.md will document:
- UX-01 verification result: footer nav analytics link confirmed; settings duplicate absent
- UX-02 verification result: sidebar localStorage persistence confirmed; single boot-time mount confirmed
- Any fixes applied if pre-plan findings were incorrect
