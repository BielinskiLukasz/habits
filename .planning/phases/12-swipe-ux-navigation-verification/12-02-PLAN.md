---
phase: 12-swipe-ux-navigation-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - js/views/today/builders.js
  - css/tokens.css
  - css/today.css
  - js/views/history.js
autonomous: true
requirements:
  - LOG4-02
  - LOG4-03

estimate:
  tokens: 55000
  raw_tokens: 27000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "today/builders.js buildTodayRow shows glyph '✕' for failed state (D-03)"
    - "today/builders.js buildTodayRow shows glyph '↷' for skipped state (D-03)"
    - "today/builders.js applies today-row--failed CSS class when status === 'failed'"
    - "css/tokens.css defines --color-failed-bg custom property"
    - "css/today.css has .today-row--failed rule using var(--color-failed-bg)"
    - "history.js swipe-right reads repo.getLog(habitId, selectedDate) to get current status before cycling"
    - "history.js swipe-right dispatches the same 4 event types as today.js (markCompleted/markFailed/markSkipped/markUncompleted)"
    - { statement: "history/builders.js already renders all 4 states with glyphs and CSS classes — no changes needed to that file", verification: backstop }
  artifacts:
    - "js/views/today/builders.js (isFailed branch, today-row--failed class, glyph spans for failed and skipped)"
    - "css/tokens.css (--color-failed-bg token)"
    - "css/today.css (.today-row--failed and .today-row--failed .today-row-name rules)"
  key_links:
    - "history.js imports nextLogState from '../domain/logStatus.js'"
    - "history.js uses repo.getLog() (available via mountHistory closure, no new import needed)"
    - "glyph spans added to today/builders.js use text: property (no .innerHTML, D-78)"
  prohibitions:
    - "today/builders.js MUST NOT use .innerHTML to insert glyph characters — use text: property in the descriptor object (D-78, flagged-unverified)"
    - "history.js MUST NOT modify the dx < -60 branch — swipe-left actions panel with Skip/Fail buttons kept intact (D-02, flagged-unverified)"
---

<objective>
Expand the tracer from Plan 01 in two directions: (1) add failed and skipped visual indicators (glyph + CSS) to today/builders.js completing LOG4-02 visual requirements, and (2) wire history.js swipe-right to use nextLogState with repo.getLog() for arbitrary-date status reads, completing LOG4-03.

Purpose: Makes all 4 log states visually distinct on Today (D-03); applies the same swipe cycling model to History so both surfaces behave identically (D-01, LOG4-03).
Output: updated today/builders.js, new CSS token, new CSS rule, updated history.js swipe handler.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-swipe-ux-navigation-verification/12-CONTEXT.md
@.planning/phases/12-swipe-ux-navigation-verification/12-PATTERNS.md
@.planning/phases/12-swipe-ux-navigation-verification/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failed/skipped visual indicators to today/builders.js and CSS</name>
  <files>js/views/today/builders.js, css/tokens.css, css/today.css</files>
  <read_first>
    - js/views/today/builders.js — read the entire buildTodayRow function (lines 149-224); note the exact isCompleted/isSkipped branches in tapChildren and the rowClasses array; see that ✓ glyph only exists for completed, skipped has no glyph
    - css/tokens.css — find the score color block (lines 61-65 area) to see the --color-score-failing: #ef4444 entry as the naming anchor for the new --color-failed-bg token
    - css/today.css — find the .today-row--completed and .today-row--skipped rules to see the established pattern for state modifier classes
    - .planning/phases/12-swipe-ux-navigation-verification/12-PATTERNS.md — read the builders.js extension pattern and CSS token/rule examples
  </read_first>
  <action>
In js/views/today/builders.js — buildTodayRow function:

1. Add `const isFailed = status === 'failed';` immediately after the existing `const isSkipped = status === 'skipped';` line.

2. Replace the tapChildren branching block. The new logic for binary habits: when isCompleted, push a glyph span with text '✓' (class 'today-row-glyph') then a name span with class 'today-row-name today-row-name--completed'. When isFailed, push a glyph span with text '✕' (class 'today-row-glyph') then a name span with class 'today-row-name today-row-name--failed'. When isSkipped, push a glyph span with text '↷' (class 'today-row-glyph') then a name span with class 'today-row-name today-row-name--skipped'. Otherwise (undefined state), push only a name span with class 'today-row-name'. Each object must use the 'text:' property for the string value — never innerHTML.

3. In the rowClasses array block, add `if (isFailed) rowClasses.push('today-row--failed');` between the isCompleted and isSkipped pushes.

In css/tokens.css — in the score color block near --color-score-failing: #ef4444:

4. Add `--color-failed-bg: rgba(239, 68, 68, 0.12);` on the next line after --color-score-failing. This subtle red tint parallels the existing score color palette (same hue, 12% opacity). No other token changes.

In css/today.css — after the .today-row--skipped rules:

5. Add a .today-row--failed rule: `background: var(--color-failed-bg);` and a nested `.today-row--failed .today-row-name { opacity: 0.75; }` rule. This parallels the existing .today-row--completed and .today-row--skipped modifier patterns.

6. Run `node --test tests/` to confirm no regressions.
  </action>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/ exits 0
    - js/views/today/builders.js contains the string 'isFailed'
    - js/views/today/builders.js contains the string 'today-row--failed'
    - js/views/today/builders.js contains the string '✕'
    - js/views/today/builders.js contains the string '↷'
    - js/views/today/builders.js does not contain '.innerHTML'
    - css/tokens.css contains '--color-failed-bg'
    - css/today.css contains '.today-row--failed'
  </acceptance_criteria>
  <done>buildTodayRow shows ✕ glyph for failed state and ↷ glyph for skipped state; today-row--failed CSS class is applied; red tint token and rule are in place; no regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire history.js swipe-right to use nextLogState</name>
  <files>js/views/history.js</files>
  <read_first>
    - js/views/history.js — read lines 1-40 to confirm repo is available via the mountHistory({repo, store}) parameter (it is — no new import needed); read lines 147-177 to see the full handleSwipeEnd function and the exact dx > 60 branch that currently calls apply({ type: 'markCompleted' }) directly
    - js/domain/logStatus.js — confirm nextLogState export signature (created in Plan 01 Task 1)
    - .planning/phases/12-swipe-ux-navigation-verification/12-PATTERNS.md — read the history.js replacement pattern (the repo.getLog chain)
  </read_first>
  <action>
1. Add import at the top of history.js: `import { nextLogState } from '../domain/logStatus.js';`

2. In handleSwipeEnd (inside mountHistory closure), replace the dx > 60 branch body. The new body when habitId is present: call `repo.getLog(habitId, selectedDate)` (repo is already in scope via the mountHistory closure — no new import needed). In the `.then((log) => { ... })` callback: read `const currentStatus = log?.status ?? null;`, call `const next = nextLogState(currentStatus);`, derive event type (next === 'completed' → 'markCompleted'; next === 'failed' → 'markFailed'; next === 'skipped' → 'markSkipped'; null/falsy → 'markUncompleted'), return `apply({ type, payload: { habitId, date: selectedDate } })`. Chain the existing `.then(() => { showUndoToast ...; render(selectedDate); })` and the existing `.catch(() => showErrorToast(...))` onto the repo.getLog chain.

3. Do NOT modify the dx < -60 branch. The left-swipe actions panel with history-swipe-skip and history-swipe-fail buttons must remain unchanged (D-02).

4. Run `node --test tests/` to confirm no regressions.
  </action>
  <reversibility rating="costly">Implements D-01 gesture contract on History view. Reverting requires updating history.js and removing the cycle logic; this also breaks the user-facing gesture symmetry with Today view.</reversibility>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <acceptance_criteria>
    - node --test tests/ exits 0
    - js/views/history.js contains "import { nextLogState } from '../domain/logStatus.js'"
    - js/views/history.js contains 'repo.getLog'
    - js/views/history.js contains 'markFailed'
    - js/views/history.js contains 'markUncompleted'
    - js/views/history.js does not contain "apply({ type: 'markCompleted', payload: { habitId, date: selectedDate } })" as the sole dx > 60 action (the unconditional markCompleted dispatch is gone)
    - js/views/history.js does not contain '.innerHTML'
  </acceptance_criteria>
  <done>history.js swipe-right reads current log status via repo.getLog and cycles to the next state using nextLogState; all tests pass; left-swipe panel untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch event → history swipe handler | dx triggers repo.getLog then apply() dispatch |
| repo.getLog return value → nextLogState | IDB log status drives which event type fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03 | Information Disclosure | glyph characters in today/builders.js | low | accept | Glyphs (✓ ✕ ↷) are hardcoded literals in JS source, not user-supplied input; text: property assignment uses textContent semantics via mount() helper |
| T-12-04 | Tampering | history.js repo.getLog chain | low | mitigate | null/undefined log returns currentStatus null, which nextLogState maps to 'markCompleted' (first cycle step) — a safe default; no unhandled status can produce an arbitrary IDB write |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages |
</threat_model>

<verification>
- node --test tests/ exits 0 after both tasks
- today/builders.js shows ✕/↷/✓ glyphs and today-row--failed CSS class for the correct states
- css/tokens.css has --color-failed-bg, css/today.css has .today-row--failed rule
- history.js swipe-right uses repo.getLog then nextLogState then dispatches the correct event type
</verification>

<success_criteria>
- All 4 log states are visually distinct on Today: ✓ completed, ✕ failed, ↷ skipped, (blank) undefined (D-03)
- history.js swipe-right cycles the same 4 states as today.js, using the same nextLogState function (LOG4-03)
- No regressions in the full test suite (node --test tests/ exits 0)
</success_criteria>

<output>
Create .planning/phases/12-swipe-ux-navigation-verification/12-02-SUMMARY.md when done
</output>

## Artifacts this plan produces

Modified symbols in existing files:
- `js/views/today/builders.js` — `buildTodayRow` gains `isFailed` const, adds `today-row--failed` class, adds ✕ glyph for failed state and ↷ glyph for skipped state using `text:` property (no innerHTML)
- `css/tokens.css` — new custom property `--color-failed-bg: rgba(239, 68, 68, 0.12)`
- `css/today.css` — new rules `.today-row--failed { background: var(--color-failed-bg); }` and `.today-row--failed .today-row-name { opacity: 0.75; }`
- `js/views/history.js` — `handleSwipeEnd` dx > 60 branch replaced: now calls `repo.getLog` then `nextLogState` then dispatches correct event type; new import of `nextLogState`
