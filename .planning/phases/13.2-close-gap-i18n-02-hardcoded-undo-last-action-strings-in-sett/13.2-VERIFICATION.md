---
phase: 13.2-close-gap-i18n-02-hardcoded-undo-last-action-strings-in-sett
verified: 2026-09-18T22:00:00Z
status: passed
score: 4/4 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 13.2: Close gap: I18N-02 — hardcoded 'Undo last action' strings — Verification Report

**Phase Goal:** Fix the I18N-02 residual so it's genuinely closed — `js/views/settings/builders.js` lines 484, 494, and 497 hardcode "Undo last action" (aria-label + text on the disabled undo-button state) instead of calling `t('settings.data.undoBtn')` like the enabled sibling button does.

**Verified:** 2026-09-18T22:00:00Z
**Status:** PASSED
**Score:** 4/4 must-haves verified

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01/D-02: The disabled undo-button's aria-label and text both resolve through `t('settings.data.undoBtn')`, not a hardcoded literal | ✓ VERIFIED | Direct read of `js/views/settings/builders.js` lines 490-498: `'aria-label': t('settings.data.undoBtn')` (line 494) and `text: t('settings.data.undoBtn')` (line 497). `git show fcfe2d3` confirms both substitutions in the committed diff. |
| 2 | D-01: The enabled undo-button's aria-label (previously also hardcoded at line 484, distinct from its already-correct text at line 486) also resolves through `t('settings.data.undoBtn')` | ✓ VERIFIED | Direct read of lines 480-487: `'aria-label': t('settings.data.undoBtn')` (line 484) and `text: t('settings.data.undoBtn')` (line 486, byte-for-byte unchanged per `git show fcfe2d3`, which shows only the `-`/`+` for line 484 and leaves 486 untouched in the diff context). |
| 3 | D-03: No new locale keys were added — `settings.data.undoBtn` already existed in both `en.js` and `pl.js` and is only reused, not redefined | ✓ VERIFIED | `git log --oneline -5 -- js/i18n/en.js js/i18n/pl.js` shows no commits touching these files after `13.2-01` work began; `git show fcfe2d3 --stat` and `git show c5045e2 --stat` confirm only `js/views/settings/builders.js` (3 lines) and `tests/unit/builders.settings.test.js` (22 lines) changed. Key confirmed present pre-existing at `js/i18n/en.js:116` (`'Undo last action'`) and `js/i18n/pl.js:114` (`'Cofnij ostatnią akcję'`). |
| 4 | D-04: A regression test proves both properties track the active locale (asserted under `'pl'`, where the correct output differs from the old hardcoded English literal) so this residual cannot silently reappear | ✓ VERIFIED | `tests/unit/builders.settings.test.js` lines 282-301: new test calls `_resetLangForTest('pl')`, builds the disabled-button tree, computes `expected = t('settings.data.undoBtn')` under `'pl'` (`'Cofnij ostatnią akcję'` — genuinely different from the English literal), and asserts both `undoBtn.attrs['aria-label']` and `undoBtn.text` equal it. Test is genuinely locale-aware, not English-coincidental. Test passes (see Test Results). |

**Score:** 4/4 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `js/views/settings/builders.js` | Lines 484, 494, 497 call `t('settings.data.undoBtn')`; line 486 unchanged | ✓ VERIFIED | Confirmed via direct read (lines 465-504) and via `git show fcfe2d3` diff (exactly 3 lines changed, all `'Undo last action'` → `t('settings.data.undoBtn')`) |
| `tests/unit/builders.settings.test.js` | New regression test added to `buildDataCard — D-65 (Undo + Reset)` describe block; new `t`/`_resetLangForTest` import | ✓ VERIFIED | Import at line 36; new test at lines 282-301, positioned exactly between the `hasUndoToken=true` test and the `'Reset block carries...'` test as planned |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `buildDataCard`'s undo-button branches (both `hasUndoToken=true` and `hasUndoToken=false`) | `js/i18n/index.js` `t()` | Direct function call, key `'settings.data.undoBtn'` | ✓ WIRED | All three previously-hardcoded sites (484, 494, 497) plus the pre-existing correct site (486) now call the same `t()` chokepoint; `t` already imported at top of `builders.js` (unchanged, no new import needed) |

---

## Test Results

| Test Suite | Command | Result | Details |
|-----------|---------|--------|---------|
| Phase test file | `node --test tests/unit/builders.settings.test.js` | ✓ PASS | 31 tests, 31 pass, 0 fail; duration 612ms; includes the new I18N-02 regression test |
| Full suite | `node --test "tests/**/*.test.js"` | ✓ PASS | 984 tests across 349 suites, all pass, 0 fail; duration ~50.5s; no regressions. (Note: plain `node --test tests/` fails with `Cannot find module 'tests'` on this Windows/Node setup — the glob form is required, matching SUMMARY.md's documented workaround.) |

Both commands were run independently by the verifier (not sourced from SUMMARY.md claims) and both exited 0.

---

## Commits Verified

| Commit | Message | Purpose | Status |
|--------|---------|---------|--------|
| `c5045e2` | test(13.2-01): add failing test for undo btn i18n | RED: new locale-switch regression test added (22 lines, test file only) | ✓ VERIFIED |
| `fcfe2d3` | feat(13.2-01): route undo btn strings through t() | GREEN: 3 literal-to-`t()` substitutions in `builders.js` (3 lines changed, production file only) | ✓ VERIFIED |

---

## Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| I18N-02 | All view builders and event handlers use `t()` — zero hardcoded UI strings remain across both shells | ✓ SATISFIED (for this residual) | The specific residual documented by `v1.2-MILESTONE-AUDIT.md` and `13-VERIFICATION.md` (lines 484/494/497 of `builders.js`) is fixed and regression-tested. `grep -rn "Undo last action" js/` returns only the legitimate `en.js` locale value and a JSDoc comment — no remaining hardcoded UI-string occurrence of this literal. Note: the milestone audit's separate, non-blocking observation about `js/views/diagnostics.js` (dev-only panel strings, "likely out of I18N-02's scope but not formally exempted") is untouched by this phase and was explicitly out of this phase's stated boundary — not a gap in this phase's own goal. |

---

## Anti-Patterns Found

None. Scanned both modified files (`js/views/settings/builders.js`, `tests/unit/builders.settings.test.js`) for `TBD`/`FIXME`/`XXX`/`TODO`/`HACK`/`PLACEHOLDER`/"not yet implemented" markers — zero matches. The change is a minimal, targeted 3-site literal-to-function-call substitution plus one new test; no stubs, no empty implementations, no hardcoded empty data introduced.

---

## Deviations from Plan

None found. The diff matches the plan's acceptance criteria byte-for-byte:
- Line 484: `'aria-label': t('settings.data.undoBtn'),` ✓
- Line 494: `'aria-label': t('settings.data.undoBtn'),` ✓
- Line 497: `text: t('settings.data.undoBtn'),` ✓
- Line 486: unchanged ✓
- `en.js`/`pl.js`: unchanged, no new keys ✓
- New test asserts under `'pl'` locale specifically, not `'en'` ✓

---

## Conclusion

**Phase goal ACHIEVED.** All three previously-hardcoded `'Undo last action'` sites in `buildDataCard`'s Undo section (enabled-branch aria-label, disabled-branch aria-label, disabled-branch text) now resolve through `t('settings.data.undoBtn')`, matching the pre-existing correct pattern. A genuinely locale-aware regression test (asserting under `'pl'`, where the translated string differs from the old hardcoded English literal) proves this and would fail if the defect silently reappeared. No new locale keys were introduced (D-03 honored). The full test suite (984 tests) passes with zero regressions, independently re-run by the verifier.

**Status: PASSED**
**Next step: Ready to proceed to next phase**

---

_Verified: 2026-09-18T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
