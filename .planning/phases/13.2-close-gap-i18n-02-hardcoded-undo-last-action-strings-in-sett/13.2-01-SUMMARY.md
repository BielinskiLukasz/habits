---
phase: 13.2-close-gap-i18n-02-hardcoded-undo-last-action-strings-in-sett
plan: 01
subsystem: ui
tags: [i18n, settings, undo, testing]

# Dependency graph
requires:
  - phase: 10-i18n-tests-verification
    provides: t()/js/i18n/index.js locale lookup and the settings.data.undoBtn key in en.js/pl.js
provides:
  - Undo button's aria-label (both enabled/disabled states) and disabled-state text now resolve through t('settings.data.undoBtn') instead of a hardcoded English literal
  - Regression test proving both properties track the active locale (pl vs en)
affects: [13-code-review-documentation, i18n]

# Actuals (#2632)
actuals:
  tokens: 600
  tasks: 1
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "TDD gap-closure: RED test asserting under a non-default locale (pl) before touching production code, to prove the assertion is a genuine regression proof rather than one that passes coincidentally under English"

key-files:
  created: []
  modified:
    - js/views/settings/builders.js
    - tests/unit/builders.settings.test.js

key-decisions:
  - "D-01/D-02 (CONTEXT.md): both disabled-branch hardcoded literals (aria-label line 494, text line 497) and the enabled-branch aria-label (line 484) all become t('settings.data.undoBtn') calls, matching the pre-existing correct pattern at line 486"
  - "D-03: no new locale keys added — settings.data.undoBtn already existed in en.js/pl.js since Phase 10"
  - "D-04: regression test asserts under 'pl' locale (not 'en') so a hardcoded English literal cannot coincidentally satisfy the assertion"

patterns-established:
  - "Locale-switch regression test pattern for i18n compliance: _resetLangForTest('pl') before build, assert against t(key) under that locale, wrap in try/finally resetting to 'en' to avoid leaking locale state across tests in the same file"

requirements-completed: [I18N-02]

coverage:
  - id: D1
    description: "All three previously-hardcoded 'Undo last action' sites in buildDataCard's Undo section (enabled aria-label, disabled aria-label, disabled text) now resolve through t('settings.data.undoBtn')"
    requirement: "I18N-02"
    verification:
      - kind: unit
        ref: "tests/unit/builders.settings.test.js#hasUndoToken=false -> disabled button aria-label AND text resolve through t(), not the hardcoded English literal (I18N-02 regression, D-04)"
        status: pass
      - kind: unit
        ref: "node --test tests/**/*.test.js (full suite, 984 tests)"
        status: pass
    human_judgment: false

# Metrics
duration: 5min
completed: 2026-09-18
status: complete
---

# Phase 13.2 Plan 01: Route undo-button strings through t() Summary

**Closed I18N-02 residual by replacing three hardcoded `'Undo last action'` literals in `buildDataCard` with `t('settings.data.undoBtn')` calls, proven by a Polish-locale regression test.**

## Performance

- **Duration:** 5 min
- **Started:** 2026-09-18T21:37:49+02:00
- **Completed:** 2026-09-18T21:42:05+02:00
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- `js/views/settings/builders.js` lines 484, 494, and 497 now call `t('settings.data.undoBtn')`; line 486 was already correct and remains byte-for-byte unchanged
- Added a regression test in `tests/unit/builders.settings.test.js` that builds the disabled undo-button under the `'pl'` locale and asserts both `aria-label` and `text` equal the Polish translation — a hardcoded English literal cannot satisfy this assertion
- Confirmed RED (test failed against the pre-fix code with `actual: 'Undo last action'` vs `expected: 'Cofnij ostatnią akcję'`) before applying the production fix, then confirmed GREEN
- Full test suite (984 tests across 349 suites) passes with zero regressions

## Task Commits

Each task was committed atomically following RED → GREEN:

1. **Task 1 RED: add failing regression test** - `c5045e2` (test)
2. **Task 1 GREEN: route undo btn strings through t()** - `fcfe2d3` (feat)

**Plan metadata:** committed via `docs(13.2-01): complete plan` (see below)

## Files Created/Modified
- `js/views/settings/builders.js` — 3 hardcoded `'Undo last action'` literals replaced with `t('settings.data.undoBtn')` calls (lines 484, 494, 497)
- `tests/unit/builders.settings.test.js` — added `t`/`_resetLangForTest` import and one new regression test in the `buildDataCard — D-65 (Undo + Reset)` describe block

## Decisions Made
- No new locale keys added (D-03) — `settings.data.undoBtn` already existed in both `en.js` and `pl.js` since Phase 10
- Regression test asserts under `'pl'` locale specifically (not `'en'`) so the assertion is a genuine proof — an English-locale assertion would have passed even against the hardcoded bug, since the hardcoded literal and the English translation are identical strings
- Test wraps its body in `try { ... } finally { _resetLangForTest('en'); }` to avoid leaking the `'pl'` locale into later tests sharing the same `js/i18n/index.js` module-level singleton

## Deviations from Plan

None - plan executed exactly as written. The exact diff (three literal-to-`t()` substitutions in `builders.js`, one new test in `builders.settings.test.js`) matches the plan's acceptance criteria byte-for-byte.

## Issues Encountered

`node --test tests/` failed with `Cannot find module 'tests'` on this Windows/Node 24 environment when passed a bare directory path (a Node/Windows path-resolution quirk unrelated to this change). Worked around by running `node --test "tests/**/*.test.js"` instead, which correctly discovered and ran all 984 tests. This is an environment-specific invocation detail, not a code or test defect — no files were changed to address it.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- I18N-02 residual fully closed; `REQUIREMENTS.md` I18N-02 checkbox (already `[x]` per milestone audit note) now genuinely reflects reality
- No blockers or concerns for subsequent work
- Manual spot-check (optional, non-blocking per plan) not performed in this automated session: switching language to Polish in a live browser session and visually confirming "Cofnij ostatnią akcję" renders on both undo-button states

## Self-Check: PASSED

- FOUND: js/views/settings/builders.js
- FOUND: tests/unit/builders.settings.test.js
- FOUND commit: c5045e2
- FOUND commit: fcfe2d3

---
*Phase: 13.2-close-gap-i18n-02-hardcoded-undo-last-action-strings-in-sett*
*Completed: 2026-09-18*
