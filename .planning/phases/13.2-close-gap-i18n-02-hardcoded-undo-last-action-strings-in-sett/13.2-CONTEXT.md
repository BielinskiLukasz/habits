# Phase 13.2: Close gap: I18N-02 — hardcoded 'Undo last action' strings - Context

**Gathered:** 2026-09-18
**Status:** Ready for planning

<domain>
## Phase Boundary

Fix the I18N-02 residual found by the v1.2 milestone audit: `js/views/settings/builders.js` lines 484, 494, and 497 hardcode the English string `"Undo last action"` (aria-label on both button states, and button text on the disabled state) instead of routing through `t()`, unlike the enabled sibling button (line 486) which correctly calls `t('settings.data.undoBtn')`. This phase brings the disabled undo-button state into i18n compliance so I18N-02 ("zero hardcoded UI strings across both shells") is genuinely satisfied. Nothing else in scope — this is a narrowly-targeted gap closure, mirroring how Phase 13.1 closed LOG4-02/LOG4-03.

</domain>

<decisions>
## Implementation Decisions

This is a mechanical 1-for-1 string-to-`t()`-call substitution with no real product ambiguity — treated as auto-decided (no user discussion needed), consistent with how Phase 13.1 closed the sibling LOG4-02/LOG4-03 gap without a discussion round.

### Fix approach
- **D-01:** Both hardcoded `aria-label: 'Undo last action'` occurrences (disabled-state button, lines 484 and 494) become `aria-label: t('settings.data.undoBtn')` — reusing the exact same locale key the enabled sibling button already uses at line 486. No new locale key is introduced.
- **D-02:** The hardcoded `text: 'Undo last action'` at line 497 (disabled-state button) becomes `text: t('settings.data.undoBtn')`, matching the enabled button's pattern exactly.
- **D-03:** No new locale keys are added to `en.js`/`pl.js` — `settings.data.undoBtn` already exists and is already translated in both locales (used by the enabled button since Phase 10). This fix only changes which code path reads it.

### Regression coverage
- **D-04:** Add or extend a unit/integration test asserting the disabled undo-button's `aria-label` and `text` both resolve through `t('settings.data.undoBtn')` — not a hardcoded literal — so this residual cannot silently reappear. Follow the existing test style in `tests/unit/i18n.test.js` / `tests/integration/` (whichever the researcher/planner judges is the better fit given `buildSettingsView`'s test coverage today).

### Claude's Discretion
- Exact test file placement (new test vs. extending an existing settings-builder test file) — no user preference stated; follow existing project conventions.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Milestone audit findings
- `.planning/v1.2-MILESTONE-AUDIT.md` — documents this exact gap (I18N-02 unsatisfied) under `gaps.requirements` and in the Requirements Coverage table; also documents the sibling LOG4-02/LOG4-03 gap closure pattern this phase should mirror.

### Requirements
- `.planning/REQUIREMENTS.md` — I18N-02: "All view builders and event handlers use t() — zero hardcoded UI strings remain across both shells." Currently shows `[x]` despite this open defect; this phase's completion should flip nothing back (it will make the checkbox true again) but note the checkbox was already incorrectly `[x]` — no action needed there beyond closing the actual defect.

### Prior phase precedent (same pattern)
- `.planning/phases/13.1-close-gap-log4-02-log4-03-fail-button-deletes-log-instead-of/13.1-01-SUMMARY.md` — sibling gap-closure phase; same narrow scope, same "fix + regression test + update tracking docs" shape.
- `.planning/phases/10-i18n-tests-verification/10-VERIFICATION.md` — original (incomplete) I18N-02 verification; explains why this residual escaped Phase 10's scan (it only checked `showErrorToast`/`showUndoToast` call sites, not raw builder object literals).

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `t()` from `js/i18n/index.js` — already imported in `js/views/settings/builders.js`; `settings.data.undoBtn` key already exists in both `js/i18n/en.js` and `js/i18n/pl.js`.

### Established Patterns
- The enabled-button branch (`js/views/settings/builders.js` ~line 480-487) is the correct reference implementation: `text: t('settings.data.undoBtn')`. The disabled-button branch (~line 488-499) is the one with the defect.

### Integration Points
- `js/views/settings/builders.js` — `buildSettingsView` (or whichever function builds the "Undo section", per the code read during the audit at lines 470-504) — the disabled undo-button object literal at lines 488-499.

</code_context>

<specifics>
## Specific Ideas

No specific UI/behavior requirements beyond fixing the two hardcoded strings — this is a pure i18n-compliance fix, not a design change. The disabled button's visual appearance and behavior must stay identical; only the source of its label/text strings changes.

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope. (No interactive discussion was held; this phase's scope is a single, unambiguous, previously-identified defect.)

</deferred>

---

*Phase: 13.2-close-gap-i18n-02-hardcoded-undo-last-action-strings-in-sett*
*Context gathered: 2026-09-18*
