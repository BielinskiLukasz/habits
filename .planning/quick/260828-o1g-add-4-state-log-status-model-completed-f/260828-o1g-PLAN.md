---
phase: quick-260828-o1g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/db/schema.js
  - js/state/apply/markCompleted.js
  - js/state/apply/markUncompleted.js
  - js/state/apply/markSkipped.js
  - js/state/apply.js
  - js/domain/scoring.js
  - js/domain/mastery.js
  - js/io/export.js
  - js/i18n/en.js
  - js/i18n/pl.js
  - js/views/today.js
  - js/views/today/builders.js
  - css/today.css
  - js/views/history/builders.js
  - tests/unit/mastery.test.js
  - tests/unit/scoring.test.js
  - tests/unit/export.csv.test.js
  - tests/unit/builders.today.test.js
  - tests/unit/builders.history.test.js
  - tests/integration/contract.fake-vs-real.test.js
autonomous: true
requirements: [LOG-4STATE]

estimate:
  tokens: 110000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - Swiping right on a Today habit row marks it completed (status:'completed' in IDB).
    - Swiping left on a Today habit row reveals Skip and Fail buttons; tapping Skip writes status:'skipped', tapping Fail writes status:'failed'.
    - Skipped log rows are excluded from the scoring denominator (S1/S2/S3) just like non-applicable days.
    - Existing IDB rows with completed:true migrate to status:'completed'; completed:false to status:'failed' on DB_VERSION bump.
    - CSV export maps status:'completed'→'1', status:'skipped'→'x', status:'failed'→'0'.
    - History view shows a distinct visual treatment for skipped days.
    - All existing tests pass after log model update; new tests cover skipped behaviour.
  artifacts:
    - js/state/apply/markSkipped.js (new handler)
    - js/db/schema.js (DB_VERSION=2, MIGRATIONS[2] cursor migration)
  key_links:
    - MIGRATIONS[2] migration runs inside the IDB upgrade transaction using openCursor callbacks — no await.
    - _recomputeLastCompletedDate filter changed from l.completed===true to l.status==='completed'.
    - LOG_COMPLETED.binary in both scoring.js and mastery.js updated from log.completed===true to log.status==='completed'.
    - RENDERERS.binary in today.js updated from log?.completed===true to log?.status==='completed'.
---

<objective>
Replace the binary `completed: boolean` log model with a 4-state `status: 'completed'|'failed'|'skipped'` field across the entire stack: DB schema, state handlers, scoring, mastery, CSV export, Today view swipe UX, history display, i18n strings, and tests.

Purpose: Users need to explicitly record "I chose to skip today" separately from "I tried and failed", so scoring, streaks, and mastery can treat each state correctly.
Output: New markSkipped handler, DB migration, updated domain logic, swipe UI on Today mobile, updated CSV export, history display, full test coverage.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@C:/my-code/vibe-coding/habits/CLAUDE.md
@C:/my-code/vibe-coding/habits/.planning/STATE.md
@C:/my-code/vibe-coding/habits/js/db/schema.js
@C:/my-code/vibe-coding/habits/js/state/apply.js
@C:/my-code/vibe-coding/habits/js/state/apply/markCompleted.js
@C:/my-code/vibe-coding/habits/js/state/apply/markUncompleted.js
@C:/my-code/vibe-coding/habits/js/domain/scoring.js
@C:/my-code/vibe-coding/habits/js/domain/mastery.js
@C:/my-code/vibe-coding/habits/js/io/export.js
@C:/my-code/vibe-coding/habits/js/views/today.js
@C:/my-code/vibe-coding/habits/js/views/today/builders.js
@C:/my-code/vibe-coding/habits/js/views/history/builders.js
@C:/my-code/vibe-coding/habits/js/i18n/en.js
@C:/my-code/vibe-coding/habits/js/i18n/pl.js
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Core 4-state log model — DB migration, state handlers, domain, i18n, CSV</name>
  <files>
    js/db/schema.js,
    js/state/apply/markCompleted.js,
    js/state/apply/markUncompleted.js,
    js/state/apply/markSkipped.js,
    js/state/apply.js,
    js/domain/scoring.js,
    js/domain/mastery.js,
    js/io/export.js,
    js/i18n/en.js,
    js/i18n/pl.js
  </files>
  <action>
Read every file listed in `<files>` before making any edits. Apply changes in this order:

**1. js/db/schema.js — DB_VERSION bump + data migration**

Change `export const DB_VERSION = 1` to `export const DB_VERSION = 2`.

Add `MIGRATIONS[2]` entry. The migration must run inside the IDB upgrade transaction using raw IDB openCursor callbacks (no async/await; the upgrade tx stays open while outstanding requests exist). Pattern:

- Call `tx.objectStore('logs').openCursor()` and attach an `onsuccess` callback.
- Inside the callback: if `event.target.result` is null, return (done). Otherwise get the cursor's value, compute the migrated shape, and call `cursor.update(migrated)` then `cursor.continue()`.
- Migration logic: if the value has `completed === true`, emit `{ ...value, status: 'completed' }` without the `completed` field. If `completed === false`, emit `{ ...value, status: 'failed' }` without the `completed` field. If neither property exists (already migrated or newer shape), leave it as-is and continue.
- Use `const { completed, ...rest } = value` to strip the `completed` boolean before spreading, or use `delete` on a shallow copy — never mutate in place before calling `cursor.update`.

Update the file-level JSDoc `@file` comment to reference this migration and note that from v2 onward log rows carry `status: 'completed'|'failed'|'skipped'` instead of `completed: boolean`.

**2. js/state/apply/markCompleted.js — write status:'completed'**

In `handleMarkCompleted`: change the `next` object from `{ habitId, date, completed: true, definitionVersion: null }` to `{ habitId, date, status: 'completed', definitionVersion: null }`. Update the JSDoc `@type` annotation on `next` to reflect the new shape.

In `handleRestoreLogRow`: the synthetic placeholder used when `prior` is null/undefined changes from `{ habitId, date, completed: false }` to `{ habitId, date, status: 'failed' }`. This ensures `_recomputeLastCompletedDate` (which will now filter on `l.status === 'completed'`) correctly excludes this placeholder from the lastCompletedDate pool. Update the inline comment explaining the synthetic row to reference `status: 'failed'` instead of `completed: false`.

Update the file-level JSDoc `@file` comment to remove references to `completed: true/false` shape and note the new `status` field.

**3. js/state/apply/markUncompleted.js — write status:'failed' + fix _recomputeLastCompletedDate**

In `handleMarkUncompleted`: change the `next` object from `{ habitId, date, completed: false, definitionVersion: null }` to `{ habitId, date, status: 'failed', definitionVersion: null }`. Update the JSDoc `@type` annotation on `next`.

In `_recomputeLastCompletedDate`: change the filter from `.filter((l) => l.completed === true)` to `.filter((l) => l.status === 'completed')`. Update the JSDoc `@param` for `currentLogRow` to describe the new shape (`status: string` instead of `completed: boolean`). Update the file-level JSDoc `@file` comment (D-74 reference — replace "completed: false" with "status: 'failed'" in the description).

**4. js/state/apply/markSkipped.js — NEW file**

Create this file from scratch following the exact handler contract from `markUncompleted.js` (same storeNames/writes/inverse shape). File-level `@file` JSDoc: "Per-event handler for `markSkipped`. Writes `{habitId, date, status: 'skipped', definitionVersion: null}` — preserves the audit log. The D-52 `lastCompletedDate` invariant is NOT affected by a skipped log (filter in `_recomputeLastCompletedDate` requires `status === 'completed'`)."

Export `handleMarkSkipped(event, repo)`:
- `const { habitId, date } = event.payload`
- `const prior = await repo.getLog(habitId, date)` — capture for inverse
- `const next = { habitId, date, status: 'skipped', definitionVersion: null }`
- No `_recomputeLastCompletedDate` call needed (skipped doesn't change lastCompletedDate)
- Return `{ storeNames: ['logs'], writes: [{ store: 'logs', value: next }], inverse: { type: 'restoreLogRow', payload: { habitId, date, prior } } }`

Export static method `handleMarkSkipped.broadcastKeys = (event) => ({ habitId: event.payload.habitId, date: event.payload.date })`.

Forbidden constructs in this file: direct calls to repo write helpers; `.innerHTML`; no switch.

**5. js/state/apply.js — register markSkipped**

Add import: `import { handleMarkSkipped } from './apply/markSkipped.js';`

Add to the HANDLERS table: `markSkipped: handleMarkSkipped,` — insert after the `markUncompleted` entry so the table stays logically grouped.

**6. js/domain/scoring.js — LOG_COMPLETED.binary + skipped exclusion from denominators**

`LOG_COMPLETED.binary`: change `log.completed === true` to `log.status === 'completed'`.

`computeS1` (daily path only — skip `_computeS1Periodic`): Before the applicable-day-count loop, build a Set of skipped dates: iterate `logsForHabit`, collecting `log.date` where `log.status === 'skipped'` into a `skippedDates` Set. In the applicable-day-count loop, add `if (skippedDates.has(dayYMD)) continue` immediately after `if (ctx.appliesToday(habit, dayYMD, ctx))` returns true — this excludes skipped days from the denominator. The `completedCount` loop already excludes skipped days automatically (since `isLogCompleted` returns false for `status !== 'completed'`), but for clarity add the same guard: skip counting a log if `log.status === 'skipped'`.

`computeS2`: The `logByDate` Map is already built. In the per-day loop, after `if (!ctx.appliesToday(habit, dayYMD, ctx)) continue`, add `if (logByDate.get(dayYMD)?.status === 'skipped') continue` — this skips both the denominator weight and the numerator contribution for that day.

`computeS3`: Same `logByDate` Map pattern. After the target-habit cadence check (`if (!ctx.appliesToday(habit, dayYMD, ctx)) continue`), add `if (logByDate.get(dayYMD)?.status === 'skipped') continue`. Note that `allHabits` load calculation (per-day load count) does NOT exclude other habits' skipped days — the load is still all applicable habits on that day; only the target habit's skipped day is removed from its own scoring.

Update the module-level JSDoc comment to mention skipped exclusion and the updated LOG_COMPLETED.binary shape.

**7. js/domain/mastery.js — LOG_COMPLETED.binary only**

`LOG_COMPLETED.binary`: change `log.completed === true` to `log.status === 'completed'`. No other changes — the context specifies only this field for mastery.

**8. js/io/export.js — CSV status mapping**

Find the inner `isCompleted(log)` function and the final binary-habit return. The existing last line is `return log.completed === true ? '1' : '0'`. Replace the binary fallback block with a status-aware check:
- If `log.status === 'skipped'` return `'x'`.
- If `log.status === 'completed'` return `'1'`.
- Otherwise (status:'failed', no status, or any unknown state) return `'0'`.

Also update the `isCompleted(log)` function's last line `return log.completed === true` to `return log.status === 'completed'` — this is the binary path within `isCompleted`, used when the log lacks `slots` and `count`.

Update the file-level JSDoc and the inline comment block that describes the cell encoding logic to reflect the 3-symbol mapping.

**9. js/i18n/en.js and js/i18n/pl.js — add 4-state action/status keys**

In `en.js`, add inside the "Today view" section:
- `'today.skip': 'Skip'`
- `'today.fail': 'Fail'`
- `'today.skipped': 'Skipped'`
- `'today.failed': 'Failed'`

In `pl.js`, add the same keys with Polish values:
- `'today.skip': 'Pomiń'`
- `'today.fail': 'Nie udało się'`
- `'today.skipped': 'Pominięto'`
- `'today.failed': 'Nie wykonano'`

Add them after the existing `'today.phaseTooltip'` entry so the Today section stays grouped.
  </action>
  <verify>
    <automated>node --test tests/unit/mastery.test.js tests/unit/scoring.test.js tests/unit/export.csv.test.js tests/unit/i18n.test.js tests/unit/schema.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>
    - DB_VERSION === 2 and MIGRATIONS[2] exists in schema.js using cursor callbacks (no await).
    - markCompleted writes status:'completed'; markUncompleted writes status:'failed'; markSkipped (new file) writes status:'skipped'.
    - markSkipped is registered in HANDLERS in apply.js.
    - LOG_COMPLETED.binary checks log.status==='completed' in both scoring.js and mastery.js.
    - Skipped dates are excluded from S1/S2/S3 denominators.
    - CSV export maps skipped→x, completed→1, failed→0.
    - i18n keys 'today.skip', 'today.fail', 'today.skipped', 'today.failed' exist in both en.js and pl.js.
    - Existing unit tests for mastery, scoring, export.csv, i18n, schema pass (tests may need fixture updates — that is done in Task 3 so failures here from stale test fixtures are expected; the PRODUCTION code changes are correct).
  </done>
</task>

<task type="auto">
  <name>Task 2: Today view swipe UI — swipe-right=done, swipe-left reveals Skip+Fail</name>
  <files>
    js/views/today.js,
    js/views/today/builders.js,
    css/today.css
  </files>
  <action>
Read all three files before editing. Read `js/views/today.js` in full before editing — the existing optimisticFlip, captureRowPriorState, revertRow, RENDERERS, handleMarkCompleteTap, and handleMarkUncompleteTap patterns must be understood and preserved.

**js/views/today/builders.js — 4-state row structure**

Update `buildTodayRow` (and the binary wrapper in today.js's RENDERERS) to accept `status` instead of `completed`. The builder should receive `{ habit, status }` where `status` is `'completed'|'failed'|'skipped'|null` (null = no log yet, i.e. pending). Change the prop from `completed: boolean` to `status: string|null`.

The row element emitted by `buildTodayRow` gains the following:
- Root element class: `today-row` plus a status modifier:
  - `today-row--completed` when status==='completed'
  - `today-row--skipped` when status==='skipped'
  - `today-row--failed` when status==='failed'
  - No modifier (just `today-row`) when status===null (pending)
- Wrap the existing row content (tap button + habit name area) in a `<div class="today-row__slide">` — this is the element that CSS transform slides left/right during the swipe gesture.
- Add a sibling `<div class="today-row__actions" aria-hidden="true">` containing two buttons:
  - `<button class="today-row__action-btn today-row__action-btn--skip" data-action="swipeSkip" data-habit-id="{habitId}" aria-label="Skip habit">{t('today.skip')}</button>`
  - `<button class="today-row__action-btn today-row__action-btn--fail" data-action="swipeFail" data-habit-id="{habitId}" aria-label="Fail habit">{t('today.fail')}</button>`
- The `aria-pressed` on the tap button: `'true'` when status==='completed', `'false'` otherwise.
- The `data-action` on the tap button: `'markUncomplete'` when status==='completed', `'markComplete'` otherwise.

The row outer element (`today-row`) wraps both `.today-row__slide` and `.today-row__actions`. Recommended structure (do NOT use innerHTML — build via the `{tag, attrs, children, text}` description tree):

```
{tag:'li', attrs:{class:'today-row ...', 'data-habit-id':habit.id}, children:[
  {tag:'div', attrs:{class:'today-row__slide'}, children:[
    {tag:'button', attrs:{class:'today-row-tap', ...}, ...},
    {tag:'span', attrs:{class:'today-row__name'}, text:habit.name},
  ]},
  {tag:'div', attrs:{class:'today-row__actions', 'aria-hidden':'true'}, children:[
    {tag:'button', attrs:{class:'today-row__action-btn today-row__action-btn--skip', 'data-action':'swipeSkip', 'data-habit-id':habit.id, 'aria-label':'Skip habit'}, text:t('today.skip')},
    {tag:'button', attrs:{class:'today-row__action-btn today-row__action-btn--fail', 'data-action':'swipeFail', 'data-habit-id':habit.id, 'aria-label':'Fail habit'}, text:t('today.fail')},
  ]},
]}
```

Note: check the actual structure of the existing buildTodayRow to preserve exact children (Polish name span, wave/stage info, etc.). The above is the structural pattern; adapt to real content.

**js/views/today.js — swipe gesture + new action handlers**

1. Update `RENDERERS.binary`: change `log?.completed === true` to `log?.status === 'completed'`. Change the call to `buildTodayRow` from `{ habit, completed: ... }` to `{ habit, status: log?.status ?? null }`.

2. Update `optimisticFlip` to handle `'skipped'` and `'failed'` states:
   - `to === 'skipped'` → add `today-row--skipped`, set aria-pressed to `'false'`, data-action to `'markComplete'`.
   - `to === 'failed'` → add `today-row--failed`, set aria-pressed to `'false'`, data-action to `'markComplete'`.
   - Keep existing `'completed'` and `'uncompleted'` branches unchanged.

3. Add swipe gesture handler attached to each `.today-row` element (not the tap button). Use `pointerdown` / `pointermove` / `pointerup` events on the `.today-row` element (setPointerCapture so dragging outside still completes).

   Swipe detection logic (vanilla JS, no framework, mirrors mindful-breathing touch pattern):
   - On `pointerdown`: record `startX = evt.clientX`, `startY = evt.clientY`, call `rowEl.setPointerCapture(evt.pointerId)`. Reset any open swipe state on this row (reset translate to 0, close actions panel).
   - On `pointermove`: compute `deltaX = evt.clientX - startX`, `deltaY = evt.clientY - startY`. If `Math.abs(deltaY) > Math.abs(deltaX)` early in the gesture, cancel (vertical scroll — do nothing and let the browser handle it). Otherwise apply CSS transform: `slideEl.style.transform = 'translateX(' + Math.max(-ACTION_WIDTH, Math.min(0, deltaX)) + 'px)'` where `ACTION_WIDTH` is the pixel width at which actions are fully revealed (use `80` as a fixed constant). For swipe-right: allow positive translate up to a small clamp (e.g. 20px) as visual feedback only, since swipe-right commits on pointerup.
   - On `pointerup`: if `deltaX > SWIPE_RIGHT_THRESHOLD` (use `60`) → trigger complete (same as tap); if `deltaX < -SWIPE_LEFT_THRESHOLD` (use `60`) → reveal actions panel by locking translate at `-ACTION_WIDTH` and setting `aria-hidden='false'` on `.today-row__actions`; if neither threshold reached → snap back (reset transform to 0, close actions).
   - Use a `SWIPE_RIGHT_THRESHOLD = 60` and `SWIPE_LEFT_THRESHOLD = 60` constant at module scope.
   - Swipe-right calls `handleMarkCompleteTap` using a synthetic event stub `{ currentTarget: tapBtn }`. Extract the tap button reference from `rowEl.querySelector('.today-row-tap')`.
   - Close any previously opened swipe panel on the SAME parent list when a new swipe starts (track the currently-open row in a module-level `_openSwipeRow` variable; reset its transform when a new pointerdown fires on a different row).

4. Add `handleSwipeSkipTap(evt)` function:
   - Get `habitId` from `evt.currentTarget.getAttribute('data-habit-id')`.
   - `const date = todayLocal()`.
   - `const rowEl = evt.currentTarget.closest('.today-row')`.
   - Optimistic: add `today-row--skipped` class; close actions panel (reset transform to 0, aria-hidden='true').
   - `await apply({ type: 'markSkipped', payload: { habitId, date } })`.
   - On resolve: `showUndoToast({ message: 'Skipped ' + habitName, undoFn: () => undo() })`.
   - On reject: revert row + `showErrorToast("Couldn't skip — try again")`.

5. Add `handleSwipeFailTap(evt)` function (same shape as skip, calls `markUncompleted` which writes `status:'failed'`):
   - Optimistic: add `today-row--failed` class; close actions panel.
   - `await apply({ type: 'markUncompleted', payload: { habitId, date } })`.
   - On resolve: undo toast "Failed {habitName}".
   - On reject: revert + error toast.

6. Wire the new handlers in the `mountToday` render/re-render flow. After `mount()` builds the DOM, iterate each `.today-row` element and:
   - Attach pointerdown/pointermove/pointerup listeners for swipe detection.
   - Wire `swipeSkip` and `swipeFail` buttons via the existing `actions` map passed to `mount()`, OR wire them directly via querySelectorAll after mount (the `actions` map pattern in `mount.js` already dispatches by `data-action`; add `swipeSkip` and `swipeFail` as entries if the mount helper supports arbitrary data-action → closure mapping — check `js/util/mount.js` to confirm).

**css/today.css — swipe layout + status colours**

Add the following CSS rules (append to the existing file, inside the appropriate `@layer view` block):

- `.today-row` → `position: relative; overflow: hidden;` — clip the slide and the actions panel.
- `.today-row__slide` → `display: flex; align-items: center; transition: transform 0.2s ease; will-change: transform;` — slides left/right during swipe.
- `.today-row__actions` → `position: absolute; right: 0; top: 0; bottom: 0; display: flex; align-items: stretch;` — sits behind the slide. When aria-hidden='true' it's visually invisible (width 0 or opacity 0 via pointer-events:none).
- `.today-row__action-btn` → `display: flex; align-items: center; justify-content: center; padding: 0 1rem; font-size: 0.875rem; font-weight: 600; border: none; cursor: pointer; min-width: 60px;`
- `.today-row__action-btn--skip` → `background: var(--color-skipped-action, #888); color: #fff;`
- `.today-row__action-btn--fail` → `background: var(--color-fail-action, #c0392b); color: #fff;`
- `.today-row--skipped .today-row__slide` → opacity/styling to indicate skipped (e.g. `opacity: 0.55; text-decoration: line-through;` or a muted background — match the existing `.today-row--completed` treatment for visual consistency; use a different muted colour).
- `.today-row--failed .today-row__slide` → e.g. a subtle red tint or strikethrough to differentiate from skipped and pending.
- Custom properties `--color-skipped-action` and `--color-fail-action` should be declared in `css/tokens.css` if token patterns are used there; otherwise inline with fallback values.

No innerHTML anywhere. All DOM wiring via event listeners attached after `mount()`.
  </action>
  <verify>
    <automated>node --test tests/unit/builders.today.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>
    - buildTodayRow accepts {habit, status} and emits today-row--completed/skipped/failed/pending modifier classes.
    - Each row has .today-row__slide wrapping existing content and .today-row__actions with Skip+Fail buttons.
    - Swipe-right triggers markCompleted; swipe-left reveals action buttons.
    - Tapping Skip dispatches markSkipped; tapping Fail dispatches markUncompleted.
    - RENDERERS.binary uses log?.status==='completed'.
    - CSS rules added for swipe layout and status colours.
    - builders.today unit tests pass (may need fixture updates in Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 3: History display + test fixtures update across all affected test files</name>
  <files>
    js/views/history/builders.js,
    tests/unit/mastery.test.js,
    tests/unit/scoring.test.js,
    tests/unit/export.csv.test.js,
    tests/unit/builders.today.test.js,
    tests/unit/builders.history.test.js,
    tests/integration/contract.fake-vs-real.test.js
  </files>
  <action>
Read every file listed in `<files>` before editing. For test files, read the full file to understand every fixture before changing any.

**js/views/history/builders.js — skipped state display**

Find where history row builders render log completion status (look for classes like `history-row--completed`, `history-cell--done`, or similar). Add handling for the `status: 'skipped'` case:
- If `log.status === 'skipped'` → add a CSS class `history-cell--skipped` (or equivalent) and render the label `t('history.skipped')` (if the key exists) or `t('today.skipped')` as a fallback. Do NOT use `.innerHTML`.
- If `log.status === 'failed'` → render with a class `history-cell--failed` (distinct from completed and skipped).
- Preserve existing `status === 'completed'` rendering unchanged.
- Update the JSDoc `@file` comment to mention 4-state log status handling.
- If history builders use `log.completed === true` anywhere, update to `log.status === 'completed'`.
- Add `history.skipped` and `history.failed` keys to `js/i18n/en.js` and `js/i18n/pl.js` if needed by the history display (use the same Polish values as the today variants; add only if the history builder actually renders these labels — check first).

**tests/unit/mastery.test.js — fixture update**

Every test that creates a log fixture with `{ completed: true }` must become `{ status: 'completed' }`. Every `{ completed: false }` must become `{ status: 'failed' }`. The `evaluateMastery` function's `LOG_COMPLETED.binary` now checks `log.status === 'completed'`, so any fixture still using `completed: boolean` will produce wrong results. Make the substitution globally across the file. Do not change test logic or assertions — only the fixture shape.

Add one new describe block: "skipped logs in window do not contribute to applicableDayCount or completedCount" — create a daily habit, add a mix of completed and skipped log fixtures, call `evaluateMastery`, assert that the skipped logs neither count as applicable days nor as completions (if mastery applies the same skipped-exclusion logic — if mastery.js was NOT changed to exclude skipped from the denominator, the test simply asserts that skipped does not count as a completion, which is already guaranteed by the LOG_COMPLETED.binary fix).

**tests/unit/scoring.test.js — fixture update + skipped-exclusion tests**

Every test fixture using `{ completed: true }` → `{ status: 'completed' }`, `{ completed: false }` → `{ status: 'failed' }`.

Add tests for skipped exclusion:
- S1 test: habit with 5 applicable days; 3 completed, 1 failed, 1 skipped. Expected denominator = 4 (skipped excluded), completedCount = 3, s1Score = Math.round(3/4 * 100) = 75.
- S2 test: same setup; verify that the skipped day contributes 0 to both numerator and denominator weight.
- S3 test: same setup; verify skipped day excluded from expectedSum and rawSum.

**tests/unit/export.csv.test.js — fixture update**

Every fixture using `{ completed: true }` → `{ status: 'completed' }`, `{ completed: false }` → `{ status: 'failed' }`.

Add tests for skipped cell value:
- `csvCellValue(binaryHabit, date, [{ habitId, date, status: 'skipped' }], ctx)` → `'x'`.
- `csvCellValue(binaryHabit, date, [{ habitId, date, status: 'failed' }], ctx)` → `'0'`.
- `csvCellValue(binaryHabit, date, [{ habitId, date, status: 'completed' }], ctx)` → `'1'`.

**tests/unit/builders.today.test.js — fixture update + new tests**

Every `{ completed: true }` → `{ status: 'completed' }`, `{ completed: false }` → `{ status: 'failed' }`. Update any assertions that check for `today-row--completed` class to also cover the new modifier classes.

Add tests:
- `buildTodayRow({ habit, status: 'skipped' })` emits an element with class `today-row--skipped`.
- `buildTodayRow({ habit, status: 'failed' })` emits an element with class `today-row--failed`.
- `buildTodayRow({ habit, status: null })` emits no status modifier class.
- Row includes `.today-row__actions` div with two children carrying `data-action='swipeSkip'` and `data-action='swipeFail'`.

**tests/unit/builders.history.test.js — skipped state tests**

Add tests:
- A log with `status: 'skipped'` renders with a class containing 'skipped' (check the actual class name emitted by the history builder).
- A log with `status: 'failed'` renders with a class containing 'failed'.

Update any fixture using `{ completed: true/false }` to `{ status: 'completed'/'failed' }`.

**tests/integration/contract.fake-vs-real.test.js — no fixture change needed**

This test verifies the fake-IDB surface matches repo.js. No log fixtures are in scope. If this file references `completed: boolean` in any log shape assertion, update to `status: string`. Otherwise read and leave unchanged.

**Final verification**

After all edits: run the full test suite. All tests must pass. Fix any remaining failures before considering this task complete.
  </action>
  <verify>
    <automated>node --test tests/ 2>&1 | tail -30</automated>
  </verify>
  <done>
    - All tests in tests/unit/ and tests/integration/ pass with zero failures.
    - History builder renders skipped logs with a distinct CSS class.
    - New skipped-exclusion tests exist in scoring.test.js covering S1/S2/S3.
    - New status→cell tests exist in export.csv.test.js.
    - New status modifier class tests exist in builders.today.test.js.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDB upgrade tx → MIGRATIONS[2] | Log rows are read and rewritten; malformed rows (missing both completed and status) must not crash the migration |
| Today view pointer events | swipe gesture reads clientX/Y from PointerEvent — user-controlled; deltaX threshold prevents accidental triggers |
| CSV export | status field value from IDB is trusted internal data; mapping to 1/0/x is a closed enum check, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-o1g-01 | Tampering | MIGRATIONS[2] cursor update | medium | mitigate | Use shallow copy (`{ ...value, status }`) before `cursor.update`; never mutate the cursor value in place (IDB spec violation). Skip rows that already have a `status` field (idempotent migration). |
| T-o1g-02 | Denial of Service | Swipe gesture loop | low | accept | Fixed thresholds (60px) and `setPointerCapture` prevent ghost inputs; vertical scroll is detected early and released to the browser. |
| T-o1g-03 | Information Disclosure | today.js console.warn | low | accept | No habit content is logged to console (existing convention); only generic error messages. |
</threat_model>

<verification>
Full suite: `node --test tests/` — all tests pass.
App boot: `node scripts/serve.js` — no console errors; IDB opens at v2, migration runs on existing v1 data.
Manual smoke: open Today on mobile viewport; swipe-right marks habit complete; swipe-left reveals Skip and Fail buttons; tapping each writes the correct status to IDB (verify via DevTools Application → IndexedDB → logs store).
CSV export: export contains 'x' for any skipped row and '0' for failed row.
History view: days with skipped logs show a distinct label/style from completed and failed days.
</verification>

<success_criteria>
- `node --test tests/` exits 0 with no failures.
- IDB logs store contains rows with `status: 'completed'|'failed'|'skipped'` — no `completed: boolean` field on new writes.
- DB_VERSION is 2; existing data migrated correctly on first open.
- Swipe-right on a Today row calls markCompleted; swipe-left reveals Skip+Fail action buttons.
- CSV export correctly encodes the three states.
- History view shows skipped state distinctly.
</success_criteria>

<output>
No SUMMARY.md required for quick tasks.
</output>
