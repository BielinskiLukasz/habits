---
phase: quick-260917-n8p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/views/desktop/waveboard.js
  - css/desktop.css
  - tests/unit/views/desktop/waveboard.builders.test.js
autonomous: true
requirements:
  - N8P-waveboard-full-history-scroll

estimate:
  tokens: 10000
  raw_tokens: 10000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - The desktop waveboard heat-map renders one column per ISO week from the earliest week with any score_snapshots data (across all currently loaded habits) through the current week, instead of a fixed last-12-weeks window
    - When the resulting grid is wider than the panel, the grid area scrolls horizontally while the leftmost habit-name column stays visually fixed in place
    - The heat-map still shows S1 status only, regardless of the active scoring model (D-118) — unaffected by this change
  artifacts:
    - js/views/desktop/waveboard.js — exported weeksInRange(startYMD, endYMD) replaces the fixed-window helper; refresh() queries repo.getSnapshotsInRange from a fixed epoch sentinel through today and derives the displayed week range from the earliest row actually returned
    - css/desktop.css — .waveboard-container rule (overflow-x: auto) added to the desktop-scoring layer, replacing the inline style previously set in JS
  key_links:
    - refresh()'s single repo.getSnapshotsInRange(...) call is the sole data source for both cachedWeeks (via weeksInRange) and cachedCellData — widening its start argument is what extends the grid; no second read path exists
    - .waveboard-sticky-column (unchanged, position sticky; left 0) plus the new .waveboard-container overflow-x rule together produce "sticky column, scrolling grid"
---

<objective>
Extend the desktop waveboard heat-map (DESKTOP-04, D-118/D-119/D-120) so its week columns span the full range of `score_snapshots` history instead of a hardcoded last-12-weeks window, and give the grid container proper horizontal scroll via the project's CSS Cascade Layers architecture instead of an inline style — while keeping the sticky habit-name column and the "always S1 status" rule (D-118) exactly as they are today.

Purpose: `writeHabitSnapshots` already writes snapshot rows from each habit's `createdAt` forward (not capped at 70 days except when `createdAt` is missing), so the heat-map's fixed 12-week window is hiding real historical data the app already has.
Output: `js/views/desktop/waveboard.js` renders every week with snapshot data, and `css/desktop.css` owns the grid's horizontal-scroll behavior.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/habits/.planning/PROJECT.md
@C:/my-code/vibe-coding/habits/.planning/STATE.md
@C:/my-code/vibe-coding/habits/js/views/desktop/waveboard.js
@C:/my-code/vibe-coding/habits/js/io/scoreSnapshots.js
@C:/my-code/vibe-coding/habits/css/desktop.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the fixed 12-week window with a full-history week range</name>
  <files>js/views/desktop/waveboard.js, tests/unit/views/desktop/waveboard.builders.test.js</files>
  <behavior>
    - `weeksInRange(day, day)` (start === end) returns exactly `[isoWeekKey(day)]` — a single-day range stays within one ISO week.
    - `weeksInRange('2025-12-01', '2026-01-20')` (a ~7-week span crossing the 2025→2026 year boundary) returns an array whose first entry equals `isoWeekKey('2025-12-01')`, whose last entry equals `isoWeekKey('2026-01-20')`, has at least 6 entries, and contains no duplicate week keys (`new Set(out).size === out.length`).
  </behavior>
  <action>
    In js/views/desktop/waveboard.js, add a module-level constant `SNAPSHOT_EPOCH = '0000-01-01'` near the top of the file (matching the "earliest possible date" sentinel convention already used for score_snapshots range queries in js/db/repo.js's getLatestSnapshot and js/io/seed.js's effectiveFrom fallback).

    Remove the fixed-window week-builder function and its JSDoc block, and add an exported function with this signature: `weeksInRange(startYMD, endYMD)`. Implementation: set a cursor to `weekMonday(isoWeekKey(startYMD))`, compute `endMonday = weekMonday(isoWeekKey(endYMD))`, then loop while the cursor is `<=` endMonday: push `isoWeekKey(cursor)` onto a results array, then advance the cursor 7 calendar days (split the cursor into y/m/d, construct `new Date(y, m-1, d+7)`, reformat via `formatLocalYMD`). Return the accumulated array once the cursor exceeds endMonday. Each step moves exactly one calendar week forward from a Monday, so the result is always chronological with no possible duplicate entries.

    In `refresh()`, replace the current fixed-window computation (fixed count of weeks ending at today, with the range start derived from that fixed window) with: call `repo.getSnapshotsInRange(SNAPSHOT_EPOCH, today)` once, inside the existing try/catch that already tolerates a missing/empty score_snapshots store. After that call resolves, scan the returned `snapshotRows` for the minimum `row.date`; if at least one row exists, that minimum date becomes the range's start, otherwise fall back to `today` itself (so an install with zero snapshots still renders exactly the current week, matching today's no-data behavior). Then set `cachedWeeks = weeksInRange(earliestDate, today)`. Leave the existing cellData-building loop (the `for (const row of snapshotRows)` block that populates `cachedCellData` and computes worst-status-per-week) completely unchanged — it already filters by `cachedWeeks.includes(week)` and works for any week-array length, so widening the range is sufficient; do not touch its status/coloring logic.

    Update the two JSDoc blocks that describe the old fixed window: the file header's "Grid structure" bullet about week columns (currently describes "12 default") and `mountWaveboard`'s "Data fetch sequence" numbered list (currently describes the query happening over a fixed window, with the weeks array built afterward from a fixed count). Rewrite both to describe the full-history behavior — the snapshot query now runs first over the full existing range, and the weeks array is derived from its result — and reorder the "Data fetch sequence" steps accordingly (query score_snapshots before deriving the weeks array, since weeks now depend on the query's returned data). Leave every mention of D-118 (wave-board always shows S1 status; scoringModel has no effect) exactly as worded — this change does not touch scoring or status logic, only which weeks are displayed.
  </action>
  <verify>
    <automated>grep -q "export function weeksInRange" js/views/desktop/waveboard.js && node --test tests/unit/views/desktop/waveboard.builders.test.js</automated>
  </verify>
  <done>
    weeksInRange is exported from waveboard.js and covered by the two new unit tests (single-day range, year-boundary multi-week range with no duplicates). refresh() derives cachedWeeks from the earliest date present in a full-range repo.getSnapshotsInRange query rather than a fixed count of weeks. All tests in tests/unit/views/desktop/waveboard.builders.test.js pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move grid horizontal scroll into the CSS Cascade Layers architecture</name>
  <files>css/desktop.css, js/views/desktop/waveboard.js</files>
  <action>
    In css/desktop.css, inside the existing `@layer desktop-scoring { ... }` block, add a new rule right next to `.waveboard-grid` / `.waveboard-sticky-column`:

    `.waveboard-container { overflow-x: auto; }`

    In js/views/desktop/waveboard.js, find where the scrollable container div is created in mountWaveboard (it currently sets its class to "waveboard-container" and then separately sets a raw inline style string for the scroll behavior). Remove the inline-style call entirely — the container should end up with only its class attribute set (`waveboard-container`), letting the new css/desktop.css rule supply the horizontal-scroll behavior instead of an inline style, consistent with how every other visual rule for this view (`.waveboard-grid`, `.waveboard-sticky-column`, `.waveboard-cell*`) is already expressed as a CSS class in that same layer. Do not change `.waveboard-sticky-column` (still `position: sticky; left: 0`) or any cell-coloring rule.
  </action>
  <verify>
    <automated>grep -q "waveboard-container" css/desktop.css && grep -q "overflow-x: auto" css/desktop.css && node --test tests/</automated>
  </verify>
  <done>
    css/desktop.css defines `.waveboard-container { overflow-x: auto; }` inside the desktop-scoring layer. waveboard.js's container element carries only the class attribute (no inline style). Full test suite (`node --test tests/`) passes with no new failures.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| none crossed | Widens an existing IndexedDB read-range query and moves an inline style to a CSS class; no new user input, no new network/IDB write path, no DOM injection API introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-n8p-01 | Tampering | js/views/desktop/waveboard.js refresh() | low | accept | Query bounds (`SNAPSHOT_EPOCH`, `today`) are fixed local constants, not user-controlled input; reuses the existing indexed repo.getSnapshotsInRange facade (no raw IDB access, D-anti-pattern-1 preserved) |
| T-n8p-02 | Denial of Service | js/views/desktop/waveboard.js refresh() | low | accept | Full-history query is bounded by the same data scale NFR-03 already budgets for (<2s for 5 years x 65 habits, exercised today by rebuildAllSnapshots); this view performs one such read, not a rebuild |
| T-n8p-03 | Information Disclosure | css/desktop.css / rendered DOM | low | accept | Single-user, fully local app; the wider grid only surfaces more weeks of data the user's own habits already store in score_snapshots, nothing new is computed or exposed |
</threat_model>

<verification>
After both tasks:
- `node --test tests/` passes with no new failures
- `js/views/desktop/waveboard.js` exports `weeksInRange` and no longer sets an inline style on the waveboard container
- `css/desktop.css` defines `.waveboard-container { overflow-x: auto; }` in the desktop-scoring layer
- Opening desktop.html → #waveboard with more than 12 weeks of score_snapshots history shows all of those weeks as columns, scrollable horizontally, with the habit-name column staying fixed on the left
</verification>

<success_criteria>
The waveboard heat-map's week-column range is derived from the earliest score_snapshots date through today (not a fixed 12-week count); the grid container's horizontal scroll is a CSS rule in css/desktop.css's desktop-scoring layer rather than an inline style; the sticky habit-name column and D-118's always-S1 rule are unchanged; all existing and new tests pass.
</success_criteria>

<output>
Create `.planning/quick/260917-n8p-extend-the-desktop-waveboard-heat-map-be/260917-n8p-SUMMARY.md` when done.
</output>
