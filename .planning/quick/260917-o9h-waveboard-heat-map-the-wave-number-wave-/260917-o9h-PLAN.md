---
phase: quick-260917-o9h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - css/desktop.css
  - tests/unit/views/desktop/waveboard.css.test.js
autonomous: true
requirements:
  - O9H-waveboard-wave-header-sticky

estimate:
  tokens: 6000
  raw_tokens: 6000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - The waveboard's wave-number/wave-name label stays visible pinned at the left edge of the heat-map while the grid is scrolled horizontally, matching how the sticky habit-name column already behaves
    - The plain (non-scrolling) desktop analytics table's wave-header row (js/views/desktop/analytics.js, class analytics-table) is visually unaffected — it keeps using the shared, non-sticky .analytics-wave-header td rule
  artifacts:
    - "css/desktop.css — new `.waveboard-grid .analytics-wave-header td` rule (position: sticky; left: 0; z-index: 1) scoped under .waveboard-grid, layered on top of (not replacing) the existing unscoped `.analytics-wave-header td` rule"
    - tests/unit/views/desktop/waveboard.css.test.js — regression test asserting the scoped rule is sticky-pinned and the bare/shared rule used by the plain analytics table remains non-sticky
  key_links:
    - "`.waveboard-grid .analytics-wave-header td`'s higher-specificity selector adds position/left/z-index on top of the existing `.analytics-wave-header td { background; font-weight; font-size }` declarations — both rules' declarations apply together only inside markup rendered under a `.waveboard-grid` table (js/views/desktop/waveboard.js's buildWaveboardRows), never inside the plain `.analytics-table` (js/views/desktop/analytics.js)"
    - "The wave-header `<td>` already left-aligns its label by default td behavior (js/views/desktop/waveboard.js buildWaveboardRows sets no text-align), so `left: 0` pins the label exactly where it already visually sits — no JS/markup change needed"
---

<objective>
Pin the waveboard heat-map's wave-number/wave-name label to the left edge of `.waveboard-container` during horizontal scroll, by scoping a `position: sticky; left: 0` rule to the wave-header `<td>` only inside `.waveboard-grid` — mirroring the existing `.waveboard-sticky-column` pattern — without touching the plain desktop analytics table, which reuses the same `analytics-wave-header` class but never scrolls horizontally.

Purpose: quick task 260917-n8p made `.waveboard-container` scroll horizontally so the grid could show full history; the wave-header row's single full-width `colspan` `<td>` (and its left-aligned label) now scrolls out of view along with the week columns instead of staying pinned like the habit-name column does.
Output: `css/desktop.css` gains one new scoped rule; a new regression test locks the scoping boundary in place.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/habits/.planning/PROJECT.md
@C:/my-code/vibe-coding/habits/.planning/STATE.md
@C:/my-code/vibe-coding/habits/css/desktop.css
@C:/my-code/vibe-coding/habits/js/views/desktop/waveboard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scope a sticky rule to the waveboard's wave-header label</name>
  <files>css/desktop.css, tests/unit/views/desktop/waveboard.css.test.js</files>
  <action>
    First create the regression test at tests/unit/views/desktop/waveboard.css.test.js (Pattern S6 source-text parsing, matching tests/integration/sw.shell.test.js's approach of regex-parsing a source file instead of a live CSSOM/DOM check, since this project has no CSS test harness). The test reads css/desktop.css via readFileSync and has two `test()` cases inside one `describe()`:
      1. Match `/\.waveboard-grid\s+\.analytics-wave-header\s+td\s*\{([^}]*)\}/` against the file contents, assert.ok the match exists, then assert.match the captured group against `/position:\s*sticky/` and against `/left:\s*0/`.
      2. Match `/^[ \t]*\.analytics-wave-header td\s*\{([^}]*)\}/m` (anchored so it only matches the selector when nothing but leading whitespace precedes `.analytics-wave-header td` on its line — this excludes the scoped `.waveboard-grid .analytics-wave-header td` selector, which has `.waveboard-grid ` before it on the same line) against the file contents, assert.ok the match exists, then assert.doesNotMatch the captured group against `/position:\s*sticky/` — this is the rule the plain (non-scrolling) analytics table in js/views/desktop/analytics.js relies on, and it must stay un-pinned.
    Run `node --test tests/unit/views/desktop/waveboard.css.test.js` now and confirm both assertions fail (RED) — the scoped rule does not exist yet.

    Then, in css/desktop.css, inside the existing `@layer desktop-scoring { ... }` block, immediately after the `.analytics-wave-header td { ... }` rule (currently: background: var(--color-surface-raised); font-weight: 600; font-size: var(--text-md);), add a new rule:

    `.waveboard-grid .analytics-wave-header td { position: sticky; left: 0; z-index: 1; }`

    Precede it with a one-line comment noting this scopes sticky-pinning to the wave-board only (mirrors `.waveboard-sticky-column`'s `position: sticky; left: 0`), leaving the plain analytics table's un-scoped `.analytics-wave-header td` rule above untouched. Do not modify the existing `.analytics-wave-header td` rule's declarations, do not touch `.analytics-row--archived` or any `.analytics-table*` rule, and do not touch js/views/desktop/waveboard.js or js/views/desktop/analytics.js — this is a CSS-only fix; the wave-header `<td>`'s markup, colspan, and text already position the label at the cell's left edge by default `<td>` alignment.
  </action>
  <verify>
    <automated>grep -q '\.waveboard-grid \.analytics-wave-header td' css/desktop.css && node --test tests/unit/views/desktop/waveboard.css.test.js</automated>
  </verify>
  <done>
    css/desktop.css defines `.waveboard-grid .analytics-wave-header td { position: sticky; left: 0; z-index: 1; }` immediately after the existing unscoped `.analytics-wave-header td` rule, inside the desktop-scoring layer. tests/unit/views/desktop/waveboard.css.test.js passes both cases: the scoped rule is sticky-pinned, and the bare `.analytics-wave-header td` rule (used by the plain analytics table) stays non-sticky. No .js files were modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Pure CSS selector addition plus a source-text-parsing regression test; no user input, no new DOM APIs, no data flow change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-o9h-01 | Tampering | css/desktop.css | low | accept | Static CSS rule addition only; no user-controlled input reaches this file; the new regression test locks the scoping boundary so a future edit cannot silently widen the sticky rule onto the plain analytics table |
</threat_model>

<verification>
After the task:
- `node --test tests/` passes with no new failures
- Opening desktop.html → #waveboard with a heat-map wider than the panel: scrolling right keeps the "Wave N — {name}" label pinned at the left edge while the week columns scroll underneath
- Opening desktop.html → #analytics (the plain, non-scrolling table) is visually unchanged
</verification>

<success_criteria>
The waveboard's wave-header label stays pinned to the left during horizontal scroll via a `.waveboard-grid`-scoped sticky rule; the plain analytics table's shared `.analytics-wave-header td` rule is untouched and non-sticky; the new regression test and full suite pass.
</success_criteria>

<output>
Create `.planning/quick/260917-o9h-waveboard-heat-map-the-wave-number-wave-/260917-o9h-SUMMARY.md` when done.
</output>
