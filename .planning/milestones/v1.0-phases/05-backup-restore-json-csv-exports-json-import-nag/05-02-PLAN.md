---
phase: 05-backup-restore-json-csv-exports-json-import-nag
plan: 02
type: tdd
wave: 2
depends_on: ["05-01"]
files_modified:
  - js/io/export.js
  - tests/unit/export.json.test.js
autonomous: true
requirements: [EXPORT-01, EXPORT-02]
user_setup: []

must_haves:
  truths:
    - "JSON export contains all 7 IDB stores as arrays under keys: habits, logs, habit_versions, events, settings, meta, score_snapshots"
    - "JSON export embeds schemaVersion field matching current DB_VERSION"
    - "JSON export is valid JSON and round-trips through JSON.parse"
  artifacts:
    - path: "js/io/export.js"
      provides: "exportJSON function"
      exports: ["exportJSON", "configureExport"]
    - path: "tests/unit/export.json.test.js"
      provides: "JSON export test suite"
      contains: "exportJSON"
  key_links:
    - from: "js/io/export.js"
      to: "js/db/repo.js"
      via: "repo.getAll*()"
      pattern: "import.*repo|_repo"
---

<objective>
Implement JSON export: a full-fidelity snapshot of all 7 IDB stores wrapped in `{schemaVersion, habits: [...], logs: [...], ...}` structure. Lock in the DI pattern (matching `js/io/seed.js`) with `configureExport({repo})` for testability.

Purpose: EXPORT-01 (full-fidelity JSON) and EXPORT-02 (schemaVersion embedding) require serializing all store data at runtime. TDD ensures the schema is correct and survives round-trip JSON serialization.

Output: `js/io/export.js` extended with `exportJSON()` function; test suite verifying all stores are exported and schemaVersion is embedded.

**Dependencies:** Depends on Plan 05-01 (CSV cell encoding module creation). Plan 02 builds on the csv cell functions by adding JSON export functions to the same `js/io/export.js` module.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-CONTEXT.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md
@.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md

@js/db/schema.js
@js/db/repo.js
@js/io/seed.js
</context>

<tasks>

<task type="tdd">
  <name>Task 1: JSON export serialization (RED→GREEN→REFACTOR)</name>
  <files>
    tests/unit/export.json.test.js
    js/io/export.js
  </files>
  <read_first>
    @js/db/schema.js
    @js/db/repo.js
    @js/io/seed.js
    @.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-39: 7 stores)
  </read_first>
  <behavior>
    - Test 1: exportJSON() with fake repo containing 2 habits → returns JSON string with habits array of length 2
    - Test 2: JSON embeds schemaVersion field at top level (value matches DB_VERSION from schema.js)
    - Test 3: JSON contains all 7 store arrays: habits, logs, habit_versions, events, settings, meta, score_snapshots
    - Test 4: JSON.parse(exportJSON()) produces an object with schemaVersion + 7 store keys (no corruption)
    - Test 5: Exported habits preserve all fields (id, name, wave, cadence, stages, archived, etc.)
    - Test 6: Exported logs preserve [habitId, date] compound key structure
    - Test 7: Empty stores (e.g., no score_snapshots yet) export as empty arrays, not undefined
    - Test 8: configureExport({repo}) allows DI injection for testing
  </behavior>
  <action>
    Create `tests/unit/export.json.test.js` following Node `--test` pattern. Write failing tests for:
    - Full-store export: fake repo with seed data returns JSON with all 7 stores
    - schemaVersion embedding: JSON.parse(output).schemaVersion matches DB_VERSION
    - Empty stores handled gracefully (score_snapshots will be empty in P5, populated in P6)
    - Round-trip: JSON.stringify and JSON.parse preserve all data
    - configureExport(deps) injection: module accepts {repo: fakeRepo}

    Then extend `js/io/export.js` to add:
    - `configureExport({repo})` — DI hook (mirrors seed.js pattern); defaults to null
    - `exportJSON()` — reads all 7 stores via repo methods and returns JSON string: `JSON.stringify({schemaVersion: DB_VERSION, habits: [...], logs: [...], habit_versions: [...], events: [...], settings: [...], meta: [...], score_snapshots: [...]})`. The order of keys in the object doesn't matter for semantics, but alphabetical is a reasonable choice for determinism.
    - Import schema.js to read DB_VERSION constant
    - Assume repo has methods: `getAllHabits()`, `getAllLogs()`, `getAllHabitVersions()`, `getAllEvents()`, `getAllSettings()`, `getAllMeta()`, `getAllScoreSnapshots()` OR a generic `getAll(storeName)` method that works for all stores. If these methods don't exist, add them to repo (or use a transactional read pattern matching the existing `repo.runTx()` shape).

    Commits (TDD gate):
    1. `test(05-02): add failing JSON export tests` — RED
    2. `feat(05-02): implement exportJSON and configureExport` — GREEN
    3. `refactor(05-02): consolidate store iteration` — (if needed; else skip)
  </action>
  <verify>
    <automated>node --test tests/unit/export.json.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/export.json.test.js` contains ≥8 test cases
    - `js/io/export.js` exports `exportJSON()` and `configureExport(deps)`
    - All tests pass (green)
    - exportJSON() returns a valid JSON string containing all 7 store arrays + schemaVersion
    - schemaVersion matches DB_VERSION from schema.js
    - Empty stores export as empty arrays (not undefined or null)
    - JSON round-trips: JSON.parse(exportJSON()) produces an object with all stores intact
  </acceptance_criteria>
  <done>
    JSON export logic complete. `exportJSON()` is ready for file-download integration (Plan 05). All 7 IDB stores are properly serialized and schemaVersion is embedded per EXPORT-01 and EXPORT-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON serialization | IDB data (habits, logs, etc.) → JSON text string |
| Schema embedding | DB_VERSION constant → JSON top-level field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | Exported JSON contains all user data | accept | User explicitly requests export; JSON is stored locally and uploaded to user's own device |
| T-05-04 | Tampering | JSON.stringify safety | mitigate | JSON.stringify is a built-in safe serializer; no .toJSON() overrides or custom reviver logic needed in P5 |

</threat_model>

<verification>
Unit test suite for JSON export: `node --test tests/unit/export.json.test.js` must pass (8+ cases).
</verification>

<success_criteria>
- JSON export contains all 7 IDB stores as top-level arrays
- schemaVersion is correctly embedded
- JSON is valid and round-trips through parse
</success_criteria>

<output>
Create `.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-02-SUMMARY.md` when done
</output>
