---
phase: 05-backup-restore-json-csv-exports-json-import-nag
plan: 06
type: execute
wave: 4
depends_on: ["05-04", "05-05"]
files_modified:
  - js/views/settings.js
  - js/views/settings/builders.js
  - css/settings.css
  - tests/integration/settings.backup-nag.test.js
autonomous: true
requirements: [EXPORT-08]
user_setup: []

must_haves:
  truths:
    - "Nag banner displays when last backup >= 7 days ago and not dismissed"
    - "Nag banner disappears when dismissed via [×] button"
    - "Nag banner reappears after 7 days of dismissal"
    - "Nag banner disappears immediately after export (lastBackupDate reset)"
    - "Nag dismissal state persists across page reload"
  artifacts:
    - path: "js/views/settings.js"
      provides: "Settings view with nag visibility logic"
      exports: ["mountSettings"]
    - path: "js/views/settings/builders.js"
      provides: "Data card builder with nag banner"
      exports: ["buildDataCard"]
    - path: "css/settings.css"
      provides: "Nag banner styling (warning color, dismiss button)"
      contains: ".nag-banner"
    - path: "tests/integration/settings.backup-nag.test.js"
      provides: "Nag visibility and dismissal integration tests"
      contains: "shouldShowNag, dismissNag"
  key_links:
    - from: "js/views/settings/builders.js"
      to: "js/io/backup-nag.js"
      via: "buildDataCard receives lastBackupDays, shouldShowNag"
      pattern: "buildDataCard.*lastBackupDays"
    - from: "js/views/settings.js"
      to: "js/io/backup-nag.js"
      via: "daysSinceLastBackup, shouldShowNag, dismissNag"
      pattern: "daysSinceLastBackup|shouldShowNag|dismissNag"
---

<objective>
Implement the backup nag banner UI: conditionally visible warning when last backup is ≥7 days old, with dismissible [×] button, reappearance after 7 days, and immediate disappearance after export. Wire dismissal logic to localStorage. Add CSS styling for warning visual treatment.

Purpose: EXPORT-08 (nag appears weekly) completes the backup reminder surface. The nag encourages users to maintain recent backups without being obtrusive.

Output: Settings Data card extended with conditional nag banner. Integration tests verify visibility logic and dismissal-reappearance. CSS styles the banner with warning color and dismiss button.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-CONTEXT.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md (D-65, D-72: Settings patterns)

@js/views/settings/builders.js
@js/views/settings.js
@js/io/backup-nag.js
@css/settings.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Data card builder with nag banner</name>
  <files>
    js/views/settings/builders.js
  </files>
  <read_first>
    @js/views/settings/builders.js
    @.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md (Pattern 4: buildDataCard with nag banner)
    @.planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md (D-76: pure builder pattern)
  </read_first>
  <action>
    Update `buildDataCard({lastBackupDays, shouldShowNag})` in `js/views/settings/builders.js`:
    - Add nag banner section at the top of the card (below h2, above "Last backup" row)
    - Banner should only appear when `shouldShowNag === true`
    - Banner structure: `<div class="nag-banner">` containing:
      - Span with message text: `"Last backup: {lastBackupDays} days ago. Export your data now."` (exact wording per D-102; can be tuned in UI-spec if needed)
      - Dismiss button: `<button data-action="dismissNag" title="Dismiss"` with text `"×"` or `"Dismiss"`
    - Use the description tree pattern ({tag, attrs, children}) per D-76
    - Conditional rendering: `if (shouldShowNag) { body.push({tag: 'div', attrs: {class: 'nag-banner'}, children: [...]}) }`
    - Include `aria-label` or role attributes for accessibility (e.g., `role="status"` + `aria-live="polite"`)
  </action>
  <verify>
    <automated>buildDataCard with shouldShowNag=true includes nag-banner div; with shouldShowNag=false excludes it (unit test of builder)</automated>
  </verify>
  <acceptance_criteria>
    - `buildDataCard()` includes nag banner only when `shouldShowNag === true`
    - Banner message includes days since last backup
    - Banner has dismiss button with `data-action="dismissNag"`
    - Builder is pure (no side effects)
  </acceptance_criteria>
  <done>
    Data card builder extended with conditional nag banner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire nag logic in settings.js mount and action handlers</name>
  <files>
    js/views/settings.js
  </files>
  <read_first>
    @js/views/settings.js
    @js/io/backup-nag.js (from Plan 04)
    @js/state/store.js (from Phase 3)
    @.planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md (D-72: store.subscribe for live refresh)
  </read_first>
  <action>
    Update `js/views/settings.js` mount logic:
    - On `mountSettings(parent, {repo, store})`:
      1. Compute `lastBackupDays = await daysSinceLastBackup()` and `shouldShowNag = await shouldShowNag()`
      2. Render Data card with these values
      3. Subscribe to `store.notify()` so when `lastBackupDate` is updated (after export), re-compute and re-render
      4. On unmount, unsubscribe
    - Add `dismissNag` action handler:
      - When button clicked, call `dismissNag()` (writes to localStorage)
      - Re-compute `shouldShowNag` (will now be false)
      - Re-render Data card to hide nag
    - After export (in `handleExportJSON` and `handleExportCSV` from Plan 05):
      - After `setSetting` completes and store.notify fires
      - The subscription in mountSettings re-renders with new `lastBackupDays = 0` and `shouldShowNag = false`
      - Nag disappears automatically (dismissal state is reset because days < 7)
    - Per Pitfall 5: When Reset-data is triggered, clear `localStorage.removeItem('nag:lastDismissed')` so the nag resets along with the backup

    Note: The live-refresh pattern (store.subscribe) is already established in Phase 3 D-72 (Data card live refresh on cross-tab settings changes). This plan reuses the same pattern for nag visibility.
  </action>
  <verify>
    <automated>Settings mounts and computes nag visibility; dismissNag action hides banner; export makes days=0 and hides banner (verified via integration test in Task 3)</automated>
  </verify>
  <acceptance_criteria>
    - On Settings mount, `daysSinceLastBackup` and `shouldShowNag` are computed asynchronously
    - Data card rendered with correct values
    - `dismissNag` action handler clears nag from UI
    - `shouldShowNag` re-computed after store.notify (export or other settings changes)
    - Reset-data clears `nag:lastDismissed` from localStorage
    - Nag reappears after 7 days of dismissal (verified via integration test with time-mock)
  </acceptance_criteria>
  <done>
    Nag logic wired in Settings mount and action handlers. Live refresh and dismissal complete.
  </done>
</task>

<task type="auto">
  <name>Task 3: Style nag banner and add integration tests</name>
  <files>
    css/settings.css
    tests/integration/settings.backup-nag.test.js
  </files>
  <read_first>
    @css/settings.css
    @.planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md (CSS conventions)
    @.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md (Pitfall 5: localStorage reset on Reset-data)
  </read_first>
  <action>
    Update `css/settings.css`:
    - Add `.nag-banner` class with warning-color background (e.g., `background-color: var(--color-warning, #fff3cd)`)
    - Add padding, border, and rounded corners for visual hierarchy
    - Style `.nag-banner button` (dismiss) to be small, right-aligned, with clear hover state
    - Ensure text is readable in warning color (good contrast)
    - Optional: icon or emoji for warning (⚠️ or 🔔)

    Create `tests/integration/settings.backup-nag.test.js`:
    - Test: Settings mount with no lastBackupDate → nag not shown
    - Test: Settings mount with lastBackupDate 7+ days ago, not dismissed → nag shown
    - Test: Settings mount with lastBackupDate 7+ days ago, dismissed < 7 days ago → nag not shown
    - Test: Click dismissNag button → nag hidden, localStorage updated
    - Test: After 7 days pass, nag reappears (mock time advancement)
    - Test: After export, lastBackupDate = today, nag hidden (days = 0)
    - Test: Reset-data clears `nag:lastDismissed` from localStorage
    - Test: Nag persists across reload (localStorage state survives)

    Integration tests should use fake-time mocking to simulate day passages (e.g., mock `new Date()` or use a test-injectable date function).
  </action>
  <verify>
    <automated>node --test tests/integration/settings.backup-nag.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `.nag-banner` CSS class styled with warning colors
    - Dismiss button is clearly visible and clickable
    - Integration tests verify nag visibility logic (7-day threshold, dismissal, reappearance)
    - Tests verify localStorage persistence
    - Tests verify Reset-data clears dismissal state
    - All tests pass (green)
  </acceptance_criteria>
  <done>
    Nag banner styled and integration tests complete. EXPORT-08 (nag appears weekly) fully implemented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dismissal state | localStorage (user preference) |
| Last backup date | IDB settings store (canonical data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Denial of Service | Nag reappearance logic | accept | Nag appears every 7 days; user can dismiss (acceptable UX for safety reminder) |
| T-05-15 | Information Disclosure | Dismissal state visible in localStorage | accept | User's own UI preference; no sensitive data |

</threat_model>

<verification>
Integration tests: `node --test tests/integration/settings.backup-nag.test.js` must pass. Manual verification: Settings view with nag visible, click dismiss, verify disappears and localStorage updated. Wait 7 days (or mock time), verify nag reappears. Export, verify nag disappears. Reset data, verify nag state clears.
</verification>

<success_criteria>
- Nag banner displays conditionally based on 7-day threshold
- Dismissal state persists in localStorage
- Nag reappears after 7 days of dismissal
- Nag disappears after export
- Reset-data clears dismissal state
- Integration tests cover all nag scenarios
</success_criteria>

<output>
Create `.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-06-SUMMARY.md` when done
</output>
