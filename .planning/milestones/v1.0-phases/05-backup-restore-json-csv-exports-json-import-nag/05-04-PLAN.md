---
phase: 05-backup-restore-json-csv-exports-json-import-nag
plan: 04
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/io/backup-nag.js
  - tests/unit/backup-nag.test.js
autonomous: true
requirements: [EXPORT-08]
user_setup: []

must_haves:
  truths:
    - "Days since last backup is calculated correctly from lastBackupDate setting"
    - "Nag should show when >= 7 days since last backup AND not dismissed in last 7 days"
    - "Dismissal state persists in localStorage and is checked on subsequent mounts"
    - "Days since 0 when export happens same day"
  artifacts:
    - path: "js/io/backup-nag.js"
      provides: "Backup nag calculation functions"
      exports: ["daysSinceLastBackup", "shouldShowNag", "dismissNag"]
    - path: "tests/unit/backup-nag.test.js"
      provides: "Backup nag test suite"
      contains: "daysSinceLastBackup, shouldShowNag"
  key_links:
    - from: "js/io/backup-nag.js"
      to: "js/db/repo.js"
      via: "repo.getSetting('lastBackupDate')"
      pattern: "import.*repo|_repo"
---

<objective>
Implement the "last backup: N days ago" calculation and "show nag?" logic that determines whether to display a weekly dismissible warning when the last backup is ≥7 days old. Lock in the dismissal state storage (localStorage) and the time-based reappearance logic.

Purpose: EXPORT-08 (nag and "Last backup" display) requires computing days elapsed and checking dismissal state. TDD ensures the date arithmetic is correct and dismissal logic is sound.

Output: `js/io/backup-nag.js` module with `daysSinceLastBackup()`, `shouldShowNag()`, and `dismissNag()` functions; test suite covering all time-based edge cases.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-CONTEXT.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md
@.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md

@js/db/repo.js
@js/util/date.js
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Backup nag calculation and dismissal (RED→GREEN→REFACTOR)</name>
  <files>
    tests/unit/backup-nag.test.js
    js/io/backup-nag.js
  </files>
  <read_first>
    @js/db/repo.js
    @js/util/date.js
    @.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md (Pattern 4, D-101, D-102, Pitfall 5)
    @.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (date key format YYYY-MM-DD)
  </read_first>
  <behavior>
    - Test 1: daysSinceLastBackup() returns null when no lastBackupDate setting exists
    - Test 2: daysSinceLastBackup() returns 0 when lastBackupDate is today
    - Test 3: daysSinceLastBackup() returns 5 when lastBackupDate is 5 days ago
    - Test 4: daysSinceLastBackup() returns 7 when lastBackupDate is exactly 7 days ago
    - Test 5: shouldShowNag() returns false when daysSinceLastBackup is null (never exported)
    - Test 6: shouldShowNag() returns false when daysSinceLastBackup < 7
    - Test 7: shouldShowNag() returns true when daysSinceLastBackup >= 7 AND not dismissed
    - Test 8: shouldShowNag() returns false when daysSinceLastBackup >= 7 BUT dismissed ≤7 days ago
    - Test 9: shouldShowNag() returns true when dismissal was >7 days ago (reappears)
    - Test 10: dismissNag() writes nag:lastDismissed to localStorage as YYYY-MM-DD
    - Test 11: dismissNag() followed by shouldShowNag() returns false
  </behavior>
  <action>
    Create `tests/unit/backup-nag.test.js` with failing tests for:
    - Time arithmetic: daysSinceLastBackup correctly computes days between two YYYY-MM-DD dates (use Math.round, not floor, per D-101 / phase 3 notes — DST edge case)
    - Nag visibility: shouldShowNag returns true only when both conditions met: days >= 7 AND (no dismissal OR dismissal > 7 days ago)
    - Dismissal persistence: dismissNag writes to localStorage; subsequent shouldShowNag checks it
    - Edge cases: zero days (same day), exactly 7 days, 8 days, dismissal exactly 7 days old (should reappear)
    - Null handling: when no lastBackupDate, should return null and shouldShowNag returns false

    Then create `js/io/backup-nag.js` with:
    - `configureBackupNag({repo})` — DI hook for testing (optional, can be called first to inject mock repo)
    - `daysSinceLastBackup()` — calls `repo.getSetting('lastBackupDate')` (or reads the setting's `.value` field per phase 3 Settings pattern). If the setting doesn't exist or value is null, return null. Otherwise, parse the YYYY-MM-DD string and calculate `Math.round((new Date(today) - new Date(lastBackupDate)) / 86400000)`. Return max(0, days) to avoid negative values.
    - `shouldShowNag()` — compute `days = await daysSinceLastBackup()`. If days is null or < 7, return false. Otherwise, read `localStorage.getItem('nag:lastDismissed')` (YYYY-MM-DD string). If no dismissal record, return true. If dismissal exists, compute days since dismissal and return true only if >= 7 days since dismissal (nag reappears).
    - `dismissNag()` — write today's YYYY-MM-DD to `localStorage.setItem('nag:lastDismissed', today)`.
    - All date formatting uses `new Date().toLocaleDateString('sv-SE')` (ISO 8601 format) matching phase 2 conventions per D-06.

    Commits (TDD gate):
    1. `test(05-04): add failing backup nag calculation tests` — RED
    2. `feat(05-04): implement daysSinceLastBackup, shouldShowNag, dismissNag` — GREEN
    3. `refactor(05-04): consolidate date parsing` — (if needed; else skip)
  </action>
  <verify>
    <automated>node --test tests/unit/backup-nag.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/backup-nag.test.js` contains ≥11 test cases
    - `js/io/backup-nag.js` exports `daysSinceLastBackup()`, `shouldShowNag()`, `dismissNag()`, and `configureBackupNag(deps)`
    - All tests pass (green)
    - daysSinceLastBackup correctly computes elapsed days (uses Math.round, not floor)
    - shouldShowNag correctly implements 7-day threshold and dismissal-reappearance logic
    - dismissNag writes to localStorage with YYYY-MM-DD format
    - Date arithmetic handles DST transitions correctly (per phase 3 decision)
  </acceptance_criteria>
  <done>
    Backup nag calculation complete. `daysSinceLastBackup()`, `shouldShowNag()`, and `dismissNag()` are ready for Settings UI integration (Plan 06). EXPORT-08 calculation logic is done.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backup nag state | localStorage (dismissal state) vs. IDB (lastBackupDate) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Denial of Service | localStorage quota | accept | Nag dismissal is tiny (8 bytes YYYY-MM-DD); no DOS risk |
| T-05-09 | Information Disclosure | lastBackupDate visible in Settings | accept | User's own data; shows when last user exported |

</threat_model>

<verification>
Unit test suite for backup nag: `node --test tests/unit/backup-nag.test.js` must pass (11+ cases).
</verification>

<success_criteria>
- Days since last backup calculated correctly
- Nag visibility logic respects 7-day threshold and dismissal state
- Dismissal persists in localStorage
- Nag reappears after 7 days of dismissal
</success_criteria>

<output>
Create `.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-04-SUMMARY.md` when done
</output>
