---
phase: 05-backup-restore-json-csv-exports-json-import-nag
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/io/import.js
  - tests/unit/import.test.js
autonomous: true
requirements: [IMPORT-01, IMPORT-02, IMPORT-03]
user_setup: []

must_haves:
  truths:
    - "Import merges stores by primary key (overwrite on collision, never delete local-only records)"
    - "Import rejects files with schemaVersion > current app version with clear error"
    - "Import accepts files with schemaVersion ≤ current (backward compatible)"
  artifacts:
    - path: "js/io/import.js"
      provides: "mergeImportedStores function"
      exports: ["mergeImportedStores", "configureImport"]
    - path: "tests/unit/import.test.js"
      provides: "JSON import merge and schema validation tests"
      contains: "mergeImportedStores"
  key_links:
    - from: "js/io/import.js"
      to: "js/db/repo.js"
      via: "repo.runTx()"
      pattern: "repo.runTx|_repo"
---

<objective>
Implement JSON import with merge-by-id semantics: overwrite-on-collision, never delete local-only records. Lock in schema version validation that rejects imports from newer app versions. Use `repo.runTx()` for atomic multi-store consistency.

Purpose: IMPORT-01 (user can upload JSON), IMPORT-02 (merge-by-id semantics), and IMPORT-03 (schema validation) are the core restore capability. TDD ensures merge logic is correct and no data is silently lost.

Output: `js/io/import.js` module with `mergeImportedStores(imported)` function; test suite covering merge-by-id, schema validation, and error handling.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-CONTEXT.md
@.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md
@.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md

@js/db/schema.js
@js/db/repo.js
@js/io/seed.js
</context>

<tasks>

<task type="tdd">
  <name>Task 1: JSON import merge-by-id and schema validation (RED→GREEN→REFACTOR)</name>
  <files>
    tests/unit/import.test.js
    js/io/import.js
  </files>
  <read_first>
    @js/db/schema.js
    @js/db/repo.js
    @js/io/seed.js
    @.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-33 merge-by-id pattern from seed.js)
    @.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-RESEARCH.md (Pitfall 3, D-98, D-99, D-100)
  </read_first>
  <behavior>
    - Test 1: Import with matching habit ID → local habit is completely replaced (overwritten)
    - Test 2: Import with new habit ID → new habit inserted, local habits untouched
    - Test 3: Import with newer schemaVersion (> DB_VERSION) → throws error with message "This backup was created with a newer version..."
    - Test 4: Import with older schemaVersion (< DB_VERSION) → merge proceeds (backward compatible)
    - Test 5: Import with equal schemaVersion → merge proceeds
    - Test 6: Merge logs by [habitId, date] compound key → colliding logs are overwritten
    - Test 7: Merge settings by key → colliding settings overwritten (e.g., lastBackupDate overwritten)
    - Test 8: All 7 stores merged in single atomic transaction (no partial merges on error)
    - Test 9: Invalid JSON import file → throws error
    - Test 10: Import with schemaVersion field missing → assumes version 1 (or current DB_VERSION per D-99 spec)
    - Test 11: configureImport({repo}) allows DI injection
  </behavior>
  <action>
    Create `tests/unit/import.test.js` with failing tests for:
    - Merge-by-id: import 1 new habit + 1 matching habit → local habit is replaced, non-colliding habits preserved
    - Schema validation: schemaVersion > DB_VERSION throws with specific error message per D-99
    - Backward compatibility: schemaVersion ≤ DB_VERSION proceeds without error
    - Multi-store consistency: all 7 stores merge within single transaction (simulate via fake-IDB transaction pattern)
    - Log merging by compound key [habitId, date]
    - Settings merging by key
    - Invalid/malformed JSON handling
    - Pitfall 3 handling: ensure tx is committed before any side effects (broadcast happens in Plan 05, not here)

    Then create `js/io/import.js` with:
    - `configureImport({repo, broadcast})` — DI hook; `broadcast` is optional (for Plan 05 wiring); defaults to null
    - `mergeImportedStores(imported)` — takes parsed JSON object `{schemaVersion, habits: [...], logs: [...], ...}`. Validates schema version per D-99 (reject if imported > DB_VERSION with error message). Then uses `repo.runTx(STORE_NAMES, 'readwrite', async (tx) => { for each store: for each row: tx.objectStore(storeName).put(row); })` to merge all stores atomically. Per D-100, if `broadcast` is configured, sends `{type: 'import:done'}` AFTER tx completes (critical: broadcast only after transaction fully commits).
    - Import DB_VERSION from schema.js for comparison
    - Assume repo.runTx() is available and works with a transaction object that exposes `tx.objectStore(name)`

    Commits (TDD gate):
    1. `test(05-03): add failing JSON import merge and validation tests` — RED
    2. `feat(05-03): implement mergeImportedStores and configureImport` — GREEN
    3. `refactor(05-03): extract merge-loop pattern` — (if needed; else skip)
  </action>
  <verify>
    <automated>node --test tests/unit/import.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/import.test.js` contains ≥11 test cases
    - `js/io/import.js` exports `mergeImportedStores(imported)` and `configureImport(deps)`
    - All tests pass (green)
    - mergeImportedStores correctly merges all 7 stores by primary key
    - Schema version validation rejects with proper error message for newer versions
    - Backward compatibility accepted for older/equal versions
    - Invalid JSON throws appropriate error
    - Transaction is atomic (no partial merges)
  </acceptance_criteria>
  <done>
    JSON import merge-by-id logic complete. `mergeImportedStores()` is ready for file-upload integration (Plan 05) and broadcast wiring (Plan 05). IMPORT-01, IMPORT-02, and IMPORT-03 are complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON import | User-uploaded JSON file → IDB merge |
| Schema validation | imported.schemaVersion → DB_VERSION comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | Imported JSON structure | mitigate | Validate schemaVersion before merging; reject if newer (D-99); merge logic uses simple `tx.put()`, no custom deserialization |
| T-05-06 | Information Disclosure | Merge may restore deleted data | accept | User uploads their own backup; restored data is user's own content per D-98 semantics |
| T-05-07 | Denial of Service | Very large import file | accept | No size limits in P5; user controls file size. Could add sanity check (50MB limit) post-v1 if zip-bomb risk emerges |

</threat_model>

<verification>
Unit test suite for JSON import: `node --test tests/unit/import.test.js` must pass (11+ cases).
</verification>

<success_criteria>
- JSON import merges all 7 stores by primary key
- Schema version validation rejects newer versions with clear error
- Backward compatibility for older/equal versions
- Multi-store merge is atomic
- No test data is silently lost
</success_criteria>

<output>
Create `.planning/phases/05-backup-restore-json-csv-exports-json-import-nag/05-03-SUMMARY.md` when done
</output>
