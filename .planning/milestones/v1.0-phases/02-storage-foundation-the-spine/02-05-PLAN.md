---
phase: 02-storage-foundation-the-spine
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
status: pending
autonomous: false
requirements:
  - DATA-04
  - DATA-07
  - DATA-08
files_modified:
  - js/views/diagnostics.js
  - js/main.js
  - js/desktop.js
  - sw.js
tags:
  - reset-data
  - boot-wiring
  - sw-shell
  - checkpoint
must_haves:
  truths:
    - "`js/views/diagnostics.js` Reset-data button is functional (D-44): clicking it triggers a D-06-style confirm; on OK the `habits` IndexedDB DB is deleted and the page reloads"
    - "`js/main.js` and `js/desktop.js` call `configureApply({repo, broadcast, trackTx})`, `configureUndo({repo})`, `configureSeed({repo, storage: navigator.storage, fetch: globalThis.fetch})`, `bootSync()`, `bootLifecycle()`, then `await bootSeed()`, then `await hydrate()` in that order at startup"
    - "`sw.js` SHELL list contains every P2-shipped JS file plus `./seed/habits.json` so first-run-offline succeeds (Pitfall 8 strategy (a), Assumption A5)"
    - "After 02-04's loader ships and this plan wires it, a real browser opens `index.html`, sees the seed run, and shows the 8 habits + 8 events + meta + settings in DevTools → IndexedDB → habits — observable in the manual smoke checklist"
    - "Real BroadcastChannel between two tabs delivers `{type, event, keys, at, origin}` payloads where keys = {habitId, date} only (no value leak) — observable in the manual smoke checklist (DATA-07 + Pitfall 8)"
    - "Real `visibilitychange → hidden` awaits in-flight tx; `beforeunload` is grep-banned in `js/platform/lifecycle.js` (DATA-08, T-02-13, MDN)"
    - "Reset-data confirm copy reads verbatim: `Reset data — delete the habits IndexedDB database. Service worker + caches NOT affected. Reload to re-seed.` (D-44 / D-06 style)"
  artifacts:
    - path: "js/views/diagnostics.js"
      provides: "Reset-data button wiring (D-44, SETTINGS-07 carry-forward)"
    - path: "js/main.js"
      provides: "Mobile boot wiring for P2 spine (DATA-04 + DATA-07 + DATA-08 + DATA-03)"
    - path: "js/desktop.js"
      provides: "Desktop boot wiring for P2 spine"
    - path: "sw.js"
      provides: "Extended SHELL list with P2 modules + seed/habits.json (Pitfall 8 strategy (a))"
  key_links:
    - from: "js/main.js"
      to: "js/io/seed.js"
      via: "named import `bootSeed` invoked at startup"
      pattern: "from './io/seed.js'"
    - from: "js/views/diagnostics.js (Reset-data button)"
      to: "indexedDB.deleteDatabase('habits') (D-44)"
      via: "click handler"
      pattern: "indexedDB\\.deleteDatabase\\('habits'\\)"
    - from: "sw.js (SHELL list)"
      to: "./seed/habits.json"
      via: "Cache-first install (Pitfall 8 strategy (a), Assumption A5)"
      pattern: "'./seed/habits.json'"
---

<objective>
Wire 02-04's seed loader and 02-03's state spine into production: extend the Reset-data button in diagnostics (D-44), append P2 boot calls to `js/main.js` + `js/desktop.js`, extend `sw.js`'s SHELL list with every P2 JS file + `./seed/habits.json`, then run the manual browser smoke checklist as the phase's only checkpoint:human-verify.

Purpose: This is the "first run actually does something in a real browser" plan. By the end:
- Opening `index.html` for the very first time triggers `bootSeed()` → 8 seed habits land in IDB, settings defaults are written, `navigator.storage.persist()` is called.
- On reload, `bootSeed()` short-circuits via `meta.seededIds` — no duplicate writes, no re-prompt.
- The Reset-data button (a P1 placeholder with `disabled` + tooltip) now actually deletes the `habits` IDB database.
- `bootSync()` + `bootLifecycle()` register the cross-tab and flush hooks so subsequent (P3) user-driven `apply()` calls broadcast and flush correctly.

This plan contains the only `checkpoint:human-verify` task in the phase — the manual smoke checklist MUST be eyeballed in a real browser before plan 02-06 bumps APP_VERSION.

Output:
- `js/views/diagnostics.js` (Reset-data wired)
- `js/main.js` + `js/desktop.js` (boot calls appended)
- `sw.js` (SHELL list extended)
- Manual smoke checklist outcomes recorded
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md
@.planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md
@.planning/phases/02-storage-foundation-the-spine/02-PATTERNS.md
@.planning/phases/02-storage-foundation-the-spine/02-VALIDATION.md
@.planning/phases/02-storage-foundation-the-spine/02-04-SUMMARY.md
@js/views/diagnostics.js
@js/main.js
@js/desktop.js
@sw.js
@js/db/repo.js
@js/state/apply.js
@js/state/undo.js
@js/state/store.js
@js/io/seed.js
@js/platform/sync.js
@js/platform/lifecycle.js
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reset-data button → IndexedDB deletion | Destructive action requires explicit confirm (D-06 verbatim phrasing). |
| Boot wiring → real `repo` / `navigator.storage` / `globalThis.fetch` | First time the configure() seams are called against real browser APIs (DI seams from 02-03 + 02-04). |
| SW SHELL list → first-run-offline | If `./seed/habits.json` or any P2 JS file is missing from SHELL, offline first-run breaks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-DEL | Tampering | Reset-data deletes wrong DB | mitigate | Hardcoded literal `'habits'` (D-30); confirm dialog uses D-06-style phrasing; reload after deletion ensures stale handles are dropped. |
| T-02-BOOT | Tampering | Boot order races — broadcast/lifecycle attached after the first write | mitigate | Boot order locked: configure DI seams first, then attach platform listeners (`bootSync`, `bootLifecycle`) BEFORE `bootSeed` (the seed tx is the first write); then `await bootSeed()`; then `await hydrate()`. Verifiable by reading `js/main.js` source. |
| T-02-SHELL | Tampering | SW SHELL list incomplete → offline first-run fails | mitigate | Extend SHELL with every new P2 JS file + `./seed/habits.json`. Verified by automated grep in Task 3 + by the manual smoke item "file:// graceful" (item 8). |
| T-02-13C | Tampering | beforeunload sneaks in during wiring | mitigate | `js/platform/lifecycle.js` (shipped in 02-03) is grep-banned; this plan does NOT modify it. The discipline test from 02-03 still passes here. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Wire Reset-data button in js/views/diagnostics.js (D-44)</name>
  <files>js/views/diagnostics.js</files>
  <read_first>
    - js/views/diagnostics.js (the existing Reset-shell handler at lines 139-170 is the exact-shape template; the Reset-data placeholder at lines 172-178 is what we're replacing)
    - .planning/phases/02-storage-foundation-the-spine/02-PATTERNS.md (§`js/views/diagnostics.js` lines 825-892 — verbatim replacement block + the swallow-and-continue discipline)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-44 Reset-data wired in diagnostics in P2; uses D-06 verbatim phrasing)
    - .planning/phases/01-pwa-shell-tooling-hygiene/01-CONTEXT.md (D-05 + D-06 — the original phrasings)
  </read_first>
  <action>
    Replace lines 172-178 of `js/views/diagnostics.js` (the current placeholder Reset-data button with `disabled = true` + tooltip "available in P2") with a wired Reset-data button per 02-PATTERNS.md §`js/views/diagnostics.js` lines 825-892.

    Concrete replacement: REMOVE the lines that set `resetDataBtn.disabled = true` and `resetDataBtn.setAttribute('title', 'available in P2')`. ADD a click handler whose `confirm()` call uses the verbatim copy (per D-44 / D-06 style): `Reset data — delete the habits IndexedDB database. Service worker + caches NOT affected. Reload to re-seed.`. The handler awaits a small Promise wrapper around `indexedDB.deleteDatabase('habits')` that resolves on the request's `onsuccess`, rejects on `onerror`, and ALSO resolves on `onblocked` (per 02-PATTERNS.md — so the reload still fixes blocked-by-open-tabs cases). On any thrown rejection — swallow per the existing Reset-shell discipline (try/catch with no rethrow). Then call `location.reload()`.

    Keep all existing P1 disciplines: `textContent` only (no innerHTML), early-return on confirm cancel, swallow-and-continue on the destructive operation, eventual reload no matter what.

    Update the file-level JSDoc header to reflect that Reset-data is now wired (D-44 replaces the "P2 placeholder" note): change the existing reference to D-05's "Reset data button (placeholder + tooltip 'wired in P2')" to "Reset data button (wired per D-44 — deletes IndexedDB 'habits' DB)".
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('./js/views/diagnostics.js', 'utf8'); if (!c.includes(\"indexedDB.deleteDatabase('habits')\")) { console.error('Reset-data handler missing'); process.exit(1); } if (c.includes('resetDataBtn.disabled = true')) { console.error('placeholder still present'); process.exit(1); } if (!c.includes('Reset data — delete the habits IndexedDB database')) { console.error('D-44 confirm copy missing'); process.exit(1); } console.log('Reset-data wiring OK');"</automated>
    <human-check>Browser smoke (handled in checkpoint task 4 below).</human-check>
  </verify>
  <done>
    `js/views/diagnostics.js` contains the literal substring `indexedDB.deleteDatabase('habits')` (D-30 + D-44). The `resetDataBtn.disabled = true` placeholder is GONE. The verbatim confirm copy `Reset data — delete the habits IndexedDB database. Service worker + caches NOT affected. Reload to re-seed.` is present.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire boot calls in js/main.js + js/desktop.js</name>
  <files>js/main.js, js/desktop.js</files>
  <read_first>
    - js/main.js (existing boot sequence — append-only)
    - js/desktop.js (existing desktop entry — parallel boot)
    - .planning/phases/02-storage-foundation-the-spine/02-PATTERNS.md (§`js/main.js` lines 898-931 — append-only P2 boot wiring; order rationale)
    - js/db/repo.js, js/state/apply.js, js/state/undo.js, js/state/store.js, js/io/seed.js, js/platform/sync.js, js/platform/lifecycle.js (the modules being wired)
  </read_first>
  <action>
    Append P2 boot wiring to `js/main.js` AFTER the existing P1 wiring (Registering SW, mounting diagnostics, attaching long-press). Add the following module-namespace imports at the top of the file (all relative paths per D-19):

    - `* as repo` from `./db/repo.js`
    - `configure as configureApply` from `./state/apply.js`
    - `configureUndo` from `./state/undo.js`
    - `configureSeed`, `bootSeed` from `./io/seed.js`
    - `hydrate` from `./state/store.js`
    - `bootSync`, `broadcast` from `./platform/sync.js`
    - `bootLifecycle`, `trackTx` from `./platform/lifecycle.js`

    After the existing `attachLongPress` block, append the P2 spine boot. Use top-level `await` directly — `index.html` already uses `<script type="module">` and top-level await has been Baseline Widely Available since 2022 (N1 fix — no IIFE wrapper needed; this is purely a scoping convenience that the original plan misjudged as a cross-browser/CI concern). Wrap each `await` call in a try/catch so a failure does not abort the rest of the boot (diagnostics must still render).

    Boot order (locked — do not reorder; T-02-BOOT):

    1. `configureApply({ repo, broadcast, trackTx })`
    2. `configureUndo({ repo })`
    3. `configureSeed({ repo, storage: navigator.storage, fetch: globalThis.fetch })`
    4. `bootSync()` — must precede the first write so the seed tx's post-commit broadcast (if any in P3+) is observable in other tabs
    5. `bootLifecycle()` — must precede the first write so the seed tx is awaitable by visibilitychange
    6. `try { await bootSeed(); } catch (e) { /* swallow — diagnostics surfaces persistence state separately in P3 */ }`
    7. `try { await hydrate(); } catch (_e) { /* swallow */ }`

    Add a single explanatory `//` comment block above the boot section explaining the order rationale (per the truth in must_haves): "P2 spine boot — DATA-03/04/07/08, SEED-01..05. Configure DI seams first; attach platform listeners; then bootSeed (first write); then hydrate. Top-level await is fine here — index.html uses type=module and top-level await is Baseline Widely Available since 2022 (N1)."

    Mirror the same imports + boot block in `js/desktop.js` (P1 ships `desktop.js` as a minimal stub per D-04; the import set is identical because both shells share the spine per DESKTOP-02). If `js/desktop.js` was a smaller stub that doesn't yet register the SW, leave any P1 desktop-specific calls intact and only add the P2 spine block.

    All new imports MUST use relative paths (`./db/repo.js`, etc.) per D-19.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('./js/main.js', 'utf8'); const need = [\"from './io/seed.js'\", \"from './platform/sync.js'\", \"from './platform/lifecycle.js'\", 'configureApply', 'bootSeed', 'bootSync', 'bootLifecycle', 'hydrate']; for (const n of need) { if (!c.includes(n)) { console.error('main.js missing:', n); process.exit(1); } } if (c.includes('(async () =>')) { console.error('IIFE wrapper still present — N1 says use top-level await'); process.exit(1); } console.log('main.js boot wiring OK');"</automated>
    <human-check>Real-browser boot check is handled in the checkpoint task below.</human-check>
  </verify>
  <done>
    `js/main.js` imports `repo`, `configureApply`, `configureUndo`, `configureSeed`, `bootSeed`, `hydrate`, `bootSync`, `broadcast`, `bootLifecycle`, `trackTx`. The P2 boot block uses top-level await (no IIFE — N1). `js/desktop.js` has the same boot block. All new imports are relative (`./...`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend sw.js SHELL list with P2 modules + seed/habits.json</name>
  <files>sw.js</files>
  <read_first>
    - sw.js (existing SHELL list at lines 62-80)
    - .planning/phases/02-storage-foundation-the-spine/02-RESEARCH.md (§Pitfall 8 — strategy (a) recommendation; §Assumption A5)
    - .planning/phases/02-storage-foundation-the-spine/02-PATTERNS.md (§`sw.js` lines 935-978 — append-only P2 candidates list)
  </read_first>
  <action>
    Append entries to the existing `SHELL` array in `sw.js`, immediately after the last current entry `'./js/views/toast.js'` and before the closing `];`. Group them under a single `// P2 storage spine — added in phase 02-storage-foundation-the-spine plan 04 (seed) / 05 (wiring).` comment line. Use the exact relative paths (D-19) — all of these are string literals already quoted with single quotes per the existing SHELL style:

    - `./js/util/date.js`
    - `./js/util/id.js`
    - `./js/db/idb.js`
    - `./js/db/schema.js`
    - `./js/db/repo.js`
    - `./js/state/store.js`
    - `./js/state/apply.js`
    - `./js/state/apply/markCompleted.js`
    - `./js/state/undo.js`
    - `./js/platform/sync.js`
    - `./js/platform/lifecycle.js`
    - `./js/io/seed.js`
    - `./seed/habits.json`

    Per Pitfall 8 strategy (a) + Assumption A5 — this guarantees offline first-run boot. `./seed/habits.json` MUST be in SHELL because `bootSeed()` fetches it via `fetch('./seed/habits.json')`; without it, offline-first-run fails.

    Do NOT modify any other part of `sw.js`. The SWR strategy for `/js/**` (D-11) still applies to these files on subsequent fetches; the SHELL list just guarantees they're cached on first install. Do NOT change `APP_VERSION` here — plan 02-06 owns the bump.

    Update the file-level JSDoc note (the comment block at lines 57-61 about "Plans 03 + 04 ship the remaining JS files referenced here") to reflect that 02-04 (seed) + 02-05 (wiring) add the new spine files.
  </action>
  <verify>
    <automated>node -e "const c = require('fs').readFileSync('./sw.js', 'utf8'); const need = ['./js/util/date.js', './js/db/idb.js', './js/db/schema.js', './js/db/repo.js', './js/state/apply.js', './js/state/apply/markCompleted.js', './js/state/undo.js', './js/platform/sync.js', './js/platform/lifecycle.js', './js/io/seed.js', './seed/habits.json']; for (const n of need) { if (!c.includes(\"'\" + n + \"'\")) { console.error('sw.js SHELL missing:', n); process.exit(1); } } console.log('sw.js SHELL extension OK');"</automated>
  </verify>
  <done>
    `sw.js` SHELL array contains all 13 new entries listed above. Existing SHELL entries are unchanged. APP_VERSION is unchanged (plan 02-06 owns the bump).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — first-run round-trip + Reset-data + manual smoke checklist</name>
  <read_first>
    - .planning/phases/02-storage-foundation-the-spine/02-VALIDATION.md (§Manual Browser Smoke Checklist — the eight items the verifier must hit)
    - .planning/phases/02-storage-foundation-the-spine/02-CONTEXT.md (D-44 Reset-data confirm copy + D-41 persist trigger + D-30 BroadcastChannel name)
  </read_first>
  <what-built>
    Plan 02-04 shipped the seed loader; this plan wired the boot sequence (configure seams + `bootSync` + `bootLifecycle` + `bootSeed` + `hydrate`), the Reset-data button, and extended the sw.js SHELL list. Plans 02-01..03 shipped the underlying spine modules. This checkpoint runs the full 02-VALIDATION.md §Manual Browser Smoke Checklist BEFORE plan 02-06 bumps APP_VERSION.
  </what-built>
  <how-to-verify>
    Pre-reqs: `node --test tests/` is green (re-run as a sanity check).

    Start the dev server: `node scripts/serve.js`. Open Chrome / Edge to `http://localhost:8080/`.

    Run each item from 02-VALIDATION.md §Manual Browser Smoke Checklist (also reproduced inline below):

    1. **Cold boot — real IDB persistence:**
       - First load triggers seed. DevTools → Application → IndexedDB → `habits` v1 — verify all 7 stores exist: `habits`, `habit_versions`, `logs`, `events`, `settings`, `meta`, `score_snapshots`.
       - Verify `habits` has 8 rows. Verify `events` has 8 rows of `type: 'seed:createHabit'`. Verify `meta.seededIds` contains 8 UUIDs.
       - Verify `settings.defaultThreshold = 0.9`, `settings.defaultWindowDays = 70`, `settings.schemaVersion = 1`.
       - Close tab. Reopen. Verify rows persist (no duplicates).

    2. **`navigator.storage.persist()` actually fires:**
       - Open DevTools BEFORE first load. In the console: `await navigator.storage.persisted()` returns `true` (Firefox) or `false` (Chrome may decline silently if engagement metrics not met — both acceptable per Pitfall 3).
       - Verify `meta.persistResult` exists in IDB after first load.
       - Reload — `persist()` is NOT called again (no extra entries in `meta.persistResult` or no extra prompts).

    3. **Real BroadcastChannel between two tabs:**
       - Open `http://localhost:8080/` in two tabs (Tab A + Tab B). In Tab B console: `(await import('./js/platform/sync.js')).onMessage(d => console.log('B got:', d));`
       - In Tab A console: first read the first seeded habit id via `const seeded = (await (await fetch('./seed/habits.json')).json()).habits[0].id;` and then `(await import('./js/state/apply.js')).apply({type: 'markCompleted', payload: {habitId: seeded, date: '2026-05-26'}});`
       - Tab B's console logs an object whose shape matches `{type: 'mutation', event: 'markCompleted', keys: {habitId, date: '2026-05-26'}, at, origin}`. Tab A's `onMessage` listener (if subscribed) MUST NOT fire (origin filter — own-message suppression).

    4. **Real `visibilitychange` flush:**
       - In Tab A's console, time an apply via `console.time` / `console.timeEnd` around `(await import('./js/state/apply.js')).apply({type: 'markCompleted', payload: {habitId: seeded, date: '2026-05-27'}})`. Note the timing (should be < 50ms).
       - Switch to another tab quickly after the apply call. Wait 5 seconds. Return to Tab A. Inspect IDB — verify the 2026-05-27 log row is present (visibilitychange flush awaited the tx before yielding).
       - Verify `js/platform/lifecycle.js` source: grep for `beforeunload` — zero occurrences.

    5. **Undo across reload:**
       - In Tab A: `apply(markCompleted ...)` as above. Verify the log row exists.
       - Reload. Open DevTools and inspect `meta.undoToken` — has a value pointing to the markCompleted event.
       - In console: `(await import('./js/state/undo.js')).undo()`. Verify the log row is reverted (or removed if `prior` was undefined).

    6. **Reset-data button:**
       - Open `http://localhost:8080/?debug=1` (diagnostics panel via query param per D-02). Click "Reset data".
       - Verify the confirm dialog text reads VERBATIM: `Reset data — delete the habits IndexedDB database. Service worker + caches NOT affected. Reload to re-seed.`
       - Click OK. Verify the page reloads. Verify in DevTools that the `habits` IDB DB was deleted and then re-seeded (`habits` store has 8 rows again).
       - Click "Reset data" again, then click Cancel. Verify the IDB is unchanged (no reload).

    7. **Cross-shell:**
       - Open `http://localhost:8080/desktop.html`. Verify diagnostics panel still works (long-press or `?debug=1`). Verify IDB is shared (the 8 habits + events are visible — same `habits` DB).

    8. **`file://` graceful:**
       - Open `index.html` directly via `file://`. Verify the page loads. The SW does NOT register (silent). `bootSeed()` still runs (fetches relative `./seed/habits.json` which works on `file://`). IDB still works (the storage panel shows the `habits` DB).
       - Verify `crypto.randomUUID()` works (Chrome/Firefox honor it on file://). On Safari-file:// edge case (Pitfall 13), the fallback in `js/util/id.js` kicks in.

    Record outcomes for each of the 8 items in the plan summary (next task). For each item: ✓ pass / ✗ fail (with notes).
  </how-to-verify>
  <resume-signal>Type "approved" (all 8 items pass) or describe failing items so the planner can issue a fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Wave 5 full-suite green check + summary stub</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/02-storage-foundation-the-spine/02-VALIDATION.md (§Sampling Rate)
  </read_first>
  <action>
    Run `node --test tests/` — all tests across all five plans must be green. Confirm the smoke-checklist outcome from task 4 is recorded (in your working notes for the summary).
  </action>
  <verify>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    Full suite (~14 test files) green. All 8 manual smoke items from task 4 pass.
  </done>
</task>

</tasks>

<verification>
- `node --test tests/` exits 0
- `js/views/diagnostics.js` Reset-data button is wired (contains `indexedDB.deleteDatabase('habits')`; placeholder `disabled = true` is removed)
- `js/main.js` and `js/desktop.js` contain the P2 boot block (imports + top-level-await calls to configureApply/configureUndo/configureSeed/bootSync/bootLifecycle/bootSeed/hydrate) — NO IIFE wrapper (N1)
- `sw.js` SHELL list contains all 13 new P2 entries including `./seed/habits.json`
- Manual smoke checklist (8 items) recorded in task 4
</verification>

<manual_verification>
This plan contains the **only manual checkpoint** of Phase 2 — task 4 above is the gate. The full 02-VALIDATION.md §Manual Browser Smoke Checklist is exercised inside task 4.

The acceptance criteria for the checkpoint:
1. All 7 IDB stores exist; 8 habits + 8 seed events + meta + settings present (DATA-01, SEED-02, SEED-04, D-45)
2. `navigator.storage.persist()` called once; result recorded in `meta.persistResult`; not re-called on reload (DATA-03, Pitfall 11)
3. Real BroadcastChannel cross-tab message delivered with `keys` payload, no value leak (DATA-07, Pitfall 8)
4. Real `visibilitychange → hidden` awaits in-flight tx; `beforeunload` not registered (DATA-08, MDN)
5. Undo across reload restores the log row using `meta.undoToken` (UNDO-02, D-43)
6. Reset-data button deletes the `habits` IDB and reloads (D-44)
7. Cross-shell IDB-share works (DESKTOP-02 carry-forward)
8. `file://` graceful: SW silent-fails, IDB still works, `crypto.randomUUID` honored or fallback fires (NFR-09, Pitfall 13)
</manual_verification>

<success_criteria>
- First-run on any user's device: 8 habits + 8 events + meta + settings + persist() — exactly once (DATA-01, DATA-03, SEED-01..05, D-41, D-45)
- Reset-data is functional and uses the D-06 verbatim phrasing (D-44)
- Boot wiring puts the chokepoint into production (DATA-04 + DATA-07 + DATA-08 live for the first time)
- Offline-first-run works because every new JS file + the seed are in the SW SHELL list (Pitfall 8)
- Manual smoke checklist (8 items) passes before phase advances to plan 02-06 (APP_VERSION bump)
</success_criteria>

<output>
Create `.planning/phases/02-storage-foundation-the-spine/02-05-SUMMARY.md` describing:
- The Reset-data wiring and its D-06 verbatim confirm copy
- The boot-wiring order in main.js + desktop.js with rationale (configure → listeners → seed → hydrate; top-level await per N1)
- The SHELL extension list (13 new entries)
- Task 4 manual smoke checklist outcomes (item-by-item ✓/✗ with notes)
Cite DATA-04, DATA-07, DATA-08, D-30, D-44, Pitfall 8.
</output>
