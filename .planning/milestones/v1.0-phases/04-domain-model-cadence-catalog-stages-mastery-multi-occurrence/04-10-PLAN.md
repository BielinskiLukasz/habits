---
phase: 04-domain-model-cadence-catalog-stages-mastery-multi-occurrence
plan: 10
type: execute
wave: 4
depends_on:
  - 04-07
  - 04-08
  - 04-09
files_modified:
  - seed/habits.json
  - sw.js
  - tests/integration/sw.shell.test.js
  - js/util/version.js
  - VERSIONING.md
  - README.md
autonomous: true
requirements:
  - CADENCE-01
  - CADENCE-02
  - CADENCE-03
  - CADENCE-04
  - CADENCE-05
  - NFR-10

must_haves:
  truths:
    - "seed/habits.json enriched with stages, targetType, and target fields for all 8 seed habits (or representative subset)"
    - "sw.js SHELL array contains all new P4 JS and CSS files"
    - "sw.shell.test.js P4_REQUIRED list covers all new P4 shell assets; existing P3_REQUIRED list unchanged"
    - "APP_VERSION bumped from 0.3.0 to 0.4.0 in js/util/version.js"
    - "VERSIONING.md has v0.4.0 release entry"
    - "Full test suite green after version bump (sw.shell.test.js version coverage includes 0.4.0)"
  artifacts:
    - path: "seed/habits.json"
      provides: "Updated seed with stage arrays and full targetType coverage"
      contains: "stages"
    - path: "sw.js"
      provides: "P4 SHELL entries added"
      contains: "catalog.css"
    - path: "tests/integration/sw.shell.test.js"
      provides: "P4_REQUIRED list added"
      contains: "P4_REQUIRED"
    - path: "js/util/version.js"
      provides: "APP_VERSION = '0.4.0'"
      contains: "0.4.0"
  key_links:
    - from: "sw.js SHELL"
      to: "All P4 new files"
      via: "P4_REQUIRED list in sw.shell.test.js guards against drift"
      pattern: "catalog.css"
---

<objective>
Phase 4 closeout: enrich seed/habits.json with full P4-compatible fields (stages, targetType, stageStartedAt), update sw.js SHELL with all new P4 files, extend sw.shell.test.js with a P4_REQUIRED locked list, bump APP_VERSION to 0.4.0, and update VERSIONING.md.

Purpose: Closeout tasks are the phase-completion contract. The SW shell extension + test coverage ensures the offline PWA serves all new P4 assets. The seed extension gives the app real data for first-run testing against P4 features (stages, numeric/slot logging).

Output: seed.json enriched, sw.js SHELL extended, shell test P4_REQUIRED list, version 0.4.0.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/projects/habits/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-CONTEXT.md

@seed/habits.json
@sw.js
@tests/integration/sw.shell.test.js
@js/util/version.js
@VERSIONING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enrich seed/habits.json with P4 fields</name>
  <files>seed/habits.json</files>

  <read_first>
  - `seed/habits.json` — read ALL 8 habits; understand current shape before adding fields
  - `js/db/schema.js` — confirm `habits` store keyPath and any field constraints
  - `.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-CONTEXT.md` — D-83 stage array shape `{label, target, allowManual?, targetWeekDate?, advanceAfterDays?}`, D-88 numeric logging with `target`, D-89 slot-checklist with `slots`
  </read_first>

  <action>
  Update `seed/habits.json` to add P4-required fields to each existing habit:

  Fields to add to every habit row (where missing):
  - `"stages": []` — empty stages array (no stage progression yet; preserves future extensibility)
  - `"currentStageIndex": 0`
  - `"stageStartedAt": null`
  - `"masteryThresholdOverride": null`
  - `"masteryWindowOverride": null`
  - `"startDate": null` — null means "always active from createdAt" (no future scheduling)

  Rename `"logShape"` to `"targetType"` per the locked convention in CONTEXT.md (the schema used `targetType` not `logShape`):
  - `"logShape": "binary"` → `"targetType": "binary"` (for all binary habits)
  - `"logShape": "numeric"` → `"targetType": "numeric"` (for the numeric habits)
  - `"logShape": "slot-checklist"` → `"targetType": "slot-checklist"` (for the slot habit if any)

  For the "5 things grateful for" habit (target:5, logShape:numeric) and "7 meatless meals" habit (if present as slot-checklist):
  - Ensure `"target"` field is present with the correct numeric value.
  - For the slot habit: add `"slots": [{"name":"Meal 1"},{"name":"Meal 2"},{"name":"Meal 3"},{"name":"Meal 4"},{"name":"Meal 5"},{"name":"Meal 6"},{"name":"Meal 7"}]` (anonymous pre-labeled slots matching the English name convention per D-35).

  Add representative stage data to one habit to validate the stage UI in P4 UAT:
  - "Morning walk" (daily, Wave 1): add `"stages": [{"label":"Stage 1","target":1,"allowManual":true,"advanceAfterDays":30},{"label":"Stage 2","target":1,"allowManual":true,"advanceAfterDays":30},{"label":"Stage 3","target":1,"allowManual":true}]` so the stage advance button appears on Today for this habit.

  Bump `"seedVersion": 1` → `"seedVersion": 2` to signal the enriched seed.

  The `io/seed.js` idempotent loader uses `merge-by-id` — existing habits in IDB are NOT overwritten on `seedVersion` bump; the version bump is for new installs only. (The seed loader's `seededIds` + `seedVersion` check: read `io/seed.js` to confirm behavior before changing `seedVersion`.)
  </action>

  <verify>
    <automated>npm test -- tests/unit/seed.shape.test.js</automated>
  </verify>

  <done>
  - seed/habits.json valid JSON with all 8 habits having targetType (not logShape), stages:[], currentStageIndex:0, stageStartedAt:null, masteryThresholdOverride:null, masteryWindowOverride:null
  - "Morning walk" has 3-stage array
  - "5 things grateful for" has targetType:"numeric", target:5
  - Seed shape test still passes
  </done>
</task>

<task type="auto">
  <name>Task 2: SW SHELL P4 extension + shell test P4_REQUIRED + version bump</name>
  <files>sw.js, tests/integration/sw.shell.test.js, js/util/version.js, VERSIONING.md, README.md</files>

  <read_first>
  - `sw.js` — read ALL; find SHELL array; understand the existing pattern for adding entries and the SWR exception comment
  - `tests/integration/sw.shell.test.js` — read ALL; understand P2_BASELINE and P3_REQUIRED list format; understand how to add P4_REQUIRED list
  - `js/util/version.js` — read to confirm APP_VERSION constant location and format
  - `VERSIONING.md` — read to understand release history entry format
  </read_first>

  <action>
  **`sw.js` SHELL extension:**
  Add these entries to the SHELL array (all new P4 source files that must be cached for offline use):
  ```
  './js/views/catalog.js',
  './js/views/catalog/builders.js',
  './js/views/history.js',
  './js/views/history/builders.js',
  './js/state/apply/createHabit.js',
  './js/state/apply/editHabit.js',
  './js/state/apply/archiveHabit.js',
  './js/state/apply/advanceStage.js',
  './js/state/apply/logNumeric.js',
  './js/state/apply/logSlot.js',
  './js/state/apply/setMasteryThreshold.js',
  './js/state/apply/setMasteryWindow.js',
  './js/domain/mastery.js',
  './js/domain/stage.js',
  './js/domain/waveAggregates.js',
  './css/catalog.css',
  './css/history.css',
  ```
  Note: `seed/waves.json` is SWR-excepted (same as D-81 pattern for `seed/habits.json`); do NOT add it to SHELL. The new domain files and apply handlers ARE in SHELL (they're ES module imports on the critical boot path).

  **`tests/integration/sw.shell.test.js` P4_REQUIRED list:**
  Add a new `const P4_REQUIRED` array (after P3_REQUIRED) with the same entries as added to SHELL above (minus any SWR exceptions). Add a new test: `test('P4 required shell entries are present', ...)` that asserts each P4_REQUIRED entry is in the extracted shell set. Follow exact same pattern as P3_REQUIRED test.

  **`js/util/version.js`:** change `APP_VERSION` from `'0.3.0'` to `'0.4.0'`.

  **`VERSIONING.md`:** add a `v0.4.0` release entry with date `2026-06-XX` (use today), summary of Phase 4 features (catalog CRUD, stages, mastery, multi-occurrence logging, history navigation, wave aggregates).

  **`README.md`:** update the "Version history" section to mention v0.4.0.
  </action>

  <verify>
    <automated>npm test -- tests/integration/sw.shell.test.js</automated>
  </verify>

  <done>
  - sw.shell.test.js P4_REQUIRED test passes (all 17 new P4 entries in SHELL)
  - APP_VERSION is '0.4.0'
  - `npm test` full suite green
  </done>
</task>

</tasks>

<acceptance_criteria>
- `npm test -- tests/unit/seed.shape.test.js` green after seed enrichment
- `npm test -- tests/integration/sw.shell.test.js` green with P4_REQUIRED tests passing
- `npm test` full suite green (all tests preserved; new P4 tests included)
- `seed/habits.json` has `seedVersion: 2`; all habits have `targetType` (not `logShape`); `stages` array present on all habits; "Morning walk" has 3 stage objects
- `js/util/version.js` APP_VERSION === '0.4.0'
- `VERSIONING.md` has v0.4.0 entry
- sw.js SHELL contains `'./js/views/catalog.js'` and `'./css/catalog.css'`
</acceptance_criteria>

## Artifacts this phase produces

**Extended files:**
- `seed/habits.json` — seedVersion:2, all habits have targetType, stages, stageStartedAt, masteryThresholdOverride, masteryWindowOverride
- `sw.js` — SHELL extended with 17 new P4 entries
- `tests/integration/sw.shell.test.js` — P4_REQUIRED locked list + test
- `js/util/version.js` — APP_VERSION = '0.4.0'
- `VERSIONING.md` — v0.4.0 release entry
- `README.md` — version history updated

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sw.js SHELL → offline cache | SHELL array controls which files are cached for offline use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Denial of Service | Missing P4 file in SHELL → offline breakage | mitigate | sw.shell.test.js P4_REQUIRED locked list; CI fails if any P4 file is missing from SHELL |
| T-04-10b | Tampering | seed re-run overwrites user data on seedVersion bump | accept | io/seed.js merge-by-id; existing records updated only where IDs collide; new install behavior; documented in seed.js file header |
</threat_model>

<output>
Create `.planning/phases/04-domain-model-cadence-catalog-stages-mastery-multi-occurrence/04-10-SUMMARY.md` when done.
</output>
