---
phase: 03-today-view-settings-v1-first-usable-slice
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - js/views/toast.js
  - js/views/today.js
  - tests/unit/toast.test.js
  - tests/integration/toast.undo.test.js
  - tests/integration/today.undo.test.js
autonomous: true
requirements:
  - UNDO-01
  - UNDO-02
  - UNDO-03
must_haves:
  truths:
    - "After every successful `markCompleted` or `markUncompleted` from a Today row tap, a toast renders with verb+habit-name copy per D-71 (e.g. `\"Marked Spacer rano complete — Undo · ×\"` / `\"Marked Trening siłowy uncomplete — Undo · ×\"`) (UNDO-01, UNDO-02, D-68, D-71)"
    - "Clicking the toast's `Undo` button calls `undo()` from `js/state/undo.js`; the inverse `restoreLogRow` event lands in the same chokepoint tx and the Today row's class / `aria-pressed` flips back to the pre-mark state via the existing `store.subscribe()` re-render path — no manual DOM patching (UNDO-02)"
    - "Single-toast invariant (D-70): when a second tap fires while a toast is already mounted, the existing toast is removed and a new one renders for the latest action with a fresh `autoDismissMs` timer. Stale toasts NEVER coexist with a newer one and NEVER point at a no-longer-most-recent `meta.undoToken`"
    - "Auto-dismiss timer of 5000 ms per D-69; hovering / pointerentering the toast clears the timer (`clearTimeout(dismissTimer)`); pointerleaving restarts a fresh 5000 ms timer. The toast is removed when the timer fires (`toastEl.remove(); toastEl = null;`)"
    - "D-08 regression guard (LOCKED): `showUpdateToast()` continues to render with NO `autoDismissMs` — the SW-update toast persists until the user clicks Reload or × (DO NOT touch the existing `showUpdateToast` signature; the discipline test `tests/unit/toast.test.js` asserts `showUpdateToast`'s internal call to `_showToast` omits `autoDismissMs`)"
    - "On `apply()` reject in `js/views/today.js`'s `handleMarkCompleteTap` / `handleMarkUncompleteTap`, the existing `console.warn('[today] markCompleted failed', err)` placeholder (planted in 03-03 with the `// 03-04: replace with showErrorToast(...)` comment) is replaced by `showErrorToast(\"Couldn't mark — try again\")` (D-53, D-73)"
    - "`undo()` itself returning `null` is silent (UNDO-03 single-step model: when `meta.undoToken` is empty there is nothing to undo and the disabled-button path is the affordance — the toast's `undoFn` is only wired when `meta.undoToken` was non-empty at the moment of the originating tap). `undo()` throwing surfaces a `showErrorToast(\"Couldn't undo — try again\")` (D-73)"
    - "Toast accessibility unchanged from D-08 baseline: `role=\"status\" aria-live=\"polite\"` on the wrapper div; Undo + Dismiss buttons carry text or `aria-label` per D-79"
    - "D-78 grep gate stays green — every new DOM construction in `toast.js` goes through `document.createElement` + `textContent` + `setAttribute` (no `.innerHTML` / `.outerHTML` / `.insertAdjacentHTML` introduced)"
  artifacts:
    - path: "js/views/toast.js"
      provides: "Toast primitive extended with autoDismiss timer, `showUndoToast`, and `showErrorToast`; `showUpdateToast` D-08 semantics preserved"
      exports: ["showUpdateToast", "showUndoToast", "showErrorToast", "_resetToastForTest"]
    - path: "js/views/today.js"
      provides: "Mark/unmark tap success path fires `showUndoToast({message, undoFn})`; apply()-reject path fires `showErrorToast`. The 03-03 `console.warn` placeholder is removed."
    - path: "tests/unit/toast.test.js"
      provides: "Unit coverage for `showUndoToast` lifecycle (mount, action callback, auto-dismiss timer, hover-reset, single-toast invariant) + `showErrorToast` + D-08 regression for `showUpdateToast`"
    - path: "tests/integration/toast.undo.test.js"
      provides: "Toast Undo button click invokes `undo()`; the inverse event lands; cache refreshes via notify; subscriber sees the reverted log row"
    - path: "tests/integration/today.undo.test.js"
      provides: "End-to-end: tap Today row → toast appears with verb+habit copy → click Undo → row flips back to pre-tap state via the existing store.subscribe re-render path"
  key_links:
    - from: "js/views/today.js"
      to: "js/views/toast.js"
      via: "imports showUndoToast + showErrorToast and calls them inside the tap success / reject paths"
      pattern: "showUndoToast|showErrorToast"
    - from: "js/views/today.js"
      to: "js/state/undo.js"
      via: "passes `undo` as the `undoFn` closure on showUndoToast"
      pattern: "from '.*state/undo\\.js'"
    - from: "js/views/toast.js"
      to: "(none — purely visual primitive)"
      via: "no app-state imports; entirely DOM-side"
      pattern: ""
---

<objective>
Surface UNDO in the Today flow. After Slice 3 wired the chokepoint write path, this slice lands the **first user-visible undo affordance**: every Today mark/unmark fires an auto-dismissing toast with verb+habit-name copy (D-71) and an `Undo` button that calls `undo()`. Replaces the 03-03 `console.warn` placeholder with `showErrorToast("Couldn't mark — try again")`. After this plan ships, the user gets the locked UNDO-01/02/03 behaviour against the Today surface; the second undo surface (Settings Data card) lands in 03-05.

Purpose: Deliver UNDO-01 (toast surface), UNDO-02 (single-step undo wired to `meta.undoToken`), UNDO-03 (single-step model — most-recent action only). Toast lifecycle (auto-dismiss + hover-pause + single-toast invariant) implements D-69 + D-70 verbatim. D-08's `showUpdateToast` no-auto-dismiss invariant is regression-guarded so the SW-update toast stays as it was in P1.
Output: 1 modified `js/views/toast.js` (extended with `{autoDismissMs}` + `showUndoToast` + `showErrorToast` + a shared `_showToast` helper), 1 modified `js/views/today.js` (toast wiring on tap success + error toast on reject), 1 new unit test, 2 new integration tests. The 03-03 placeholder `console.warn` is removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend toast.js with `{autoDismissMs}`, `showUndoToast`, `showErrorToast`; preserve D-08 — TDD</name>
  <files>js/views/toast.js, tests/unit/toast.test.js</files>
  <read_first>
    - js/views/toast.js (current state — `showUpdateToast` no-auto-dismiss baseline per D-08; the single-toast guard `let toastEl = null` is on line 18)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md decisions D-08 (LOCKED — no auto-dismiss for update toast), D-69 (5s auto-dismiss + hover-pause), D-70 (single toast, replace on new), D-71 (copy template), D-73 (undo error variant)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md "js/views/toast.js (modification — extend with autoDismiss + error variant)" lines 430-481
    - tests/unit/toast.test.js if it already exists, OR tests/unit/discipline.xss.test.js (after 03-01 — verify no `.innerHTML` etc. introduced)
    - tests/helpers/fake-document.js (createFakeDocument — exposes `_listeners`, `_appendedChildren`, supports `.click()` + dispatched pointer events)
  </read_first>
  <behavior>
    Internal `_showToast({message, action, autoDismissMs, variant})` helper (NOT exported, but exposed for test via `_resetToastForTest()`):
    - Replaces the existing toast (D-70): if `toastEl` is non-null, `toastEl.remove()`, clear any active `dismissTimer`.
    - Builds a fresh `<div class="toast">` (or `<div class="toast toast--<variant>">` when `variant` is set) with `role="status" aria-live="polite"`.
    - Appends a `<span class="toast-msg">` with `textContent = message` (XSS-safe; D-78).
    - When `action` is `{label, fn}` and `fn` is callable: appends a `<button class="toast-action">` with `textContent = label`; click handler `fn(); _dismissToast();`.
    - Always appends a `<button class="toast-close" aria-label="Dismiss">×</button>` with click handler `_dismissToast();`.
    - When `autoDismissMs` is a positive number: `dismissTimer = setTimeout(_dismissToast, autoDismissMs);` and registers `pointerenter` → `clearTimeout(dismissTimer); dismissTimer = null;` and `pointerleave` → `dismissTimer = setTimeout(_dismissToast, autoDismissMs);` so hover pauses + un-hover restarts.
    - `_dismissToast()` is `if (toastEl) { toastEl.remove(); toastEl = null; } if (dismissTimer) { clearTimeout(dismissTimer); dismissTimer = null; }`.

    `showUpdateToast()` LOCKED (D-08): No-op rewrite. Continues to render without `autoDismissMs`. After this plan, its body delegates to `_showToast({message: 'New version ready', action: {label: 'Reload', fn: () => location.reload()}})` — note the OMITTED `autoDismissMs`. A regression test in `toast.test.js` asserts that calling `showUpdateToast()` followed by `clock.tick(60_000)` leaves the toast mounted.

    `showUndoToast({message, undoFn, autoDismissMs = 5000})` (D-69, D-71): delegates to `_showToast({message, action: {label: 'Undo', fn: undoFn}, autoDismissMs})`. `undoFn` is the closure provided by the caller (in today.js it is `() => undo()`); when `undoFn` throws or returns a rejected promise, `_showToast`'s click handler swallows the failure and triggers `showErrorToast("Couldn't undo — try again")` (D-73). When `undoFn` returns `null`, no error toast (silent — UNDO-03 single-step model means undo-of-nothing is expected).

    `showErrorToast(message)` (D-73): delegates to `_showToast({message, variant: 'error', autoDismissMs: 4000})`. No action button. Used by today.js for apply()-reject and the undo-error path.

    `_resetToastForTest()` (exported, test-only): force-clears `toastEl` + `dismissTimer` between tests; mirrors the `_resetTodayForTest` / `_resetStoreForTest` pattern.

    Critical accessibility: every toast variant keeps `role="status" aria-live="polite"` (already present on the update toast — generalized to all variants). The Dismiss button keeps `aria-label="Dismiss"`. No regression to D-79's baseline.
  </behavior>
  <action>
    RED — Step 1: Create / extend `tests/unit/toast.test.js` (use Pattern S7 paired-cache-bust if `tests/unit/toast.test.js` already exists from P1; otherwise create fresh):
      - `describe('D-08 regression — showUpdateToast remains no-auto-dismiss')`:
        - Test: call `showUpdateToast()`; advance virtual time by 60_000 ms (use a small `installFakeClock()` helper that monkey-patches `setTimeout`/`clearTimeout` on `globalThis`); assert `document.body.firstChild` is still the toast (not removed).
        - Test: call `showUpdateToast()` twice; assert the second call is a NO-OP (the existing P1 idempotent guard) — the body still has exactly one toast.
      - `describe('showUndoToast — D-69 + D-71')`:
        - Test "renders with verb+habit copy": `showUndoToast({message: 'Marked Spacer rano complete', undoFn: () => {}})`; assert `document.body.firstChild.className.includes('toast')`; assert `body.firstChild.textContent.includes('Marked Spacer rano complete')`; assert `body.firstChild.textContent.includes('Undo')`; assert `body.firstChild.textContent.includes('×')`.
        - Test "auto-dismisses after 5 seconds": install fake clock; `showUndoToast(...)`; assert toast mounted; `clock.tick(4999)`; assert still mounted; `clock.tick(2)`; assert toast removed (`document.body.firstChild === null`).
        - Test "hover pauses the timer": `showUndoToast(...)`; `clock.tick(2000)`; dispatch a synthetic `pointerenter` on the toast el; `clock.tick(10_000)`; assert still mounted; dispatch `pointerleave`; `clock.tick(4999)`; assert still mounted; `clock.tick(2)`; assert removed.
        - Test "Undo click invokes undoFn and dismisses the toast": create a spy `let called = 0; const undoFn = () => { called++; };`; `showUndoToast({message: 'x', undoFn})`; find the `.toast-action` button; `actionBtn.click()`; assert `called === 1`; assert toast removed.
        - Test "second showUndoToast replaces the first (D-70 single-toast)": `showUndoToast({message: 'first', undoFn: () => {}})`; `showUndoToast({message: 'second', undoFn: () => {}})`; assert `document.body.children.length === 1`; assert visible message is `'second'`; assert the first toast's timer is no longer scheduled.
        - Test "undoFn throw shows error toast": `showUndoToast({message: 'x', undoFn: () => { throw new Error('boom'); }})`; click the action button; assert the now-mounted toast has class `'toast toast--error'` and message `"Couldn't undo — try again"`.
      - `describe('showErrorToast — D-73')`:
        - Test "renders with error variant + auto-dismisses": `showErrorToast("Couldn't mark — try again")`; assert `className === 'toast toast--error'`; install clock; `clock.tick(4001)`; assert removed.
        - Test "no action button": `showErrorToast('x')`; assert `document.querySelector('.toast-action') === null`.
      - `describe('XSS-safety')`:
        - Test "no .innerHTML in showUndoToast / showErrorToast / showUpdateToast": read the toast.js source; assert it does not match `/\.(innerHTML|outerHTML|insertAdjacentHTML)\b/` (cheap belt-and-suspenders even though `tests/unit/discipline.xss.test.js` covers it).

      Run `node --test tests/unit/toast.test.js` — every new test fails (functions don't exist, `_showToast` doesn't exist, D-08 regression test passes trivially against P1 code).

    GREEN — Step 2: Edit `js/views/toast.js`:
      - Extend the file header JSDoc: cross-reference D-69, D-70, D-71, D-73; preserve the D-08 paragraph verbatim; add a one-line "Forbidden constructs" footer matching Pattern S1.
      - Add `let dismissTimer = null;` next to `let toastEl = null;`.
      - Add internal `function _dismissToast()` per the behavior spec.
      - Add internal `function _showToast({message, action, autoDismissMs, variant})` per the behavior spec. Use `document.createElement` + `textContent` + `setAttribute` exclusively (D-77 / D-78).
      - Rewrite `showUpdateToast` body to delegate to `_showToast({message: 'New version ready', action: {label: 'Reload', fn: () => location.reload()}})` — note: NO `autoDismissMs` (LOCKED per D-08). Keep the idempotent guard `if (toastEl && toastEl.dataset.variant === 'update') return;` so a second `showUpdateToast()` while one is mounted is a no-op (preserves P1 behavior). Tag the wrapper `toastEl.dataset.variant = 'update';` inside `_showToast` when `variant === 'update'` OR when called from `showUpdateToast` (cleanest: pass `variant: 'update'` from `showUpdateToast`'s delegation; the discipline test then asserts via dataset).
        - Alternative simpler shape: keep `showUpdateToast` as it was (its DOM lines are short and the no-auto-dismiss is structurally enforced by NOT delegating into `_showToast`). The discipline test then asserts `showUpdateToast`'s source code does not call `_showToast(...)` with `autoDismissMs`. Pick whichever shape keeps the regression test cheap.
      - Add `export function showUndoToast({message, undoFn, autoDismissMs = 5000})` per the behavior spec. Wrap `undoFn` invocation in `try { const r = undoFn(); if (r && typeof r.then === 'function') r.catch(_handleUndoError); } catch (e) { _handleUndoError(e); }` where `_handleUndoError = () => showErrorToast("Couldn't undo — try again")`.
      - Add `export function showErrorToast(message)` per the behavior spec.
      - Add `export function _resetToastForTest()` per the behavior spec.

    REFACTOR — Step 3: Run `node --test tests/unit/toast.test.js` — every test green. Run `node --test tests/unit/discipline.xss.test.js` — green. Run the full unit suite `node --test tests/unit/` — green.
  </action>
  <verify>
    <automated>node --test tests/unit/toast.test.js</automated>
    <automated>node --test tests/unit/discipline.xss.test.js</automated>
    <automated>node --test tests/unit/</automated>
  </verify>
  <done>
    `js/views/toast.js` exports `showUpdateToast` (unchanged contract — no auto-dismiss), `showUndoToast({message, undoFn, autoDismissMs=5000})`, `showErrorToast(message)`, `_resetToastForTest()`. The internal `_showToast` helper centralizes DOM construction. `tests/unit/toast.test.js` covers the D-08 regression + D-69 + D-70 + D-71 + D-73 contracts. Discipline tests still green.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Wire showUndoToast from Today mark/unmark success path (D-71 verb+habit copy) — TDD integration</name>
  <files>js/views/today.js, tests/integration/toast.undo.test.js, tests/integration/today.undo.test.js</files>
  <read_first>
    - js/views/today.js (after 03-03 — `handleMarkCompleteTap`, `handleMarkUncompleteTap`, the `// 03-04: replace with showErrorToast('Couldn\\'t mark — try again')` comment in the catch block)
    - js/state/undo.js (existing P2 `undo()` — returns the most recent event or null if `meta.undoToken` empty)
    - js/state/apply.js (the `notify({event, keys})` call that fires after every successful tx — confirms timing of cache refresh)
    - js/state/store.js (after 03-03 Task 2 — `notify` is async-aware and refreshes cache before subscribers)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md decisions D-68, D-71, D-72, D-73
    - tests/integration/today.tap.test.js (the 03-03 integration test — mirror its fake-document + paired-cache-bust setup)
    - tests/helpers/fake-document.js (button `.click()` walks `_listeners.get('click')`)
  </read_first>
  <behavior>
    Toast wiring inside `handleMarkCompleteTap` and `handleMarkUncompleteTap`:
    - On `apply()` resolve: read `habit.name` from the cache via `store.getCachedHabits().find(h => h.id === habitId)?.name ?? '(habit)'`; build copy per D-71:
      - markCompleted success → `\`Marked ${habit.name} complete\`` (no trailing punctuation in the message; the toast appends ` — Undo · ×` visually via the action + close buttons).
      - markUncompleted success → `\`Marked ${habit.name} uncomplete\``.
    - Call `showUndoToast({message, undoFn: () => undo()})`. The `undo` import comes from `js/state/undo.js`.
    - On `apply()` reject: call `showErrorToast("Couldn't mark — try again")` AND `revertRow(rowEl, priorState)` (already present from 03-03). The `console.warn('[today] markCompleted failed', err)` placeholder is REMOVED in this slice; replace with the `showErrorToast` call. Add a single `// D-53 + D-73: user-visible error feedback on apply() reject` comment in the catch block.
    - Habit-name lookup MUST be performed AFTER `apply()` resolves (the cache is fresh post-notify per 03-03 Task 2). Pre-tap habit-name lookup risks reading a habit whose name was edited cross-tab between tap and toast render — small but real per Pitfall 2.

    Edge cases:
    - When the cache miss happens (e.g. habit row was archived cross-tab during the tap), the message falls back to `\`Marked (habit) complete\`` — graceful degradation, no throw.
    - When `undo()` itself returns `null` at the moment of the Undo click (rare race: the user took two actions, then clicks Undo on the first toast — but D-70 already removed the first toast when the second was mounted, so this is unreachable in practice). The toast code already swallows this silently per D-73.
    - When `undo()` throws (IDB-level failure), `js/views/toast.js`'s catch chain shows `showErrorToast("Couldn't undo — try again")` (Task 1).

    Integration test `tests/integration/toast.undo.test.js`:
    - Pattern S7 paired-cache-bust import of `js/state/apply.js`, `js/state/undo.js`, `js/state/store.js`, `js/views/toast.js` with the SAME query tag.
    - Seed habit `{id: 'h1', name: 'Drink water', cadence: {type: 'daily'}}`; configure apply with fake repo + fake broadcast.
    - Test "Undo button click invokes undo() and reverts the log row":
      - `await apply({type: 'markCompleted', payload: {habitId: 'h1', date: todayLocal()}})`.
      - Assert log row written: `repo._stores.logs.get(JSON.stringify(['h1', todayLocal()]))` is `{completed: true, ...}`.
      - Call `showUndoToast({message: 'Marked Drink water complete', undoFn: () => undo()})`.
      - Find `.toast-action` button on the fake doc; `actionBtn.click()`.
      - Await one microtask tick.
      - Assert: the log row is now the prior state (in this test, undefined — no prior row existed before the markCompleted).
      - Assert: toast is removed (single-toast invariant + action-click dismiss).

    Integration test `tests/integration/today.undo.test.js`:
    - Pattern S7 + the `freshTodayView()` import shape from 03-03's `tests/integration/today.tap.test.js`.
    - Mount Today onto fake doc; tap a row; assert the toast appears with message `"Marked Spacer rano complete"` (or whatever the seeded habit name is); click the toast's Undo button; await tick; assert the row's `aria-pressed === 'false'` (reverted via store.subscribe re-render).
    - Test "apply() reject surfaces showErrorToast (not console.warn)":
      - Configure apply with a `brokenRepo` whose `runTx` rejects.
      - Tap a row; await one tick; assert:
        - A toast with class `toast toast--error` is mounted with message `"Couldn't mark — try again"`.
        - The row's `aria-pressed === 'false'` (reverted via 03-03's `revertRow`).
        - No `console.warn` was called (use a stub: `const originalWarn = console.warn; let warnCalls = 0; console.warn = () => { warnCalls++; }; try { ... } finally { console.warn = originalWarn; } assert.equal(warnCalls, 0);`).
  </behavior>
  <action>
    RED — Step 1: Create `tests/integration/toast.undo.test.js` per the behavior spec.
    Create `tests/integration/today.undo.test.js` per the behavior spec.
    Run `node --test tests/integration/toast.undo.test.js tests/integration/today.undo.test.js` — every test fails (today.js still uses `console.warn`).

    GREEN — Step 2: Edit `js/views/today.js`:
      - Add `import {showUndoToast, showErrorToast} from './toast.js';` (alongside the existing `import {apply} from '../state/apply.js'`).
      - Add `import {undo} from '../state/undo.js';`.
      - Inside `handleMarkCompleteTap` AFTER `await apply(...)` resolves, before the function returns:
        - `const habitsCached = (await import('../state/store.js')).getCachedHabits();` — OR if the view already has a `store` ref in scope from `mountToday(parent, {repo, store})`, use it directly. Prefer the existing scope handle (less coupling).
        - `const habitName = habitsCached.find(h => h.id === habitId)?.name ?? '(habit)';`
        - `showUndoToast({message: \`Marked ${habitName} complete\`, undoFn: () => undo()});`
      - Mirror in `handleMarkUncompleteTap` with `\`Marked ${habitName} uncomplete\``.
      - In BOTH catch blocks: replace the `console.warn('[today] markCompleted failed', err)` line with `showErrorToast("Couldn't mark — try again");` AND keep the existing `revertRow(rowEl, priorState);` call. Add a single `// D-53 + D-73: user-visible error feedback on apply() reject` comment.
      - Remove the `// 03-04: replace with showErrorToast(...)` planted comment (its replacement is now the actual call).
      - Update the file-header JSDoc to cross-reference D-71, D-73.

    REFACTOR — Step 3: Run `node --test tests/integration/today.undo.test.js tests/integration/toast.undo.test.js tests/integration/today.tap.test.js` — all green. Run full suite `node --test tests/` — green.
  </action>
  <verify>
    <automated>node --test tests/integration/toast.undo.test.js</automated>
    <automated>node --test tests/integration/today.undo.test.js</automated>
    <automated>node --test tests/integration/today.tap.test.js</automated>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    `js/views/today.js` imports `showUndoToast` + `showErrorToast` + `undo`. Mark/unmark success paths fire a toast with D-71 verb+habit copy. Mark/unmark reject paths call `showErrorToast("Couldn't mark — try again")` (the 03-03 `console.warn` placeholder is gone). The two new integration tests pass. Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| habit-name read at toast-time → toast DOM | The cached habit row crosses from store cache into a user-visible string; XSS-safety via `textContent` (D-77) keeps the boundary safe |
| undoFn closure → undo() side-effects | Toast click handler crosses into the chokepoint write path; the closure is the only place the toast-side touches IDB |
| auto-dismiss timer → DOM removal | The setTimeout callback removes the toast el; race with a second `showUndoToast()` already handled by D-70's replace-on-new + `clearTimeout` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-20 | Tampering | habit.name with embedded HTML (`<script>`) leaks into toast | mitigate | Every `message` flows into `<span>.textContent`; no `.innerHTML`. Discipline test `tests/unit/discipline.xss.test.js` (D-78) catches accidental regressions; unit test in this plan additionally greps `toast.js` source. |
| T-03-21 | Tampering | D-08 regression — `showUpdateToast` accidentally takes `autoDismissMs` and quietly removes itself before the user clicks Reload | mitigate | `tests/unit/toast.test.js` regression test ticks 60s of fake clock after `showUpdateToast()` and asserts the toast is still mounted. PLUS: a source-grep assertion that `showUpdateToast`'s body does not pass `autoDismissMs` to `_showToast`. |
| T-03-22 | Repudiation | Undo button on a stale toast undoes the wrong action | mitigate | D-70 single-toast invariant: every new `showUndoToast` clears the prior toast + its timer BEFORE mounting. The `undoFn` closure always points at `() => undo()`, which always operates on the current `meta.undoToken` — i.e. the most recent event. Race-free by construction. |
| T-03-23 | Denial of Service | rapid taps spam toasts | mitigate | D-70 single-toast + the apply-chokepoint mutex (T-02 series) bound the worst case to one toast per tap; each new tap replaces the prior. Timer is cleared and re-armed atomically; no leaked timers. |
| T-03-24 | Information Disclosure | error toast leaks IDB error details | accept | `showErrorToast("Couldn't mark — try again")` is a fixed string; no `err.message` interpolation. Stack traces stay in the dev console (not in this plan — the `console.warn` is REMOVED) — only general-user prose surfaces. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | NO package installs in this plan. Zero supply-chain surface. |
</threat_model>

<verification>
- `node --test tests/` exits 0 (full suite green including 03-01..03-03's tests)
- `js/views/toast.js` exports `showUpdateToast` (signature unchanged), `showUndoToast({message, undoFn, autoDismissMs?})`, `showErrorToast(message)`, `_resetToastForTest`
- `js/views/today.js` no longer contains `console.warn('[today]` literal (placeholder removed)
- `js/views/today.js` imports `showUndoToast` + `showErrorToast` from `./toast.js` and `undo` from `../state/undo.js`
- `tests/unit/toast.test.js` covers D-08 regression + D-69 + D-70 + D-71 + D-73
- `tests/integration/toast.undo.test.js` proves Undo click → `undo()` → log row reverted
- `tests/integration/today.undo.test.js` proves end-to-end tap → toast → Undo → row flips back
- `tests/unit/discipline.xss.test.js` still passes (no `.innerHTML`-family tokens introduced)
</verification>

<success_criteria>
- Phase requirements covered: UNDO-01 (toast surface), UNDO-02 (single-step undo wired to `meta.undoToken` via `undo()`), UNDO-03 (single-step model — D-70 single-toast guarantees "most recent only")
- D-decisions implemented: D-08 (regression-guarded, NOT touched), D-68 (toast surface ships in P3), D-69 (5s auto-dismiss + hover-pause), D-70 (single toast, replace on new), D-71 (verb+habit-name copy), D-73 (undo + mark error variants)
- 03-03's `console.warn` placeholder is REMOVED — `showErrorToast` is the user-visible feedback for apply() reject
- All new tests pass; existing 03-01..03-03 tests stay green
</success_criteria>

<output>
Create `.planning/phases/03-today-view-settings-v1-first-usable-slice/03-04-SUMMARY.md` when done. Summary MUST include: files modified, test files added, new toast exports listed by name, and a one-line note "Undo toast surface is live on Today. Slice 5 adds the second undo surface (Settings Data card) + the rest of Settings v1."
</output>
