---
phase: 03-today-view-settings-v1-first-usable-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - js/domain/cadence.js
  - js/domain/wave.js
  - seed/waves.json
  - js/util/mount.js
  - js/util/date.js
  - js/db/repo.js
  - tests/helpers/fake-idb.js
  - tests/unit/cadence.test.js
  - tests/unit/date.weekly.test.js
  - tests/unit/mount.test.js
  - tests/unit/wave.test.js
  - tests/unit/discipline.xss.test.js
  - tests/integration/repo.range.test.js
autonomous: true
requirements:
  - CORE-04
  - CORE-05
  - NFR-01
  - NFR-07
must_haves:
  truths:
    - "Pure cadence resolver `appliesToday(habit, date, ctx)` answers correctly for daily, weekly, every-n-days, day-of-week-subset (D-48, CORE-04)"
    - "Weekly cadence hides a habit after the same week already has a completed log (D-49)"
    - "Every-n-days cadence anchors on `habit.lastCompletedDate` and falls back to `habit.createdAt` when null (D-50)"
    - "`currentWave(date)` returns the highest-numbered wave whose `startDate <= date` (D-56, CORE-05)"
    - "`mount(desc, parent, actions)` builds DOM via `textContent` + `setAttribute` only — never `.innerHTML` / `.outerHTML` / `.insertAdjacentHTML` / `document.write` (D-77, D-78)"
    - "XSS discipline test fails if any forbidden HTML-injection sink appears in `js/` outside an empty allowlist (D-78)"
    - "`repo.getAllHabits()` returns every habit row; `repo.getLogsInRange(startYMD, endYMD)` returns logs whose `date` falls in `[start, end]` (NFR-01 enables single bounded read on cold-paint)"
    - "Fake-IDB exposes the same `getAllHabits` / `getLogsInRange` surface (A7 contract preserved)"
    - "`util/date.js` exports `isoWeekStart`, `isoWeekEnd`, `daysBetween`, `formatRelative` with DST-safe arithmetic"
  artifacts:
    - path: "js/domain/cadence.js"
      provides: "Pure resolver `appliesToday(habit, date, ctx)` dispatching via RESOLVERS table"
      exports: ["appliesToday"]
    - path: "js/domain/wave.js"
      provides: "In-memory wave catalog from seed/waves.json"
      exports: ["bootWaves", "currentWave", "getWave", "getAllWaves", "configureWave"]
    - path: "seed/waves.json"
      provides: "10-entry wave catalog (Wave 0..9) with startDate per wave"
      contains: "schemaVersion: 1"
    - path: "js/util/mount.js"
      provides: "Single trusted DOM-construction helper"
      exports: ["mount"]
    - path: "js/util/date.js"
      provides: "Extended with isoWeekStart, isoWeekEnd, daysBetween, formatRelative"
      exports: ["todayLocal", "formatLocalYMD", "parseLocalYMD", "daysFrom", "isoWeekStart", "isoWeekEnd", "daysBetween", "formatRelative"]
    - path: "js/db/repo.js"
      provides: "Extended with getAllHabits + getLogsInRange"
      exports: ["getAllHabits", "getLogsInRange"]
    - path: "tests/unit/discipline.xss.test.js"
      provides: "Grep gate enforcing D-78"
    - path: "tests/unit/cadence.test.js"
      provides: "DST/leap-day/cadence-type coverage"
    - path: "tests/integration/repo.range.test.js"
      provides: "Fake-IDB roundtrip for getAllHabits + getLogsInRange"
  key_links:
    - from: "js/domain/cadence.js"
      to: "js/util/date.js"
      via: "imports parseLocalYMD, isoWeekStart, isoWeekEnd, daysBetween"
      pattern: "from '.*util/date.js'"
    - from: "js/domain/wave.js"
      to: "seed/waves.json"
      via: "fetch('./seed/waves.json') inside bootWaves()"
      pattern: "fetch\\('?\\./seed/waves\\.json"
    - from: "js/db/repo.js"
      to: "js/db/idb.js"
      via: "imports getAll, indexGetAll for the new methods"
      pattern: "import .* from '\\./idb\\.js'"
---

<objective>
Land the pure-domain foundations Slice 2 (Today renders) consumes: a cadence resolver covering the four cadence types in `seed/habits.json`, an in-memory wave catalog loaded from a new `seed/waves.json`, two new repo reads (`getAllHabits`, `getLogsInRange`) so the Today hydrate path is a single bounded read, the `mount()` XSS-safe DOM helper, four new date utilities, and the grep-gate discipline test that enforces D-78. No UI, no view code, no chokepoint changes — pure modules + tests.

Purpose: De-risk Slice 2 by landing every function the view will call BEFORE the view exists. Each module here is unit-testable in Node with no DOM polyfill (D-26 Tier 1 + the new mount.js needs a fake-document Tier-1-adjacent test).
Output: 7 new code files, 2 modified code files, 5 new test files, 1 modified test file. Test suite must remain green at end of plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md
@.planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend util/date.js with ISO-week + relative-time helpers (TDD)</name>
  <files>js/util/date.js, tests/unit/date.weekly.test.js</files>
  <read_first>
    - js/util/date.js (existing exports: todayLocal, formatLocalYMD, parseLocalYMD, daysFrom)
    - tests/unit/date.test.js (D-26 Tier 1 fixture-driven style with concrete DST/leap dates — Pattern S8)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md section "js/util/date.js (modification — add 4 helpers)"
  </read_first>
  <behavior>
    - `isoWeekStart('2026-05-28', 'mon')` returns `'2026-05-25'` (Mon of that week)
    - `isoWeekStart('2026-05-28', 'sun')` returns `'2026-05-24'` (Sun of that week)
    - `isoWeekStart('2026-05-31', 'mon')` returns `'2026-05-25'` (Sun rolls back 6 days)
    - `isoWeekStart('2026-03-29', 'mon')` returns `'2026-03-23'` (DST spring-forward week)
    - `isoWeekEnd('2026-05-25', 'mon')` returns `'2026-05-31'` (Sun, 6 days later)
    - `isoWeekEnd('2026-03-23', 'mon')` returns `'2026-03-29'` (DST spring-forward Sunday)
    - `daysBetween('2026-05-25', '2026-05-28')` returns `3`
    - `daysBetween('2026-05-28', '2026-05-25')` returns `-3`
    - `daysBetween('2026-03-28', '2026-03-30')` returns `2` across DST spring-forward (uses Math.round, NOT floor)
    - `daysBetween('2026-10-24', '2026-10-26')` returns `2` across DST fall-back
    - `formatRelative(now - 30_000, now)` returns `'just now'` (< 1 minute)
    - `formatRelative(now - 120_000, now)` returns `'2 minutes ago'`
    - `formatRelative(now - 7_200_000, now)` returns `'2 hours ago'`
    - `formatRelative(now - 172_800_000, now)` returns `'2 days ago'`
    - `formatRelative` accepts an ISO timestamp string (e.g. `'2026-05-28T10:00:00.000Z'`)
  </behavior>
  <action>
    RED: Create `tests/unit/date.weekly.test.js` using the per-fixture `describe`/`test` block style from `tests/unit/date.test.js`. Cover every behavior listed above; verify all tests fail (module exports do not exist yet) with `node --test tests/unit/date.weekly.test.js`.

    GREEN: Add four exports to `js/util/date.js`:
      - `isoWeekStart(ymd, weekStart)` — `parseLocalYMD` + compute `back` (`weekStart === 'mon' ? (dow === 0 ? 6 : dow - 1) : dow`) + `setDate(d.getDate() - back)` + `formatLocalYMD` (DST-safe per existing date.js discipline)
      - `isoWeekEnd(ymd, weekStart)` — `daysFrom(isoWeekStart(ymd, weekStart), 6)`
      - `daysBetween(aYMD, bYMD)` — `Math.round((parseLocalYMD(b) - parseLocalYMD(a)) / 86400000)` (Math.round, NOT floor — DST hour drift)
      - `formatRelative(atISO, nowMs = Date.now())` — `ms = nowMs - new Date(atISO).getTime()`; branches `< 60_000 → 'just now'`, `< 3_600_000 → '${Math.floor(ms/60_000)} minutes ago'`, `< 86_400_000 → '${Math.floor(ms/3_600_000)} hours ago'`, else `'${Math.floor(ms/86_400_000)} days ago'`

    Each new export gets a JSDoc block per D-27 with `@param` and `@returns`. Re-add the forbidden-constructs warning to the file header noting `Math.floor` is forbidden in `daysBetween`.

    REFACTOR (if needed): Extract `_dowIndex(weekStart, dow)` helper if both `isoWeekStart` and a future caller need it.
  </action>
  <verify>
    <automated>node --test tests/unit/date.weekly.test.js</automated>
    <automated>node --test tests/unit/date.test.js</automated>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    All new date helpers exported with JSDoc. `tests/unit/date.weekly.test.js` passes. Existing `tests/unit/date.test.js` still passes (no regression). Full suite remains green.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Create XSS discipline test (D-78) + mount.js helper (TDD)</name>
  <files>tests/unit/discipline.xss.test.js, tests/unit/mount.test.js, js/util/mount.js</files>
  <read_first>
    - tests/unit/apply.discipline.test.js (Pattern S6 — `readStripped` + `jsFilesIn` template; lines 21-67)
    - js/views/toast.js (Pattern S2 — XSS-safe DOM construction)
    - js/views/diagnostics.js lines 232-244 (appendRow pattern)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md section "js/util/mount.js" and "Pattern S6"
    - tests/helpers/fake-document.js (existing fake-document for mount-unit testing)
  </read_first>
  <behavior>
    Discipline test (`tests/unit/discipline.xss.test.js`):
    - Forbidden regex: `/\.(innerHTML|outerHTML|insertAdjacentHTML)\b|document\.write\s*\(/`
    - Scope: every `.js` file under `js/` (recursive via `jsFilesIn`)
    - Strip block + line comments via `readStripped` before matching
    - Allowlist: empty (zero exceptions)
    - When zero violations: test passes
    - When any forbidden token present: test fails listing every offending path

    `js/util/mount.js` `mount(desc, parent, actions)`:
    - `mount('hello world', parent, {})` appends a text node with content `'hello world'`
    - `mount({tag: 'div'}, parent, {})` appends a `<div>` with no children
    - `mount({tag: 'div', text: 'hello'}, parent, {})` appends `<div>hello</div>` (textContent, never innerHTML)
    - `mount({tag: 'button', attrs: {'aria-label': 'Dismiss'}}, parent, {})` sets aria-label via setAttribute
    - `mount({tag: 'button', attrs: {'data-action': 'markComplete'}}, parent, {markComplete: fn})` attaches click listener calling `fn` and retains the `data-action` attribute
    - `mount({tag: 'ul', children: [{tag: 'li', text: 'a'}, {tag: 'li', text: 'b'}]}, parent, {})` appends a `<ul>` with two `<li>` children
    - Returns the constructed element (or `undefined` for string children)
  </behavior>
  <action>
    RED — discipline test FIRST (so subsequent slices fail fast on any HTML-injection sink):
    Create `tests/unit/discipline.xss.test.js` using the Pattern S6 template:
      - Import `readFileSync`, `existsSync`, `readdirSync`, `statSync` from `node:fs`; `join` from `node:path`; `fileURLToPath` from `node:url`; `test`, `describe` from `node:test`; `assert` from `node:assert/strict`.
      - `ROOT = fileURLToPath(new URL('../../', import.meta.url))`
      - Inline `readStripped(path)` and `jsFilesIn(dir)` (copy verbatim from `tests/unit/apply.discipline.test.js`).
      - Single `describe('discipline: XSS-safe DOM construction (D-78)', ...)` with one `test`: iterate `jsFilesIn(join(ROOT, 'js'))`, match against `/\.(innerHTML|outerHTML|insertAdjacentHTML)\b|document\.write\s*\(/`, push violations, `assert.deepEqual(violations, [])`.

    Run `node --test tests/unit/discipline.xss.test.js` — verify it currently PASSES (the existing codebase already follows XSS discipline; this is the lock-in).

    RED — mount unit test:
    Create `tests/unit/mount.test.js` using `tests/helpers/fake-document.js`'s `createFakeDocument()` helper (if signature differs from expectation, read fake-document.js first and adapt). Test every behavior listed in the behavior block; verify all tests fail initially (no mount.js exists).

    GREEN: Create `js/util/mount.js`:
      - JSDoc file header per D-27 referencing D-77 + D-78 + Pattern S5.
      - Single export `function mount(desc, parent, actions = {})`:
        - if `typeof desc === 'string'` → `parent.appendChild(document.createTextNode(desc))`; return undefined.
        - Otherwise: `const el = document.createElement(desc.tag)`.
        - For each entry in `desc.attrs`: when `k === 'data-action'`, look up `actions[v]` and `el.addEventListener('click', fn)` if present, then `el.setAttribute(k, v)`; otherwise `el.setAttribute(k, String(v))`.
        - If `desc.text !== undefined`: `el.textContent = String(desc.text)`.
        - For each `c` in `desc.children ?? []`: recurse `mount(c, el, actions)`.
        - `parent.appendChild(el)`; return `el`.
      - Make `document` resolution Node-safe: read from `globalThis.document` if present. Tests inject a fake document via a configurable seam (`configureMount({document})`) OR by passing a parent whose `ownerDocument` provides `createElement`/`createTextNode`. Pick the latter — keep `mount()` a pure function of `parent.ownerDocument`. Update tests accordingly.

    REFACTOR: ensure no `.innerHTML`/`.outerHTML`/`.insertAdjacentHTML`/`document.write` anywhere in `mount.js`. Run the new discipline test — it must still pass.
  </action>
  <verify>
    <automated>node --test tests/unit/discipline.xss.test.js</automated>
    <automated>node --test tests/unit/mount.test.js</automated>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    `tests/unit/discipline.xss.test.js` passes against the existing codebase. `js/util/mount.js` exists with sole export `mount`. `tests/unit/mount.test.js` passes all 7 behavior cases. Full suite green.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Create domain/cadence.js with 4-cadence-type resolver (TDD)</name>
  <files>js/domain/cadence.js, tests/unit/cadence.test.js</files>
  <read_first>
    - seed/habits.json (the 4 cadence shapes shipped: daily, weekly, every-n-days, day-of-week-subset)
    - js/util/date.js (after Task 1 — uses isoWeekStart, isoWeekEnd, daysBetween, parseLocalYMD)
    - tests/unit/date.test.js (Pattern S8 — DST/leap-day fixtures)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md section "js/domain/cadence.js"
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md decisions D-48, D-49, D-50, D-51
  </read_first>
  <behavior>
    Signature: `appliesToday(habit, date, ctx)` — `habit` is the IDB row; `date` is YYYY-MM-DD; `ctx = {weekStart: 'mon'|'sun', weekCompletions: (habitId, startYMD, endYMD) => number}`.

    daily:
    - `appliesToday({cadence:{type:'daily'}}, '2026-05-28', {weekStart:'mon', weekCompletions: () => 0})` returns `true`
    - daily ignores `ctx` entirely

    weekly (D-49 log-aware):
    - Returns `true` when `ctx.weekCompletions(habitId, isoWeekStart, isoWeekEnd) === 0`
    - Returns `false` when `weekCompletions >= 1`
    - On 2026-05-28 with weekStart 'mon', the resolver MUST call `ctx.weekCompletions(habitId, '2026-05-25', '2026-05-31')`
    - On 2026-05-28 with weekStart 'sun', the resolver MUST call `ctx.weekCompletions(habitId, '2026-05-24', '2026-05-30')`

    every-n-days (D-50):
    - With `habit.lastCompletedDate = '2026-05-26'` and `cadence.n = 2`, on `'2026-05-28'` returns `true` (daysBetween === 2 >= n)
    - With `habit.lastCompletedDate = '2026-05-27'` and `cadence.n = 2`, on `'2026-05-28'` returns `false` (daysBetween === 1 < n)
    - With `habit.lastCompletedDate = null` and `habit.createdAt = '2026-05-20'` and `cadence.n = 2`, on `'2026-05-28'` returns `true` (fallback to createdAt)
    - With `habit.lastCompletedDate = null` and `habit.createdAt = '2026-05-27'` and `cadence.n = 2`, on `'2026-05-28'` returns `false`
    - With both `lastCompletedDate` AND `createdAt` undefined, returns `true` (safe default — never hide for missing anchor)

    day-of-week-subset:
    - With `cadence.days = ['mon','wed','fri']` on `'2026-05-25'` (Mon) returns `true`
    - On `'2026-05-26'` (Tue) returns `false`
    - On `'2026-05-27'` (Wed) returns `true`
    - On `'2026-05-29'` (Fri) returns `true`
    - On `'2026-05-31'` (Sun) returns `false`
    - Uses local-calendar `getDay()` via `parseLocalYMD(date).getDay()` (no UTC)

    DST/leap-day fixtures:
    - daily on `'2026-03-29'` (DST spring-forward) returns `true`
    - day-of-week-subset on `'2028-02-29'` (leap day = Tuesday) with `cadence.days = ['tue']` returns `true`
    - day-of-week-subset on `'2026-10-25'` (DST fall-back = Sunday) with `cadence.days = ['sun']` returns `true`

    Unknown cadence type:
    - `appliesToday({cadence:{type:'unknown'}}, ...)` THROWS `Error('cadence: unknown type unknown')`

    Module discipline:
    - No `.innerHTML` / `.outerHTML` / `.insertAdjacentHTML` / `document.write` (the new D-78 discipline test covers this).
    - No `switch (` statement on `habit.cadence.type` — dispatch via a `RESOLVERS` table (Anti-Pattern 4; planner extends this rule to cadence per CONTEXT.md `<specifics>`).
    - No `indexedDB.*` / `repo.*` calls — pure module.
  </behavior>
  <action>
    RED: Create `tests/unit/cadence.test.js` using `tests/unit/date.test.js`'s per-fixture `describe`/`test` style (Pattern S8). Every behavior case in the behavior block becomes a separate `test('...', () => assert.equal(appliesToday(habit, date, ctx), expected))`. Add one final discipline test (or add to `tests/unit/discipline.xss.test.js` separately):
      - `tests/unit/cadence.test.js` ends with a `describe('discipline: cadence.js dispatches via RESOLVERS (Anti-Pattern 4)', () => { ... })` block reading the file source with the same `readStripped` helper (copy from `apply.discipline.test.js`), asserting `src.includes('RESOLVERS')` and `src.match(/\bswitch\s*\(/) === null`.
    Run `node --test tests/unit/cadence.test.js` — every test fails (no module exists).

    GREEN: Create `js/domain/cadence.js`:
      - JSDoc file header per D-27 referencing D-48..D-52, naming pure-module discipline + forbidden constructs (no `switch`, no IDB/repo calls).
      - Import `parseLocalYMD`, `isoWeekStart`, `isoWeekEnd`, `daysBetween` from `'../util/date.js'`.
      - Define `const DOW = ['sun','mon','tue','wed','thu','fri','sat']` (matches `getDay()` index).
      - Define `const RESOLVERS = { ... }` with four entries:
        - `daily: () => true`
        - `weekly: (h, d, ctx) => ctx.weekCompletions(h.id, isoWeekStart(d, ctx.weekStart), isoWeekEnd(d, ctx.weekStart)) === 0`
        - `'every-n-days': (h, d) => { const anchor = h.lastCompletedDate ?? h.createdAt; if (!anchor) return true; return daysBetween(anchor, d) >= h.cadence.n; }`
        - `'day-of-week-subset': (h, d) => h.cadence.days.includes(DOW[parseLocalYMD(d).getDay()])`
      - Export `function appliesToday(habit, date, ctx)`:
        - `const resolver = RESOLVERS[habit.cadence.type]`
        - if `!resolver` → `throw new Error(\`cadence: unknown type ${habit.cadence.type}\`)`
        - return `resolver(habit, date, ctx)`
      - Add JSDoc to the export per D-27.

    REFACTOR: confirm zero `switch` keyword, zero `.innerHTML`-family tokens; cadence.test discipline assertion passes.
  </action>
  <verify>
    <automated>node --test tests/unit/cadence.test.js</automated>
    <automated>node --test tests/unit/discipline.xss.test.js</automated>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    `js/domain/cadence.js` exports `appliesToday` and dispatches via `RESOLVERS` table (no `switch`). All 18+ cadence behavior tests pass. Discipline assertions (RESOLVERS present, no switch, no XSS sinks) pass. Full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create seed/waves.json + js/domain/wave.js (boot-time loader)</name>
  <files>seed/waves.json, js/domain/wave.js, tests/unit/wave.test.js</files>
  <read_first>
    - seed/habits.json (wrapped-object shape with schemaVersion/seedVersion)
    - js/io/seed.js (configure + bootSeed pattern with fetch DI — Pattern matches D-25 testability)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-PATTERNS.md section "js/domain/wave.js" and "seed/waves.json"
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md decisions D-56, D-57, and `<specifics>` Wave-4-currency note
  </read_first>
  <action>
    Create `seed/waves.json` with this exact content (the seed file is the canonical fact about wave dates):
      ```
      {
        "schemaVersion": 1,
        "seedVersion": 1,
        "waves": [
          { "number": 0, "name": "Wave 0", "startDate": "2025-12-29", "theme": "Pre-start onboarding" },
          { "number": 1, "name": "Wave 1", "startDate": "2026-01-05", "theme": "Energy & regulation" },
          { "number": 2, "name": "Wave 2", "startDate": "2026-01-19", "theme": "Daily rhythm & eating" },
          { "number": 3, "name": "Wave 3", "startDate": "2026-02-02", "theme": "Movement & body" },
          { "number": 4, "name": "Wave 4", "startDate": "2026-04-27", "theme": "Relationships & emotions" },
          { "number": 5, "name": "Wave 5", "startDate": "2026-06-15", "theme": "Focus & deep work" },
          { "number": 6, "name": "Wave 6", "startDate": "2026-08-03", "theme": "Health & recovery" },
          { "number": 7, "name": "Wave 7", "startDate": "2026-09-21", "theme": "Creativity & expression" },
          { "number": 8, "name": "Wave 8", "startDate": "2026-11-09", "theme": "Stewardship & purpose" },
          { "number": 9, "name": "Wave 9", "startDate": "2026-12-28", "theme": "Integration & rest" }
        ]
      }
      ```
    Note: Wave 4 startDate is `2026-04-27`, chosen so `currentWave('2026-05-28')` returns Wave 4 (per `<specifics>` line "Wave 4 is current as of 2026-05-28"). The 7-week increments above are illustrative — the planner picked them so each wave dominates ~7 weeks before the next starts, leaving Wave 4 as `current` from late April through mid-June. Do NOT spread them at 1-per-week (Pattern guidance shows Wave 4 starting `2026-01-26`, which would make Wave 9 current today — that contradicts the user's stated `<specifics>`).

    Create `js/domain/wave.js` (per D-57 — in-memory only, no IDB):
      - JSDoc file header per D-27 referencing D-56, D-57, and explaining "no IDB persistence in P3 — P4 promotes to a `waves` store when WAVE-06 needs user-extensibility".
      - Module-level `let _waves = []` and `let _fetch = null` (configure-based DI mirroring `js/io/seed.js`).
      - Export `function configureWave({fetch})` — assigns `_fetch = fetch` when present (lets tests inject a fake).
      - Export `async function bootWaves()`:
        - `const fetchFn = _fetch ?? globalThis.fetch`
        - if `!fetchFn` → throw `Error('wave: no fetch available')`
        - `const res = await fetchFn('./seed/waves.json')`
        - `const data = await res.json()`
        - Validate `data && typeof data === 'object' && Array.isArray(data.waves) && data.schemaVersion === 1`; throw `Error('wave: malformed')` on fail.
        - `_waves = data.waves`
        - Idempotency-by-overwrite: re-calling `bootWaves` simply overwrites `_waves`. (No need for a singleton guard since data is replaced wholesale.)
      - Export `function currentWave(date)`:
        - Returns the highest-numbered wave whose `startDate <= date`, or `null` when none qualifies.
        - Implementation: `_waves.filter(w => w.startDate <= date).sort((a,b) => b.number - a.number)[0] ?? null`
      - Export `function getWave(number)` — `_waves.find(w => w.number === number) ?? null`
      - Export `function getAllWaves()` — `[..._waves]` (returns a copy).
      - Export `function _resetWavesForTest()` — sets `_waves = []`; documented as test-only.

    Create `tests/unit/wave.test.js` using `tests/unit/seed.shape.test.js`'s per-block style:
      - `describe('seed/waves.json shape')`: read the JSON, assert `schemaVersion === 1`, `Array.isArray(waves)`, `waves.length === 10`, every entry has `number` (0..9), `name` (`Wave N`), `startDate` matching `/^\d{4}-\d{2}-\d{2}$/`.
      - `describe('currentWave')`:
        - With fake fetch returning the 10-wave fixture: `await bootWaves()`; then
          - `currentWave('2026-05-28')` returns the Wave-4 object
          - `currentWave('2025-12-28')` returns `null` (before Wave 0 start)
          - `currentWave('2025-12-29')` returns Wave 0
          - `currentWave('2026-04-26')` returns Wave 3 (one day before Wave 4 starts)
          - `currentWave('2026-12-28')` returns Wave 9
      - `describe('getWave / getAllWaves')`:
        - `getWave(4)` returns Wave 4
        - `getWave(99)` returns `null`
        - `getAllWaves().length === 10`; mutating the returned array does NOT mutate the internal state (`getAllWaves().push(...); expect(getAllWaves().length).toBe(10)`)
      - `describe('malformed input')`:
        - `bootWaves` with fake fetch returning `{schemaVersion: 2, waves: []}` throws `'wave: malformed'`
        - `bootWaves` with fake fetch returning `{schemaVersion: 1, waves: 'not-an-array'}` throws `'wave: malformed'`
      - Each test calls `_resetWavesForTest()` in `beforeEach` and `configureWave({fetch: fakeFn})` per test.

    Note: `seed/waves.json` is fetched at boot via SWR (D-81) and is NOT in the SW SHELL precache list — Plan 03-05 will add the JS files (cadence.js, wave.js, mount.js) to SHELL but NOT `seed/waves.json` (it stays runtime-cached).
  </action>
  <verify>
    <automated>node --test tests/unit/wave.test.js</automated>
    <automated>node -e "const d=require('./seed/waves.json'); if(d.waves.length!==10 || d.waves[4].number!==4 || d.waves[4].startDate>'2026-05-28' || d.waves[5].startDate<='2026-05-28') process.exit(1)"</automated>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    `seed/waves.json` exists with 10 waves; `currentWave('2026-05-28')` returns Wave 4. `js/domain/wave.js` exports `configureWave`, `bootWaves`, `currentWave`, `getWave`, `getAllWaves`, `_resetWavesForTest`. All wave tests pass. Full suite green.
  </done>
</task>

<task type="auto">
  <name>Task 5: Extend repo.js + fake-idb.js with getAllHabits + getLogsInRange (A7 contract preserved)</name>
  <files>js/db/repo.js, tests/helpers/fake-idb.js, tests/integration/repo.range.test.js</files>
  <read_first>
    - js/db/repo.js (existing exports — getHabit, putHabit, putLog, getLog, putEvent, getEvent, getMeta, putMeta, getSetting, putSetting, runTx)
    - js/db/idb.js (existing helpers `getAll(db, store)` and `indexGetAll(db, store, indexName, query)`)
    - tests/helpers/fake-idb.js (the A7-contract fake — every new repo export MUST appear here with matching signature)
    - tests/integration/contract.fake-vs-real.test.js (enforces fake surface == real surface)
    - .planning/phases/03-today-view-settings-v1-first-usable-slice/03-CONTEXT.md line "repo.getAllHabits(), repo.getLogsInRange(today-7, today)" — these methods do NOT exist yet despite that line; this task adds them
  </read_first>
  <action>
    Add to `js/db/repo.js`:
      - `export async function getAllHabits()`:
        - JSDoc: `@returns {Promise<object[]>}` — every habit row.
        - Body: `const db = await openDB(); return getAll(db, 'habits');`
        - Add `getAll` to the existing `import { openDB, runTx as idbRunTx, get, put } from './idb.js'` line (becomes `{openDB, runTx as idbRunTx, get, put, getAll, indexGetAll}`).
      - `export async function getLogsInRange(startYMD, endYMD)`:
        - JSDoc: `@param {string} startYMD inclusive @param {string} endYMD inclusive @returns {Promise<object[]>}` — log rows whose `date` falls in `[start, end]`.
        - Body: `const db = await openDB(); return indexGetAll(db, 'logs', 'date', IDBKeyRange.bound(startYMD, endYMD));`
        - The `date` index already exists in schema.js (`logs.createIndex('date', 'date')` — D-39 schema, line 58).

    Add to `tests/helpers/fake-idb.js` (A7 contract — both methods MUST exist on the fake):
      - Inside the returned object, after `async getLog(...)`:
        - `async getAllHabits() { return Array.from(stores.habits.values()); }`
        - `async getLogsInRange(startYMD, endYMD) { const out = []; for (const log of stores.logs.values()) { if (log.date >= startYMD && log.date <= endYMD) out.push(log); } return out; }`
      - Update the JSDoc surface comment block (lines 44-58) to declare both new methods.

    Create `tests/integration/repo.range.test.js`:
      - Import `createFakeRepo` from `'../helpers/fake-idb.js'`.
      - `describe('repo.getAllHabits via fake')`:
        - Empty fake → `await getAllHabits()` returns `[]`.
        - After `await putHabit({id: 'a', name: 'X'})` and `await putHabit({id: 'b', name: 'Y'})` → `await getAllHabits()` returns an array of length 2 containing both habits.
      - `describe('repo.getLogsInRange via fake')`:
        - Empty fake → `await getLogsInRange('2026-05-20', '2026-05-30')` returns `[]`.
        - After putting logs with dates `'2026-05-19'`, `'2026-05-25'`, `'2026-05-31'` → `getLogsInRange('2026-05-20', '2026-05-30')` returns exactly one log (date `'2026-05-25'`).
        - Boundary: `getLogsInRange('2026-05-25', '2026-05-25')` returns exactly one log (date `'2026-05-25'`).

    Note: the A7 contract test in `tests/integration/contract.fake-vs-real.test.js` will assert that the fake's exported method names match the real repo's. This task keeps that test green.
  </action>
  <verify>
    <automated>node --test tests/integration/repo.range.test.js</automated>
    <automated>node --test tests/integration/contract.fake-vs-real.test.js</automated>
    <automated>node --test tests/</automated>
  </verify>
  <done>
    `js/db/repo.js` exports `getAllHabits` and `getLogsInRange`. `tests/helpers/fake-idb.js` exposes matching methods. New integration test passes. A7 contract test still passes (no drift). Full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed/waves.json → js/domain/wave.js | Untrusted-shaped JSON crosses into in-memory cache; schema-validated before assignment |
| any js/ source file → mount.js | All new view code (Slice 2+) routes DOM construction through this single seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | seed/waves.json malformed/poisoned | mitigate | `bootWaves` validates `schemaVersion === 1` and `Array.isArray(data.waves)` BEFORE writing to `_waves`; throws `Error('wave: malformed')` on fail (mirrors `js/io/seed.js`'s validation pattern for habits.json). Tested in `tests/unit/wave.test.js` "malformed input" block. |
| T-03-02 | Information Disclosure | wave.js leaking mutable internal state | mitigate | `getAllWaves()` returns a copy via `[..._waves]`; tested. Consumers can't mutate the catalog via the returned array. |
| T-03-03 | Tampering | future view code injecting HTML | mitigate | `js/util/mount.js` builds DOM via `textContent` + `setAttribute` only; `tests/unit/discipline.xss.test.js` grep-gate blocks `.innerHTML`, `.outerHTML`, `.insertAdjacentHTML`, `document.write` across all of `js/`. Belt-and-suspenders. |
| T-03-04 | Repudiation | cadence resolver throws on unknown type | accept | `appliesToday` throws `Error('cadence: unknown type X')` so an unsupported cadence is a load-time crash rather than a silent "habit never appears on Today." Loud failure preserves debuggability. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | NO package installs in this plan; constraint forbids npm/CDN entirely. Zero supply-chain surface. |
</threat_model>

<verification>
- All new and modified files have JSDoc file headers per D-27 referencing the relevant D-XX decisions
- `node --test tests/` exits 0 (full suite green)
- New discipline test `tests/unit/discipline.xss.test.js` enforces D-78 zero-tolerance across `js/`
- `js/domain/cadence.js` contains literal `RESOLVERS` and zero `switch (` (extends Anti-Pattern 4 to cadence)
- `tests/integration/contract.fake-vs-real.test.js` still passes (A7 contract preserved by adding both `getAllHabits` and `getLogsInRange` to fake-idb.js)
- No `indexedDB.open(` outside `js/db/idb.js` (existing discipline test in `apply.discipline.test.js` continues to pass)
- No `BroadcastChannel` calls outside `js/platform/sync.js` (pre-existing — not touched here)
- `seed/waves.json` validates: 10 waves, Wave 4 startDate `<=` 2026-05-28 `<` Wave 5 startDate (so currentWave today is Wave 4)
</verification>

<success_criteria>
- `node --test tests/` green at end of plan
- `js/domain/cadence.js`, `js/domain/wave.js`, `js/util/mount.js` exist; `seed/waves.json` exists; `js/util/date.js` exposes 4 new exports; `js/db/repo.js` exposes 2 new exports; `tests/helpers/fake-idb.js` mirrors the repo surface (A7 preserved)
- `tests/unit/cadence.test.js`, `tests/unit/wave.test.js`, `tests/unit/mount.test.js`, `tests/unit/date.weekly.test.js`, `tests/unit/discipline.xss.test.js`, `tests/integration/repo.range.test.js` all pass
- Zero `.innerHTML` / `.outerHTML` / `.insertAdjacentHTML` / `document.write` in `js/` (asserted by the new discipline test)
- `currentWave('2026-05-28')` returns Wave 4 (asserted by tests + the CLI verify command)
</success_criteria>

<output>
Create `.planning/phases/03-today-view-settings-v1-first-usable-slice/03-01-SUMMARY.md` when done. Summary MUST include: files created/modified, test files added, test counts before/after, the 5 new repo+util+domain exports listed by name, and a one-line note that Slice 2 (Today renders) can now read the codebase without further dependency-discovery scavenger hunts.
</output>
