---
phase: 03
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - js/views/settings.js
  - js/views/today.js
  - js/views/diagnostics.js
  - css/settings.css
  - tests/integration/settings.dataCard.test.js
  - tests/integration/today.completedToday.test.js
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - CORE-06
  - LOG-01
  - UNDO-01
  - UNDO-02
  - UNDO-03
  - SETTINGS-04
  - SETTINGS-05
  - PWA-07
  - NFR-01
  - NFR-02
  - NFR-06
  - NFR-07

must_haves:
  truths:
    - "The Settings Data card label for a setSetting event reads 'changed <key> to <value>' (not 'marked (habit) complete')"
    - "The Reset data button is visually separated from the Undo button by a border-top separator"
    - "The Diagnostics panel shows the live DB_VERSION integer (not 'n/a (P2)') and live persistence status (not 'n/a (P2)')"
    - "The Settings h1 heading has left padding equal to --space-4 so it aligns with the card content below"
    - "A habit completed today remains visible in the Today list for the rest of the day (not filtered out by cadence)"
  artifacts:
    - path: "js/views/settings.js"
      provides: "buildDataCardFromState branches on setSetting event type"
      contains: "eventRow.type === 'setSetting'"
    - path: "js/views/today.js"
      provides: "applicable filter includes habits completed today"
      contains: "getCachedLog(h.id, date)?.completed === true"
    - path: "js/views/diagnostics.js"
      provides: "Live DB_VERSION and live navigator.storage.persisted() values"
      contains: "DB_VERSION"
    - path: "css/settings.css"
      provides: "Visual separator between undo block and destructive card"
      contains: "settings-card--destructive"
    - path: "tests/integration/settings.dataCard.test.js"
      provides: "Test coverage for setSetting label in Data card"
      contains: "setSetting"
    - path: "tests/integration/today.completedToday.test.js"
      provides: "Test coverage for completedToday filter in Today view"
      contains: "completedToday"
  key_links:
    - from: "js/views/settings.js:buildDataCardFromState"
      to: "eventRow.type"
      via: "switch/if-else branches"
      pattern: "eventRow\\.type === 'setSetting'"
    - from: "js/views/today.js:renderTodayInto"
      to: "getCachedLog"
      via: "filter predicate"
      pattern: "getCachedLog\\(h\\.id, date\\)"
    - from: "js/views/diagnostics.js:mountDiagnostics"
      to: "js/db/schema.js"
      via: "import { DB_VERSION }"
      pattern: "import.*DB_VERSION.*schema"
---

<objective>
Close 5 UAT gaps discovered in Phase 3 UAT: 2 MAJOR regressions (Data card shows wrong label for setSetting events; completed habits disappear from Today list) and 1 minor + 2 cosmetic issues (diagnostics stale placeholders; Settings h1 padding; Reset/Undo visual proximity).

Purpose: Make Phase 3 fully correct per its acceptance criteria before advancing to Phase 4. No new features — only precise corrections to existing code.

Output: Updated js/views/settings.js, js/views/today.js, js/views/diagnostics.js, css/settings.css, and two updated/new test files with all 5 gaps closed and test suite remaining green.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix buildDataCardFromState label for setSetting events + add integration test</name>
  <files>js/views/settings.js, tests/integration/settings.dataCard.test.js</files>
  <read_first>
    - js/views/settings.js (lines 251-289 — the full buildDataCardFromState + readDataCardInputs functions; understand the current branch structure and the habitId/habitName lookup path)
    - tests/integration/settings.dataCard.test.js (the full file — understand the test scaffolding: createFakeDocument, createFakeRepo, freshAll, wire; the test for markCompleted label is the model for the new setSetting test)
  </read_first>
  <behavior>
    - New test: seed a `setSetting` event row (`{ id: 'evt-set-1', type: 'setSetting', payload: { key: 'weekStart', value: 'mon' }, at: <iso> }`) and `meta.undoToken = 'evt-set-1'`, call `store.notify(...)`, then assert the Data card preview text contains `"changed weekStart to mon"` and does NOT contain `"(habit)"` or `"marked"`.
    - Note: `readDataCardInputs` short-circuits when `eventRow.payload.habitId` is falsy (line 281: `if (!habitId) return { eventRow, habitName: undefined }`), so the test must verify that `buildDataCardFromState` handles `habitName: undefined` gracefully for setSetting events.
  </behavior>
  <action>
    Edit `js/views/settings.js` function `buildDataCardFromState` (lines 251-266). Replace the current two-branch if/else with a three-branch structure:

    1. If `eventRow` is falsy — return `buildDataCard({ lastEvent: '', hasUndoToken: false, relativeTime: '' })` (unchanged).
    2. Compute `relativeTime = formatRelative(eventRow.at)`.
    3. Branch on `eventRow.type`:
       - `'markCompleted'` → `lastEvent = 'marked ' + (habitName ?? '(habit)') + ' complete'`
       - `'markUncompleted'` → `lastEvent = 'marked ' + (habitName ?? '(habit)') + ' uncomplete'`
       - `'setSetting'` → `lastEvent = 'changed ' + eventRow.payload.key + ' to ' + eventRow.payload.value`
       - fallback → `lastEvent = eventRow.type` (raw type slug; never falls to the "(habit)" path)
    4. Return `buildDataCard({ lastEvent, hasUndoToken: true, relativeTime })`.

    Update the JSDoc comment above the function to document all four branches and note D-72 (notify-driven re-render) as the call context.

    Then add a new `describe` block at the bottom of `tests/integration/settings.dataCard.test.js` titled `'Settings Data card — setSetting event label (Gap 1 fix)'`. The test seeds a setSetting event (key `weekStart`, value `mon`) and a matching `meta.undoToken`, fires `store.notify`, settles microtasks (two `setTimeout(0)` awaits as per the existing pattern), then uses `collectAllText` to assert:
    - The text matches `/changed weekStart to mon/`.
    - The text does NOT match `/(habit)/`.
    - The text does NOT match `/^marked /`.
  </action>
  <verify>
    <automated>node --test tests/integration/settings.dataCard.test.js</automated>
  </verify>
  <done>All tests in settings.dataCard.test.js pass (including the new setSetting test). `grep -n "eventRow.type === 'setSetting'" js/views/settings.js` returns at least one match. `grep -n "'changed '" js/views/settings.js` returns at least one match.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix Today applicable filter to retain habits completed today + add integration test</name>
  <files>js/views/today.js, tests/integration/today.completedToday.test.js</files>
  <read_first>
    - js/views/today.js (lines 1-50 for imports; lines 238-294 for renderTodayInto — the full function including the allActive/applicable filter, pairs construction, sort, and D-58 branch logic)
    - js/state/store.js (search for getCachedLog export — confirm it is already exported and its signature: getCachedLog(habitId, date) returns the log row or undefined)
    - tests/integration/today.tap.test.js (scan for fake-repo setup pattern, fake DOM pattern, and how the test populates store cache — use this as the structural model for the new test file)
  </read_first>
  <behavior>
    - New test file `tests/integration/today.completedToday.test.js`.
    - Test "weekly habit completed today remains in applicable set":
      1. Seed a weekly habit (`cadence: { type: 'weekly' }`, `status: 'active'`) via `repo.putHabit`.
      2. Seed a `completed: true` log for that habit for today via `repo.putLog`.
      3. Call `store.hydrate()` so the cache has both the habit and the log.
      4. Assert that `getCachedLog(habitId, todayLocal())?.completed === true` (sanity-check cache is populated).
      5. Call `appliesToday(habit, todayLocal(), ctx)` and assert it returns `false` (the weekly habit is already done — cadence says it is not applicable for a second completion today).
      6. Now import and invoke the filter predicate as written after the fix: `appliesToday(h, date, ctx) || getCachedLog(h.id, date)?.completed === true` — assert the combined predicate returns `true` for this habit.
    - This tests the logical fix in isolation without needing a full DOM render, keeping the test fast and focused.
  </behavior>
  <action>
    Edit `js/views/today.js` lines 260-261. Change:

    ```
    const applicable = allActive.filter((h) => appliesToday(h, date, ctx));
    ```

    To:

    ```
    const applicable = allActive.filter(
      (h) => appliesToday(h, date, ctx) || getCachedLog(h.id, date)?.completed === true
    );
    ```

    No imports need to change — `getCachedLog` is already imported from `../state/store.js` (verify with grep before editing). No changes to the pairs/sort/D-58 branch logic below line 261.

    Update the JSDoc comment on `renderTodayInto` (or the inline comment at L261) to document the OR-clause: "Habits completed today remain visible (sorted last per D-54) even when cadence returns false — provides visual confirmation of same-day completions."

    Create `tests/integration/today.completedToday.test.js` with the test described in `<behavior>`. The test must import `appliesToday` from `../../js/domain/cadence.js` and `getCachedLog` from `../../js/state/store.js`. Use `createFakeRepo` from `../helpers/fake-idb.js`. Wire the store via `store.configureStore({ repo })`. Pattern the boilerplate (globalThis.document stub, beforeEach/afterEach resets) on `tests/integration/today.tap.test.js`.
  </action>
  <verify>
    <automated>node --test tests/integration/today.completedToday.test.js</automated>
  </verify>
  <done>New test file passes. `grep -n "getCachedLog(h.id, date)?.completed === true" js/views/today.js` returns exactly one match on the applicable filter line. The existing 279-test suite still passes: `node --test tests/**/*.test.js` (or equivalent project test command).</done>
</task>

<task type="auto">
  <name>Task 3: Fix diagnostics stale placeholders + CSS separator + Settings h1 padding</name>
  <files>js/views/diagnostics.js, css/settings.css</files>
  <read_first>
    - js/views/diagnostics.js (lines 1-20 for existing imports; lines 100-135 for the full mountDiagnostics body covering all six dl rows — Row 2 'Schema version' and Row 6 'Persistence' are the targets; inspect the Cache name async pattern at lines 113-124 to use as the model for the Persistence async pattern)
    - js/db/schema.js (lines 1-40 — confirm `export const DB_VERSION = 1` is the export name and the relative path from diagnostics.js is `'../db/schema.js'`)
    - css/settings.css (the full file — locate the `.settings-data-undo` rule (line 70-72) and the `.settings-card--destructive button` rule (line 77-86); the new separator rule targets `.settings-card--destructive` the element itself, not the button inside it)
    - index.html (search for `data-route="settings"` and the `<h1` immediately inside — confirm the h1 is a direct child of `section[data-route="settings"]` and not inside `.settings-panel`, so the CSS selector `section[data-route="settings"] > h1` is correct)
  </read_first>
  <action>
    GAP 3 — diagnostics.js two-row fix:

    1. Add `import { DB_VERSION } from '../db/schema.js';` at the top of `js/views/diagnostics.js`, after the existing import block. Maintain JSDoc file header unchanged.

    2. Row 2 ('Schema version'): change `appendRow(dl, 'Schema version', 'n/a (P2)')` to `appendRow(dl, 'Schema version', String(DB_VERSION))`. Update the inline comment from "D-03: deferred to P2" to "D-03: DB_VERSION from schema.js (live value)".

    3. Row 6 ('Persistence'): replace the single-line `appendRow(dl, 'Persistence', 'n/a (P2)')` with the async pattern (mirror the Cache name pattern at the existing lines 113-124 exactly):
       - `const persistDd = appendRow(dl, 'Persistence', 'loading…');`
       - Guard: `if (navigator.storage && typeof navigator.storage.persisted === 'function') {`
       - Inside: `navigator.storage.persisted().then(p => { persistDd.textContent = p ? 'yes' : 'no'; }).catch(() => { persistDd.textContent = 'n/a'; });`
       - Else branch: `persistDd.textContent = 'n/a';`
       - Update the inline comment from "D-03: deferred to P2" to "D-03: navigator.storage.persisted() (live value)".

    GAP 2 — css/settings.css Reset separator:

    Inside the `@layer view { }` block, add a new rule after the `.settings-data-undo` block (after line 72) and BEFORE the existing `.settings-card--destructive button` rule:

    ```css
    .settings-card--destructive {
      margin-top: var(--space-4);
      padding-top: var(--space-4);
      border-top: 1px solid var(--color-border);
    }
    ```

    This targets the card element itself, not the button inside it. The existing `.settings-card--destructive button` rule for the red button styling is preserved unchanged.

    GAP 4 — css/settings.css Settings h1 padding:

    Inside the same `@layer view { }` block, add a new rule (can be placed near the top of the block, after `.settings-panel`):

    ```css
    section[data-route="settings"] > h1 {
      padding-left: var(--space-4);
    }
    ```

    This aligns the Settings page title with the left edge of the card content below it, matching the Today header alignment pattern.
  </action>
  <verify>
    <automated>node -e "import('./js/views/diagnostics.js').then(() => console.log('import OK')).catch(e => { console.error(e); process.exit(1); }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import.*DB_VERSION.*schema" js/views/diagnostics.js` returns exactly one match.
    - `grep -n "n/a (P2)" js/views/diagnostics.js` returns zero matches (both stale placeholders replaced).
    - `grep -n "String(DB_VERSION)" js/views/diagnostics.js` returns one match.
    - `grep -n "navigator.storage.persisted" js/views/diagnostics.js` returns at least one match.
    - `grep -v "button" css/settings.css | grep -c "settings-card--destructive"` returns at least 1 (the card-level rule exists).
    - `grep -c "border-top: 1px solid var(--color-border)" css/settings.css` returns at least 1.
    - `grep -c "section\[data-route=\"settings\"\] > h1" css/settings.css` returns at least 1.
    - `grep -c "padding-left: var(--space-4)" css/settings.css` returns at least 1.
    - The full test suite still passes after these changes: `node --test tests/**/*.test.js` exits 0.
  </acceptance_criteria>
  <done>Zero `n/a (P2)` strings remain in diagnostics.js. Both new CSS rules are present in settings.css within the @layer view block. The import resolves cleanly. All tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| diagnostics.js → navigator.storage.persisted() | Async browser API — result is untrusted; handled via .catch fallback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Information Disclosure | diagnostics.js — DB_VERSION exposure | accept | DB_VERSION is a schema constant (integer 1), not PII; single-user app, no server |
| T-03-07-02 | Tampering | settings.js — setSetting label renders eventRow.payload.key + value | mitigate | Values come from the IDB events store, written only by apply.js chokepoint; no user-controlled freeform text path in P3 settings keys (weekStart only); no innerHTML used — textContent exclusively |
| T-03-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; vanilla JS only |
</threat_model>

<verification>
Run the full project test suite after all three tasks complete:

```
node --test tests/**/*.test.js
```

Expected: all 279 existing tests + 2 new tests (Gap 1 setSetting label test + Gap 5 completedToday filter test) pass. Exit code 0.

Spot-check structural assertions:
1. `grep -c "eventRow.type === 'setSetting'" js/views/settings.js` → 1
2. `grep -c "getCachedLog(h.id, date)?.completed === true" js/views/today.js` → 1
3. `grep -c "n/a (P2)" js/views/diagnostics.js` → 0
4. `grep -c "String(DB_VERSION)" js/views/diagnostics.js` → 1
5. `grep -c "border-top: 1px solid var(--color-border)" css/settings.css` → 1
6. `grep -c "section\[data-route=\"settings\"\] > h1" css/settings.css` → 1
</verification>

<success_criteria>
- Gap 1 closed: Settings Data card displays "changed weekStart to mon" (not "marked (habit) complete") when the most-recent event is a setSetting action. Confirmed by new integration test.
- Gap 2 closed: The Reset data button has a visible border-top separator and extra top margin separating it from the Undo button block. Confirmed by CSS rule presence.
- Gap 3 closed: Diagnostics panel Row 2 shows the integer value of DB_VERSION (currently "1") and Row 6 shows "yes" or "no" from navigator.storage.persisted(). Confirmed by grep assertions.
- Gap 4 closed: Settings h1 heading has padding-left equal to --space-4, aligning it with the card content below. Confirmed by CSS rule presence.
- Gap 5 closed: A weekly habit (or any cadence-excluded habit) that the user completed today continues to appear in the Today list for the remainder of the day, sorted to the bottom per D-54. Confirmed by new integration test.
- Full test suite: 281+ tests pass, exit code 0.
</success_criteria>

<output>
Create `.planning/phases/03-today-view-settings-v1-first-usable-slice/03-07-SUMMARY.md` when done.
</output>
