---
phase: 08
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - js/views/catalog/builders.js
  - tests/views/catalog/builders.test.js
  - tests/views/today.test.js
autonomous: true
requirements:
  - CAT-01
  - CAT-04

must_haves:
  truths:
    - buildUpcomingListItem function exists in js/views/catalog/builders.js
    - buildUpcomingListItem accepts a habit object and returns {tag, attrs, children} description (pure builder pattern)
    - Returned description has tag='li' with class='catalog-upcoming-item' and data-habit-id attribute (covered: D-16 DOM structure)
    - Returned description includes habit.name in a span.catalog-habit-name (covered: D-01 simplified row)
    - Returned description includes wave badge with text "Wave N" (covered: D-04 wave info display per CAT-04)
    - Returned description includes ISO startDate badge (covered: D-02 ISO format YYYY-MM-DD)
    - Returned description includes Edit button with data-action='edit' and data-habit-id (covered: D-03 edit affordance)
    - Returned description includes Promote button with data-action='promote' and data-habit-id (covered: SCHED-04 action)
    - Today view filter already works for CAT-01 (line 391 filters to status==='active' only)
    - CAT-01 verification test confirms Today check-in shows zero habits when all are scheduled (covered: verified existing filter works with scheduled status)
    - No DOM access inside buildUpcomingListItem (builder is pure per D-77)
    - No innerHTML usage (D-78 grep gate preserved)
  artifacts:
    - buildUpcomingListItem function in `js/views/catalog/builders.js`
    - test cases in `tests/views/catalog/builders.test.js` for buildUpcomingListItem
    - CAT-01 test case in `tests/views/today.test.js`
  key_links:
    - buildUpcomingListItem output is consumed by catalog.js mount() in plan 08-03
    - CAT-01 filter validation confirms Today uses status==='active' (already present, just needs test)

---

<objective>
Add the buildUpcomingListItem DOM builder for displaying scheduled habits in Catalog's Upcoming section, and verify that Today view correctly filters out scheduled habits (CAT-01).

Purpose: Completes the UI builder layer for Upcoming items (CAT-04) and validates that Today view does not show scheduled habits.
Output: buildUpcomingListItem function + test coverage + CAT-01 verification test
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-today-catalog-upcoming-section/08-CONTEXT.md
@.planning/phases/08-today-catalog-upcoming-section/08-PATTERNS.md
@.planning/phases/08-today-catalog-upcoming-section/08-UI-SPEC.md
@js/views/catalog/builders.js
@js/views/today.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildUpcomingListItem DOM builder with tests (CAT-04)</name>
  <files>js/views/catalog/builders.js, tests/views/catalog/builders.test.js</files>
  <read_first>
    - js/views/catalog/builders.js (existing buildCatalogHeader and buildHabitListItem patterns)
    - .planning/phases/08-today-catalog-upcoming-section/08-PATTERNS.md (pattern assignment "js/views/catalog/builders.js")
    - .planning/phases/08-today-catalog-upcoming-section/08-UI-SPEC.md (Component Specifications section for Upcoming List Item structure)
  </read_first>
  <behavior>
    Input: habit = {id: 'uuid', name: 'Morning walk', wave: 0, startDate: '2026-08-15'}
    
    Output: {
      tag: 'li',
      attrs: {class: 'catalog-upcoming-item', 'data-habit-id': 'uuid'},
      children: [
        {tag: 'div', attrs: {class: 'catalog-habit-info'}, children: [
          {tag: 'span', attrs: {class: 'catalog-habit-name'}, text: 'Morning walk'},
          {tag: 'div', attrs: {class: 'catalog-habit-badges'}, children: [
            {tag: 'span', attrs: {class: 'catalog-habit-wave'}, text: 'Wave 0'},
            {tag: 'span', attrs: {class: 'catalog-upcoming-date'}, text: '2026-08-15'}
          ]}
        ]},
        {tag: 'div', attrs: {class: 'catalog-habit-actions'}, children: [
          {tag: 'button', attrs: {class: 'catalog-btn catalog-btn--edit', 'data-action': 'edit', 'data-habit-id': 'uuid', 'aria-label': 'Edit Morning walk'}, text: 'Edit'},
          {tag: 'button', attrs: {class: 'catalog-btn catalog-btn--promote', 'data-action': 'promote', 'data-habit-id': 'uuid', 'aria-label': 'Promote Morning walk to active'}, text: 'Promote'}
        ]}
      ]
    }
    
    Cases:
    - Given a complete scheduled habit, returns full description with all badges and buttons
    - Given a habit with long name (30+ chars), wave badge and date badge still render (text truncation handled by CSS, not builder)
    - Returned object has no DOM access, textContent, or innerHTML — pure description tree only (D-78 grep gate)
  </behavior>
  <action>
    Add buildUpcomingListItem(habit) function to js/views/catalog/builders.js after the existing buildHabitListItem function. Function accepts a habit object and returns a pure {tag, attrs, children} tree matching the structure shown in the UI-SPEC Component Specifications section for Upcoming List Item. Include JSDoc comment (D-27) describing the builder's purpose and parameter/return types. The function mirrors buildHabitListItem pattern (lines 67–179 reference) but simplified: include habit.name, wave badge (text "Wave N" where N is habit.wave), ISO startDate badge (habit.startDate), Edit button, Promote button. Do not include stage info, mastery badge, or archive button — those are for active habits only. Create tests/views/catalog/builders.test.js or extend existing test file if present with test cases for: (1) complete habit produces correct tree structure, (2) all required attributes and classes present, (3) button data-action and aria-label values correct, (4) no DOM access in function (inspection).
  </action>
  <verify>
    <automated>npm test -- tests/views/catalog/builders.test.js -x</automated>
  </verify>
  <done>
    buildUpcomingListItem function exports from js/views/catalog/builders.js; test file has at least 4 test cases covering structure, attributes, buttons, and purity; all tests pass; no DOM access in function; JSDoc header present on function
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CAT-01 verification test (Today view filters scheduled habits)</name>
  <files>tests/views/today.test.js</files>
  <read_first>
    - js/views/today.js line 391 (the filter: `getCachedHabits().filter((h) => h.status === 'active')`)
    - .planning/REQUIREMENTS.md (CAT-01 requirement text)
    - tests/views/today.test.js or tests/state/store.test.js (existing test patterns for getCachedHabits mock)
  </read_first>
  <behavior>
    Precondition: cache contains 3 habits with statuses: 'active', 'scheduled', 'mastered'
    
    Test case: When renderTodayInto is called, the returned rendered habits list includes only status==='active' and status==='mastered' habits (habits that can appear on Today check-in). Scheduled habits are excluded.
    
    Equivalent: getCachedHabits().filter((h) => h.status === 'active') should return only active habits, excluding scheduled.
    
    This is a VERIFICATION test — the filter already exists in the code (Phase 7 did not touch this; status==='active' is already the filter). The test confirms it works correctly with the new 'scheduled' status value from Phase 7.
  </behavior>
  <action>
    Create or extend tests/views/today.test.js with a test case (e.g., "Today view excludes scheduled habits from the check-in list") that mocks getCachedHabits to return a mix of habits with different statuses (active, scheduled, mastered, archived). Call renderTodayInto (or the equivalent function that filters for Today). Assert that the rendered habit rows include only the active/mastered habits and exclude scheduled/archived. The test verifies that line 391 of today.js (the filter) correctly excludes scheduled status. No code change to today.js is needed; this test is purely verification that the existing code works with the new status.
  </action>
  <verify>
    <automated>npm test -- tests/views/today.test.js -x</automated>
  </verify>
  <done>
    CAT-01 verification test exists in tests/views/today.test.js; test passes; test asserts that scheduled habits are not rendered in Today's check-in list; no code changes to today.js (verification only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| builder→mount | Pure {tag, attrs, children} output is consumed by mount() helper, which constructs DOM safely via textContent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03 | Tampering | buildUpcomingListItem output | low | mitigate | Builder output is pure description tree; no string interpolation or innerHTML; mount() layer uses textContent for safety (D-78) |

</threat_model>

<verification>
Phase 8 UI builder verified when:
1. buildUpcomingListItem.test.js passes (npm test)
2. CAT-01 verification test passes (Today filters scheduled habits)
3. No DOM access in builder functions (grep -n 'document\|createElement\|innerHTML' js/views/catalog/builders.js for negation)
</verification>

<success_criteria>
- buildUpcomingListItem function exists and exports from js/views/catalog/builders.js
- Function accepts habit object and returns {tag, attrs, children} tree
- Returned tree includes all elements specified in UI-SPEC (name, wave badge, date badge, Edit button, Promote button)
- Test file has at least 4 test cases, all passing
- CAT-01 verification test passes (Today excludes scheduled)
- No DOM access or innerHTML in builders (D-78 compliance)
- JSDoc header on function references D-16, D-01, CAT-04
</success_criteria>

<output>
Create `.planning/phases/08-today-catalog-upcoming-section/08-02-SUMMARY.md` when done
</output>
