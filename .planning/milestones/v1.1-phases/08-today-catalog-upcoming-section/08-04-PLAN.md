---
phase: 08-today-catalog-upcoming-section
plan: 04
type: execute
wave: 1
depends_on: []
files_modified: [css/catalog.css]
autonomous: true
gap_closure: true
gap_ids: [G-08-4]
requirements: [CAT-04]
must_haves:
  truths:
    - "Upcoming habit list renders with no visible bullet dots"
    - "Edit and Promote buttons on Upcoming items are compact, matching active habit row widths"
    - "Upcoming items have consistent visual spacing and alignment"
  artifacts:
    - "css/catalog.css — two new rules (.catalog-upcoming-list and .catalog-upcoming-item)"
  key_links:
    - "html/catalog.html → imports css/main.css → @import css/catalog.css"
    - "js/ui/catalog.js buildUpcomingListItem() → renders <li class='catalog-upcoming-item'> inside <ul class='catalog-upcoming-list'>"
---

<objective>
Close gap G-08-4: Add missing CSS rules for Upcoming section visual consistency.

**Purpose:** Remove bullet dots and properly flex-layout the Edit/Promote buttons so they stay compact and aligned with active habit rows.

**Output:** Updated css/catalog.css with two new rules applied correctly.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-today-catalog-upcoming-section/08-UAT.md

# Source reference — existing catalog styles
@css/catalog.css
</context>

<tasks>

<task type="auto">
  <name>Task: Add CSS rules for .catalog-upcoming-list and .catalog-upcoming-item</name>
  <files>css/catalog.css</files>
  <action>
Open css/catalog.css. After the `.catalog-habit-list` rule block (currently ending at line 34), insert the `.catalog-upcoming-list` rule.

`.catalog-upcoming-list` should be:
```
.catalog-upcoming-list {
  list-style: none;
  margin: 0;
  padding: var(--space-2) 0;
}
```

Then, after the `.catalog-habit-row` rule block (currently ending at line 43), insert the `.catalog-upcoming-item` rule.

`.catalog-upcoming-item` should be:
```
.catalog-upcoming-item {
  display: flex;
  align-items: flex-start;
  gap: var(--space-3);
  padding: var(--space-3) var(--space-4);
  border-bottom: 1px solid var(--color-border, #e5e7eb);
  transition: opacity 0.2s ease;
}
```

Both rules stay inside the `@layer view { … }` block. Use exact formatting matching the existing rules (blank lines before rule, consistent indentation, no trailing comments).
  </action>
  <verify>
    <automated>grep -q "\.catalog-upcoming-list {" css/catalog.css && grep -q "\.catalog-upcoming-item {" css/catalog.css</automated>
  </verify>
  <done>
css/catalog.css contains both new rules:
- `.catalog-upcoming-list` with `list-style: none`, `margin: 0`, `padding: var(--space-2) 0`
- `.catalog-upcoming-item` with `display: flex`, `align-items: flex-start`, and all five properties correctly specified
- Both rules are placed after their active-list analogs, inside the `@layer view` block
- File has no syntax errors; other rules remain unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None (CSS-only, no data flow or external input).

## STRIDE Threat Register

No applicable threats (styling change, no security surface).
</threat_model>

<verification>
Once the task is complete and verified, open the Catalog page in a browser and visually confirm:
1. The Upcoming section list has no bullet dots
2. The Edit and Promote buttons are compact and aligned (same widths as active habit rows)
3. The Upcoming item rows have consistent spacing (padding, borders) matching active rows
</verification>

<success_criteria>
- Gap G-08-4 is closed
- Test 4 ("Upcoming Items Show Name, Wave, Start Date, Edit & Promote Buttons") now passes with no reported cosmetic issues
- CSS file parses without errors
- All seven UAT tests pass
</success_criteria>

<output>
Create `.planning/phases/08-today-catalog-upcoming-section/08-04-SUMMARY.md` when done.
</output>
