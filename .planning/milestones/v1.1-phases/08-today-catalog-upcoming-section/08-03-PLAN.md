---
phase: 08
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - js/views/catalog.js
  - js/main.js
  - js/desktop.js
autonomous: true
requirements:
  - CAT-02
  - CAT-03
  - SCHED-04

must_haves:
  truths:
    - catalog.js splits getCachedHabits into two filtered arrays: active (status !== 'scheduled') and scheduled (status === 'scheduled')
    - Active list renders using existing buildHabitListItem for active/mastered/archived habits (covered: D-14 explicit filter, CAT-02)
    - Upcoming section is hidden when scheduledHabits.length === 0 (covered: D-10 empty hiding)
    - Upcoming section is rendered below active list when scheduledHabits.length > 0 (covered: CAT-03 section presence)
    - Upcoming section includes h2 heading with text "Upcoming" (covered: D-12 heading text)
    - Upcoming list items are sorted by startDate ascending (soonest first) (covered: D-11 sort order)
    - Each Upcoming item rendered using buildUpcomingListItem (covered: D-15 builder usage, CAT-04 display)
    - Promote button in Upcoming items wired to apply({type:'promoteHabit', payload:{habitId}}) (covered: D-08 handler, SCHED-04 action)
    - Promote action emits {type:'habit:put', habitId} on BroadcastChannel('habits') automatically via apply.js (covered: D-07 cross-tab sync)
    - After promote, Catalog re-renders immediately (habit disappears from Upcoming, appears in active list) (covered: D-09 re-render, SCHED-04 result)
    - configurePromoteHabit({repo}) called in main.js P2 boot section after configureScheduled (covered: D-08 boot wiring)
    - configurePromoteHabit({repo}) called in desktop.js P2 boot section after configureScheduled (covered: D-08 both shells)
    - promoteHabit handler is configured and ready before Catalog UI is mounted (covered: boot ordering)
    - Edge probe: Upcoming section loading state — if IDB read takes >100ms, section may flash (backstop: behavioral test required in UAT)
    - Edge probe: Upcoming section with zero items renders nothing, not even heading (covered: D-10 conditional rendering)
    - Edge probe: Upcoming section with one item displays correctly without layout shift (backstop: visual test required in UAT)
    - Edge probe: Upcoming section with many items (20+) scrolls correctly within catalog page (covered: CSS flex layout, page scroll)
    - Edge probe: Promote button error handling — if IDB write fails, error indicator shown on row (backstop: integration test required)
  artifacts:
    - Modified `js/views/catalog.js` with split list logic and Upcoming section rendering
    - Modified `js/main.js` with configurePromoteHabit call
    - Modified `js/desktop.js` with configurePromoteHabit call
  key_links:
    - Promote action handler wired by configurePromoteHabit (plan 08-01 creates the handler)
    - buildUpcomingListItem used to render each scheduled item (plan 08-02 creates the builder)
    - BroadcastChannel broadcast happens automatically through apply.js (existing system)
    - Today view sees promoted habit immediately via re-render on habit:put broadcast (existing system)

---

<objective>
Integrate the promoteHabit handler and buildUpcomingListItem builder into Catalog UI: split the active/scheduled lists, render the Upcoming section, wire the Promote action, and configure the handler in both shells.

Purpose: Delivers the full Upcoming section feature (CAT-02, CAT-03, SCHED-04) to users.
Output: Catalog shows active and scheduled habits separately with working Promote action
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-today-catalog-upcoming-section/08-CONTEXT.md
@.planning/phases/08-today-catalog-upcoming-section/08-PATTERNS.md
@.planning/phases/08-today-catalog-upcoming-section/08-UI-SPEC.md
@js/views/catalog.js
@js/main.js
@js/desktop.js
@js/state/apply.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split Catalog list and add Upcoming section rendering (CAT-02, CAT-03)</name>
  <files>js/views/catalog.js</files>
  <read_first>
    - js/views/catalog.js (existing renderCatalogInto function, sortHabits logic, lines 217–258)
    - .planning/phases/08-today-catalog-upcoming-section/08-PATTERNS.md (pattern assignment "js/views/catalog.js", the Render Function Pattern section)
    - .planning/phases/08-today-catalog-upcoming-section/08-UI-SPEC.md (Upcoming Section Container section)
    - js/views/catalog/builders.js (buildUpcomingListItem import)
  </read_first>
  <behavior>
    Input: habits array from getCachedHabits() containing active, mastered, archived, and scheduled habits
    
    Existing behavior: renderCatalogInto loops all habits, renders them all in one list
    
    New behavior:
    - Filter sorted habits into two arrays: activeHabits = sorted.filter(h => h.status !== 'scheduled'), scheduledHabits = sorted.filter(h => h.status === 'scheduled')
    - Render active list as before (using buildHabitListItem for each)
    - After active list, conditionally render Upcoming section IF scheduledHabits.length > 0:
      - Render h2 heading with text "Upcoming"
      - Sort scheduledHabits by startDate ascending: scheduledHabits.sort((a,b) => (a.startDate ?? '').localeCompare(b.startDate ?? ''))
      - Render each scheduled habit using buildUpcomingListItem (imported from builders.js)
    - If scheduledHabits.length === 0, Upcoming section does not render at all (no heading, no ul element)
  </behavior>
  <action>
    In js/views/catalog.js, locate the renderCatalogInto function (line 217 reference from PATTERNS). After the line that sorts habits (const sorted = sortHabits(habits)), add the filter split: create activeHabits = sorted.filter((h) => h.status !== 'scheduled'). Then create scheduledHabits = sorted.filter((h) => h.status === 'scheduled').sort((a, b) => (a.startDate ?? '').localeCompare(b.startDate ?? '')). Update the existing habit list loop to use activeHabits instead of sorted. After the active list is rendered, add a conditional block: if (scheduledHabits.length > 0) { render Upcoming section with h2 heading "Upcoming" + ul.catalog-upcoming-list with li items built via buildUpcomingListItem(habit) for each scheduled habit }. Import buildUpcomingListItem from './catalog/builders.js' at the top of the file. Verify that the active list still uses mount(buildHabitListItem()) for each item, and Upcoming section uses mount(buildUpcomingListItem()) for each item.
  </action>
  <verify>
    <automated>grep -n 'status !== .scheduled' js/views/catalog.js && grep -n 'catalog-upcoming-list' js/views/catalog.js && grep -n 'buildUpcomingListItem' js/views/catalog.js</automated>
  </verify>
  <done>
    Catalog.js filters active list (status !== 'scheduled'); Upcoming section conditionally renders when scheduled habits exist; buildUpcomingListItem used for Upcoming items; sorted by startDate ascending; h2 heading "Upcoming" present; no scheduled habits appear in active list
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Promote button action handler in Catalog (SCHED-04)</name>
  <files>js/views/catalog.js</files>
  <read_first>
    - js/views/catalog.js buildActions function (lines 332–356 reference for archive/restore pattern)
    - .planning/phases/08-today-catalog-upcoming-section/08-PATTERNS.md (Action Handler for Promote section)
    - .planning/phases/08-today-catalog-upcoming-section/08-UI-SPEC.md (Promote Button section)
  </read_first>
  <behavior>
    Input: Promote button click event with data-habit-id attribute set to a habitId UUID
    
    Expected behavior:
    - Handler extracts habitId from event target's data-habit-id attribute
    - Calls apply({type:'promoteHabit', payload:{habitId}})
    - If successful, store.subscribe triggers re-render automatically via habit:put broadcast
    - If error, displays error toast: "Couldn't promote habit — try again"
    
    The handler should follow the same try/catch + showErrorToast pattern as archive/restore actions.
  </behavior>
  <action>
    In js/views/catalog.js, locate the buildActions function (around line 332 from patterns reference). Add a new action handler named 'promote' following the same pattern as the existing 'archive' handler. Handler should: (1) extract habitId from event.currentTarget or event.target data-habit-id attribute, (2) return early if habitId is falsy, (3) call apply({type:'promoteHabit', payload:{habitId}}) in a try block, (4) catch errors and call showErrorToast("Couldn't promote habit — try again"). The store.subscribe will trigger re-render automatically when habit:put broadcast is received. No confirmation dialog is needed (D-05).
  </action>
  <verify>
    <automated>grep -n "promote.*async" js/views/catalog.js && grep -n "type: 'promoteHabit'" js/views/catalog.js && grep -n "showErrorToast" js/views/catalog.js</automated>
  </verify>
  <done>
    Promote action handler exists in buildActions; accepts habitId from button data-attribute; calls apply({type:'promoteHabit', ...}); includes try/catch with error toast; no confirmation dialog shown
  </done>
</task>

<task type="auto">
  <name>Task 3: Boot wiring — configurePromoteHabit in main.js and desktop.js (SCHED-04)</name>
  <files>js/main.js, js/desktop.js</files>
  <read_first>
    - js/main.js P2 boot section (lines 85–105)
    - js/desktop.js P2 boot section (lines 79–97 from PATTERNS reference)
    - .planning/phases/08-today-catalog-upcoming-section/08-PATTERNS.md (Configure Boot Pattern sections for both files)
    - js/state/apply/promoteHabit.js (the handler file created in plan 08-01)
  </read_first>
  <behavior>
    Current state in main.js (line 98): configureScheduled({repo}) is called as part of P2 boot
    
    New state: After configureScheduled, add configurePromoteHabit({repo}) call (same DI pattern)
    
    Same for desktop.js (line 88 reference): add configurePromoteHabit({repo}) after configureScheduled
    
    Import statement: Add `import { configurePromoteHabit } from './state/apply/promoteHabit.js';` at the top of both files alongside other state/apply imports
    
    This wiring registers the promoteHabit handler in the apply system so that apply({type:'promoteHabit', ...}) calls work.
  </behavior>
  <action>
    In js/main.js, add import statement near line 55 (with other apply imports): `import { configurePromoteHabit } from './state/apply/promoteHabit.js';`. Then in the P2 boot block (around line 98), add the line `configurePromoteHabit({ repo });` after the configureScheduled call. Repeat the same two changes in js/desktop.js (import around line 62, configure call around line 88). This registers the handler before any UI mounts, ensuring promote actions work.
  </action>
  <verify>
    <automated>grep -n 'import.*configurePromoteHabit' js/main.js && grep -n 'configurePromoteHabit.*repo' js/main.js && grep -n 'import.*configurePromoteHabit' js/desktop.js && grep -n 'configurePromoteHabit.*repo' js/desktop.js</automated>
  </verify>
  <done>
    configurePromoteHabit imported in both main.js and desktop.js; configurePromoteHabit({repo}) called in both P2 boot sections after configureScheduled; handler is registered before UI mounts
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| button click → action handler | Promote button data-habit-id is opaque UUID; no risk of injection |
| action handler → apply | apply() validates event structure before calling registered handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Tampering | Promote button action | low | mitigate | Action handler validates habitId is present before calling apply; apply layer enforces handler registration (no unregistered events accepted) |
| T-08-05 | Denial of Service | Upcoming section rendering | low | accept | Catalog page already handles large habit lists via scroll; Upcoming section is same architecture; no performance regression expected |

</threat_model>

<verification>
Phase 8 catalog integration verified when:
1. Catalog.js test suite passes (if applicable; mainly integration test)
2. Catalog page renders active list without scheduled habits (CAT-02 compliance check)
3. Catalog page renders Upcoming section with scheduled habits sorted by startDate (CAT-03 compliance check)
4. Promote button click transitions habit from scheduled to active (SCHED-04 end-to-end test)
5. Boot wiring in place in both shells (grep verification)
6. No unregistered apply event type errors in console
</verification>

<success_criteria>
- catalog.js splits habits into active and scheduled lists (status !== 'scheduled' filter present)
- Active list renders using buildHabitListItem (no scheduled habits in this list)
- Upcoming section renders conditionally (hidden when empty) with h2 "Upcoming" heading
- Upcoming items use buildUpcomingListItem builder and are sorted by startDate ascending
- Promote button in Upcoming items wired to apply({type:'promoteHabit', payload:{habitId}})
- Error handling includes error toast on promote failure
- configurePromoteHabit imported and called in both main.js and desktop.js
- After promote, habit moves from Upcoming to active list (verified by end-to-end test or manual check)
- No scheduled habits visible in active Catalog list (CAT-02 compliance)
- All edge probes covered or marked backstop for UAT (loading states, errors, zero/one/many items)
</success_criteria>

<output>
Create `.planning/phases/08-today-catalog-upcoming-section/08-03-SUMMARY.md` when done
</output>
