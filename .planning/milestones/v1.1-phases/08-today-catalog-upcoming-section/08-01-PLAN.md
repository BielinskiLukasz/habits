---
phase: 08
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/state/apply/promoteHabit.js
autonomous: true
requirements:
  - SCHED-04

must_haves:
  truths:
    - promoteHabit handler exists at `js/state/apply/promoteHabit.js`
    - Handler function signature matches archiveHabit pattern: `handlePromoteHabit(event, repo) -> {storeNames, writes, inverse}`
    - Reads current habit from repo.getHabit(habitId) for undo capture
    - Updates habit.status from any value to 'active' (covered: handler works for scheduled→active transition)
    - Returns structured result with storeNames=['habits'], writes=[{store:'habits', value:updated}], inverse={type:'demoteHabit', payload:{habitId}}
    - broadcastKeys function defined: `handlePromoteHabit.broadcastKeys = (event) => ({habitId: event.payload.habitId})`
    - Handler accepts events with shape {type:'promoteHabit', payload:{habitId:string}}
    - No direct repo.put/update calls (handler output is consumed by apply.js apply chokepoint)
    - JSDoc file header present (D-27 compliance): @file + rationale + D-XX references
    - Undo inverse type is 'demoteHabit' for future undo/redo support
  artifacts:
    - `js/state/apply/promoteHabit.js` — new file
  key_links:
    - Handler is wired by configurePromoteHabit({repo}) in main.js and desktop.js (Phase 8 plan 03)
    - Handler emits via BroadcastChannel('habits') automatically through apply.js integration
    - Tests verify handler mutation logic in isolation (RED phase)

---

<objective>
Implement the promoteHabit handler (SCHED-04) that transitions a habit from scheduled to active status.

Purpose: Enables the "Promote to active" action on Upcoming list items. Handler provides the state mutation contract consumed by apply.js chokepoint.
Output: Tested promoteHabit.js handler with RED/GREEN/REFACTOR cycle
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-today-catalog-upcoming-section/08-CONTEXT.md
@.planning/phases/08-today-catalog-upcoming-section/08-PATTERNS.md
@js/state/apply/archiveHabit.js
@js/db/repo.js
</context>

<feature>
  <name>promoteHabit handler (SCHED-04)</name>
  <files>js/state/apply/promoteHabit.js, tests/state/apply/promoteHabit.test.js</files>
  <behavior>
    Input: event = {type:'promoteHabit', payload:{habitId: 'uuid-123'}}
    Input: repo = {getHabit(id) -> Promise<habit>}
    
    Cases:
    - Given a habit with status='scheduled', handlePromoteHabit returns {storeNames:['habits'], writes:[{store:'habits', value:{...habit, status:'active'}}], inverse:{type:'demoteHabit', payload:{habitId}}}
    - Given a habit with status='active' (idempotent case), returns same mutation structure (handler does not validate; apply.js layer will enforce constraints if needed)
    - Given a missing habit (undefined), handler should throw or repo.getHabit rejects, allowing error to bubble for apply.js retry logic
    - broadcastKeys(event) returns {habitId: event.payload.habitId}
  </behavior>
  <implementation>
    1. RED: Create test file tests/state/apply/promoteHabit.test.js
       - Test 1: handlePromoteHabit accepts event with habitId, reads habit, returns structured result
       - Test 2: habit.status is set to 'active' in the writes value
       - Test 3: inverse type is 'demoteHabit' with same habitId payload
       - Test 4: broadcastKeys function returns object with habitId key
       - Test 5: handler does not call repo.put or any write method (integration test in apply.js)
       Run test — all RED (failing)
       Commit: `test(sched-04): add failing tests for promoteHabit handler`

    2. GREEN: Implement promoteHabit.js handler
       - Copy structure from archiveHabit.js (file header, function signature, JSDoc comments, exports)
       - Export handlePromoteHabit(event, repo): extracts habitId, awaits repo.getHabit(habitId), spreads habit, sets status='active'
       - Define handlePromoteHabit.broadcastKeys = (event) => ({habitId: event.payload.habitId})
       - Return {storeNames:['habits'], writes:[{store:'habits', value:updated}], inverse:{type:'demoteHabit', payload:{habitId}}}
       - Add JSDoc: @file header referencing D-08 (SCHED-04, promote handler), mention that this is a status mutation handler (like archiveHabit), note that history integrity is preserved
       Run test — all GREEN
       Commit: `feat(sched-04): implement promoteHabit handler`

    3. REFACTOR: Polish JSDoc comments and ensure pattern consistency with archiveHabit
       - Verify that the file header matches archiveHabit.js structure: @file line, handler contract explanation, forbidden constructs list
       - Verify that the function JSDoc includes @param and @returns with full type hints
       - Verify broadcastKeys is documented as a static property
       Run test — all still GREEN
       Commit: `refactor(sched-04): align promoteHabit JSDoc and pattern consistency`
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>TDD: promoteHabit handler (RED/GREEN/REFACTOR)</name>
  <files>js/state/apply/promoteHabit.js, tests/state/apply/promoteHabit.test.js</files>
  <read_first>
    - js/state/apply/archiveHabit.js (exact pattern to mirror)
    - js/db/repo.js (getHabit signature)
    - .planning/phases/08-today-catalog-upcoming-section/08-CONTEXT.md (decisions D-08, D-07)
    - .planning/phases/08-today-catalog-upcoming-section/08-PATTERNS.md (pattern assignment section "js/state/apply/promoteHabit.js")
  </read_first>
  <action>
    Execute the RED/GREEN/REFACTOR cycle described in the feature section above. Start with a failing test file that asserts the handler contract (input shape, output shape, status mutation, broadcastKeys function). Then implement the handler by copying the structure from archiveHabit.js, changing only the status value ('active' instead of 'archived') and the inverse type name ('demoteHabit'). Ensure the file header uses JSDoc (D-27) and references D-08 + SCHED-04. After GREEN, refactor comments for consistency with the reference pattern. All three commits (RED test, GREEN impl, REFACTOR polish) must be atomic and within this task.
  </action>
  <verify>
    <automated>npm test -- tests/state/apply/promoteHabit.test.js -x</automated>
  </verify>
  <done>
    promoteHabit.js exists with handlePromoteHabit function and broadcastKeys property; all tests pass; three commits created (test/feat/refactor); JSDoc header present with D-08 reference; no forbidden constructs (no switch, no direct repo.put); inverse type is 'demoteHabit'
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→state | habitId parameter is opaque UUID; repo.getHabit validates IDB lookup |
| handler→apply.js | Handler output is consumed by apply.js chokepoint, which enforces event-handler registration and BroadcastChannel broadcast |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | promoteHabit handler | low | mitigate | Handler reads prior habit state for undo capture; apply.js layer verifies event structure before calling handler |
| T-08-02 | Information Disclosure | IDB access | low | accept | Handler only reads/writes to habits store; no sensitive data beyond user's own habit records |

</threat_model>

<verification>
Phase 8 handler implementation verified when:
1. promoteHabit.js test suite passes (npm test)
2. Handler reads habit from repo without side effects
3. Mutation structure matches archiveHabit pattern
4. BroadcastChannel integration deferred to plan 08-03 (boot wiring)
</verification>

<success_criteria>
- promoteHabit.js exists and exports handlePromoteHabit function with correct signature
- promoteHabit.js exports broadcastKeys static property
- All behavior cases in the feature section have passing tests
- File has JSDoc header (D-27) with D-08 reference
- Three commits created: test (RED), feat (GREEN), refactor (polish)
- No forbidden constructs found in code
</success_criteria>

<output>
Create `.planning/phases/08-today-catalog-upcoming-section/08-01-SUMMARY.md` when done
</output>
