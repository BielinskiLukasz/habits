---
phase: 09-desktop-waveboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/wavePlanning.test.js
  - js/views/desktop/wavePlanning.js
  - js/views/desktop/waveboard.js
autonomous: true
requirements: [WAVE-01, WAVE-02, WAVE-03, WAVE-04]

must_haves:
  truths:
    - buildWavePlanningSection returns a section.waveplanning description tree with one div.waveplanning-wave per wave in getAllWaves() order, each containing a button.waveplanning-wave-header that carries the wave startDate text (WAVE-01)
    - each wave header contains span.waveplanning-wave-meta > span.waveplanning-wave-counts with text "{N} active · {N} scheduled" — "active" in count = status active OR mastered; "scheduled" = status scheduled only (WAVE-02 copywriting contract)
    - expanded habit list for a wave with scheduled habits contains li.waveplanning-scheduled-row elements each with span.waveplanning-scheduled-date showing habit.startDate (ISO string) and button.waveplanning-promote-btn[data-habit-id] (WAVE-03)
    - expanded habit list contains li.waveplanning-habit-row elements for active/mastered habits with span.waveplanning-habit-status, span.waveplanning-habit-stage, span.waveplanning-habit-cadence (WAVE-04)
    - health badge is span.waveplanning-badge--upcoming for waves where no habit has status === 'active' (mastered-only waves also get Upcoming — D-06)
    - health badge slug maps worstStatus of active (status==='active' only) habits' current-week S1 snapshots — badgeText "At Risk" when worst is "At-risk", "No data" when no snapshots exist (D-05)
    - archived habits do not appear in the description tree — caller passes pre-filtered habits (D-11)
    - cadenceSummary uses a TYPE_LABEL dispatch object — no switch statement (Anti-Pattern 4); returns Daily / Weekly / Monthly / Every N days / day-of-week abbreviations
    - scheduled sub-header is h3.waveplanning-scheduled-heading (not li, not h4) per UI-SPEC DOM Contract
    - wave with zero non-archived habits shows li.waveplanning-empty with text "No habits in this wave." in expanded list
    - "[ASSUMED] WAVE-01 adjacency: two waves with equal startDates render as separate rows keyed by wave.number — no merging"
    - "[ASSUMED] WAVE-01 empty: getAllWaves() returning [] renders section with h2 only and no wave rows; null treated as []"
    - "[ASSUMED] WAVE-02 empty: zero non-archived habits in a wave renders '0 active · 0 scheduled' — count span never omitted"
    - "[ASSUMED] WAVE-02 ordering: within each group habits appear in getCachedHabits() return order — no secondary sort applied"
    - "[ASSUMED] WAVE-03 unclassified: scheduled habit with null startDate renders empty date span without crashing — Promote button still shown"
    - "[ASSUMED] WAVE-04 unclassified: habit with unknown status rendered in active/mastered section as defensive fallback; stage rendered as raw number from habit.stage"
  artifacts:
    - js/views/desktop/wavePlanning.js (new — exports buildWavePlanningSection)
    - js/views/desktop/waveboard.js (modified — worstStatus, statusSlug, isoWeekKey now exported)
    - tests/unit/wavePlanning.test.js (new — unit tests for WAVE-01 through WAVE-04)
  key_links:
    - wavePlanning.js imports { worstStatus, statusSlug, isoWeekKey } from ./waveboard.js (pitfall 3 fix — these are currently private functions with no export keyword)
    - buildWavePlanningSection is pure: no DOM access, no async, no side effects (D-26 Pattern S8)
    - caller pre-filters archived habits before passing to builder — D-11 compliance is the caller's responsibility
    - list id format is "wave-{N}-list" per UI-SPEC DOM Contract (NOT "waveplanning-list-wave-{N}" which was a draft name)
  prohibitions:
    - No merging wave rows for equal startDates — rows keyed by wave.number
    - No converting startDate from ISO YYYY-MM-DD to locale format
    - No switch statement for cadence type dispatch — TYPE_LABEL dispatch object mandatory (Anti-Pattern 4)
    - No archived habits in counts or lists (D-11)
    - No omitting zero counts — "0 active · 0 scheduled" always renders
    - No Promote button on active or mastered habit rows (D-12)
    - No .innerHTML anywhere in wavePlanning.js (D-78)
---

<objective>
Build and TDD-verify the pure `buildWavePlanningSection` builder that produces the description tree for the Wave Planning accordion section (WAVE-01 through WAVE-04).

Purpose: Establish the complete builder contract with unit tests before wiring into the DOM in Plan 09-02. The pure builder approach (D-26 / Pattern S8) means all business logic — wave grouping, status counts, health badge, habit list construction, cadence summary — is unit-testable without a DOM environment, a real repo, or async overhead.

Output: `js/views/desktop/wavePlanning.js` (builder module), `tests/unit/wavePlanning.test.js` (unit tests), `js/views/desktop/waveboard.js` (3 helpers now exported so wavePlanning.js can import them).
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/habits/.planning/PROJECT.md
@C:/my-code/vibe-coding/habits/.planning/ROADMAP.md
@C:/my-code/vibe-coding/habits/.planning/STATE.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-CONTEXT.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-RESEARCH.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-PATTERNS.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED/GREEN — builder skeleton + WAVE-01 wave header with startDate</name>
  <files>
    js/views/desktop/waveboard.js,
    js/views/desktop/wavePlanning.js,
    tests/unit/wavePlanning.test.js
  </files>
  <read_first>
    - js/views/desktop/waveboard.js lines 62–70: isoWeekKey function declaration — currently no export keyword; add export
    - js/views/desktop/waveboard.js lines 154–160: worstStatus function declaration — currently no export keyword; add export
    - js/views/desktop/waveboard.js lines 165–170: statusSlug function declaration — currently no export keyword; add export
    - js/views/desktop/waveboard.js lines 326–350: mountWaveboard function and waveboardContainer creation — understand call pattern Plan 09-02 will follow
    - js/util/mount.js: mount() signature — confirms description-tree shape ({ tag, attrs, text, children })
    - js/domain/cadence.js lines 1–100: RESOLVERS dispatch table — confirms cadence type string keys (daily, weekly, monthly, every-n-days, day-of-week-subset)
    - .planning/phases/09-desktop-waveboard/09-PATTERNS.md: full file — file-level JSDoc pattern, imports block, buildWaveItem shape, test fixture pattern with findAll/findFirst/allText tree helpers
    - .planning/phases/09-desktop-waveboard/09-UI-SPEC.md §DOM Contract: authoritative structure — button carries aria-label="Toggle habit list for Wave {N}", list id is "wave-{N}-list", meta wrapper is span.waveplanning-wave-meta containing date + counts + badge spans
  </read_first>
  <behavior>
    RED commit (test(09-01): add failing WAVE-01 test for buildWavePlanningSection):
    - Import buildWavePlanningSection from ../../js/views/desktop/wavePlanning.js
    - Fixtures: WAVES = [{number:1, name:'Morning rituals', startDate:'2026-01-05', theme:'morning'}, {number:2, name:'Focus', startDate:'2026-06-15', theme:'focus'}]; HABITS = []; SNAPSHOTS = new Map(); CURRENT_WEEK = '2026-W01'
    - Tree helpers: findAll(node, pred) recursively collects matching nodes; findFirst(node, pred) returns first match; allText(node) collects all text strings
    - Test: 'renders button.waveplanning-wave-header per wave containing startDate text' — passes WAVES (pre-filtered, no archived) to buildWavePlanningSection({waves:WAVES, habits:[], currentWeekKey:CURRENT_WEEK, snapshotsByWeek:SNAPSHOTS})
    - Assertions: headers array length === WAVES.length AND allText(headers[0]).join(' ').includes('2026-01-05')
    - Run `node --test tests/unit/wavePlanning.test.js` — MUST FAIL (module not found)

    GREEN commit (feat(09-01): implement buildWavePlanningSection skeleton — WAVE-01 green):
    - buildWavePlanningSection({ waves, habits, currentWeekKey, snapshotsByWeek }) returns section.waveplanning description tree
    - Section children: [h2 waveplanning-title, ...waves.map(w => buildWaveItem(w, waveHabits, currentWeekKey, snapshotsByWeek))]
    - buildWaveItem returns div.waveplanning-wave with headerButton + ul stub
    - headerButton: tag:'button', class:'waveplanning-wave-header', aria-expanded:'false', aria-controls:`wave-${wave.number}-list`, aria-label:`Toggle habit list for Wave ${wave.number}`, data-wave-number:String(wave.number)
    - headerButton children: [chevronSpan, nameSpan, metaSpan]
    - metaSpan: tag:'span', class:'waveplanning-wave-meta', children:[dateSpan, countsSpan, badgeSpan(stub)]
    - dateSpan: tag:'span', class:'waveplanning-wave-date', text: wave.startDate ?? ''
    - countsSpan: stub text '0 active · 0 scheduled' (expanded in Task 2)
    - badgeSpan stub: tag:'span', class:'waveplanning-badge waveplanning-badge--upcoming', text:'Upcoming'
    - ul stub: tag:'ul', class:'waveplanning-habit-list', id:`wave-${wave.number}-list`, hidden:'', children:[]
    - Run `node --test tests/unit/wavePlanning.test.js` — MUST PASS
  </behavior>
  <action>
    STEP 1 — Export helpers from waveboard.js: open js/views/desktop/waveboard.js. Locate isoWeekKey function at line 64, worstStatus at line 156, statusSlug at line 167. Add the `export` keyword to each function declaration. Change `function isoWeekKey(` to `export function isoWeekKey(`, `function worstStatus(` to `export function worstStatus(`, `function statusSlug(` to `export function statusSlug(`. No logic changes — add `export` keyword only. These three functions are private today; wavePlanning.js will import them.

    STEP 2 — Create tests/unit/wavePlanning.test.js with the RED failing test. File starts with JSDoc file header per D-27: /** @file wavePlanning.test.js unit tests for buildWavePlanningSection (WAVE-01–04, D-11, D-05, D-06). */. Import buildWavePlanningSection from ../../js/views/desktop/wavePlanning.js using node:test and node:assert. Define three tree-traversal helpers: findAll(node, pred) (depth-first recursive, collects attrs + text + children nodes), findFirst(node, pred), allText(node) (collects all text string values into array). Define minimal WAVES array (2 waves), empty HABITS, empty SNAPSHOTS Map, CURRENT_WEEK string. Write one test inside describe 'WAVE-01' that calls buildWavePlanningSection and finds all nodes where attrs.class includes 'waveplanning-wave-header', asserts count equals WAVES.length, asserts allText(headers[0]) joined includes '2026-01-05'. Run `node --test tests/unit/wavePlanning.test.js` and confirm it fails with a module-not-found error. Commit: test(09-01): add failing WAVE-01 test for buildWavePlanningSection.

    STEP 3 — Create js/views/desktop/wavePlanning.js with the minimal GREEN implementation. File starts with JSDoc file header per D-27 citing WAVE-01, WAVE-02, WAVE-03, WAVE-04, D-11, D-26, D-78. Import worstStatus, statusSlug, isoWeekKey from ./waveboard.js. Import mount from ../../util/mount.js. Import todayLocal from ../../util/date.js. Import getAllWaves from ../../domain/wave.js. Import getCachedHabits from ../../state/store.js. Import apply from ../../state/apply.js. Implement buildWavePlanningSection({ waves, habits, currentWeekKey, snapshotsByWeek }) with the section tree structure described in the behavior block above. Use the exact UI-SPEC DOM Contract node shape: button carries aria-label, aria-controls uses "wave-{N}-list" format, meta wrapper span contains date/counts/badge children. Keep counts and badge as stubs for now. Export buildWavePlanningSection as the only named export. Run `node --test tests/unit/wavePlanning.test.js` — MUST PASS. Commit: feat(09-01): implement buildWavePlanningSection skeleton — WAVE-01 green.
  </action>
  <verify>
    <automated>node --test tests/unit/wavePlanning.test.js</automated>
    All tests pass. RED commit fails, GREEN commit passes.
  </verify>
  <acceptance_criteria>
    - tests/unit/wavePlanning.test.js exists with at least one passing WAVE-01 test
    - js/views/desktop/wavePlanning.js exists and exports buildWavePlanningSection
    - js/views/desktop/waveboard.js shows `export function isoWeekKey`, `export function worstStatus`, `export function statusSlug`
    - `node --test tests/unit/wavePlanning.test.js` exits 0 after GREEN commit
    - Two atomic commits present: RED test commit then GREEN implementation commit
    - wave header button uses aria-controls="wave-{N}-list" format (not waveplanning-list-wave-{N})
  </acceptance_criteria>
  <done>
    tests/unit/wavePlanning.test.js has a passing WAVE-01 test. js/views/desktop/wavePlanning.js exports buildWavePlanningSection with the correct DOM Contract shape. waveboard.js exports isoWeekKey, worstStatus, statusSlug. Two atomic commits present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED/GREEN/REFACTOR — expand builder for WAVE-02, WAVE-03, WAVE-04</name>
  <files>
    js/views/desktop/wavePlanning.js,
    tests/unit/wavePlanning.test.js
  </files>
  <read_first>
    - js/views/desktop/wavePlanning.js: current state after Task 1 — stubs to expand
    - tests/unit/wavePlanning.test.js: current state — add new describe blocks below existing WAVE-01 tests
    - .planning/phases/09-desktop-waveboard/09-PATTERNS.md: buildHabitList, buildActiveHabitRow, buildScheduledHabitRow, buildHealthBadge, cadenceSummary implementation patterns
    - .planning/phases/09-desktop-waveboard/09-UI-SPEC.md §Copywriting Contract: exact label strings — "N active · N scheduled", badge text "Healthy"/"Watch"/"At Risk"/"Failing"/"No data"/"Upcoming", sub-header text "Scheduled", empty state "No habits in this wave.", promote button text "Promote to active"
    - .planning/phases/09-desktop-waveboard/09-UI-SPEC.md §DOM Contract: h3.waveplanning-scheduled-heading for sub-header (not li, not h4), li.waveplanning-empty for empty state, button.waveplanning-promote-btn aria-label="Promote {habit name} to active"
    - js/domain/cadence.js: cadence type string values to confirm TYPE_LABEL dispatch keys
  </read_first>
  <behavior>
    RED commit (test(09-01): add failing WAVE-02/03/04 tests):
    - Extended fixture: HABITS = [{id:'h1',wave:1,status:'active',stage:2,name:'Morning walk',cadence:{type:'daily'}}, {id:'h2',wave:1,status:'scheduled',startDate:'2026-02-01',name:'Evening read',cadence:{type:'daily'}}, {id:'h3',wave:1,status:'archived',name:'Old habit',cadence:{type:'daily'}}, {id:'h4',wave:2,status:'scheduled',startDate:'2026-06-15',name:'Deep work',cadence:{type:'weekly'}}]
    - Pass HABITS.filter(h=>h.status!=='archived') to buildWavePlanningSection (caller filters archived)
    - SNAPSHOTS = new Map([['h1', new Map([['2026-W01', 'Healthy']])]])
    - WAVE-02 test: find countsSpan in wave 1 header; assert text === '1 active · 1 scheduled'
    - WAVE-03 test: expand wave 1 list; find li.waveplanning-scheduled-row; assert contains span.waveplanning-scheduled-date with text '2026-02-01' AND button.waveplanning-promote-btn with data-habit-id 'h2' and aria-label 'Promote Evening read to active'
    - WAVE-04 test: find li.waveplanning-habit-row in wave 1; assert contains span.waveplanning-habit-status text 'active', span.waveplanning-habit-cadence text 'Daily'
    - D-11 test: assert allText of full tree does NOT include 'Old habit' (archived h3 must be absent)
    - D-06 test: wave 2 has only scheduled habits (zero status==='active'); assert wave 2 header badge has class including 'waveplanning-badge--upcoming'
    - D-05 test: wave 1 has active habit h1 with 'Healthy' snapshot; assert wave 1 badge has class including 'waveplanning-badge--healthy'
    - Run `node --test tests/unit/wavePlanning.test.js` — new tests MUST FAIL

    GREEN commit (feat(09-01): expand buildWavePlanningSection — WAVE-02/03/04 green):
    - buildWaveItem full implementation: compute activeHabits = waveHabits.filter(h=>h.status==='active'||h.status==='mastered'); scheduledHabits = waveHabits.filter(h=>h.status==='scheduled'); countsSpan text = `${activeHabits.length} active · ${scheduledHabits.length} scheduled`
    - buildHealthBadge: activeOnly = waveHabits.filter(h=>h.status==='active'); if (activeOnly.length===0) return { tag:'span', attrs:{class:'waveplanning-badge waveplanning-badge--upcoming'}, text:'Upcoming' }; else iterate activeOnly accumulating worstStatus from snapshotsByWeek.get(h.id)?.get(currentWeekKey); if worst===null badge is { class:'waveplanning-badge waveplanning-badge--na', text:'No data' }; else badge slug=statusSlug(worst), badgeText via STATUS_LABEL map {Healthy:'Healthy',Watch:'Watch','At-risk':'At Risk',Failing:'Failing'}
    - buildHabitList: children=[...activeHabits.map(buildActiveHabitRow)]; if scheduledHabits.length>0 push h3.waveplanning-scheduled-heading text 'Scheduled', then ...scheduledHabits.map(buildScheduledHabitRow); if activeHabits.length+scheduledHabits.length===0 children=[li.waveplanning-empty 'No habits in this wave.']; return ul tag with id, class, hidden attribute
    - buildActiveHabitRow(habit): li.waveplanning-habit-row with children [span.waveplanning-habit-name habit.name, span.waveplanning-habit-status habit.status, span.waveplanning-habit-stage String(habit.stage??''), span.waveplanning-habit-cadence cadenceSummary(habit.cadence)]
    - buildScheduledHabitRow(habit): li.waveplanning-scheduled-row with children [span.waveplanning-habit-name habit.name, span.waveplanning-scheduled-date habit.startDate??'', button.waveplanning-promote-btn attrs:{aria-label:`Promote ${habit.name} to active`,'data-habit-id':habit.id} text:'Promote to active']
    - cadenceSummary(cadence): if (!cadence) return ''; TYPE_LABEL dispatch object: {daily:'Daily',weekly:'Weekly',monthly:'Monthly','every-n-days':`Every ${cadence.n} days`,'day-of-week-subset': cadence.days?.map(d=>DOW_LABEL[d]??d).join('/')}; DOW_LABEL={sun:'Sun',mon:'Mon',tue:'Tue',wed:'Wed',thu:'Thu',fri:'Fri',sat:'Sat'}; fallback return cadence.type
    - Run `node --test tests/unit/wavePlanning.test.js` — all tests MUST PASS

    REFACTOR commit (refactor(09-01): discipline constraints check) if changes needed:
    - grep wavePlanning.js for 'switch(' — must return 0
    - grep wavePlanning.js for '.innerHTML' — must return 0
    - Confirm JSDoc on buildWavePlanningSection and cadenceSummary exported function
    - Run `node --test tests/` — full suite must pass
  </behavior>
  <action>
    STEP 1 — Add failing tests to tests/unit/wavePlanning.test.js. Extend the HABITS fixture and SNAPSHOTS Map as described in the behavior block. Add describe blocks for WAVE-02, WAVE-03, WAVE-04, D-11 filter, D-05 health badge, D-06 Upcoming badge. Run `node --test tests/unit/wavePlanning.test.js` and confirm new tests fail (stubs return wrong values). Commit: test(09-01): add failing WAVE-02/03/04 tests.

    STEP 2 — Expand js/views/desktop/wavePlanning.js. Replace all stubs with full implementations per the behavior block above. Key implementation rules: (a) count string uses (active+mastered).length and (scheduled).length — the copywriting contract specifies "active" includes mastered in the count label; (b) D-06 Upcoming badge triggers when waveHabits.filter(h=>h.status==='active').length===0 — mastered does NOT count as active for this check; (c) STATUS_LABEL object maps 'At-risk' to display text 'At Risk' (capital R, space, no hyphen) — do not pass the internal string directly to badge text; (d) cadenceSummary must use a TYPE_LABEL object literal dispatch — no switch statement; (e) buildScheduledHabitRow handles habit.startDate===null by rendering '' not throwing; (f) the scheduled sub-header node is tag:'h3' with class:'waveplanning-scheduled-heading' per UI-SPEC DOM Contract (not tag:'li', not tag:'h4'); (g) empty state li uses tag:'li' class:'waveplanning-empty' text:'No habits in this wave.' — note the period. Run `node --test tests/unit/wavePlanning.test.js` — MUST PASS for all tests. Commit: feat(09-01): expand buildWavePlanningSection — WAVE-02/03/04 green.

    STEP 3 — Refactor check: run `grep -c 'switch(' js/views/desktop/wavePlanning.js` (expect 0). Run `grep -c '\.innerHTML' js/views/desktop/wavePlanning.js` (expect 0). Confirm JSDoc header is present on buildWavePlanningSection export and cadenceSummary function. Run `node --test tests/` — full suite must pass with no regressions. If no changes needed, no refactor commit required.
  </action>
  <verify>
    <automated>node --test tests/unit/wavePlanning.test.js && node --test tests/</automated>
    All wavePlanning.test.js tests pass. Full test suite passes. No regression.
  </verify>
  <acceptance_criteria>
    - `node --test tests/unit/wavePlanning.test.js` exits 0 — covers WAVE-01, WAVE-02, WAVE-03, WAVE-04, D-11, D-05, D-06
    - `node --test tests/` exits 0 — no regressions in existing 756 tests
    - `grep -c 'switch(' js/views/desktop/wavePlanning.js` outputs 0
    - `grep -c '\.innerHTML' js/views/desktop/wavePlanning.js` outputs 0
    - Count format "1 active · 1 scheduled" uses middle dot (·) not asterisk or dash
    - Wave with only scheduled/mastered habits (zero status==='active') has badge class waveplanning-badge--upcoming
    - Badge text for 'At-risk' status displays as "At Risk" (two words, capital R, no hyphen)
    - Scheduled sub-header node uses tag:'h3' with class:'waveplanning-scheduled-heading'
    - Archived habit 'Old habit' does not appear in allText of any wave's description tree
    - buildWavePlanningSection is the only named export from wavePlanning.js at end of this plan
  </acceptance_criteria>
  <done>
    buildWavePlanningSection fully implements WAVE-01 through WAVE-04. Unit tests cover all four requirements, D-11 filter, D-05/D-06 health badge logic, cadenceSummary dispatch table. Full test suite green. No switch statements or innerHTML in the builder file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| habit.name → description tree text | Habit names are user-authored strings passed as text: values in the description tree; mount() will assign them via textContent — no HTML injection path. |
| habit.startDate → description tree text | startDate is a YYYY-MM-DD string from IDB seed data; rendered as text content only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | habit.name rendered in buildScheduledHabitRow / buildActiveHabitRow | low | mitigate | All text values assigned via mount() text: field → textContent (never innerHTML). D-78 grep gate enforced by acceptance_criteria check on this plan. |
| T-09-02 | Tampering | habit.startDate rendered in buildScheduledHabitRow | low | accept | startDate is always a YYYY-MM-DD string from controlled seed data; textContent is safe; null coalesced to empty string. |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages in this phase — constraint per CLAUDE.md. Package legitimacy gate N/A. |
</threat_model>

<verification>
After both tasks complete:
1. `node --test tests/unit/wavePlanning.test.js` exits 0
2. `node --test tests/` exits 0 — no regressions
3. `grep -c 'export function' js/views/desktop/waveboard.js` increased (isoWeekKey, worstStatus, statusSlug now exported)
4. `grep -c 'switch(' js/views/desktop/wavePlanning.js` outputs 0
5. `grep -c '\.innerHTML' js/views/desktop/wavePlanning.js` outputs 0
</verification>

<success_criteria>
- buildWavePlanningSection is a pure exported function from wavePlanning.js
- Unit tests cover all 4 requirements (WAVE-01 through WAVE-04) plus D-11, D-05, D-06
- waveboard.js exports isoWeekKey, worstStatus, statusSlug
- No switch statement in wavePlanning.js
- Full test suite (node --test tests/) passes with no regressions
</success_criteria>

## Source Audit

| Source | Item | Status | Plan/Task |
|--------|------|--------|-----------|
| GOAL | Give desktop Waveboard planning-level overview of every wave — startDate, active vs scheduled counts, drillable habit lists | COVERED | 09-01 Tasks 1+2, 09-02 |
| REQ: WAVE-01 | Desktop Waveboard shows each wave with its planned startDate | COVERED | 09-01 Task 1 (tracer) |
| REQ: WAVE-02 | Each wave row shows counts: active habits vs scheduled habits | COVERED | 09-01 Task 2 |
| REQ: WAVE-03 | Scheduled habits listed per wave with startDate + Promote button | COVERED | 09-01 Task 2 |
| REQ: WAVE-04 | Active habits listed per wave with current status/stage/cadence | COVERED | 09-01 Task 2 |
| CONTEXT: D-01 | Wave Planning at top, heat-map below | COVERED | 09-02 Task 1 |
| CONTEXT: D-02 | Wave Planning is separate DOM element from heat-map | COVERED | 09-02 Task 1 |
| CONTEXT: D-03 | Wave header: name + startDate + counts + health badge | COVERED | 09-01 Tasks 1+2 |
| CONTEXT: D-04 | startDate from getAllWaves() | COVERED | 09-01 Task 1 |
| CONTEXT: D-05 | Health badge = worstStatus of active habits current week | COVERED | 09-01 Task 2 |
| CONTEXT: D-06 | Zero active habits → Upcoming badge | COVERED | 09-01 Task 2 |
| CONTEXT: D-07 | Collapsed by default, chevron shows state | COVERED | 09-01 Task 1 (aria-expanded='false') |
| CONTEXT: D-08 | Entire header row clickable to toggle | COVERED | 09-02 Task 1 (delegated listener) |
| CONTEXT: D-09 | Habit rows: name + status + stage + cadence | COVERED | 09-01 Task 2 |
| CONTEXT: D-10 | Active/mastered first, then Scheduled sub-header, then scheduled | COVERED | 09-01 Task 2 |
| CONTEXT: D-11 | Archived habits not shown | COVERED | 09-01 Task 2 |
| CONTEXT: D-12 | Promote button on scheduled habits | COVERED | 09-01 Task 2 |
| CONTEXT: D-13 | No confirmation dialog on Promote | COVERED | 09-02 Task 1 |
| CONTEXT: D-14 | Section re-renders via store.subscribe | COVERED | 09-02 Task 1 |

<output>
Create `.planning/phases/09-desktop-waveboard/09-01-SUMMARY.md` when done.
</output>
