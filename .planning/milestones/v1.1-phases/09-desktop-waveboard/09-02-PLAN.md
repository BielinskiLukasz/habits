---
phase: 09-desktop-waveboard
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - js/views/desktop/wavePlanning.js
  - js/views/desktop/waveboard.js
  - css/desktop.css
autonomous: true
requirements: [WAVE-01, WAVE-02, WAVE-03, WAVE-04]

must_haves:
  truths:
    - Wave Planning accordion section appears above the 12-week S1 heat-map in desktop.html#waveboard — user sees planning overview first (D-01)
    - mountWavePlanning(parent, {repo, store}) is called inside mountWaveboard before waveboardContainer is created (D-01 wiring constraint)
    - accordion delegated listener is attached to the waveplanning-wrapper container ONCE at mount time — never inside rerenderSection (Pitfall 1)
    - promote delegated listener is attached to container ONCE; in-flight guard (btn.disabled=true) prevents double-fire (D-13)
    - expanded state per wave (aria-expanded values) is saved before rerenderSection clears the DOM and restored after mount() (Pitfall 4)
    - rerenderSection awaits repo.getSnapshotsInRange() BEFORE calling buildWavePlanningSection (Pitfall 2 — health badge data race)
    - store.subscribe is called exactly once inside mountWavePlanning (idempotency guard prevents duplicate subscriptions)
    - hr.waveplanning-heatmap-separator is appended to parent after the waveplanning-wrapper, before the existing heat-map (UI-SPEC DOM Contract)
    - all waveplanning-* CSS classes from UI-SPEC Component Inventory are present in css/desktop.css within @layer desktop-scoring
    - waveplanning-promote-btn uses background: var(--color-accent) with color: #000 — NOT color: var(--color-accent) as border/text (WCAG ~10:1 contrast on amber)
    - waveplanning-badge--na and waveplanning-badge--upcoming have distinct rules: --na uses var(--color-score-na), --upcoming uses var(--color-surface-raised) with color var(--color-fg-muted)
    - "[ASSUMED] WAVE-03 promote: apply({type:'promoteHabit',payload:{habitId}}) dispatched without confirmation dialog (D-13); E5 transient error shown near button on IDB write failure"
    - "[ASSUMED] WAVE-04 unknown status: habit with unknown status shown in active/mastered section as defensive fallback (asserted in Plan 09-01 builder)"
  artifacts:
    - js/views/desktop/wavePlanning.js (modified — adds exported mountWavePlanning)
    - js/views/desktop/waveboard.js (modified — imports and calls mountWavePlanning)
    - css/desktop.css (modified — 30+ .waveplanning-* CSS declarations in @layer desktop-scoring)
  key_links:
    - mountWaveboard calls mountWavePlanning(parent, {repo, store}) before the toggleLabel / waveboardContainer creation at line ~333
    - store.subscribe(async () => rerenderSection()) wired once at mount time — BroadcastChannel signal triggers re-render (D-14)
    - apply({type:'promoteHabit',payload:{habitId}}) dispatched from promote delegated click handler (reuses Phase 8 chokepoint)
    - repo.getSnapshotsInRange(sevenDaysAgoYMD, todayYMD) fetched in rerenderSection; snapshotsByWeek Map keyed by habitId then isoWeekKey
  prohibitions:
    - No rendering Wave Planning inside the existing waveboardContainer (D-02 — separate DOM element)
    - No confirmation dialog before Promote (D-13)
    - No showing scheduled habit startDate as relative label — habit.startDate ISO string rendered as-is
    - No re-attaching event listeners on each rerenderSection call (delegated pattern on container)
    - No direct indexedDB.* calls in wavePlanning.js — use repo.getSnapshotsInRange() only (Anti-Pattern 1)
    - No .innerHTML in wavePlanning.js (D-78)
---

<objective>
Wire `mountWavePlanning` into the live desktop shell and apply all UI-SPEC CSS styles, completing the end-to-end Wave Planning accordion section visible in desktop.html#waveboard.

Purpose: Connect the tested pure builder from Plan 09-01 to real DOM via mount(), plug into store.subscribe reactivity for live updates after Promote, and apply the full visual contract from the approved UI-SPEC.

Output: `js/views/desktop/wavePlanning.js` gains `mountWavePlanning` export; `js/views/desktop/waveboard.js` imports and calls it before the heat-map container; `css/desktop.css` gains all `.waveplanning-*` classes inside `@layer desktop-scoring`.
</objective>

<execution_context>
@C:/my-code/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/my-code/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:/my-code/vibe-coding/habits/.planning/PROJECT.md
@C:/my-code/vibe-coding/habits/.planning/ROADMAP.md
@C:/my-code/vibe-coding/habits/.planning/STATE.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-CONTEXT.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-RESEARCH.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-PATTERNS.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-UI-SPEC.md
@C:/my-code/vibe-coding/habits/.planning/phases/09-desktop-waveboard/09-01-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Wire mountWavePlanning into desktop shell — end-to-end live section</name>
  <files>
    js/views/desktop/wavePlanning.js,
    js/views/desktop/waveboard.js
  </files>
  <read_first>
    - js/views/desktop/wavePlanning.js: full file after Plan 09-01 — append mountWavePlanning below buildWavePlanningSection
    - js/views/desktop/waveboard.js lines 1–35: existing imports block — add import for mountWavePlanning here
    - js/views/desktop/waveboard.js lines 326–360: mountWaveboard function — idempotency guard at line 328, toggleLabel creation starts around line 333; mountWavePlanning(parent,{repo,store}) call goes immediately after the guard check and before the toggleLabel line
    - .planning/phases/09-desktop-waveboard/09-PATTERNS.md §mountWavePlanning pattern: full function with idempotency guard, container creation, delegated accordion listener, delegated promote listener, saveExpandedState, restoreExpandedState, clearChildren, rerenderSection, store.subscribe
    - .planning/phases/09-desktop-waveboard/09-RESEARCH.md §Pitfall 1 (listener accumulation), §Pitfall 2 (snapshot data race), §Pitfall 4 (collapsed state on re-render)
    - .planning/phases/09-desktop-waveboard/09-UI-SPEC.md §DOM Contract: hr.waveplanning-heatmap-separator goes between Wave Planning section and heat-map
    - js/state/store.js lines 218–230: subscribe(fn) signature — verify return value is unsubscribe closure and confirm it accepts async callbacks
    - js/db/repo.js: getSnapshotsInRange(startDateYMD, endDateYMD) signature — confirm parameter and return shape ({habitId, date, s1Status, ...} array)
    - js/util/date.js: todayLocal() — returns YYYY-MM-DD string for today in local timezone
  </read_first>
  <action>
    STEP 1 — Add mountWavePlanning to js/views/desktop/wavePlanning.js (as a second named export after buildWavePlanningSection). Implement the following shape:

    Idempotency guard: check parent.dataset.wavePlanningMounted; return early if truthy; set to 'true' before proceeding.

    Container creation: create div element, set class to 'waveplanning-wrapper', append to parent. Create hr element, set class to 'waveplanning-heatmap-separator', append to parent (after container, before the existing heat-map the caller creates next).

    Delegated accordion listener (ONE, on container at mount time): listen for 'click' on container; inside the handler use e.target.closest('button.waveplanning-wave-header') to identify the target; if not found return; toggle aria-expanded ('false'→'true'→'false'); find the controlled list via document.getElementById(btn.getAttribute('aria-controls')); toggle the hidden attribute on the list (removeAttribute('hidden') to show, setAttribute('hidden','') to hide).

    Delegated promote listener (ONE, on container at mount time, separate event listener): listen for 'click' on container; inside the handler use e.target.closest('button.waveplanning-promote-btn') to identify the target; if not found or btn.disabled return; set btn.disabled=true; extract habitId from btn.dataset.habitId; in a try block await apply({type:'promoteHabit',payload:{habitId}}); in the catch block set btn.disabled=false and show a transient inline error message near the button (create a span with class 'waveplanning-promote-error' and text 'Failed — try again', append near the button, remove after 3000ms). The try/catch on the promote listener is the E5 error handling per UI-SPEC.

    saveExpandedState(): query container.querySelectorAll('button.waveplanning-wave-header'); return new Map of waveNumber (from data-wave-number attribute) to boolean (aria-expanded === 'true').

    restoreExpandedState(state): query same buttons; for each button look up its data-wave-number in the Map; if found as true, set aria-expanded='true' and removeAttribute('hidden') on its controlled list; if not found leave as false/hidden.

    clearChildren(el): while (el.firstChild) el.removeChild(el.firstChild).

    rerenderSection() (async, private): (1) call saveExpandedState() to capture current accordion state; (2) call clearChildren(container); (3) get waves = getAllWaves(); get habits = getCachedHabits().filter(h=>h.status!=='archived'); (4) compute today = todayLocal(); compute sevenDaysAgo = new Date(today) minus 6 days formatted as YYYY-MM-DD; (5) fetch snapshotRows = await repo.getSnapshotsInRange(sevenDaysAgo, today) — wrap in try/catch, use [] on error; (6) build snapshotsByWeek: new Map where for each snapshotRow, get or create a Map keyed by habitId, then set isoWeekKey(row.date) → row.s1Status; (7) currentWeekKey = isoWeekKey(today); (8) desc = buildWavePlanningSection({waves, habits, currentWeekKey, snapshotsByWeek}); (9) mount(desc, container); (10) restoreExpandedState(expandedState).

    store.subscribe call: store.subscribe(async () => { await rerenderSection(); }) — called once after the listeners are set up.

    Initial render: call rerenderSection() without await at the end of mountWavePlanning (fire-and-forget; the section will populate asynchronously).

    STEP 2 — Add import to js/views/desktop/waveboard.js: at the top of the imports block (with other ./wavePlanning.js or local imports), add: import { mountWavePlanning } from './wavePlanning.js';

    STEP 3 — Call mountWavePlanning inside mountWaveboard: immediately after the idempotency guard (the guard is around line 328–329: `if (parent.dataset.mounted === 'waveboard') return; parent.dataset.mounted = 'waveboard';`), before any toggleLabel or waveboardContainer creation, add the call: mountWavePlanning(parent, { repo, store }); This ensures the Wave Planning div and the separator hr are in the DOM before the existing heat-map elements are appended, honoring D-01 (Wave Planning above heat-map).

    Do NOT modify toggleLabel, waveboardContainer, the existing refresh() function, or any existing heat-map rendering logic in waveboard.js.
  </action>
  <verify>
    <automated>node --test tests/</automated>
    <human-check>
      1. Run: node scripts/serve.js
      2. Open: http://localhost:8080/desktop.html#waveboard
      3. Confirm: "Wave Planning" h2 heading appears above the "Show archived" checkbox and heat-map grid
      4. Confirm: 10 wave rows are visible, each showing wave name + startDate (ISO YYYY-MM-DD) in the header
      5. Click any wave header row — the habit list expands below the header; click again to collapse
      6. If any scheduled habits exist: expand the wave, click "Promote to active" — the section re-renders, the habit moves from Scheduled sub-list to the active list, and counts update
      7. Expand two waves, then promote a habit in one — both waves remain expanded after the re-render
    </human-check>
  </verify>
  <acceptance_criteria>
    - `node --test tests/` exits 0 — no regressions
    - js/views/desktop/waveboard.js contains the import line: import { mountWavePlanning } from './wavePlanning.js'
    - mountWavePlanning is called inside mountWaveboard before toggleLabel creation (verified by reading the file)
    - js/views/desktop/wavePlanning.js exports mountWavePlanning as a named export
    - `grep -c 'dataset.wavePlanningMounted' js/views/desktop/wavePlanning.js` outputs >= 1 (idempotency guard present)
    - `grep -c 'store.subscribe' js/views/desktop/wavePlanning.js` outputs 1 (subscribed exactly once)
    - `grep -c '\.innerHTML' js/views/desktop/wavePlanning.js` outputs 0
    - rerenderSection contains an await before calling buildWavePlanningSection (Pitfall 2 avoidance — snapshot fetch happens before build)
  </acceptance_criteria>
  <done>
    Wave Planning section renders live in desktop.html#waveboard above the heat-map. Accordion toggle works per wave. Promote action dispatches without confirmation dialog and triggers a reactive re-render. Expanded state is preserved across re-renders. Full test suite passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: CSS — append all .waveplanning-* classes to css/desktop.css</name>
  <files>
    css/desktop.css
  </files>
  <read_first>
    - css/desktop.css lines 86–182: existing @layer desktop-scoring block — new classes go inside this block, before the closing brace at line 182
    - css/tokens.css: confirm these custom properties exist — --color-score-na, --color-score-healthy, --color-score-watch, --color-score-atrisk, --color-score-failing, --color-surface-raised, --color-fg-muted, --color-fg-faint, --color-accent, --color-border, --color-fg, --color-surface, --radius-sm, --space-1 through --space-6, --text-sm, --text-md, --text-lg
    - .planning/phases/09-desktop-waveboard/09-UI-SPEC.md §Component Inventory: authoritative CSS class list with property values — use these values verbatim; PATTERNS.md has draft values that may differ
  </read_first>
  <action>
    Open css/desktop.css. Locate the closing brace of the @layer desktop-scoring block at line 182. Using Edit (scoped replacement), insert the following CSS block immediately before the closing brace, inside the layer:

    Append a comment line: /* Wave Planning accordion — Phase 9 (WAVE-01, WAVE-02, WAVE-03, WAVE-04) */ then add CSS declarations for all .waveplanning-* classes from the UI-SPEC Component Inventory in this order:

    1. .waveplanning — margin-bottom: var(--space-6)
    2. .waveplanning-title — font-size: var(--text-lg); font-weight: 600; margin: 0 0 var(--space-3) 0; color: var(--color-fg)
    3. .waveplanning-wave — border-bottom: 1px solid var(--color-border)
    4. .waveplanning-wave-header — display: flex; align-items: center; gap: var(--space-2); padding: var(--space-3) var(--space-4); min-height: 44px; cursor: pointer; background: none; border: none; width: 100%; text-align: left; color: var(--color-fg)
    5. .waveplanning-wave-header:hover, .waveplanning-wave-header:focus-visible — background: var(--color-surface); outline: 2px solid var(--color-accent); outline-offset: -2px
    6. .waveplanning-chevron — font-size: var(--text-sm); color: var(--color-fg-muted); flex-shrink: 0; display: inline-block; transition: transform 0.15s ease
    7. .waveplanning-wave-header[aria-expanded="true"] .waveplanning-chevron — transform: rotate(90deg)
    8. .waveplanning-wave-name — flex: 1; font-size: var(--text-md); font-weight: 600; white-space: normal; word-break: break-word
    9. .waveplanning-wave-meta — display: flex; align-items: center; gap: var(--space-3); flex-shrink: 0
    10. .waveplanning-wave-date — font-size: var(--text-sm); color: var(--color-fg-muted); white-space: nowrap
    11. .waveplanning-wave-counts — font-size: var(--text-sm); color: var(--color-fg-muted); white-space: nowrap
    12. .waveplanning-badge — display: inline-block; padding: 2px var(--space-2); border-radius: 4px; font-size: var(--text-sm); font-weight: 600; color: #fff; white-space: nowrap
    13. .waveplanning-badge--healthy — background: var(--color-score-healthy)
    14. .waveplanning-badge--watch — background: var(--color-score-watch)
    15. .waveplanning-badge--atrisk — background: var(--color-score-atrisk)
    16. .waveplanning-badge--failing — background: var(--color-score-failing)
    17. .waveplanning-badge--na — background: var(--color-score-na)
    18. .waveplanning-badge--upcoming — background: var(--color-surface-raised); color: var(--color-fg-muted) [this overrides color:#fff from .waveplanning-badge; place after .waveplanning-badge]
    19. .waveplanning-habit-list — list-style: none; margin: 0; padding: 0 0 var(--space-2) var(--space-5)
    20. .waveplanning-habit-list[hidden] — display: none
    21. .waveplanning-habit-row — display: flex; align-items: center; gap: var(--space-3); padding: var(--space-2) 0; border-bottom: 1px solid var(--color-border); font-size: var(--text-md)
    22. .waveplanning-habit-name — flex: 1; font-weight: 400; word-break: break-word
    23. .waveplanning-habit-status — font-size: var(--text-sm); color: var(--color-fg-muted); flex-shrink: 0; white-space: nowrap
    24. .waveplanning-habit-stage — font-size: var(--text-sm); color: var(--color-fg-muted); flex-shrink: 0; white-space: nowrap
    25. .waveplanning-habit-cadence — font-size: var(--text-sm); color: var(--color-fg-muted); flex-shrink: 0; white-space: nowrap
    26. .waveplanning-scheduled-heading — font-size: var(--text-sm); font-weight: 600; color: var(--color-fg-muted); padding: var(--space-3) 0 var(--space-2) 0; margin: 0; border-top: 1px solid var(--color-border)
    27. .waveplanning-scheduled-row — display: flex; align-items: center; gap: var(--space-3); padding: var(--space-2) 0; border-bottom: 1px solid var(--color-border); font-size: var(--text-md)
    28. .waveplanning-scheduled-date — font-size: var(--text-sm); color: var(--color-fg-muted); flex-shrink: 0; white-space: nowrap
    29. .waveplanning-promote-btn — display: inline-flex; align-items: center; justify-content: center; padding: var(--space-1) var(--space-3); min-height: 32px; background: var(--color-accent); color: #000; border: none; border-radius: var(--radius-sm); font-size: var(--text-sm); font-weight: 600; cursor: pointer; flex-shrink: 0; white-space: nowrap
    30. .waveplanning-promote-btn:hover, .waveplanning-promote-btn:focus-visible — opacity: 0.88; outline: 2px solid var(--color-accent); outline-offset: 2px
    31. .waveplanning-promote-btn:disabled — opacity: 0.5; cursor: not-allowed
    32. .waveplanning-empty — padding: var(--space-3) 0; font-size: var(--text-sm); color: var(--color-fg-faint); font-style: italic; list-style: none
    33. .waveplanning-heatmap-separator — border: none; border-top: 1px solid var(--color-border); margin: var(--space-5) 0
    34. .waveplanning-promote-error — font-size: var(--text-sm); color: var(--color-score-failing); margin-left: var(--space-2)

    Use Edit (scoped replacement) targeting the last declaration inside @layer desktop-scoring before its closing brace. Do NOT modify any existing rules in desktop.css.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('css/desktop.css','utf8');const n=(c.match(/\.waveplanning-/g)||[]).length;console.log('waveplanning- count:',n);process.exit(n>=20?0:1);"</automated>
    <automated>node --test tests/</automated>
    <human-check>
      1. Hard-refresh desktop.html#waveboard in browser (Ctrl+Shift+R or Cmd+Shift+R)
      2. Wave rows show visible separator borders between them (border-bottom 1px solid)
      3. Click a wave header — habit list expands below with correct indentation (padding-left --space-5)
      4. Promote button shows amber/accent background with dark (#000) text — clearly readable
      5. Health badge pill visible with correct background color per wave health status
      6. Collapsed wave shows ▶ chevron pointing right; expanded wave shows ▶ rotated 90 degrees (smooth CSS transition)
      7. Wave with no habits shows "No habits in this wave." in italic muted text when expanded
      8. hr separator line visible between Wave Planning and the heat-map grid below
    </human-check>
  </verify>
  <acceptance_criteria>
    - `node -e "..." css/desktop.css` check (as above) exits 0 — at least 20 .waveplanning- occurrences
    - css/desktop.css contains all 4 health badge variants: waveplanning-badge--healthy, --watch, --atrisk, --failing
    - css/desktop.css contains waveplanning-badge--na AND waveplanning-badge--upcoming with distinct background properties
    - .waveplanning-promote-btn rule uses `background: var(--color-accent)` and `color: #000` (not color: var(--color-accent))
    - .waveplanning-habit-list[hidden] rule has `display: none`
    - .waveplanning-wave-header[aria-expanded="true"] .waveplanning-chevron rule has `transform: rotate(90deg)`
    - No existing @layer desktop-scoring rules modified (append only)
    - `node --test tests/` exits 0
  </acceptance_criteria>
  <done>
    All .waveplanning-* CSS classes from the approved UI-SPEC Component Inventory are in css/desktop.css inside @layer desktop-scoring. Wave Planning section is fully styled: wave rows have borders, habit rows have indentation, health badges have semantic colors, Promote button is amber/dark, chevron animates on expand/collapse.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| habit.name → DOM textContent | Habit names passed to buildScheduledHabitRow and rendered via mount() text: field — textContent assignment only, no innerHTML path. |
| promote click → IDB write | apply({ type:'promoteHabit' }) dispatches to the established single-mutator chokepoint in apply.js — no new IDB surface in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-03 | Tampering | delegated promote listener — triggers apply() on click | low | mitigate | In-flight guard (btn.disabled=true on first click, reset on error) prevents double-dispatch; apply() chokepoint enforces atomic transaction invariants. |
| T-09-04 | Denial of Service | store.subscribe called in mount loop | low | mitigate | Idempotency guard (dataset.wavePlanningMounted) ensures subscribe is called exactly once even if mountWaveboard is somehow re-entered. Acceptance criteria grep gate enforces this. |
| T-09-05 | Information Disclosure | habit names visible in expanded wave lists | low | accept | Single-user personal app; no auth surface; no network; data is the user's own. Privacy constraint in CLAUDE.md satisfied (no network calls). |
| T-09-SC | Tampering | npm/pip/cargo installs | low | accept | No external packages in this phase. Package legitimacy gate N/A. |
</threat_model>

<verification>
After both tasks complete:
1. `node --test tests/` exits 0 — no regressions
2. `grep -c 'mountWavePlanning' js/views/desktop/waveboard.js` outputs >= 2 (import line + call line)
3. `grep -c 'store.subscribe' js/views/desktop/wavePlanning.js` outputs 1
4. `grep -c '\.innerHTML' js/views/desktop/wavePlanning.js` outputs 0
5. `grep -c '\.waveplanning-' css/desktop.css` outputs >= 20
6. Visual: Wave Planning accordion visible above heat-map in desktop.html#waveboard
7. Visual: Promote action re-renders section reactively with no confirmation dialog
</verification>

<success_criteria>
- Wave Planning accordion section renders live in desktop.html#waveboard above the 12-week S1 heat-map
- All 10 waves show their planned startDate and active/scheduled counts
- Accordion toggle (expand/collapse per wave) works via entire header row click
- Promote action fires without confirmation dialog, re-renders section reactively
- Expanded state preserved across re-renders
- Full CSS styling applied per UI-SPEC Component Inventory
- Full test suite (node --test tests/) passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/09-desktop-waveboard/09-02-SUMMARY.md` when done.
</output>
