---
phase: 07-scheduled-status-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - js/main.js
  - js/desktop.js
  - scripts/convert-nawyki.js
  - js/io/import.js
autonomous: true
requirements:
  - DATA-01
  - DATA-02

must_haves:
  truths:
    - "js/main.js imports configureScheduled and bootScheduled from ./domain/scheduled.js"
    - "js/main.js calls configureScheduled({repo}) in the P2 configure block"
    - "js/main.js calls await bootScheduled() with a swallowed try/catch after bootSeed() and before hydrate()"
    - "js/desktop.js has the same three additions as main.js"
    - "scripts/convert-nawyki.js outputs status:'scheduled' for habits whose startDate is in the future at run time"
    - "js/io/import.js mergeImportedStores correctly round-trips status:'scheduled' from imported JSON (no changes needed — verified by reading the file)"
  artifacts:
    - js/main.js (modified)
    - js/desktop.js (modified)
    - scripts/convert-nawyki.js (modified)
  key_links:
    - "bootScheduled() must be called AFTER bootSeed() and BEFORE hydrate() in both shells — hydrate() pre-warms cache with final correct statuses"
    - "configureScheduled({repo}) must be called in the P2 configure block alongside configureApply, configureUndo, configureSeed"
    - "scripts/convert-nawyki.js must define TODAY before the main loop and use it for status derivation"
---

<objective>
Wire bootScheduled() into the boot sequence of both HTML shells (main.js and desktop.js) and fix the status output in scripts/convert-nawyki.js. Verify that js/io/import.js already satisfies DATA-02 with no changes.

Purpose: Activates the scheduled domain module on every app boot. After this plan, opening either shell triggers the migration pass (first boot) and the promotion pass (every boot), so IDB always has correct statuses before the UI hydrates.

Output: Modified main.js, desktop.js, convert-nawyki.js. No new files.
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-scheduled-status-foundation/07-CONTEXT.md
@.planning/phases/07-scheduled-status-foundation/07-PATTERNS.md
@.planning/phases/07-scheduled-status-foundation/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire bootScheduled into main.js and desktop.js boot sequences</name>
  <files>js/main.js, js/desktop.js</files>
  <read_first>
    - js/main.js — read in full; locate: (a) the P2 spine imports block (~line 50–71) where configureWave is imported from ./domain/wave.js — add configureScheduled/bootScheduled import alongside it; (b) the P2 configure block (~lines 85–99) where configureWave({fetch}) is called — add configureScheduled({repo}) after configureUndo({repo}); (c) the try/catch boot sequence (~lines 102–111) where bootSeed and hydrate are called — add bootScheduled between them
    - js/desktop.js — read in full; locate same three insertion points: P6 imports block (line ~58 where configureWave is imported); configure block (line ~83–101 where configureUndo is called); boot sequence (lines ~89–90 between bootSeed and hydrate)
    - js/domain/scheduled.js — confirm exports: configureScheduled, bootScheduled (created by plan 07-01)
  </read_first>
  <action>
    In js/main.js:
    1. Import block: add `import { configureScheduled, bootScheduled } from './domain/scheduled.js';` on the line immediately after the `import { configureWave, bootWaves } from './domain/wave.js';` line (D-09 — domain module alongside other domain imports)
    2. Configure block: add `configureScheduled({ repo });` immediately after the `configureUndo({ repo });` call (D-10 — DI seam in P2 wiring block)
    3. Boot sequence: add `try { await bootScheduled(); } catch (_e) { /* swallow — promotion/migration non-critical on failure */ }` on a new line immediately after the existing `try { await bootSeed(); } catch (_e) { /* swallow ... */ }` line and BEFORE the `try { await hydrate(); } catch (_e) { /* swallow */ }` line (D-08 — before hydrate so cache sees final statuses)
    4. Update the file-level JSDoc comment to add `await bootScheduled()` in the boot sequence list (between step 9 and 10 in the numbered list)

    In js/desktop.js:
    Same four changes mirrored exactly:
    1. Import: add `import { configureScheduled, bootScheduled } from './domain/scheduled.js';` after `import { configureWave, bootWaves } from './domain/wave.js';` (line ~58)
    2. Configure: add `configureScheduled({ repo });` after `configureUndo({ repo });` in the P2 configure block
    3. Boot sequence: add `try { await bootScheduled(); } catch (_e) { /* swallow */ }` after bootSeed and before hydrate (between lines ~89 and ~90)
    4. Update file-level JSDoc boot sequence list to include `await bootScheduled()` between steps 8 and 9

    Per D-11: both shells must wire bootScheduled — the desktop shell may be the first tab opened.
  </action>
  <verify>
    <automated>node --test tests/unit/ && node --test tests/integration/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - js/main.js contains the import line: `import { configureScheduled, bootScheduled } from './domain/scheduled.js';`
    - js/main.js contains `configureScheduled({ repo });` in the configure block
    - js/main.js contains `await bootScheduled()` in a try/catch between bootSeed and hydrate
    - js/desktop.js contains all three of the same additions
    - node --test tests/unit/ and node --test tests/integration/ both exit 0 with no new failures
  </acceptance_criteria>
  <done>Both main.js and desktop.js import and wire bootScheduled; all existing tests pass</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix convert-nawyki.js status output + verify import.js DATA-02 compliance</name>
  <files>scripts/convert-nawyki.js, js/io/import.js</files>
  <read_first>
    - scripts/convert-nawyki.js — read in full; locate: (a) near the top where constants are defined (~lines 1–25) — add a TODAY constant after the existing constant block; (b) inside the main loop habits.push() call (~line 278) where status:'active' is hardcoded — replace with conditional
    - js/io/import.js — read mergeImportedStores function; confirm it uses raw IDB put() or repo.runTx with put that copies all fields including status from the imported JSON — verify DATA-02 is satisfied without changes
  </read_first>
  <action>
    In scripts/convert-nawyki.js:
    1. After the `const ROOT = path.join(__dirname, '..');` line at the top of the file, add: `const TODAY = new Date().toISOString().slice(0, 10);`
    2. In the habits.push() call, change the `status: 'active',` line to: `status: startDate > TODAY ? 'scheduled' : 'active',`
       Note: startDate is the local variable already in scope at that point in the loop (assigned at line ~262 as `const startDate = fields[5].trim() || null;`)
       Edge case: if startDate is null (empty in CSV), `null > TODAY` evaluates to false → status stays 'active' — correct behavior per D-02 (null startDate = start now = active)
    3. No other changes to the file

    In js/io/import.js:
    - Read the file and verify that mergeImportedStores uses a put-based upsert that preserves all fields from the imported JSON including status (D-12: raw put() upsert preserves whatever status is in the imported JSON)
    - If confirmed: make NO changes to js/io/import.js (DATA-02 is already satisfied)
    - If not confirmed: add to the summary noting what was found
  </action>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('scripts/convert-nawyki.js','utf8'); const hasToday = /const TODAY = new Date/.test(s); const hasConditional = /startDate > TODAY/.test(s); console.log('TODAY:', hasToday, 'conditional:', hasConditional); process.exit(hasToday && hasConditional ? 0 : 1);"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/convert-nawyki.js contains `const TODAY = new Date().toISOString().slice(0, 10);` near the top of the file
    - scripts/convert-nawyki.js contains `status: startDate > TODAY ? 'scheduled' : 'active',` in the habits.push() block
    - The literal string `status: 'active',` no longer appears in the habits.push() block of convert-nawyki.js
    - js/io/import.js is unchanged (DATA-02 verified by reading)
    - node --test tests/unit/import.test.js exits 0
  </acceptance_criteria>
  <done>convert-nawyki.js outputs correct status for future-startDate habits; import.js verified as DATA-02 compliant without changes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boot sequence order | bootScheduled must fire after bootSeed and before hydrate |
| CSV import → convert-nawyki.js | startDate comes from CSV field 5; null check needed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-07-04 | Tampering | boot order violation | medium | mitigate | Code placement enforces order (after bootSeed, before hydrate); JSDoc comment updated to document the constraint |
| T-07-05 | Information Disclosure | convert-nawyki.js null startDate | low | accept | null > TODAY is false in JS → status:'active' which is the correct fallback (D-02) |
</threat_model>

<verification>
node --test tests/unit/ → all pass
node --test tests/integration/ → all pass
Manual: node --test tests/integration/apply.createHabit.test.js → scheduled status tests pass (from plan 07-02)
Manual: node --test tests/unit/scheduled.test.js → all scheduled domain tests pass (from plan 07-01)
</verification>

<success_criteria>
- js/main.js and js/desktop.js both wire configureScheduled and bootScheduled in the correct boot position
- scripts/convert-nawyki.js correctly outputs status:'scheduled' for future-startDate habits
- js/io/import.js confirmed as DATA-02 compliant with no changes required
- Full test suite passes: node --test tests/unit/ && node --test tests/integration/
</success_criteria>

## Artifacts this phase produces

Modified symbols:
- `js/main.js` — three additions: import, configure call, boot call
- `js/desktop.js` — same three additions as main.js
- `scripts/convert-nawyki.js` — TODAY constant added; status field made conditional
- `js/io/import.js` — read-only verification (no changes expected)

<output>
Create .planning/phases/07-scheduled-status-foundation/07-03-SUMMARY.md when done
</output>
