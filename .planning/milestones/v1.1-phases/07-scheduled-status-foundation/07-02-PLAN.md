---
phase: 07-scheduled-status-foundation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - js/state/apply/createHabit.js
  - tests/integration/apply.createHabit.test.js
autonomous: true
requirements:
  - SCHED-01
  - SCHED-02

must_haves:
  truths:
    - "handleCreateHabit with startDate:'2099-01-01' returns a habitRow with status:'scheduled'"
    - "handleCreateHabit with startDate omitted (defaults to today) returns a habitRow with status:'active'"
    - "handleCreateHabit with startDate equal to today returns a habitRow with status:'active'"
    - "handleCreateHabit with startDate in the past returns a habitRow with status:'active'"
    - "status derivation is done inside the handler — callers cannot pass status in the event payload"
  artifacts:
    - js/state/apply/createHabit.js (modified)
    - tests/integration/apply.createHabit.test.js (extended)
  key_links:
    - "status field in habitRow is derived as: sd > todayLocal() ? 'scheduled' : 'active' where sd = startDate ?? todayLocal()"
    - "todayLocal() is already imported in createHabit.js — no new import needed"
    - "sd variable is already computed at line 78 — the change replaces the hardcoded 'active' string at line 86"
---

<objective>
Fix js/state/apply/createHabit.js to derive habit status from startDate instead of hardcoding 'active'. Extend the existing test file with cases for future startDate → 'scheduled' and past/today → 'active'. Existing tests that assert status === 'active' for habits without startDate must continue to pass.

Purpose: Delivers SCHED-01 (scheduled status supported) and SCHED-02 (habits with future startDate stored as scheduled). The seed habits with future startDates (waves 5–9) will automatically receive status:'scheduled' when seeded via this same apply path (D-03).

Output: Modified createHabit.js + extended test file (RED commit for new cases → GREEN commit for fix)
</objective>

<execution_context>
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/habits/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/lukasz.bielinski/OneDrive - Accenture/my-folders/my-projects/vibe-coding/habits/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-scheduled-status-foundation/07-CONTEXT.md
@.planning/phases/07-scheduled-status-foundation/07-PATTERNS.md
</context>

<feature>
  <name>createHabit status derivation from startDate</name>
  <files>js/state/apply/createHabit.js, tests/integration/apply.createHabit.test.js</files>
  <read_first>
    - js/state/apply/createHabit.js — full file; understand current status:'active' hardcode at line 86, the sd variable at line 78, and the todayLocal import at line 32
    - tests/integration/apply.createHabit.test.js — full file; understand existing test structure and the todayYMD() helper; note existing test 'createHabit habit row has correct default fields' asserts status === 'active' — this must continue to pass (habit with no startDate defaults to today → active)
    - js/util/date.js — todayLocal() signature to confirm it returns YYYY-MM-DD string
  </read_first>
  <behavior>
    RED tests to add to tests/integration/apply.createHabit.test.js:

    - 'createHabit with future startDate stores habit as scheduled':
      payload: {name:'Future habit', wave:5, cadence:{type:'daily'}, targetType:'binary', startDate:'2099-06-01'}
      assertion: habits[0].status === 'scheduled'

    - 'createHabit with past startDate stores habit as active':
      payload: {name:'Past habit', wave:1, cadence:{type:'daily'}, targetType:'binary', startDate:'2020-01-01'}
      assertion: habits[0].status === 'active'

    - 'createHabit with startDate equal to today stores habit as active':
      payload: {name:'Today habit', wave:1, cadence:{type:'daily'}, targetType:'binary', startDate: todayYMD()}
      assertion: habits[0].status === 'active'

    Existing test 'createHabit habit row has correct default fields' asserts status === 'active' — this test must still pass after the fix (no startDate → sd = todayLocal() → status:'active').

    Implementation change in js/state/apply/createHabit.js:
    - At line 86, replace the literal string 'active' with the expression: sd > todayLocal() ? 'scheduled' : 'active'
    - sd is already computed at line 78 as: const sd = startDate ?? todayLocal();
    - todayLocal is already imported at line 32
    - No other changes needed
  </behavior>
  <implementation>
    In js/state/apply/createHabit.js:
    - Change only the status field in habitRow (line 86)
    - From: status: 'active',
    - To: status: sd > todayLocal() ? 'scheduled' : 'active',
    - Update the JSDoc file header to reference SCHED-01, SCHED-02 in the locked decisions list
    - No other changes to the file

    In tests/integration/apply.createHabit.test.js:
    - Add three new test cases inside the existing describe block (or a new nested describe)
    - Reuse the existing freshApply() and todayYMD() helpers already defined in the file
    - Use node:test + node:assert/strict only (no new imports)
    - Keep existing tests unchanged — they must still pass
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event.payload.startDate → status derivation | Caller-provided startDate determines status; no validation on string format |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|-----------|----------|-----------|----------|-------------|------------|
| T-07-03 | Tampering | startDate string comparison | low | accept | IDB stores whatever string is provided; status derivation is a string comparison (lexicographic); malformed dates would produce incorrect status but are a caller-contract violation, not a security concern in a single-user offline app |
</threat_model>

<verification>
RED phase: node --test tests/integration/apply.createHabit.test.js → new test cases FAIL (status still 'active' for future startDate)
GREEN phase: node --test tests/integration/apply.createHabit.test.js → ALL tests pass including new cases
Full suite: node --test tests/ → no regressions across all test files
</verification>

<success_criteria>
- node --test tests/integration/apply.createHabit.test.js passes all tests including new cases
- Habit created with startDate:'2099-06-01' → status:'scheduled' in IDB
- Habit created with no startDate or past startDate → status:'active' in IDB
- No regressions: node --test tests/ passes
</success_criteria>

## Artifacts this phase produces

Modified symbols:
- `js/state/apply/createHabit.js` — status field derivation changed from hardcoded 'active' to conditional expression
- `tests/integration/apply.createHabit.test.js` — three new test cases added

<output>
Create .planning/phases/07-scheduled-status-foundation/07-02-SUMMARY.md when done
</output>
