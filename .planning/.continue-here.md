---
context: phase
phase: 08-today-catalog-upcoming-section
task: null
total_tasks: null
status: ready_to_plan
last_updated: "2026-07-02T22:40:02.888Z"
---

# Continue Here — Phase 8 Ready to Plan

_No blocking constraints. No anti-patterns identified._

<current_state>
Between phases. Phase 7 (Scheduled Status Foundation) is COMPLETE — all 3 plans executed, verified, committed. Working tree is clean. STATE.md correctly identifies Phase 8 as the current phase but its progress block is slightly stale (shows 0/3 phases for v1.1 because the STATE.md progress counters were not updated after Phase 7 execution).

Phase 8 has NO directory, NO plans, NO executions. It must be planned from scratch.
</current_state>

<completed_work>

**v1.0 MVP — COMPLETE (2026-06-30)**
- 6 phases: PWA Shell → Storage → Today → Domain → Backup/Restore → Desktop Analytics
- 309 commits, 44 plans, 123 requirements, 756 tests
- APP_VERSION: 0.5.0, git tag: v1.0

**v1.1 Phase 7 — Scheduled Status Foundation — COMPLETE (2026-07-01)**
- `js/domain/scheduled.js` — boot service: `runMigration()` (one-time DATA-03 pass) + `runPromotion()` (every boot SCHED-03 pass) + `bootScheduled()` exported entry point
- `js/state/apply/createHabit.js` — status derived from startDate: `startDate > todayLocal() ? 'scheduled' : 'active'`
- `scripts/convert-nawyki.js` — `status: 'scheduled'` for future-startDate habits
- Both `js/main.js` and `js/desktop.js` wired: `configureScheduled({repo})` → `bootScheduled()` in boot sequence
- 11 unit tests (scheduled.test.js) + 8 integration tests (apply.createHabit.test.js) — all pass
- REQUIREMENTS covered: SCHED-01, SCHED-02, SCHED-03, DATA-01, DATA-02, DATA-03
</completed_work>

<remaining_work>

**Phase 8 — Today & Catalog — Upcoming Section** (NOT STARTED)
Requirements to cover:
- SCHED-02 — Scheduled habits hidden from Today view and active Catalog list
- SCHED-03 — Catalog shows scheduled habits in a dedicated "Upcoming" section
- SCHED-06 — Manual promote-to-active action available from the Upcoming section
- SCHED-04 — (per VERIFICATION.md: scoped to Phase 8) User can manually promote a scheduled habit before startDate

**Phase 9 — Desktop Waveboard** (NOT STARTED)
Requirements to cover:
- SCHED-04 — Desktop Waveboard surfaces scheduled habits per wave with their startDate

**STATE.md cleanup** — After Phase 8 planning, update progress counters (currently stale at 0%).
</remaining_work>

<decisions_made>

v1.1 locked decisions (all from Phase 7 context, still binding):
- `scheduled` is the 4th habit status alongside `active` / `mastered` / `archived`
- Auto-transition `scheduled → active` fires on app boot when `startDate <= today` (already implemented in Phase 7)
- Existing habits with `status: 'active'` and `startDate > today` are migrated on first boot — one-time pass (already implemented)
- Catalog Upcoming section sorted by `startDate` ascending
- Waveboard is informational only in v1.1 — no edit actions on wave definitions

Phase 7 implementation decisions (documented in 07-CONTEXT.md, still binding for Phase 8):
- `status` is never in the event payload; it is derived by domain rules — callers cannot accidentally create a wrong-status habit
- `bootScheduled()` runs AFTER `bootSeed()` and BEFORE `hydrate()` in both shells
- `repo.runTx()` used directly in migration/promotion — these are not user actions, no `events` store rows
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current milestone/phase state
2. `.planning/PROJECT.md` — active requirements (SCHED-01 through SCHED-07)
3. `.planning/phases/07-scheduled-status-foundation/07-VERIFICATION.md` — what Phase 7 delivered exactly (Phase 8 builds on this)
4. `.planning/phases/07-scheduled-status-foundation/07-CONTEXT.md` — architecture decisions still binding in Phase 8

## Infrastructure State
- Dev server: `node scripts/serve.js` → `http://localhost:8080/` (not running; start manually)
- Tests: `node --test tests/unit/*.test.js` and `node --test tests/integration/*.test.js`
- Pre-existing test failures (NOT introduced by Phase 7, not blocking):
  - `tests/unit/import.test.js`: 2 failing broadcast stub tests
  - `tests/integration/import.test.js`: 2 failing broadcast stub tests
  - `tests/integration/apply.mastery.test.js`: 1 failing stub
  - `tests/integration/apply.wave.test.js`: 1 failing stub

<context>
Phase 7 delivered the complete data foundation for scheduled habits. Phase 8 is the UI layer — it needs to:
1. Filter scheduled habits out of Today view and the active Catalog list (SCHED-02)
2. Add a "Upcoming" section to the Catalog showing scheduled habits sorted by startDate (SCHED-03)
3. Add a "Promote to active" action on each Upcoming item (SCHED-06)

The repo layer already knows about scheduled status; the UI just needs to filter/render correctly. This is primarily a `js/ui/` and possibly `js/state/apply/` change. The Catalog is on the mobile shell (`index.html` / `js/ui/catalog/`).
</context>

<next_action>
Start with: `/gsd-plan-phase 8`
This will research and plan the Today & Catalog — Upcoming Section UI work. Phase 8 has no directory yet — the planner will create it.
</next_action>
