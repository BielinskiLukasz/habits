---
context: default
phase: null
task: null
total_tasks: null
status: in_progress
last_updated: 2026-09-17T15:46:23.431Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] CONSTRAINT: S1 fix visibility — score_snapshots are precomputed and cached; views (waveboard, analytics) never recompute S1/S2/S3 live from scoring.js. After any change to js/domain/scoring.js, existing snapshot rows still reflect the OLD formula until Settings > Recompute Scores is run. Do not conclude a scoring fix is wrong just because the live app still shows old numbers — rule out stale snapshots (and stale service-worker-cached JS) first.

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Judging a scoring fix by live app values alone | S1/S2/S3 are read from `score_snapshots`, not recomputed on render (D-113/D-114). A correct code fix can still show old numbers until `rebuildAllSnapshots` (Settings > Recompute Scores) runs. | advisory | Always ask "was Recompute Scores run after this fix?" before treating a live discrepancy as a code defect. |

<current_state>
No active phase/spike/sketch/deliberation — this session was pure ad-hoc work: 3 quick tasks (all shipped/committed) plus one debug session that is fix-complete but not yet committed.

Debug session `.planning/debug/s1-weekly-period-premature.md` (slug: `s1-weekly-period-premature`) is at an **awaiting-human-verify checkpoint**. Root cause confirmed, fix applied to `js/domain/scoring.js` (`_computeS1Periodic` — a weekly/monthly period is now only counted as "expected" once it has actually elapsed, or already has a completion, instead of counting the still-open current period as an immediate miss). 4 new regression tests added to `tests/unit/scoring.test.js`; 22/22 in that file, 968/968 full suite at last run. **Both files are uncommitted in the working tree right now.**

User said "I'll check the live app first" (via Settings > Recompute Scores) and has not yet reported back the result.
</current_state>

<completed_work>

Completed this session (all committed):
- quick-260917-lst: Catalog action buttons (edit/archive/activate) — column → row layout. Commit `6d3ab1f` (chain: 87df886, 7abb45f, aeeaf01, 6d3ab1f). Also closed backlog item B-004.
- quick-260917-n8p: Waveboard heat-map — hardcoded last-12-weeks → full history from score_snapshots, horizontal scroll added, sticky habit-name column preserved. Commit `dd29965` (chain: 24d1935, 9291c32, 6c7d78c, dd29965).
- quick-260917-o9h: Waveboard wave-header row (colspan label) — was scrolling with the grid, now pinned via a scoped `.waveboard-grid .analytics-wave-header td { position: sticky; left: 0; }` rule (doesn't affect the plain non-scrolling analytics table, which shares the bare class name). Commit `38c2393` (chain: 77abdd8, 95349a2, 55780e2, 38c2393).
- Debug session s1-weekly-period-premature: RCA confirmed, TDD red→green cycle complete, fix-acceptance guardrail passed (revert-and-reconfirm proved causality). **NOT YET COMMITTED** — waiting on user's live confirmation.
- Answered (no code change needed): "Woda: wypicie 2 litrów" habit not scoring in S1 — confirmed as intended 7-day grace-period behavior (habit startDate 2026-09-14, only 3 days old per `data/habits-import-2026-09-17.json`), not a bug.
</completed_work>

<remaining_work>

- Get the user's answer: did Recompute Scores fix the live S1 display for the weekly habit?
  - If YES: commit `js/domain/scoring.js` + `tests/unit/scoring.test.js`, then move `.planning/debug/s1-weekly-period-premature.md` to `.planning/debug/resolved/`, append an entry to `.planning/debug/knowledge-base.md`, and update STATE.md's Quick Tasks / debug tracking as appropriate.
  - If NO (still wrong after recompute + hard refresh): resume `gsd-debug-session-manager` for slug `s1-weekly-period-premature` with `resume: true`, `resume_status` and `resume_next_action` carrying the new evidence, to reopen investigation — the original fix may be incomplete or there may be a second, related defect.
- No other open work.
</remaining_work>

<decisions_made>

- Waveboard now shows full history (not capped at 12 weeks) with horizontal scroll, because `score_snapshots` are written from `habit.createdAt` to today (not capped at 70 days), so the old 12-week cutoff was a pure UI limitation, not a data limitation.
- Wave-header sticky fix scoped to `.waveboard-grid .analytics-wave-header td` specifically (not the bare shared class), to avoid affecting the plain desktop analytics table which never scrolls horizontally.
- "Woda: wypicie 2 litrów" treated as correct grace-period behavior, not a bug — confirmed via the actual import JSON data (startDate 3 days before "today", only 3 log rows), not guessed.
</decisions_made>

<blockers>
- S1 fix live-verification: user is checking the app themselves (Settings > Recompute Scores) and hasn't reported back yet. This is the only blocker — everything else this session is fully shipped.
</blockers>

## Required Reading (in order)
1. `.planning/debug/s1-weekly-period-premature.md` — full RCA, fix, and test evidence for the open debug session; read the `Current Focus` and `Resolution` sections first.
2. `js/domain/scoring.js` (`_computeS1Periodic`, ~line 93-140) — the actual uncommitted fix, to see exactly what changed before deciding to commit or revise.
3. `js/io/scoreSnapshots.js` (top-of-file docstring + `writeHabitSnapshots`) — explains why views never see a scoring.js change until `rebuildAllSnapshots` runs; needed context if the user reports the fix "didn't work."

## Infrastructure State
- No servers/watchers running. This is a static, no-build, file://-or-static-HTTP app — no dev server was started this session.
- Working tree has exactly 2 uncommitted modified files (`js/domain/scoring.js`, `tests/unit/scoring.test.js`) and 1 untracked file (`.planning/debug/s1-weekly-period-premature.md`). Nothing else is dirty.

<context>
This was an ad-hoc bug-triage session (no formal phase), working through 4 issues the user raised about scoring/analytics: (1) waveboard week range — fixed, (2) weekly/monthly S1 counting the current open period as an immediate fail — fixed in code, pending live confirmation, (3) a specific habit not scoring — turned out to be correct grace-period behavior, (4) wave-header row scrolling — fixed. Everything routed through the project's mandated GSD quick-task / debug workflows per this repo's CLAUDE.md. The only loose end is waiting on the user to actually click "Recompute Scores" and look at their real data, since the debug session correctly treats "human sees it live" as the final gate before committing a scoring-algorithm change.
</context>

<next_action>
Start with: ask the user "Did you run Settings > Recompute Scores? What does the weekly habit's S1 show now?" Branch per their answer as described in <remaining_work> above.
</next_action>
